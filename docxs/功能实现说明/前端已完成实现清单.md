# 前端已完成实现清单

> 本文记录 FoodMate 前端截至 2026-08-13 已实现并完成对应验证的功能边界。
> 本文只记录真实代码、真实测试和实际浏览器检查结果，不把 Figma 目标稿、后端目标接口或未完成的全量迁移写成已完成。
> “页面代码边界完成”表示该页面的代码、交互和本地验收已完成；不等同于真实后端接口、Figma 实际画板和整个 G 阶段全部完成。

> 当前状态（2026-09-06）：页面代码和本地视觉/交互验收不等于完整生产完成。后端已实现 `food_logs` 明细创建/查询/编辑/删除/恢复、today/7d/30d 分析、60 条营养食材 seed 和 60 条官方 foodPortions 换算规则的 matched/pending 分支、餐食计划完整资源生命周期和 `meal_plan.save_plan` 确认；`food_log_writer` 的 create/update/delete/restore 及 rejected/failed/superseded、revision 冲突和幂等重放已通过真实 PostgreSQL HTTP/RocketMQ 各 11/11 跨进程回归。M2-1 公共知识库、M2-2 deterministic Tool/SQL 和 M2-3 管理核心切片已有业务证据；真实云服务、性能和故障矩阵仍后置。本次实际复核全量 Vitest 为 43 个测试文件、268 个用例全部通过；Home 定向测试为 8/8 通过。typecheck、build、`format:check`、全量 `lint` 和 Figma 证据结构校验通过；Workspace/Home、Agent Chat、Meal Planning 和 Knowledge 三种状态页已完成当前批次视觉收口，最新 Figma 证据仍保持 `DIFF_REVIEW`。不得把本清单的页面完成项改写为生产级完成。

## 当前批次：头像运行时统一渲染与回退收口（2026-09-06）

- [x] 新增 `src/components/common/AvatarImage.tsx` 作为运行时人物头像的统一渲染入口；Workspace、Chat 专用状态、Profile、Admin 侧栏和 Admin 用户详情均通过该组件渲染。
- [x] `resolveAvatarUrl` 对空头像、历史 `/assets/figma/**` 人物素材和未知性别统一回退到项目登记的默认 SVG；已知女性回退 `default-female.svg`，男性或未知性别回退 `default-male.svg`。
- [x] 有效的真实用户头像仍优先展示；真实头像加载失败后由 `AvatarImage` 按性别回退，不改变真实模式的上传、删除和后端头像 URL 语义。
- [x] Profile 不再显示首字母占位；Admin Fixture 用户即使没有头像和性别也不再显示首字母。
- [x] Chrome 实际检查 Workspace Home、Chat 默认态、Chat 写入确认态、Chat SSE 重连态、Profile Basic、Admin User Detail 和 Admin Overview；头像 DOM 来源仅为 `/assets/avatars/default-male.svg` 与 `/assets/avatars/default-female.svg`。
- [x] 两份默认 SVG 与用户附件逐字节一致：男性 SHA-256 为 `EE00AF66515C1807ED24738774776C9EBCAAECCBDCD28F15B1B43B6DBBF67D0D`，女性 SHA-256 为 `6F12B013242789D28BA4D8949F7345986956D474F07442C3DC9B23FE634ACF34`；生产构建产物未发现旧 Figma 人物头像路径。
- [x] 本大点统一验证：Vitest `44/44` 个测试文件、`274/274` 个用例，`typecheck`、`lint`、`format:check`、`build`、`qa:figma:validate` 和 `git diff --check` 均通过；Figma 证据结构校验为 `structuralPass=true`、`strictDprPass=true`、`diffReview=105`、`errors=[]`。
- [ ] 本大点没有重新采集全部 105 个画板；头像替换影响的历史视觉证据尚未全部复采集，105 项仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。

## 当前批次：共享壳层头像入口与 Figma Token 收口（2026-09-06）

- [x] `WorkspaceLayout` 的 `avatarSrc`、`sidebarAvatarSrc` 和 `topAvatarSrc` 现在全部经过 `resolveAvatarUrl`；历史 `/assets/figma/**` 人物路径即使通过覆盖参数传入，也不会进入运行时 DOM。
- [x] Admin 侧栏头像和 Chat 消息头像已统一经过头像解析层；真实用户上传头像仍保留，空头像和历史 Fixture 人物素材按性别回退到登记的男性/女性 SVG。
- [x] 共享工作台的成功状态和规划顶栏表面已改用现有 FoodMate 语义 Token，未改变已确认的侧栏、顶栏和头像几何尺寸。
- [x] 受影响回归为 4 个测试文件、`76/76` 个用例；`npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run build`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [x] Chrome 实际检查 Workspace Home、Agent Chat 和 Admin Overview：运行时头像来源分别仅为登记的 `default-male.svg` 和 `default-female.svg`；两份 SVG 与用户附件 SHA-256 一致。
- [ ] 本批次没有重新采集全部 105 个画板；现有画板视觉汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，不能将本批次改写为像素级完成。

## 当前批次：默认头像运行时解析与构建产物收口（2026-09-06）

- [x] 用户提供的男性 SVG SHA-256 为 `EE00AF66515C1807ED24738774776C9EBCAAECCBDCD28F15B1B43B6DBBF67D0D`，女性 SVG SHA-256 为 `6F12B013242789D28BA4D8949F7345986956D474F07442C3DC9B23FE634ACF34`；两份附件与 `public/assets/avatars/default-male.svg`、`default-female.svg` 内容逐字节一致。
- [x] `resolveAvatarUrl` 现在拒绝历史 Figma 人物素材路径，并按已知性别回退到对应默认 SVG；性别未知的遗留人物素材统一回退男性 SVG。真实用户上传头像仍优先保留。
- [x] Profile 基本资料页已接入同一解析层，历史 Figma 头像不能绕过统一策略；Workspace、Chat、Knowledge、Diet Records、Analysis、Planning 和 Admin 的 Fixture 头像仍统一通过 `DEFAULT_AVATARS`。
- [x] 头像影响范围回归为 9 个测试文件、`129/129` 个用例通过；改动文件 lint、Prettier、typecheck 和生产构建通过。
- [x] 本地浏览器已实际检查 Workspace Home、Chat 写入确认态、Profile Basic 和 Admin User Detail，页面中默认头像均显示为登记的男性/女性 SVG。
- [x] 构建后的可执行 JavaScript 中未发现旧 Figma 人物头像路径；`public/assets/figma/**` 中的旧 PNG 仅作为历史设计/验收证据保留，不属于运行时默认头像来源。
- [ ] 本批次没有重新采集全部 105 个画板；受头像资源变化影响的画板仍需在后续逐页复采集后重新生成 diff，不能提前标记 `PASS`。

## 当前批次：Workspace/Home 与 Agent Chat Figma 资源收口（2026-09-06）

- [x] `WorkspaceLayout`、`SidebarSessionList`、`Composer`、`HomePage` 和 `ChatPage` 已接入对应 Figma 导出的 Workspace/Home 与 Chat SVG 资源；真实模式继续保留 Lucide fallback 和既有业务逻辑。
- [x] 新增 `src/components/workspace/FigmaWorkspaceAsset.tsx` 作为唯一 Workspace fixture 资源 wrapper，固定资源尺寸，未创建虚构 iconfont glyph 或 `@font-face`。
- [x] Home 与 Chat 的 Figma fixture 已覆盖窗口控制点、导航、会话状态点、搜索、通知、发送/附件、营养环和活动箭头等当前已登记资源。
- [x] Workspace Home（`640:256`）和 Agent Chat（`640:428`）已完成 `1440×1024`、DPR 1、字体加载完成、无横向溢出的浏览器证据采集。
- [x] 当前批次定向测试为 5 个测试文件、55/55 个用例通过；`npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build`、`npm run qa:figma:validate` 和 `git diff --check` 均通过，Figma 105 项结构校验保持 `structuralPass=true`、`strictDprPass=true`。
- [ ] Workspace Home 的最新 diff 为 `24.3005%`，Agent Chat 的最新 diff 为 `11.7179%`；两项均保持 `DIFF_REVIEW`，不能写成像素级 `PASS`。
- [ ] 本批次没有重新验收全部 105 个画板；既有 105 项映射和历史证据未被本批次视为全量重新验收结果。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，继续保持 `BLOCKED`。

## 当前批次：默认头像资源策略收口（2026-09-06）

- [x] 项目默认头像的唯一资源入口为 `foodmate-ui/src/lib/avatar.ts` 中的 `DEFAULT_AVATARS`：男性使用 `public/assets/avatars/default-male.svg`，女性使用 `public/assets/avatars/default-female.svg`。
- [x] Workspace、Chat、Knowledge、Profile、Diet Records、Intake Analysis、Meal Planning 和 Admin 的 Figma fixture 已停止加载真人头像 PNG；男性示例统一使用男性 SVG，Chat 女性消息示例使用女性 SVG。
- [x] 真实模式仍优先保留后端返回或用户主动上传的 `avatarUrl`；只有头像为空时才按性别回退到默认 SVG，未改变上传、删除和预览能力。
- [x] 旧 Figma PNG 仅保留为历史设计/验收素材，不再作为前端运行时头像资源；本批次不删除已有证据文件。
- [x] 已同步更新相关页面测试断言，并增加 fixture 头像映射测试，防止真人 PNG 重新进入运行时引用链。
- [ ] 本批次尚未重新采集全部 105 个画板；受头像资源变化影响的局部视觉证据需要在后续目标画板复采集后重新生成 diff，不能提前标记 `PASS`。

## 当前批次：前端质量门禁基线收口（2026-09-06）

- [x] 已按项目现有 Prettier 规则统一本批次触及的 Admin 文件格式，`npm run format:check` 全量通过。
- [x] 已修正 Admin 用户详情请求的 Hook 依赖，使用稳定的 `selectedUserId` 触发加载，`npm run lint` 全量通过且无 warning 门禁阻断。
- [x] Admin 相关回归测试为 4 个测试文件、34 个用例通过，`npm run typecheck` 通过。
- [x] 本批次未改变后端协议、业务数据和 Figma 画板；质量门禁收口不等于 105 个画板像素级 `PASS`。

## 当前批次：Workspace/Home 四个状态画板收口（2026-09-06）

本批次只处理 Workspace/Home 的 Loading、Empty、Error 和 Input States 四个 Figma 画板，不重新验收全部 105 个画板。Figma 文件保持只读，真实模式的工作台和 Composer 业务逻辑未改变。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---:|---:|---:|---:|---|
| Workspace Home Loading | `692:1049` | `/?state=loading` | `recaptured/dpr1-workspace-home-loading-browser-2026-09-06.png` | `1440×1024 / 1` | 28.6855% | 2.840815 | 15.845699 | 242 | `DIFF_REVIEW` |
| Workspace Home Empty | `692:1208` | `/?view=records&state=empty` | `recaptured/dpr1-workspace-home-empty-browser-2026-09-06.png` | `1440×1024 / 1` | 14.0877% | 2.790551 | 17.911125 | 255 | `DIFF_REVIEW` |
| Workspace Home Error | `692:1313` | `/?view=records&state=error` | `recaptured/dpr1-workspace-home-error-browser-2026-09-06.png` | `1440×1024 / 1` | 63.7491% | 4.709468 | 18.429956 | 255 | `DIFF_REVIEW` |
| Workspace Home Input States | `1015:4` | `/?state=input-states` | `recaptured/dpr1-workspace-home-input-states-browser-2026-09-06.png` | `1440×1024 / 1` | 19.1847% | 2.949158 | 17.665746 | 244 | `DIFF_REVIEW` |

- [x] Home 四个状态共用 Figma Workspace 壳层；Loading 骨架、Empty/Error 状态卡和 Input States 三列状态面板已实现。
- [x] 侧栏状态 fixture 已登记 `共 15 条会话`、`1 / 3` 分页和对应浏览器入口；Input States 的发送/停止展示使用 shadcn Button。
- [x] 四项浏览器证据均由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体加载完成且无横向溢出条件下采集；mapping、diff 和运行时记录已同步。
- [x] Home 定向测试为 `8/8`，typecheck 和本批次文件 Prettier 检查通过。
- [ ] 四项自动 diff 均为非零，人工复核确认仍存在字体、局部几何和浏览器光栅化差异，继续保持 `DIFF_REVIEW`，不能标记像素级 `PASS`。
- [ ] 本批次没有重新验收其余 101 个画板；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 当前批次：Knowledge 三种状态 Figma 资源收口（2026-09-06）

本批次只处理 Knowledge 默认工作台壳层、检索失败和来源不可用三个状态，不重新采集或验收其它画板。Figma 文件保持只读，真实模式的知识检索、引用选择和来源访问逻辑未改变。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---:|---:|---:|---:|---|
| Knowledge Empty | `795:786` | `/knowledge?state=empty` | `recaptured/dpr1-user-knowledge-empty-browser-2026-09-06.png` | `1440×1024 / 1` | 40.3875% | 1.486179 | 9.587399 | 204 | `DIFF_REVIEW` |
| Knowledge Search Failed | `795:968` | `/knowledge?state=search-failed` | `recaptured/dpr1-user-knowledge-search-failed-browser-2026-09-06.png` | `1440×1024 / 1` | 39.4229% | 1.585677 | 10.442787 | 204 | `DIFF_REVIEW` |
| Knowledge Source Unavailable | `795:1151` | `/knowledge?state=source-unavailable` | `recaptured/dpr1-user-knowledge-source-unavailable-browser-2026-09-06.png` | `1440×1024 / 1` | 39.4018% | 1.575112 | 10.423397 | 204 | `DIFF_REVIEW` |

- [x] 新增 Knowledge 专属 Figma SVG 资源目录 `foodmate-ui/public/assets/figma/workspace/knowledge/`，并通过 `FigmaWorkspaceAsset` 统一加载窗口控制点、导航、搜索、通知和状态图标。
- [x] 新增 Knowledge 专属侧栏头像和顶栏头像：`foodmate-ui/public/assets/figma/knowledge/sidebar-avatar.png`、`topbar-avatar.png`。
- [x] 三项浏览器证据均使用 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体加载完成且无横向溢出；mapping、diff 和运行时证据已同步。
- [x] 本批次统一验证通过：Vitest `40/40` 个测试文件、`257/257` 个用例，`typecheck`、`lint`、`format:check`、`build`、`qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] 三项自动 diff 均为非零，仍存在字体、头像、图标和浏览器光栅化差异；三项继续保持 `DIFF_REVIEW`，不能标记像素级 `PASS`。
- [ ] 本批次没有重新验收全部 105 个画板；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 当前批次：Profile Basic Figma fixture 收口（2026-09-06）

本批次只处理 Profile Basic 画板 `806:1119`，不重新采集或重新判定其它画板。Figma 设计稿保持只读，真实模式的 Profile 权限、头像上传和资料保存逻辑未改变。

| 画板 | Figma 节点 | 前端入口 | Figma PNG | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---|---:|---:|---:|---:|---|
| Profile Basic | `806:1119` | `/profile?state=basic` | `recaptured-figma/profile-basic-live-2026-09-06.png` | `recaptured/dpr1-profile-basic-browser-2026-09-06.png` | `1440×1024 / 1` | 60.2509% | 3.464978 | 18.459664 | 230 | `DIFF_REVIEW` |

- [x] Profile fixture 新增独立 `profile` Workspace 资源 variant，并登记 Figma 导出的窗口控制、导航、搜索、通知和状态资源；未创建虚构 iconfont glyph 或 `@font-face`。
- [x] Figma Basic 中的“头像与账号概览”“账号状态”“最近登录”“常用单位”“时区”“偏好速览”和“饮食与身体目标”已恢复到 fixture；真实模式不受 fixture 文案覆盖影响。
- [x] Figma fixture 的角色展示为“首席运营”，仅覆盖显示文案，不改变 `AuthUser.role` 的真实权限语义。
- [x] 浏览器证据由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体加载完成且无横向溢出条件下采集；Figma PNG 使用节点 `806:1119` 的实时原始尺寸截图。
- [x] 自动 diff、视口、DPR、字体、文字溢出和人工视觉复核证据已登记；人工复核确认顶栏资源、Profile Basic 内容和主要几何已对齐，但仍存在字体、头像、图标和浏览器光栅化差异。
- [x] 本大点统一验证通过：Vitest `40/40` 个测试文件、`257/257` 个用例，`typecheck`、`lint`、`format:check`、`build`、`qa:figma:validate` 和 `git diff --check` 均通过；Figma 证据校验为 `structuralPass=true`、`strictDprPass=true`。
- [ ] 本画板仍为 `DIFF_REVIEW`，不能标记为像素级 `PASS`；本批次没有重新验收全部 105 个画板。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 当前批次：Profile Memories Figma fixture 收口（2026-09-06）

本批次只处理 Profile Memories 画板 `806:1281`，不重新采集或重新判定其它画板。Figma 设计稿保持只读；真实模式的记忆查询、编辑、删除、确认和来源会话边界未改变。

| 画板 | Figma 节点 | 前端入口 | Figma PNG | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---|---:|---:|---:|---:|---|
| Profile Memories | `806:1281` | `/profile?state=memories` | `recaptured-figma/profile-memories-live-2026-09-06.png` | `recaptured/dpr1-profile-memories-browser-2026-09-06.png` | `1440×1024 / 1` | 21.5618% | 4.279536 | 21.369497 | 226 | `DIFF_REVIEW` |

- [x] Figma Memories 的介绍卡、全部/待确认/已确认筛选、分类选择、三条记忆卡和长期记忆管理卡已按实时节点复核；Figma 专属查看、编辑和删除 SVG 已通过 `FigmaWorkspaceAsset` 的 `profile` variant 加载。
- [x] 浏览器证据由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体加载完成且无横向溢出条件下采集；实时 Figma PNG 已保存到 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured-figma/`。
- [x] `scripts/png-diff.mjs` 同尺寸结果为 `differentPixels=317942`、差异比例 `21.5618%`、`MAE=4.279536`、`RMSE=21.369497`、最大通道差异 `226`。
- [x] 已完成人工视觉复核；页面结构、记忆卡、筛选区和长期记忆管理卡已确认，仍存在字体、头像、控件状态、图标和局部文字换行差异，因此继续保持 `DIFF_REVIEW`。
- [x] 本大点完成后统一执行 `npm run test`（40/40 个测试文件、257/257 个用例）、`npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build`、`npm run qa:figma:validate` 和 `git diff --check`，均通过；证据校验为 `structuralPass=true`、`strictDprPass=true`、`mappedPass=0`、`diffReview=105`、`errors=[]`。本批次未重新验收全部 105 个画板。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 当前批次：Profile Memories Empty Figma fixture 收口（2026-09-06）

本批次只处理 Profile Memories Empty 画板 `1013:2`，不重新采集或重新判定其它画板。Figma 设计稿保持只读；真实模式的记忆查询、编辑、删除、确认和来源会话边界未改变。

| 画板 | Figma 节点 | 前端入口 | Figma PNG | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---|---:|---:|---:|---:|---|
| Profile Memories Empty | `1013:2` | `/profile?state=memories-empty` | `figma-png/profile-memories-empty.png` | `recaptured/dpr1-profile-memories-empty-browser-2026-09-06.png` | `1440×1024 / 1` | 14.3330% | 2.524803 | 15.902038 | 226 | `DIFF_REVIEW` |

- [x] `memories-empty` 已从 overlay fixture 调整为独立 Profile Memories 页面状态；空记忆数据下保留介绍卡、筛选栏和分类选择，并隐藏长期记忆管理卡。
- [x] 空态卡按实时 Figma 节点几何收口到 `x=500、y=270、width=680、height=320`，卡内内容使用 Figma 的 `32px` 左内边距和 `28px` 顶内边距。
- [x] 浏览器证据由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体加载完成且无横向溢出条件下采集；`ProfilePage.test.tsx` 的空态页面回归通过。
- [x] `scripts/png-diff.mjs` 同尺寸结果为 `differentPixels=211349`、差异比例 `14.3330%`、`MAE=2.524803`、`RMSE=15.902038`、最大通道差异 `226`。
- [x] 已完成人工视觉复核；空态卡、介绍卡和筛选区的主要几何已对齐，壳层导航、头像、控件箭头、文字光栅化和局部颜色仍存在差异，因此继续保持 `DIFF_REVIEW`。
- [ ] 本批次没有重新验收全部 105 个画板；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 功能名称 | FoodMate 前端 G1-G6 已完成实现清单 |
| 功能编号/阶段 | G1、G2、G3、G4、G5-1 至 G5-7、G6；关联 M1-2、M1-3、M1-4 前端接入 |
| 文档版本 | v1.2 |
| 实现日期 | 2026-08-11 |
| 适用环境 | local / dev |
| 实现状态 | 已验收（按已完成代码边界）；整体前端重构仍未结束 |
| 关联实现入口 | [前端实现指南](../实现/前端实现指南.md) |
| 关联设计审查 | [Figma 设计稿功能覆盖审查与修改清单](../设计/Figma设计稿功能覆盖审查与修改清单.md) |

## 2. 清单范围与总体结论

### 2.1 本文已纳入的完成范围

- G1 前端基础设施：Vite/TypeScript 别名、Tailwind、shadcn/ui 基础目录、FoodMate 语义 Token、`cn` 工具和图标边界。
- G2 基础组件：Button、Input、Textarea、Dialog、Sheet、Tooltip、Tabs、Select、Table、Card、Badge、Alert、Progress、Skeleton、DropdownMenu 等已使用或已完成基础验证的组件。
- G3 工作台：工作台壳层、会话列表、Composer、Agent 状态条、消息流、工具轨迹、引用、追问和确认交互。
- G4 用户业务页：饮食记录、摄入分析、餐食规划、知识库、个人中心五个页面及其主要默认态、空态、加载态、错误态和操作反馈。

其中饮食记录、摄入分析和餐食规划表示前端页面代码/本地交互边界已完成；后端已有真实数据创建/查询/编辑/删除/恢复、60 条营养食材 seed 与 60 条官方换算规则的 matched/pending 分支、分析、餐食计划完整资源生命周期、一条计划写确认切片和完整 `food_log_writer` 写入操作，计划生命周期和 writer 各 11/11 跨进程回归也已完成真实 HTTP/MQ 验证；更广泛业务 Tool/SQL 属于后续阶段。
- G5 管理端代码边界：管理概览、工具注册表、删除资源、管理操作状态、用户详情、Run/Tool Call/SQL Audit/Trace 四个治理 Tab、权限矩阵和独立操作审计页。
- G6 清理边界：源码中无 Arco 运行时 import、Arco 主题入口或 `@arco-design/web-react` 依赖；shadcn/Radix primitives、FoodMate Token 和 Lucide 图标入口已保留。
- 与 M1-2/M1-3/M1-4 已完成后端能力保持一致的前端真实接入：认证恢复、会话消息、AgentRun SSE、取消、断线续传、预算追加、continuation 和安全降级展示。

### 2.2 分阶段状态总览

| 阶段 | 已完成边界 | 当前结论 |
|---|---|---|
| G1 | 前端构建、别名、Tailwind、语义 Token、shadcn 入口和图标封装 | 已完成并通过构建、类型和基础交互检查 |
| G2 | 基础控件和可访问交互 | 已完成；组件回归测试 15/15 |
| G3 | 工作台、会话列表、Composer、Agent 基础状态 | 已完成；真实 SSE/取消/续传语义保持不变 |
| G4 | 五个用户业务页面 | 已完成；每页独立回归和四视口检查通过 |
| G5-1 | 管理概览 | 页面代码边界完成 |
| G5-2 | 工具注册表 | 页面代码边界完成 |
| G5-3 | 删除资源 | 页面代码边界完成 |
| G5-4 | 管理操作状态 | 页面代码边界完成 |
| G5-5 | 用户详情五个 Tab | 页面代码边界完成 |
| G5-6 | Run、Tool Call、SQL Audit、Trace | 列表/详情真实接口已接入；Figma 实际画板与生产观测平台仍有限制 |
| G5-7 | 普通用户/operator/admin 权限矩阵、独立操作审计页 | 前端代码矩阵和本地验证完成；真实后端授权仍需接口级验收 |
| G5 全量 | G5-1 至 G5-7 前端页面、权限和交互边界 | 前端代码边界完成；真实数据与 Figma 实际画板仍有限制 |
| G6 | 清除 Arco 运行时依赖和主题入口 | 已完成静态核验；不等同于所有页面已完成 Figma 像素级重构 |

## 3. 前端路由与服务边界

### 3.1 已完成页面路由

| 路由 | 页面功能 | 完成边界 |
|---|---|---|
| `/` | 工作台首页、快捷任务和当前 Agent 概况 | G3 |
| `/login` | 登录入口、登录态反馈和认证恢复入口；`?state=` 可复现 Figma 登录异常状态 | M1-2 前端接入 |
| `/register` | 注册、密码规则和确认密码 | 认证页面代码边界完成 |
| `/forgot-password` | 密码找回邮箱提交和邮件已发送双卡状态 | 认证页面代码边界完成 |
| `/reset-password` | 新密码、强度和 token 防护 | 认证页面代码边界完成 |
| `/token-status` | 无效、过期和已使用 Token 状态 | 认证状态代码边界完成 |
| `/chat/:session_id?` | 会话消息、Composer、Agent 状态、工具轨迹和 SSE | G3、M1-2、M1-3 |
| `/analysis` | 摄入分析、指标、趋势、洞察和数据质量 | G4 |
| `/planning` | 餐食规划、约束校验、冲突处理、购物清单 | G4 |
| `/knowledge` | 知识检索、结果、引用、空态和失败态 | G4；真实 `public_published` 检索已接入 |
| `/profile` | 基本资料、记忆与偏好、安全与设备、数据与隐私 | G4 |
| `/admin` | 管理概览和运行摘要 | G5-1 |
| `/admin/users` | 用户列表和用户详情五个 Tab | G5-5；真实详情接口未完全接入 |
| `/admin/runs` | AgentRun、Tool Call、SQL Audit、Trace | G5-6；Trace 列表和脱敏详情已接入真实接口，其他治理资源仍按各自接口状态展示 |
| `/admin/tools` | 原工具调用页和工具注册表 Tab | G5-2 |
| `/admin/deleted` | 软删除资源归档、详情和恢复 | G5-3 |
| `/admin?view=audit` | 管理操作审计筛选、分页和只读详情 | G5-7 |

`/admin/knowledge` 和 `/admin/usage` 已纳入管理权限矩阵。知识库管理在 real 模式使用批次上传、状态查询、SSE、失败重试和可见性接口；未接入真实详情资源的数据仍显示明确空态，不伪造 fixture 成功。

### 3.2 真实接口和 mock 边界

- M1-2 已接入真实认证、登录态恢复、会话列表、搜索、创建、重命名、归档、软删除、恢复和用户消息读取/写入；前端通过统一 API client 处理 Cookie、CSRF、401、403 和网络错误。
- M1-3 已接入真实 AgentRun 创建、查询、取消、SSE 订阅和 `Last-Event-ID` 续传；前端按事件序列去重并映射运行状态。
- M1-4 的预算追加、continuation、`superseded`、Runtime 协调故障提示、Eval 延迟正文和安全降级展示已接入前端状态语义并完成本地验证。
- 管理概览、工具注册表和删除资源在 `VITE_AGENT_MODE=real` 下复用已有管理服务或恢复服务；未接入真实详情接口的数据不伪造成功，而是显示明确空态或 `-`。
- 管理页面在 mock 模式使用本地可重复样例数据；mock 数据不能作为真实后端数据已存在的证据。

## 4. G1 前端基础设施

### 4.1 TypeScript/Vite 路径别名

- `foodmate-ui/tsconfig.json` 和 `foodmate-ui/vite.config.ts` 统一配置 `@/*` 指向 `src/*`。
- 页面、组件和服务使用同一别名规则，避免 Vite 能解析但 TypeScript 无法检查的分裂配置。
- 该边界已通过 `npm run typecheck` 和 `npm run build`。

### 4.2 Tailwind、shadcn/ui 与 FoodMate 语义 Token

- `src/styles/tokens.css` 建立 FoodMate 颜色、字体、间距、圆角、边框和状态变量，并作为 shadcn 组件和页面样式的语义来源。
- Tailwind 已接入 Vite，组件目录位于 `src/components/ui`，基础工具位于 `src/lib/utils.ts`。
- shadcn/ui 只负责可组合控件和可访问行为，工作台、消息流、Agent 轨迹和页面布局仍由 FoodMate 领域组件负责。
- 当前完成的是 shadcn/Radix 基础设施和 Arco 清理代码边界；这不表示所有领域页面都已完成 Figma 实际画板对照，也不把 CSS Modules 领域布局误写成 shadcn 组件本身。

### 4.3 图标使用边界

- 标准命令图标使用项目统一图标组件和 Lucide，已覆盖发送、停止、搜索、编辑、删除、复制、下载、刷新、展开等操作。
- 页面不直接写入 iconfont 字码、Unicode 图标或未经登记的内联矢量。
- 领域图标只有在资源来源、授权、字形类名和适配背景确认后才允许进入 iconfont 封装。
- 当前 iconfont 实体字体包和最终资源注册仍未确认，因此本阶段只关闭标准命令图标边界，iconfont 资源登记保持 `BLOCKED`。

## 5. G2 基础组件

### 5.1 基础控件与可访问交互

`src/components/ui` 已形成项目内可复用的基础组件边界，包括：

- `Button`：variant、size、disabled 和命令图标按钮语义。
- `Input`、`Textarea`：可访问名称、输入状态和表单承载。
- `Dialog`、`Sheet`：Escape 关闭、焦点归还、确认操作和详情侧栏。
- `Tooltip`：不熟悉图标的说明和 Provider 生命周期。
- `Tabs`、`Select`：键盘切换、选项提交和管理端查询 Tab。
- `Table`：原生表格语义、窄屏横向滚动容器和稳定列布局。
- `Card`、`Badge`、`Alert`、`Progress`、`Skeleton`、`DropdownMenu`：指标、状态、反馈、进度、加载和菜单场景。

`src/components/ui/components.test.tsx` 覆盖 15 条基础组件测试，验证 Button、Input/Textarea、Dialog/Sheet、Tooltip、Tabs、Select、Badge、Alert 和 Table 的关键行为。

## 6. G3 工作台与会话体验

### 6.1 工作台壳层

- `WorkspaceLayout` 提供左侧会话栏、顶部区域、主工作区和可选右侧 Trace rail。
- Chat 页面按 Figma `agent-chat-v2` 保持 `260px` 侧栏、`860px` 对话区、`320px` Trace rail 和 `68px` 顶部栏；消息区、Trace 和输入区分别保持局部滚动与稳定高度。
- 页面局部滚动限制在会话列表、消息区、Trace 和宽表容器，避免整个页面出现非设计稿滚动。

### 6.2 会话列表与 Composer

- 会话列表复用真实会话服务，保留搜索、归档、恢复、删除和历史分页语义。
- Composer 使用 shadcn `Input` 与 `Button`，保留发送、停止、真实 API/SSE 回调和禁用状态；`agent-chat-v2` 设计稿当前只提供追问输入和发送入口，未虚构附件控件。
- Enter 发送排除 IME 组合输入和 `keyCode=229`，Shift+Enter 保留换行，运行中可停止当前 Run。
- 首页快捷任务先填充 Composer，并通过可访问的输入/发送控件进入会话。
- 移动端快捷任务在 390x844 下按两列换行，内容宽度为 `343px`，不产生页面级横向溢出。

### 6.3 Agent 状态、消息流与轨迹

- `AgentStatusStrip`、`ResultCard`、`CitationBlock`、`ClarificationCard`、`ConfirmationCard` 和 `ToolTraceItem` 分别承载运行状态、结果、引用、追问、写入确认和工具轨迹。
- 已保留 `waiting_user`、确认、失败、取消、流式输出和完成等状态展示。
- 真实 SSE 事件、取消、断点续传和 mock replay 使用同一 UI 状态映射，不因视觉迁移重写服务端事件契约。
- 工具输入/输出只展示脱敏摘要，不展示隐藏推理、数据库凭据或未授权的敏感参数。

## 7. G4 用户业务页面

### 7.1 饮食记录页 `DietRecordsPage`

- 路由：`/analysis?view=records`，页面边界由 `DietRecordsPage` 承载；首页和路由入口按现有应用配置访问。
- 对照 Figma `diet-records-v2`，完成日期导航、四项营养指标、Breakfast/Lunch 餐次卡和记录详情布局。
- 保留自然语言记录、编辑、复制、分析、软删除和新增食物 Dialog；状态区分已确认、待确认和待估算。
- 目标几何：`1440x1024` 下记录区为 `1180x548`，内容内边距 `32px`，指标卡为 `267x88`，早餐卡为 `1116x118`，午餐卡为 `1116x170`，详情区为 `1116x220`。
- 视觉契约：界面文本使用 `Noto Sans SC`，指标数值和百分比使用 `Space Mono Bold`，C/P/F 宏量使用 `Space Mono Regular`；颜色以 Figma 的 `#FCFCFC`、`#FFFFFF`、`#F4F6F5`、`#333333`、`#6B7280`、`#A6D997`、`#C79654`、`#DBB4F4`、`#FF7576` 为准。
- 交互实现使用现有 shadcn `Button`、`Dialog`、`Input` 和 Lucide 标准命令图标；不引入未经登记的 iconfont 字形。
- 验收：`1440x1024`、`1366x768`、`1024x768`、`390x844`；`DietRecordsPage.test.tsx` 两条回归测试；最新前端全量测试 `60/60`，typecheck 通过，页面改动文件 Prettier 通过。

### 7.2 摄入分析页 `AnalysisPage`

- 路由：`/analysis`。
- 完成时间范围筛选、三项摘要指标、能量趋势图、Agent 洞察和数据质量面板。
- 保留导出 CSV、让 Agent 解读和基于分析制定计划的交互语义；移动端筛选条使用容器内横向滚动。
- 视觉契约：以 Figma `intake-analysis-v2`（节点 `640:773`）为唯一来源；界面文本使用 `Noto Sans SC`，摘要数值使用 `Space Mono Bold`，颜色使用 `#FCFCFC`、`#FFFFFF`、`#F4F6F5`、`#333333`、`#6B7280`、`#B58CC4`、`#ECCCFF`、`#A6D997`、`#C79654` 和 `#FCFAFF` 等节点值。
- 实测几何：`1440x1024` 下主内容为 `1180px`，摘要行 `1116x107px`，趋势卡 `1116x303px`，图表区 `1068x220px`，趋势内层 `1010x140px`，洞察卡 `1116x219px`，数据质量面板 `1116x140px` 且从全局 `y=873` 开始。
- 交互使用现有 shadcn `Button` 与原生语义按钮；标准命令图标继续由 Lucide 提供，不引入未经登记的 iconfont 字形。
- 验收：浏览器覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`；四个视口均无 document/body 横向溢出，`1024px` 摘要切换为单列并隐藏工作区搜索避免挤压，移动端洞察文本自然换行；范围切换、通知、导出排队状态和两个跨页入口均已实测。
- 自动化验证：`AnalysisPage.test.tsx` 两条测试通过；全量 `npm test -- --run`、`npm run typecheck`、`npm run build` 和页面改动文件 Prettier 检查均通过。

### 7.3 餐食规划页 `PlanningPage`

- 路由：`/planning`。
- 完成日期 Tab、重新生成、保存计划、空计划入口、约束校验、购物清单勾选和列表筛选。
- 覆盖加载中、空态、错误态、计划列表、三步创建向导、冲突解决、购物清单、生成中、取消、重试、保存草稿、冲突应用和导出反馈。
- 默认态按 Figma `meal-planning-v2`（节点 `640:901`）重构；`1440x1024` 下采用 `260px + 840px + 340px` 结构，右栏从全局 `y=0` 开始，主计划区内边距为 `32px`。
- 关键几何与 Figma 节点一致：计划头部 `776x97px`，日程区域 `776x389px`，日期 Tab 高 `40px`，首行日程留白 `100px`，早餐/午餐/晚餐餐卡均为 `74px`，右栏约束区 `292x246px`、购物清单区 `292x214px`。
- 视觉契约只使用 Figma 回读值：界面文本为 `Noto Sans SC`，餐卡 `420 kcal` 为 `Space Mono Regular`；渐变、边框、标签、Pass/Review 状态和圆角均按 `640:901` 节点值落地。
- 共享 `WorkspaceLayout` 增加 `340px` 宽右栏能力；规划页在 `1366px` 等中等桌面宽度隐藏工作区搜索以避免导航挤压，`1024px` 隐藏右栏并保持餐表无重叠，移动端计划头部恢复自然高度。
- 浏览器验收覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`：四个视口均无 document/body 横向溢出；已实测日期切换、购物清单勾选、保存计划提示，以及 loading、empty、error、wizard、conflict、shopping-list、generating 状态入口。
- 自动化验证：`PlanningPage.test.tsx` 五条测试保留；全量 `npm test -- --run` 为 `16` 个测试文件、`60/60`；`npm run typecheck`、`npm run build` 和本页/共享布局改动文件 Prettier 检查通过。
- 本页仍不关闭真实计划生成、冲突解决、统计数据、导出任务、Agent 任务接口、生产字体加载、iconfont 实体资源登记和生产浏览器兼容性；iconfont 继续保持 `BLOCKED`，标准命令图标使用 Lucide。

### 7.4 知识库页 `KnowledgePage`

- 路由：`/knowledge`。
- 完成关键词搜索、状态筛选、结果卡、引用切换、空态、检索失败重试、来源不可用和“就此提问”。
- 按 Figma 默认画板及空态、失败态节点调整结果区和引用区，移除设计稿中不存在的搜索按钮。
- 引用切换按引用条目标识绑定，避免数组索引变化导致引用内容错位；移动端移除固定 `760px` 高度，使用自然纵向布局。
- 已按 Figma `795:838`、`795:963`、`795:1145`、`795:1328` 校准颜色、字体和几何：主内容 `1180px`，结果区 `840px`，引用区 `340px`，结果卡 `776x157px`，来源卡 `292x127px`，引用摘要 `292x26px`，推荐主题卡 `292x56px`。
- 视觉契约：界面文本使用 `Noto Sans SC`；匹配度、更新时间、DOC ID、访问缓存、错误码、来源状态、引用摘要和引用数量使用 `Space Mono`；标准命令图标使用 Lucide，不引入未经登记的 iconfont。
- 知识库顶栏已补齐五项主导航；桌面工作区搜索、右侧操作区和知识库活动态按 Figma 宽屏关系对齐；移动端结果卡在中文换行后自然增高，状态层覆盖全宽并位于顶部导航下方。
- 验收：浏览器覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`，均无横向溢出、裁切或非预期重叠；已实际验证搜索、主题筛选、引用切换、原始来源重试、空态清除筛选、检索失败重试和“就此提问”。
- 自动化验证：`KnowledgePage.test.tsx` 四条测试；最新全量测试为 `16` 个测试文件、`60/60`；`npm run typecheck`、`npm run build` 和本次变更文件 Prettier 检查通过。
- 真实文档导入、RAG 检索、ACL 过滤、引用详情接口、iconfont 实体资源登记和生产浏览器兼容性不属于本页面已完成的真实后端闭环；下一步继续迁移个人中心，并单独收口 shadcn 基础设施和 Token 映射。

### 7.5 个人中心页 `ProfilePage`

- 路由：`/profile` 及个人中心的记忆、安全、数据子路径。
- 完成基本资料、记忆与偏好、安全与设备、数据与隐私四个主 Tab。
- 覆盖记忆空态、退出其他设备确认、注销账号确认、头像上传中/失败、未保存离开、密码提交中/成功/失败、数据导出排队/运行/过期和注销提交中/成功/失败。
- 复用 shadcn `Button`、`Input`、`Card`、`Dialog`、`Badge`、`Progress`、`Select`、`Textarea`；继续使用已确认的 FoodMate 语义颜色和字体契约。
- 本轮完成 shadcn 基础设施收口：`Button`、`Card`、`Input`、`Select`、`Textarea` 的控件/容器圆角统一映射 `--radius-control`、`--radius-container`，页面字体统一入口为 `--font-ui`，状态色和间距基础值集中在 `src/styles/tokens.css`。
- 真实资料读写已接入；头像对象存储和部分个人数据扩展仍按 M1-1 后续范围建设。
- 验收：覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844` 及四个个人中心子路由；四个路由均无页面级横向溢出，安全页设备信息在移动端自然换行，数据导出宽表仅在自身容器内横向滚动；最新全量测试 `16` 个文件、`60/60`，`npm run typecheck`、`npm run build` 和本轮改动文件 Prettier 检查通过。

## 8. G5 管理端页面代码边界

### 8.1 G5-1 管理概览 `OverviewTab`

- 路由：`/admin`。
- 完成 3 项指标、时间/结果/降级筛选、运行/用户搜索、10 列运行表格、6 行首屏数据、分页和 3 张分析卡。
- 使用 shadcn `Select`、`Input`、`Button`、`Card` 和语义 `Table`；管理壳层固定 `260px` 侧栏、`64px` 顶栏和 `32px` 内容留白。
- 保留 `loadAdminDashboard` 的真实分支、mock fallback、admin/operator/普通用户可见性判断、admin 写操作确认和审计写入语义。
- 验收：`1440x1024`、`1366x768`、`1024x768`、`390x844` 无页面级横向溢出；新增 `AdminPage.test.tsx`，覆盖结构、筛选搜索和精确导航。

### 8.2 G5-2 工具注册表 `ToolsRegistryTab`

- 路由：`/admin/tools?tab=registry`；原工具调用页 `/admin/tools` 保留。
- 完成工具名、状态、风险等级、权限范围筛选，搜索、指标、分页、固定 10 列表格和配置详情只读 Dialog。
- 表格展示名称、版本、状态、风险、超时、重试策略、权限范围、最近调用和失败率；不提供当前没有真实接口依据的创建、编辑和删除按钮。
- admin 可发起启停确认；operator 只能查看，写操作进入无权限状态。
- 新增 `ToolsRegistryTab.test.tsx` 三条测试；在该阶段全量测试达到 `44/44`，typecheck/build 和定向 ESLint/Prettier 通过。

### 8.3 G5-3 删除资源 `DeletedResourcesTab`

- 路由：`/admin/deleted`。
- 完成资源类型、删除者、删除时间和关键词筛选，审计存档区提示，8 列归档表格、分页和资源详情 Dialog。
- 表格展示资源 ID、类型、摘要、原所有者、删除者、删除时间、可恢复状态和操作；不提供永久删除操作。
- 恢复操作继续调用 `restoreAdminResource`；mock 模式经过统一 admin 二次确认、审计写入和局部移除；不可恢复项保持禁用。
- 新增 `DeletedResourcesTab.test.tsx` 三条测试；阶段全量测试目标为 `47/47`，typecheck/build 和定向 ESLint/Prettier 通过。

### 8.4 G5-4 管理操作状态 `AdminOperationStatus`

- 状态契约为 `idle`、`no-permission`、`confirm`、`submitting`、`success`、`failed`。
- 工具启停等高风险写操作统一经过确认、提交中、成功或失败状态；operator 进入无权限横幅，失败状态展示稳定错误码和 `request_id`。
- 失败写入 `result=failed` 的操作审计记录；成功保留 `result=success`、`request_id` 和 `trace_id`；重试必须由用户明确点击，不自动重放。
- 使用 shadcn `Dialog`、`Alert`、`Card`、`Button` 和 Lucide；确认弹窗约 `480x344`，提交中约 `480x288`，失败约 `480x338`。
- 验收：`AdminOperationStatus.test.tsx` 定向 `9/9`；与既有管理测试合并时全量前端测试为 `50/50`；四个视口的页面宽度和弹窗边界已检查。

### 8.5 G5-5 用户详情 `UsersTab`

- 路由：`/admin/users`。
- 完成用户列表 + 用户详情侧栏，详情包含资料、饮食画像、登录会话、业务会话、操作历史五个 Tab。
- 资料和饮食画像展示用户 ID、账号资料、角色/状态、注册/登录信息、失败次数、锁定时间、身高、体重、活动水平、饮食目标、热量/蛋白质目标、过敏原、忌口和单位等字段。
- operator 可查看但不能执行状态或会话写操作；admin/superadmin 的锁定、禁用、启用和撤销会话复用 G5-4 的状态机和审计入口。
- mock 模式提供可重复详情数据；real 模式只展示真实用户列表返回的数据，未接入真实详情接口的会话和历史数据明确显示空态。
- 新增 `UsersTab.test.tsx` 三条测试；全量前端测试为 `53/53`，typecheck/build 和定向 ESLint/Prettier 通过；四视口无页面级横向溢出。

### 8.6 G5-6 Run、Tool Call、SQL Audit、Trace `RunsTab`

- 路由：`/admin/runs`，支持 `?tab=tool`、`?tab=sql`、`?tab=trace` 直接定位 Tab。
- 四个治理 Tab 共用 Run ID、用户、Session ID、Trace ID、状态、结果类型和错误码筛选；Tool Call、SQL Audit、Trace 复用追踪关键词和状态筛选。
- Run 展示阶段、结果类型、错误码、模型、请求 ID、脱敏输入/输出摘要；Tool Call 展示调用状态、重试、输入/输出和错误；SQL Audit 展示策略、查询哈希和返回行数；Trace 展示根服务、Span 数量和链路节点。
- 所有记录均可打开 shadcn `Sheet` 详情；Run 可进入关联 Tool Call，Tool Call/SQL Audit 可进入关联 Trace，Trace 可回到关联 Run；ID 复制使用 Lucide `Copy`。
- real 模式读取 `/api/admin/queries/traces` 分页列表，并在打开详情时读取 `/api/admin/queries/traces/{traceId}`；接口没有返回的字段显示 `-`，真实接口无数据时显示明确空态；mock 模式只使用本地可追溯样例数据。响应只包含脱敏 span 和关联元数据，不包含 Prompt、原始载荷或完整业务内容。
- 新增 `RunsTab.test.tsx` 三条测试，覆盖筛选重置、Tab URL、Run -> Tool Call -> Trace 和 SQL Audit 详情；最新前端全量测试为 `56/56`，typecheck/build 和治理改动定向 ESLint/Prettier 通过。
- 浏览器验收覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`；document/body/root 无页面级横向溢出，详情 Sheet 在移动端不超过视口。

## 9. 状态、权限与敏感数据边界

### 9.1 已完成的状态处理

- 页面已覆盖默认、加载、空、错误、提交中、成功、失败和必要的过期状态。
- Agent 交互已覆盖运行中、追问、确认、取消、SSE 重连、预算提示、预算耗尽和安全降级等前端展示语义。
- 管理写操作已覆盖 operator 无权限、admin 二次确认、提交中、成功和失败；失败显示稳定错误码和请求标识。
- real 模式无法取得后端数据时显示空态、错误态或 `-`，不把 mock 样例伪装成真实数据。

### 9.2 当前已实现的权限边界

- 顶部管理入口仅对 `admin/operator` 展示；普通用户不应进入管理后台。
- operator 可查看管理数据，但管理写操作保持禁用或进入无权限状态。
- admin/superadmin 可执行已接入的高风险操作，并必须经过确认和审计状态机。
- 用户侧会话、消息和 AgentRun 继续依赖服务端当前用户归属校验；前端不自行扩大数据可见范围。

这里记录的是前端已实现并通过本地测试的权限矩阵；真实 API 的服务端授权、并发竞态和部署环境回归仍需独立验收。

### 9.3 敏感数据处理

- Tool、SQL 和 Trace 详情只展示脱敏摘要、查询哈希、策略、返回行数和链路元数据。
- 不在页面返回数据库凭据、访问令牌、密码、令牌哈希或隐藏推理内容。
- ID 复制只复制页面允许展示的公开标识，不绕过后端权限读取完整对象。

## 10. 数据库与外部依赖边界

前端不直接访问 PostgreSQL、Redis、MinIO 或数据库表，不新增数据库表，也不在页面代码中执行 SQL。前端通过 HTTP、Cookie/CSRF、SSE 和管理服务契约消费后端能力。

对应后端数据和基础设施说明见：

- [M0-1 数据库与本地环境实现逻辑](./M0-1-数据库与本地环境实现逻辑.md)
- [M0-2 Java 真实持久化实现逻辑](./M0-2-Java真实持久化实现逻辑.md)
- [M1-1 账户授权与个人数据](./M1-1-账户授权与个人数据.md)
- [M1-2 会话消息与前端真实接入实现逻辑](./M1-2-会话消息与前端真实接入实现逻辑.md)
- [M1-3 AgentRun 与真实 SSE 最小闭环实现逻辑](./M1-3-AgentRun与真实SSE最小闭环实现逻辑.md)
- [M1-4-1 Continuation 接续与预算快照实现逻辑](./M1-4-1-Continuation接续与预算快照实现逻辑.md)
- [M1-4 Python Agent Runtime 实现逻辑](./M1-4-Python-Agent-Runtime实现逻辑.md)

## 11. 验收记录

### 11.1 自动化验证

| 验收项 | 验证方式 | 结果 | 验证日期 | 证据位置 |
|---|---|---|---|---|
| 基础组件行为 | `npm run test`；`src/components/ui/components.test.tsx` | 15/15，通过 | 2026-08-09 | `foodmate-ui/src/components/ui/components.test.tsx` |
| 工作台和 Composer | `npm run test`；`Composer.test.tsx`、Chat/Home 回归 | 20/20，通过 | 2026-08-09 | [前端实现指南](../实现/前端实现指南.md) G3 记录 |
| G4 用户业务页 | `npm run test`；Diet/Analysis/Planning/Knowledge/Profile 页面测试 | 16 个测试文件、60/60，通过 | 2026-08-12 | `foodmate-ui/src/pages/` |
| G5 管理操作状态 | `AdminOperationStatus.test.tsx` | 9/9，通过 | 2026-08-09 | `foodmate-ui/src/pages/AdminPage/tabs/AdminOperationStatus.test.tsx` |
| G5 用户详情 | `UsersTab.test.tsx` | 3 条通过；当时全量 53/53 | 2026-08-09 | `foodmate-ui/src/pages/AdminPage/tabs/UsersTab.test.tsx` |
| G5 Run/Tool/SQL/Trace | `RunsTab.test.tsx` | 3 条通过；最新全量 56/56 | 2026-08-09 | `foodmate-ui/src/pages/AdminPage/tabs/RunsTab.test.tsx` |
| G5 权限与操作审计 | `AdminPage.test.tsx`、`OperationAuditTab.test.tsx` | 定向管理端 7/7；最新全量 60/60 | 2026-08-10 | `foodmate-ui/src/pages/AdminPage/AdminPage.test.tsx`、`foodmate-ui/src/pages/AdminPage/tabs/OperationAuditTab.test.tsx` |
| 类型检查 | `npm run typecheck` | 通过 | 2026-08-12 | `foodmate-ui/package.json` scripts |
| 生产构建 | `npm run build` | 通过 | 2026-08-12 | `foodmate-ui/package.json` scripts |
| Arco 清理核验 | `rg` 检查源码、依赖和入口 | 未发现 `@arco-design`、`arco-theme` 或 Arco 组件 import | 2026-08-10 | `foodmate-ui/package.json`、`src/main.tsx`、`src/` |
| 定向静态检查 | 对本轮改动文件执行 ESLint/Prettier | 通过 | 2026-08-09 | 各阶段实现记录 |

### 11.2 浏览器与响应式验收

- 已检查的主要视口为 `1440x1024`、`1366x768`、`1024x768`、`390x844`；个人中心四个子路由和管理端页面均纳入 `1440x1024` 检查。
- G3、G4 和 G5 已完成页面级 `document.body.scrollWidth === innerWidth` 检查，未发现页面级横向溢出。
- 宽表只在表格容器内横向滚动；个人中心数据隐私页导出表在 `390px` 下保留设计要求的容器内横向滚动，页面根节点仍无横向溢出；移动端筛选区、Sheet、确认弹窗和详情卡未超出视口。
- 已检查消息区、Composer、Trace rail、计划 Grid、管理表格、详情 Sheet 和管理确认弹窗的重叠/裁切风险。
- 视觉验收依据 Figma 目标节点、项目 Token 和状态矩阵；浏览器字体渲染差异不被表述为逐像素完全一致。

### 11.3 全仓验证限制

- 截至 2026-09-06，项目级 `format:check` 和全仓 `lint` 已完成一次集中收口并通过；后续新增文件仍需继续遵守同一门禁。
- 本文保留的历史阶段验证记录只代表当时的改动边界，不覆盖 Java、Python、数据库、真实云服务或生产容量验收。
- 本文的 `60/60` 指前端测试阶段记录，不替代 Java、Python、数据库、真实云服务或生产容量验收。

## 12. 未完成项与后续工作

以下内容明确不属于本次“已完成清单”：

1. **Figma 全部 Admin 实际画板的后续扩展验收**：核心 Admin 画板和 G5-6 治理画板已完成 `1440x1024`、字体、颜色、结构、同级重叠和 Prototype 回读；仍需继续按同一标准覆盖新增状态画板和真实后端数据联调，不再使用 `1064:*` 流程占位卡作为当前状态依据。
2. **iconfont 最终资源登记**：Figma 中虽有 `Iconfont Resource Registry` 节点，但实体字体包、最终字形类名、资源来源、授权范围和实际资产仍缺失，状态保持 `BLOCKED`。
3. **真实授权与数据收口**：前端普通用户/operator/admin 权限矩阵已完成代码和本地测试；服务端授权、真实接口返回、并发冲突和生产失败重试仍需接口级验收。
4. **Figma 实际画板对照**：操作审计已按真实画板 `995:1499` 完成截图和 Plugin API 回读；后续新增审计状态仍需单独补充画板级验收。
5. **真实管理数据收口**：用户详情的部分 Tab、知识库和模型用量仍依赖可选字段或明确空态；Run/Tool/SQL/Trace 已有真实查询接口，但生产级 Trace 平台、告警和大数据量治理仍未完成。
6. **shadcn 页面重构收尾**：基础组件、Token 和页面使用边界已落地，个人中心四个路由已完成本轮代码与响应式收口；仍需对后续新增状态和生产环境字体/浏览器差异执行独立验收，不能把 G6 清理等同于全量页面重构完成。
7. **iconfont 最终资源登记**：实体字体包、最终字形类名、资源来源、授权范围和实际 Figma 资产仍缺失，状态保持 `BLOCKED`。
8. **生产级前端质量验证**：生产网络、真实云模型长稳、真实 RAG、真实工具/SQL 交互、大数据量管理表格、浏览器兼容矩阵和端到端故障注入仍需独立执行。

## 13. 关联代码文件

### 13.1 基础设施与共享组件

- `foodmate-ui/src/components/ui/`
- `foodmate-ui/src/styles/tokens.css`
- `foodmate-ui/src/styles/global.css`
- `foodmate-ui/src/lib/utils.ts`
- `foodmate-ui/src/components/agent/`
- `foodmate-ui/src/components/workspace/`
- `foodmate-ui/src/layouts/WorkspaceLayout/`

### 13.2 页面与测试

- `foodmate-ui/src/pages/HomePage/`
- `foodmate-ui/src/pages/ChatPage/`
- `foodmate-ui/src/pages/DietRecordsPage/`
- `foodmate-ui/src/pages/AnalysisPage/`
- `foodmate-ui/src/pages/PlanningPage/`
- `foodmate-ui/src/pages/KnowledgePage/`
- `foodmate-ui/src/pages/ProfilePage/`
- `foodmate-ui/src/pages/AdminPage/`

## 14. 2026-08-30 Workspace Home 非设计说明面板收口

- [x] 依据 Figma 节点 `640:256`，移除前端额外渲染的“任务入口与状态”实现说明面板；活跃会话、待确认队列、快捷操作和指标卡等业务内容保持不变，Figma 设计稿未修改。
- [x] `HomePage.test.tsx` 定向测试为 `4/4`；断言覆盖说明面板不渲染，浏览器 `1440×1024` 复核标题、快捷操作、指标卡和页面无横向溢出。
- [x] 主映射已切换到有效 RGBA PNG，并登记独立 diff；结果为 `37.5817% / MAE 3.236364 / RMSE 17.110668 / maxChannelDelta 253`，画板继续为 `DIFF_REVIEW`。
- [ ] 本小点只完成 Workspace Home 的非设计说明层清理和证据收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 15. 2026-08-30 Agent Write Confirmation 当前证据收口

- [x] 重新采集 `/chat?state=write-confirmation` 当前运行状态；写入确认卡片运行时为 `305×319px`，详情区为 `265×168px`，确认写入、取消、来源和估算假设交互保持存在。
- [x] 主映射已切换为最新 RGBA PNG，原始浏览器捕获按真实 JPEG 格式登记；独立 diff 结果为 `16.8129% / MAE 1.844870 / RMSE 13.046592 / maxChannelDelta 232`，画板继续为 `DIFF_REVIEW`。
- [ ] 本小点只完成当前写入确认态证据更新，不代表该画板、105 张画板或 shadcn 全页面迁移达到像素级 `PASS`；iconfont 实体资源登记继续为 `BLOCKED`。

### 13.3 服务与契约适配

- `foodmate-ui/src/services/apiClient.ts`
- `foodmate-ui/src/services/authService.ts`
- `foodmate-ui/src/services/sessionService.ts`
- `foodmate-ui/src/services/agentRunService.ts`
- `foodmate-ui/src/services/realAgentService.ts`
- `foodmate-ui/src/services/adminService.ts`
- `foodmate-ui/src/services/types.ts`

## 14. 2026-08-10 G5-7 权限矩阵与操作审计

- [x] 普通用户访问 `/admin` 时进入 `AUTH_FORBIDDEN`，不会获得管理后台入口。
- [x] `operator` 可以进入管理后台并查看用户、运行、工具、知识库和模型用量；写操作、软删除资源恢复和操作审计入口保持锁定或无权限状态。
- [x] `admin` 与 `superadmin` 可以执行已接入的管理写操作、恢复资源并查看操作审计；高风险动作继续经过 `confirm -> submitting -> success/failed` 状态机。
- [x] 新增 `/admin?view=audit` 独立操作审计页：动作、目标、结果、时间和关键词筛选，分页，真实模式空态，以及包含 `request_id`、`trace_id`、请求摘要、前后状态、错误码和客户端信息的只读详情 Dialog。
- [x] 新增 `OperationAuditTab.test.tsx`，覆盖审计页独立渲染、结果筛选、详情 Dialog、普通用户/operator/admin/superadmin 权限解析；管理端定向测试 `7/7`，全量前端测试 `60/60`。
- [x] 浏览器实际检查 `1440x1000` 和 `390x844`：筛选、关键词查询、详情 Dialog 均可操作；移动端表格只在自身横向滚动容器内展开，页面和弹窗无横向溢出，控制台无 error/warning。

## 16. 2026-08-10 Figma Admin 画板验收补充

- [x] 已对 8 张核心 Admin 画板执行截图检查和程序化回读：Admin Overview `995:977`、User Detail `801:215`、Operation Confirmation `692:4539`、Operation Audit `995:1499`、Run Detail `797:212`、Tool Calls `797:359`、SQL Audit `797:490`、Trace `797:621`。
- [x] 所有画板尺寸为 `1440x1024`，核心颜色回读包含 `#FCFCFC`、`#FFFFFF`、`#F4F6F5`、`#333333`、`#6B7280`、`#FF7576`、`#D67676`、`#C79654`、`#5DA9B2`、`#89B27C`；同级节点没有非预期重叠，弹窗遮罩和进度填充属于设计定义的有意叠层。
- [x] 已清理 Admin 画板中的 `Inter` 混用，当前文本字体集合统一为 `Noto Sans SC` 与 `Space Mono`；已清理完全重复的 Prototype reactions，并回读关键跳转目标有效。
- [ ] iconfont 实体字体包、最终字形类名、资源来源、授权范围和实际 Figma 字形资产仍缺失，继续保持 `BLOCKED`；标准命令图标使用 Lucide。
- [ ] 真实管理数据、服务端权限授权、并发冲突和生产失败重试仍需接口级验收。

## 17. 2026-08-10 Figma 用户业务首页画板验收

- [x] 已完成 `workspace-home-v2`（`640:256`）实际画板的 Plugin API 回读和 `1440x1024` 截图检查；画板包含侧栏、主区、输入器、高频任务、营养摘要、近期任务、待确认队列和任务状态说明。
- [x] 该画板共 255 个节点，未发现可见文本零尺寸、越界、裁切或非预期可见同级重叠；营养环进度与百分比文字的覆盖属于设计定义。
- [x] 已清理该画板全部 `Inter` 遗留，字体集合为 `Noto Sans SC` 与 `Space Mono`；营养数值和百分比使用 `Space Mono Bold`，中文和界面文本使用 Noto Sans SC。
- [x] 已复核工作台到加载、空态、错误态、知识库空态、会话搜索、Agent 空态/对话、历史分页、饮食记录、摄入分析、餐食规划和输入器状态的 Prototype 目标，目标节点均存在。
- [ ] 用户业务页面 Figma 实际验收仍按 `agent-chat-v2`、`diet-records-v2`、`intake-analysis-v2`、`meal-planning-v2`、知识库和个人中心顺序继续；本项不等同于前端全部页面视觉重构完成。

## 18. 2026-08-10 Figma Agent 对话页画板验收

- [x] 已完成 `agent-chat-v2`（`640:428`）实际画板的 Plugin API 回读和 `1440x1024` 截图检查；对话区、运行轨迹区、确认卡、引用和停止运行入口均有实际内容。
- [x] 该画板共 229 个节点，`Inter` 遗留为 0；普通正文使用 Noto Sans SC，引用使用 `Space Mono Italic`，RUN ID 和毫秒耗时使用 `Space Mono Bold`。
- [x] 已修复助手英文回答被气泡裁切的问题：内容层改为填充 `560px` 气泡，正文使用 `528px` 固定宽度并按高度换行；截图确认回复、引用和确认卡完整显示。
- [x] 已复核 11 个 Agent 状态以及工作台、会话搜索、历史分页、饮食记录、摄入分析、餐食规划和停止运行 Prototype 目标，目标节点均存在。
- [ ] 用户业务页面 Figma 实际验收继续按 `diet-records-v2`、`intake-analysis-v2`、`meal-planning-v2`、知识库和个人中心推进；真实 Agent 运行闭环仍需独立验收。

## 19. 2026-08-10 Figma 饮食记录页画板验收

- [x] 已完成 `diet-records-v2`（`640:588`）实际画板的 Plugin API 回读和 `1440x1024` 截图检查；日期导航、日/周视图、营养目标、早餐/午餐分组、确认状态和详情操作均有实际内容。
- [x] 该画板共 273 个节点，`Inter` 遗留为 0；营养数值/百分比使用 `Space Mono Bold`，目标单位和 C/P/F 字段使用 `Space Mono Regular`，其余界面文本使用 Noto Sans SC。
- [x] 截图未发现可见文本零尺寸、越界、裁切或非预期重叠；营养环和详情交互覆盖层按设计定义保留。
- [x] 已复核加载、空态、错误态、Agent 空态、会话搜索、工作台、Agent 对话、历史分页、摄入分析、餐食规划和写入确认 Prototype 目标，目标节点均存在。
- [ ] 下一步继续验收 `intake-analysis-v2`；真实饮食数据接口、估算/软删除后端闭环仍未因此项关闭。

## 20. 2026-08-10 Figma 摄入分析页画板验收

- [x] 已完成 `intake-analysis-v2`（`640:773`）实际画板的 Plugin API 回读和 `1440x1024` 截图检查；筛选、三项摘要、目标对比图、Agent 洞察、制定计划和导出说明均有实际内容。
- [x] 该画板共 218 个节点，`Inter` 遗留为 0；`1,940 kcal`、`114 g`、`6 / 7 Days` 使用 `Space Mono Bold`，其余界面文本使用 Noto Sans SC。
- [x] 截图未发现可见文本零尺寸、越界、裁切或非预期重叠；加载、空态、错误态和跨页 Prototype 目标均已存在。
- [ ] 下一步继续验收 `meal-planning-v2`；真实统计、导出任务和 Agent 解读闭环仍需独立验收。

## 21. 2026-08-10 Figma 餐食规划页画板验收

- [x] 已完成 `meal-planning-v2`（`640:901`）实际画板的 Plugin API 回读和 `1440x1024` 截图检查；计划区、约束校验、餐表和购物清单均有实际内容。
- [x] 该画板共 293 个节点，`Inter` 遗留为 0；`420 kcal` 营养单位使用 `Space Mono Regular`，其余界面文本使用 Noto Sans SC。
- [x] 餐卡英文菜名的 `ENDING` 省略属于节点明确配置；截图未发现可见文本零尺寸、越界、非预期裁切或重叠。
- [x] 已复核计划列表、加载、空态、错误态、工作台、Agent 对话、历史分页、饮食记录、摄入分析和购物清单 Prototype 目标，目标节点均存在。
- [ ] 下一阶段继续检查知识库和个人中心相关实际画板；计划生成、冲突解决和真实任务接口仍需独立验收。

## 15. G6 Arco 清理与剩余边界

- [x] `foodmate-ui/package.json` 不包含 `@arco-design/web-react`；`src/main.tsx` 不包含 Arco 主题入口；源码搜索未发现 Arco 组件 import 或 `arco-theme` 引用。
- [x] shadcn/Radix 风格基础组件继续集中在 `src/components/ui`，FoodMate Token 在 `src/styles/tokens.css`，标准命令图标继续使用 Lucide；未凭空创建 iconfont 字体包。
- [ ] iconfont 实体资源登记仍为 `BLOCKED`，等待可追溯的字体包、字形类名、授权和 Figma 资产。
- [x] Figma Admin 核心实际画板已完成首轮验收：`995:977`、`801:215`、`692:4539`、`995:1499`、`797:212`、`797:359`、`797:490`、`797:621` 均为 `1440x1024`，字体统一为 `Noto Sans SC + Space Mono`，Prototype 重复反应已清理；旧 `1064:*` 占位卡不再作为当前验收依据。
- [ ] 用户业务页面的逐页 Figma 实际画板对照、真实管理数据收口和生产级浏览器兼容矩阵仍未完成。

## 22. 2026-08-10 Figma 用户知识库页画板验收

- [x] 已完成知识库默认/空态容器 `795:786`、默认内容 `795:838`、检索失败 `795:968`/`795:1145`、来源不可用 `795:1151`/`795:1328` 的 Plugin API 回读和 `1440x1024` 截图复核。
- [x] 三张主画板回读文本节点数为 96/97/97；`Inter` 遗留为 0。界面文本使用 `Noto Sans SC`，匹配百分比使用 `Space Mono Bold`，更新时间、引用数量、DOC ID、错误码、来源状态和英文引用使用对应的 `Space Mono` 字重。
- [x] 可见文本越界检查为 0；引用摘录和会话名称的 `ENDING` 截断均为节点明确配置，没有发现非预期裁切、零尺寸文本或重叠。
- [x] Prototype 目标有效数量为 `18/18`、`17/17`、`17/17`，空态、检索失败、来源不可用的返回和重试目标均存在。
- [ ] 本项只代表 Figma 画板视觉、状态层和 Prototype 验收完成；知识库真实导入、RAG 检索、ACL 过滤和引用接口已有 deterministic 本地业务闭环，但真实云服务、生产质量和 iconfont 资源登记仍未完成，后者继续保持 `BLOCKED`。

## 23. 2026-08-10 Figma 用户个人中心页画板验收

- [x] 已完成个人中心四个主画板 `806:1119`、`806:1281`、`806:1445`、`806:1585`，以及记忆空态、退出设备确认、注销确认和 12 张上传/密码/导出/注销异步状态画板的 Plugin API 回读与截图复核，共 19 张 `1440x1024` 画板。
- [x] 所有画板 `Inter` 遗留为 0；普通界面文本使用 `Noto Sans SC`，账号 ID、邮箱、IP、时间、request ID、导出 ID、设备状态和大小等技术字段使用对应的 `Space Mono` 字重，记忆引用使用 `Space Mono Italic`。
- [x] 主页面文本节点数为 `103/75/88/70`，核心状态为 `80/94/78`，异步状态为 `89/90/90/76/76/78/70/70/71/70/70/72`；可见文本越界、零尺寸和非预期裁切检查均通过。
- [x] 截图确认基本资料、记忆、安全、数据隐私、危险区域、确认框和异步状态卡无字体引发的裁切或非预期重叠；有意遮罩层按设计稿保留。
- [x] 19 张画板 Prototype 目标全部存在，状态关闭/返回、密码反馈、导出重试和注销重试路径可回读。
- [ ] 本项只代表个人中心 Figma 视觉、状态层和 Prototype 验收完成；真实头像存储、导出/注销任务、后端授权和 iconfont 资源登记仍未完成。

## 24. 2026-08-11 Figma 用户流程状态画板补充验收

以下内容是 Figma 设计验收记录，不计入“前端代码已完成”统计。它用于约束后续按设计稿进行的页面重构，不能替代真实接口、后端授权、异步任务或浏览器生产环境验收。

| 流程画板 | 节点 | 文本节点 | Prototype 反应 | 结果 |
|---|---|---:|---:|---|
| `workspace-home-input-states` | `1015:4` | 101 | 19 | 通过 |
| `meal-plan-conflict` | `692:3375` | 93 | 17 | 通过 |
| `agent-chat-session-actions` | `806:212` | 85 | 20 | 通过 |
| `meal-plan-shopping-list` | `692:3569` | 82 | 17 | 通过 |
| `agent-chat-archived` / `agent-chat-trash` | `806:662` / `806:887` | 82 / 82 | 18 / 18 | 通过 |
| `agent-chat-running-stop` | `1013:653` | 82 | 20 | 通过 |
| `agent-chat-renamed` | `806:438` | 81 | 18 | 通过 |
| `agent-chat-history-page-2` / `page-3` | `740:212` / `740:426` | 75 / 75 | 16 / 17 | 通过 |
| `agent-chat-search-results` | `742:212` | 75 | 16 | 通过 |
| `intake-analysis-empty` / `loading` / `error` | `692:2026` / `692:1901` / `692:2139` | 61 / 56 / 54 | 16 / 16 / 16 | 通过 |
| `meal-plan-generating` | `692:3746` | 53 | 18 | 通过 |
| `meal-plan-wizard-step1` / `step2` / `step3` | `692:2801` / `692:2934` / `692:3078` | 79 / 78 / 79 | 18 / 18 / 19 | 通过 |
| `meal-plan-list` | `692:2662` | 79 | 20 | 通过 |

- [x] 19 张画板全部为 `1440x1024`，合计 `1452` 个文本节点和 `337` 条 Prototype 反应；`Inter` 遗留为 `0`。
- [x] 根画板边界内的可见文本越界和零尺寸检查均为 `0`；9 张剩余截图与此前 10 张截图合计完成本组 19 张画板的视觉复核，没有发现非预期裁切、可见同级重叠或状态层遮挡错误。
- [x] Prototype 目标全部有效，覆盖输入器、会话归档/恢复/重命名、停止运行、历史分页/检索、摄入分析三态、餐食规划冲突/生成中/向导/列表和购物清单。
- [ ] 该记录不代表对应页面已经完成前端代码迁移；后续仍按 Figma 逐页实现颜色、字体、样式、Token、状态和交互。真实 Agent、统计、餐食生成、会话管理和后端任务闭环仍需独立验收；iconfont 资源登记继续为 `BLOCKED`。

## 25. 2026-08-11 Figma Agent 对话页代码迁移

- [x] 已完成 `agent-chat-v2`（`640:428`）到 `foodmate-ui/src/pages/ChatPage/` 的首轮完整视觉迁移；实现边界以 Figma 实测结构为准：`260px` 侧栏、`860px` 对话区、`320px` Trace rail、`68px` 顶栏、`45px` 状态条、`799px` 消息区和 `112px` Composer。
- [x] 默认 mock 场景已与画板内容对齐：野生三文鱼/藜麦回答、USDA 引用、周二午餐写入确认、四条运行轨迹步骤、RUN ID 和毫秒耗时；Agent 头像、顶部用户头像和消息头像使用从 Figma 节点下载并登记到本地的图片资源。
- [x] 保留原有真实模式的会话读取、消息发送、AgentRun SSE、停止、断点恢复、预算追加、错误状态和原始 JSON 切换；确认卡提交/取消仍可操作。
- [x] 浏览器实际验收：`1440x1024` 关键几何与 Figma 对齐；`390x844` 无页面级横向溢出，Trace rail/桌面侧栏按响应式规则隐藏，消息操作卡在移动端自然增高避免文本重叠。
- [x] 自动化验证：前端全量测试 `60/60` 通过；`npm run typecheck` 通过；桌面交互 smoke test 覆盖 JSON 切换、确认提交和保存结果保留。
- [ ] 本项只表示 Chat 页面代码迁移边界完成，不表示真实 Agent 数据、生产字体加载、iconfont 实体登记或全部用户页面迁移完成；`diet-records-v2` 已在本轮完成，下一页继续 `intake-analysis-v2`。

## 32. 2026-08-12 页面级样式 Token 收口

本节记录本轮前端剩余样式收口。改动只涉及前端视觉 Token 和 CSS 映射，不改变后端接口、业务数据来源或 Agent 运行闭环。

- [x] 共享布局与领域组件已迁移基础背景、容器、文字、焦点环、错误状态和控件圆角：`WorkspaceLayout`、`Composer`、`SidebarSessionList`、`AgentStatusStrip`、`ToolTraceItem`、`TaskCard`、`EmptyState`、`MetricCard`。
- [x] 用户页面已迁移 Figma 已确认的公共基础值：`HomePage`、`ChatPage`、`DietRecordsPage`、`AnalysisPage`、`PlanningPage`、`KnowledgePage`；页面专属图表色、渐变、营养环轨道和状态色保持独立，未做机械合并。
- [x] 登录页和管理端已完成壳层、导航、顶部栏、基础弹窗、权限提示和通用字体入口收口；管理端危险色、权限色、审计状态和表格字段仍按语义独立保留。
- [x] 新增共享视觉 Token：hover surface、strong hover surface、focus ring、control hover、avatar surface/ink、error surface/border；信息色引用已统一到已定义的 `--fm-color-info`。
- [x] 验证结果：`npm run test` 为 16 个测试文件、61/61；`npm run typecheck` 通过；`npm run build` 通过；改动范围 Prettier 通过；`git diff --check` 通过。
- [x] 浏览器 smoke 已实际访问 `/`、`/chat`、`/analysis`、`/analysis?view=records`、`/planning`、`/knowledge`、`/login` 和 `/admin`；稳定渲染后 `document.body.scrollWidth === innerWidth`，本地页面无控制台 error。宿主环境的 Statsig 网络超时不属于本地前端请求。
- [ ] 本轮不包含后端闭环、真实生产数据、真实字体文件加载、Figma 全量逐页像素级复核或 iconfont 实体登记；iconfont 继续保持 `BLOCKED`，没有创建虚构字体包或类名。

## 33. 2026-08-12 shadcn 基础设施与个人中心收口

本节记录个人中心迁移后的基础设施收口结果。视觉实现仍以 Figma 个人中心节点 `806:1119`、`806:1281`、`806:1445`、`806:1585` 为来源，不使用旧前端样式反推设计。

- [x] `tokens.css` 已补充 Figma 语义 Token：成功、警告、信息、危险、紫色状态及其背景/边框，`4/8/12/16/20/24/32/40/48px` 间距刻度，`8/12/999px` 控件/容器/胶囊圆角，raised/overlay 阴影和 `Noto Sans SC`/`Space Mono` 字体入口。
- [x] shadcn 基础组件已按 Token 收口：`Button` 使用控件圆角，`Card` 使用容器圆角，`Input`、`Textarea`、`SelectTrigger` 使用控件圆角；`primary-foreground`、`destructive` 和全局焦点色与 Figma 个人中心保存/危险操作基准一致。
- [x] `ProfilePage` 的页面字体改为 `var(--font-ui)`；移动端设备授权信息允许自然换行，避免 IP/日期文本超出自身布局；导出宽表继续限制在表格容器内滚动。
- [x] 浏览器复验四个路由：`/profile`、`/profile/memories`、`/profile/security`、`/profile/data` 在 `1440x1024`、`1366x768`、`1024x768`、`390x844` 下均无页面级横向溢出；`390x844` 下安全页内部文本溢出为 0，数据页仅保留导出表容器内部 `620px` 宽滚动。
- [x] 自动化复验：`npm run test` 为 16 个测试文件、61/61；`npm run typecheck` 和 `npm run build` 通过；`git diff --check` 通过。本轮格式检查只针对改动边界，项目全量 `format:check` 仍受既有无关文件阻塞。
- [ ] iconfont 仍为 `BLOCKED`：没有实体字体包、最终字形类名、来源、授权范围和实际 Figma 字形资产时，不创建字体文件、不登记虚构类名；标准命令图标继续使用 Lucide。
- [ ] 真实头像对象存储、导出任务、注销 Worker、后端授权、生产字体加载和生产浏览器兼容性仍不属于本轮已完成范围。

## 33. 2026-08-12 共享 shadcn primitives Token 收口

本节记录共享基础组件的 Token 收口结果。组件行为、Radix 可访问性和业务交互契约保持不变；视觉来源仍为 Figma Foundations 已核实的 `8px/12px/999px` 圆角与 raised/overlay 阴影契约。

- [x] `Alert`、`Card`、`Dialog`、`Sheet`、`DropdownMenu`、`SelectContent` 使用容器圆角 `--radius-container`；`Button`、`Input`、`Textarea`、`Tabs`、菜单项、关闭按钮、`Tooltip` 和 `Skeleton` 使用控件圆角 `--radius-control`。
- [x] `Badge`、`Progress` 使用胶囊圆角 `--radius-pill`；`Card`、按钮、Tooltip、Dialog、Sheet、Select 和 Dropdown 浮层的阴影已映射到 Figma `--elevation-raised` 或 `--elevation-overlay`。
- [x] 新增基础组件 Token 契约测试，锁定控件、容器和胶囊圆角类名；共享组件定向 ESLint、Prettier 均通过。
- [x] 验证结果：全量前端测试 `16` 个测试文件、`61/61`；`npm run typecheck`、`npm run build`、共享组件定向 Prettier/ESLint 和 `git diff --check` 通过。
- [x] 浏览器 smoke：个人中心四路由可正常切换，桌面根节点无横向溢出，Dialog/Sheet/Dropdown/Tooltip 仍复用现有交互链路，控制台无 error。
- [ ] 该项不代表全量页面 CSS 已完全消除历史硬编码色值；后续逐页对照 Figma 时继续迁移页面级色值和状态样式。iconfont 实体资源仍为 `BLOCKED`。

## 34. 2026-08-12 生产字体、真实记忆页与 Figma 全量验收

以下为当前实现状态的最新汇总，用于覆盖前文阶段性记录中的临时待办。

- [x] 生产字体依赖已登记：`@fontsource/noto-sans-sc@5.3.0`、`@fontsource/space-mono@5.3.0`、`@fontsource/montserrat@5.3.0`；入口加载 Noto Sans SC `400/500/700/900`、Space Mono `400/italic/700` 和 Montserrat `900`。`npm run build` 已确认生产产物包含真实 `.woff2` 字体文件。
- [x] 真实模式的个人中心记忆页已使用已有后端 `/api/memories` 的列表、确认、编辑和逻辑删除接口；请求字段与 `MemoryUpdateRequest.memoryValue` 对齐，并兼容 Java record 默认 camelCase 响应和 snake_case 响应。
- [x] 记忆页真实数据展示已完成后端类型映射：`preference/constraint/routine/plan/allergy/goal/unit/meal_type` 等类型转换为产品中文分类；时间、确认状态、加载失败和操作失败均有可读中文反馈。
- [x] Figma 全文件回读完成：文件 `MX18RZCfAmgprNzxItkHUH` 的 `🎨 :: Design` 有 105 张顶层画板、19,985 个节点、7,102 个文本节点；最终可见文本越界 `0`、零尺寸文本 `0`、空顶层画板 `0`。
- [x] Figma 全量 Prototype 回读完成：1,940 条 reaction，所有带目标 reaction 的目标节点有效；Figma 截图请求 105/105 成功。长英文消息气泡和 1366x768 底部侧栏的两个真实布局问题已在 Figma 中修正并复核。
- [x] Figma 设计系统实体状态已记录：`02 Components - User` 24 个 Component Set、`03 Components - Admin` 14 个 Component Set、`01 Foundations` 5 组 Variables 和 8 个本地样式；`🎨 :: Design` 页面自身没有 Component Set，不再与全文件状态混淆。
- [ ] 尚未完成前端运行页面与 Figma 105 张画板的自动化逐页 pixel diff 报告。现有浏览器截图、Figma PNG 抽查和几何测量属于视觉验收证据，但不等价于全量像素差异报告。
- [ ] iconfont 仍为 `BLOCKED`：`Iconfont Resource Registry` 节点 `1065:2` 已登记所需字段和 fallback 规则，但缺少实体字体包、CSS 映射、来源 URL、许可证和真实 Figma 字形映射；标准命令图标继续使用 Lucide。
- [ ] 其他用户业务页和管理页的真实数据、异步任务、RAG、统计、餐食规划生成及后端授权仍按已有后端契约单独推进，不在本节扩大为“后端闭环完成”。

## 38. 2026-08-13 餐食规划三态 Figma 映射与验收

- [x] Loading 画板 `692:2256` 已对应 `/planning?state=loading`；计划区与右侧校验/购物骨架均已在当前 `PlanningPage` 中渲染。
- [x] Empty 画板 `692:2446` 已对应 `/planning?state=empty`；空态创建入口已实际进入 Agent 创建路径。
- [x] Error 画板 `692:2542` 已对应 `/planning?state=error`；错误码、重试和返回工作台入口已实际渲染，重试恢复默认计划态。
- [x] `PlanningPage.test.tsx` 已覆盖三态渲染/恢复和既有列表、向导、冲突、购物清单交互；浏览器三态 `1440×1024` 无页面级横向溢出。
- [x] Figma/浏览器证据及 RGBA 归一化截图已登记到 `foodmate-ui/.qa/figma-pixel-acceptance/`；自动 diff 为 Loading `26.74% / RMSE 13.19`、Empty `16.98% / RMSE 16.88`、Error 最新 `13.2237% / RMSE 9.91903`，全部为 `DIFF_REVIEW`。
- [ ] 本项不代表真实计划读取/生成、冲突解决、购物清单持久化、后端任务闭环或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 39. 2026-08-13 餐食规划流程状态 Figma 映射与验收

- [x] 已登记向导步骤 1/2/3：`692:2801`、`692:2934`、`692:3078`，入口分别为 `/planning?state=wizard-step1|wizard-step2|wizard-step3`。
- [x] 已登记冲突解决、购物清单、生成中和计划列表：`692:3375`、`692:3569`、`692:3746`、`692:2662`，入口分别为 `/planning?state=conflict|shopping-list|generating|list`。
- [x] `PlanningPage.test.tsx` 已实际覆盖向导推进/取消生成、冲突应用和购物清单导出反馈；七个状态的 `1440×1024` 浏览器截图均无页面级横向溢出。
- [x] 自动 diff 结果全部为 `DIFF_REVIEW`：向导步骤 1/2/3 为 `40.49%/21.93`、`42.86%/22.56`、`43.15%/23.92`；冲突 `37.28%/25.12`；购物清单 `24.35%/17.23`；生成中 `13.69%/16.84`；计划列表 `43.22%/19.78`。
- [ ] 本项不代表真实计划生成、冲突解决、购物清单持久化、任务状态或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 37. 2026-08-13 认证页面与异常状态迁移

- [x] 注册页 680:216 已迁移到 /register，实现用户名、邮箱、密码、确认密码、密码规则、注册提交和返回登录；代码入口为 src/pages/RegisterPage/RegisterPage.tsx。
- [x] 找回密码页 680:275 已迁移到 /forgot-password，实现邮箱提交、邮件已发送双卡状态、成功状态提示和返回登录；代码入口为 src/pages/ForgotPasswordPage/ForgotPasswordPage.tsx。
- [x] 重置密码页 680:307 已迁移到 /reset-password，实现双密码字段、密码强度条、缺失 token 防护、真实模式确认重置和返回登录；代码入口为 src/pages/ResetPasswordPage/ResetPasswordPage.tsx。
- [x] 登录页保留 Figma 647:214 默认页面和真实登录 service，同时通过 query state 建立 6 个异常/异步画板入口：提交中 680:408、字段错误 680:445、凭证错误 680:483、账号锁定 680:524、账号禁用 680:564、服务不可用 680:606。
- [x] Token 状态页 680:738、680:757、680:776 已迁移到 /token-status?state=invalid|expired|used，代码入口为 src/pages/TokenStatusPage/TokenStatusPage.tsx；重新发送入口回到真实密码找回页面，不伪造邮件发送结果。
- [x] 认证页面统一使用 Figma 已核实的粉色底、薄荷色斜切层、Noto Sans SC、Montserrat Black 品牌字标、字段/按钮尺寸和状态色；标准图标继续使用 Lucide，iconfont 仍保持 BLOCKED。
- [x] 浏览器行为验收覆盖 1440x900 的 10 个登录/Token 状态和 390x844 注册页；10 个桌面状态 scrollWidth === clientWidth，移动注册页 4 个输入框均完整落在视口内。
- [x] 认证回归测试当前为 18 个测试文件、74/74；新增状态契约覆盖提交中、字段错误、凭证错误、锁定、禁用和服务不可用。
- [ ] 新增状态的 PNG 对照全部保留 DIFF_REVIEW，不能因为同尺寸或人工接近而写成 PASS；证据位于 foodmate-ui/.qa/figma-pixel-acceptance/login-*-browser-rgba.png 和 token-*-browser-rgba.png。
- [ ] shadcn 基础设施已经存在并用于认证 Button/Input，但全量页面逐页 shadcn 重构仍是后续任务；后续继续按 Figma 页面顺序迁移，不能把本节认证完成扩大为全量前端完成。

## 35. 2026-08-12 生产字体包、像素验收和 iconfont 登记收口

- [x] 登录页按 Figma `647:214` 的真实几何重新校准：表单 `400x471`、`x=490`、`y=214.5`；品牌区 `163px`、字段与忘记密码区 `156px`、登录按钮 `52px`、分隔区 `56px`、注册行 `44px`。
- [x] 登录页补齐 Figma 画板中可见的邮箱、密码、显示密码、忘记密码、登录、或者和注册节点；真实登录、注册和密码找回 service 入口保持不变。
- [x] 生产字体包已由 `@fontsource/montserrat@5.3.0`、`@fontsource/noto-sans-sc@5.3.0`、`@fontsource/space-mono@5.3.0` 提供，入口已加载所需字重；接入 iconfont 前不与文字字体混用。
- [x] 新增 [Figma前端像素级验收报告](./Figma前端像素级验收报告.md)，记录 Design 页 105 张画板、19,985 个节点、1,940 条 reaction、无效目标 0、截图 105/105，以及 105 个前端映射页/状态的可重复 diff 证据。
- [x] 当前 105 个映射页/状态均明确为 `DIFF_REVIEW`；登录页最新同尺寸比较为 `1440x900`、差异比例 `99.19%`、RMSE `7.54`；Admin User Detail (`801:215`) 比较为 `1440x1024`、差异比例 `22.36%`、RMSE `17.62`；餐食规划和 Agent 状态指标见验收报告，不能伪报 PASS；当前 `UNMAPPED=0`。
- [x] Admin User Detail 已按 Figma `801:215` 完成 `/admin/users` 视觉映射：四行用户 fixture、筛选条、固定列宽表格、分页、五个详情 Tab、详情说明卡、角色/状态颜色和头像资源均有实际浏览器截图证据；真实模式的用户加载、状态变更、撤销会话接口和权限状态机保持不变。
- [x] 新增 [iconfont实体资源登记](../设计/iconfont实体资源登记.md)，登记实体包、CSS 映射、来源、许可证、glyph、Unicode、Figma 映射和无障碍字段；当前状态保持 `BLOCKED`。
- [x] 已有后端闭环边界继续保持真实模式：认证、用户资料、记忆、会话/消息、AgentRun SSE、取消、预算追加、checkpoint 恢复，以及管理概览、工具状态和资源恢复使用实际 API；M1-5 已有本地饮食记录、分析、餐食计划生命周期 Controller，但前端页面尚未全部接入，继续明确区分 mock/空态与真实结果，不伪造完整业务闭环。

## 36. 2026-08-12 Admin Operation Status 视觉与状态收口

- [x] 已按 Figma `692:4319`、`692:4539`、`692:4766`、`692:4995`、`692:5207` 对齐工具注册表的无权限、确认、提交中、成功和失败五个状态。
- [x] 无权限与成功反馈已提升到工作区顶层，使用 Figma 的 `48px` 单行信息横幅；Operator 工具操作按钮显示锁图标并保持禁用，不触发伪写操作。
- [x] 确认弹窗保持 `480px` 宽、`32px` 内边距、`16px` 圆角和 Figma 警告色；提交中保留“确认停用工具”标题，补齐 `4px` 红色进度条、服务集群同步文案和禁用操作按钮；失败态补齐 `GATEWAY_TIMEOUT`、集群未响应、配置未改变、错误码和请求 ID。
- [x] 真实后端调用、统一确认、提交中、成功/失败状态机和操作审计写入保持不变；失败仍只能由用户明确点击“重新尝试”触发，不自动重放。
- [x] `AdminOperationStatus.test.tsx` 与 `ToolsRegistryTab.test.tsx` 定向测试 `6/6`；前端全量测试 `16` 个测试文件、`61/61`；`npm run typecheck`、`npm run build`、改动边界 Prettier 和 `git diff --check` 通过；浏览器已实际检查默认表格、确认弹窗、提交中弹窗和成功横幅。

## 40. 2026-08-14 Agent 空态逐页迁移

- [x] Figma `agent-empty` 节点 `687:219` 已迁移到 `/chat?state=empty`；实现无 Trace 右栏的居中引导、三张推荐问题卡和底部 Composer。
- [x] 推荐问题卡使用 Figma 对应的标题、说明、橙色图标底、边框、圆角和间距；标准命令图标继续使用 Lucide，未写入虚构 iconfont glyph。
- [x] 推荐问题卡可填充 Composer；发送后实际进入 `/chat?prompt=...`，该 query 只用于前端验收和进入既有 Agent 流程，不代表后端运行已经完成。
- [x] 桌面 `1440×1024` 和移动 `390×844` 已完成浏览器实测；桌面 Composer 为 `112px`，移动端通过明确内容区高度修复底部裁切，页面无横向溢出。
- [x] 证据已登记：`foodmate-ui/.qa/figma-pixel-acceptance/chat-agent-empty-figma.png`、浏览器原始帧、RGBA 归一化帧；同尺寸 diff 为 `15.7739% / RMSE 14.7325 / DIFF_REVIEW`。
- [x] `ChatPage` 定向测试 `2/2`、`npm run typecheck`、本次触及文件 Prettier 和 `git diff --check` 通过。
- [ ] 全量 `npm run format:check` 仍受 8 个无关既有文件阻断；本项不扩大到无关格式清理，也不代表全量 Figma 105 张画板已完成像素级 diff。
- [ ] iconfont 实体资源仍为 `BLOCKED`；shadcn/Radix 基础设施继续作为后续剩余页面逐页重构的实现约束，不能把本项视为全量迁移完成。

## 41. 2026-08-14 Agent Planning 状态迁移

- [x] Figma 节点 `687:342` 已对应 `/chat?state=planning`，实现隐藏 Trace 右栏并展示 Planning 状态条、用户消息、规划步骤卡和运行中 Composer。
- [x] 桌面主区为 `1180px`，状态条为 `45px`，Composer 为 `y=912/h=112`；移动 `390×844` 无页面级横向溢出。
- [x] Planning 输入框按设计禁用，停止按钮保持可用；该状态 query 只用于前端视觉/交互验收，不声明真实 AgentRun 完成。
- [x] 已登记 Figma、桌面/移动浏览器和 RGBA 证据；自动 diff `14.9956% / RMSE 14.0682 / DIFF_REVIEW`。
- [x] `ChatPage` 定向测试 `3/3`、`npm run typecheck`、本次触及文件 Prettier 和 `git diff --check` 已通过。
- [ ] iconfont 实体资源继续为 `BLOCKED`；shadcn/Radix 只作为实现底座，不替代 Figma 逐页颜色、字体、样式和像素验收。

## 42. 2026-08-14 Agent Tool Executing 状态迁移

- [x] Figma 节点 `687:475` 已对应 `/chat?state=tool-executing`；共享壳层使用 `260px` 侧栏、`860px` 对话区和 `320px` Trace rail。
- [x] 已实现 `Planning ✓ / Retrieving ✓ / Executing ● / Composing ○` 状态条、三行工具执行卡、Trace Run/Tool 状态以及禁用输入/可用停止按钮。
- [x] Figma 关键几何已实测：工具气泡 `181×250`、用户气泡 `228×49`、Trace body `y=107`、首个 Trace 卡 `y=135`、Composer `y=912/h=112`。
- [x] 桌面与移动浏览器证据、RGBA 归一化证据已写入 `foodmate-ui/.qa/figma-pixel-acceptance/`；移动 `390px` 无横向溢出且 Trace 自动隐藏。
- [x] `ChatPage` 定向测试 `4/4`、`npm run typecheck`、本次触及文件 Prettier 和 `git diff --check` 已通过。
- [ ] 自动 diff `50.5259% / RMSE 23.1703`，按规则保持 `DIFF_REVIEW`；不将 fixture 视为真实后端闭环。
- [ ] iconfont 实体资源仍为 `BLOCKED`，继续使用 Lucide；下一步迁移 `agent-awaiting-clarification`。

## 43. 2026-08-15 Agent Awaiting Clarification 完成记录

- [x] 新增 `/chat?state=awaiting-clarification` 前端状态 fixture，对应 Figma 节点 `687:642` 和 `1440×1024` 画板。
- [x] 实现用户消息、Planning 状态条、澄清卡两个选项和可输入 Composer；选项具备 `aria-pressed` 语义及选中态回调。
- [x] 共享 `WorkspaceLayout` 支持 Figma fixture 的头像、用户名、用户 ID 和顶部导航覆盖；override 优先级高于当前认证用户，确保 `Anddy / 1234567` 与设计稿一致。
- [x] 已登记三个真实本地头像资源：`sidebar-avatar.png`、`topbar-avatar.png`、`message-avatar.png`。
- [x] 浏览器实测桌面 `1440×1024`、移动 `390×844` 均无页面级横向溢出；桌面澄清卡 `222×193`，Composer `1180×112`。
- [x] `ChatPage.test.tsx` 定向测试 `5/5`，`npm run typecheck` 通过；本轮截图证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/`。
- [x] 自动 diff 结果如实记录为桌面 `DIFF_REVIEW`；移动因缺少同尺寸 Figma 参考图记录 `SIZE_MISMATCH`。
- [ ] 本记录不表示真实 AgentRun、SSE、澄清提交、后端任务或业务数据闭环完成。
- [ ] iconfont 实体资源仍因缺少真实字体包、CSS、来源和许可证保持 `BLOCKED`，不得创建虚构 glyph 映射。

## 44. 2026-08-15 默认头像资源替换

- [x] 登记用户提供的男性和女性 SVG 默认头像，分别为 `foodmate-ui/public/assets/avatars/default-male.svg` 与 `foodmate-ui/public/assets/avatars/default-female.svg`。
- [x] 新增统一头像解析规则：已有上传头像优先，明确性别映射到对应 SVG；未设置性别的管理端用户继续保留首字母，不擅自猜测性别。
- [x] 工作区、普通 Agent 消息、Planning/Tool Executing 状态、知识库和个人资料已切换到默认头像解析；Figma 专用 fixture 头像保持独立。
- [x] 新增头像策略单测；全量前端测试 `19/19` 个测试文件、`86/86` 个用例通过，生产构建通过。
## 45. 2026-08-15 剩余前端重构与验收收口

- [x] Agent 六个剩余 query fixture 已落地：`write-confirmation`、`budget-limit`、`tool-failed-retryable`、`safety-degraded`、`user-cancelled`、`sse-reconnecting`。
- [x] Agent SSE 前端契约已补充 `connecting`、`connected`、`reconnecting`、`closed`、`exhausted`，并记录 `attempt`、`maxAttempts`、`lastEventId`；终态关闭连接，组件卸载、会话切换和用户取消关闭当前连接。
- [x] `sse_event_id`、SSE `Last-Event-ID` 查询续接、重连上限、终态关闭、预算追加、写入确认/取消、失败重试和降级追问均有定向测试或代码侧边界。
- [x] 105 个 Figma 画板已建立完整映射；当前引用的 105 个 Figma PNG 与 105 个浏览器 PNG 均通过文件头和尺寸校验，汇总为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/`。
- [x] 2026-08-18 运行时复核完成：`geometryPass=105/105`、`textPass=105/105`、`dprPass=105/105`、字体 `loaded=105/105`；机器结果见 `figma-105-runtime-checks.json`，不替代人工视觉复核。
- [x] 全量前端验证曾在此前变更完成时通过：`npm run test`（20 个测试文件、94 个用例）、`npm run typecheck`、`npm run build`、`git diff --check`；本次移除无引用兼容层后需重新执行。
- [x] `UNMAPPED=0`；105 个画板均有浏览器 fixture/路由证据，105/105 已完成人工视觉复核登记；复核结论均保留为 `DIFF_REVIEW`，因为仍存在需要修正或保留的可见差异。
- [ ] `PASS` 仍为 `0`：自动 diff、几何、文字、DPR 和人工复核均已有证据，但 105 项尚未同时满足像素通过阈值，不能关闭为 `PASS`。
- [ ] iconfont 实体包、CSS 映射、来源、许可证、glyph-Figma 映射和生产验收仍缺失，状态继续为 `BLOCKED`；标准命令图标继续使用 Lucide。
- [x] `AdminPrimitives` 已确认无业务代码和测试引用，2026-08-18 已移除；Admin 页继续直接使用 `src/components/ui` 的 shadcn/Radix 基础组件。
- [ ] shadcn 基础设施已可用，但其它页面的全量逐页迁移仍需持续按 Figma 完成；不能把本轮状态 fixture 和证据收口写成全量页面重构完成。

## 46. 2026-08-22 业务页 shadcn 控件迁移验收

本节只记录页面控件基础设施迁移，不扩大为 Figma 全量像素验收、iconfont 接入或后端闭环完成。视觉尺寸仍以对应 Figma 画板和现有页面语义 CSS 为准，shadcn `Button` 只提供可访问行为与基础控件能力。

- [x] Planning 流程导航、计划列表 Tab、过敏源 Chip、添加过敏源入口和计划卡片更多操作已统一使用 `src/components/ui/button.tsx` 的 `Button`；对应提交为 `bdae711`、`1a255b6` 和 `7263706`。
- [x] Profile 基本资料、记忆筛选、图标操作、安全活动和数据导出文本操作已统一使用 shadcn `Button`；对应提交为 `ed78a17`、`6066c5b`。
- [x] `MealPlanningFlow.tsx` 的定向 Prettier 格式已收口，提交为 `3bd0a10`；Planning 定向测试 `6/6`、Profile 定向测试 `10/10` 通过。
- [x] 全前端业务代码扫描未发现原生 `<button>` 或 `AdminPrimitives` 直接依赖；Knowledge、Admin 页面现有控件均使用 `src/components/ui` 基础组件。
- [x] 全量前端验证：`npm run test` 为 `25` 个测试文件、`134/134`；`npm run typecheck` 通过；`npm run build` 通过；本次提交边界的 `git diff --check` 通过。
- [x] 浏览器 smoke 实测 Planning `/planning?state=wizard-step2`：过敏源 Chip 实际高度 `26px`，步骤导航 `32px`，无页面级横向溢出或可见裁切；Profile 基本资料的过敏原标签实际高度 `32px`。
- [x] Planning 复选控件已迁移：膳食偏好、购物清单、全选和侧栏购物项目使用 `src/components/ui/checkbox.tsx`；Radix Checkbox/RadioGroup 依赖和基础组件提交为 `87b7e15`，复选样式提交为 `13608c1`。
- [x] Planning 冲突解决的 4 个 radio 已迁移为两个可访问 `RadioGroup`，保留原有选择状态和 Figma 圆点样式；提交为 `1ace282`。浏览器 `/planning?state=conflict` 已确认两个 `radiogroup`、默认选中项和切换状态。
- [x] Planning 菜系原生 `select` 已迁移为 shadcn `Select`，提交为 `8edd9e8`；定向测试覆盖触发器、三个 option 和受控值变更。
- [x] Chat 历史写入确认卡的两个原生 radio 已迁移为 shadcn `RadioGroup`，提交为 `3eac7ff`；浏览器 `/chat?state=history-page-2` 已确认默认选中、切换状态和卡片布局。
- [x] 页面级原生 `<button>`、`<select>`、`type="radio"`、`type="checkbox"` 均为 `0`，`AdminPrimitives` 直接依赖为 `0`；Home、Profile、Admin Knowledge 保留的 3 个 `type="file"` 仅用于浏览器文件选择，不替代为伪造按钮。
- [x] 本轮全量验证：`npm run test` 为 `25` 个测试文件、`136/136`；`npm run typecheck`、`npm run build`、`git diff --check` 均通过。Planning 定向测试 `7/7`，Chat 定向测试 `25/25`。
- [x] 2026-08-22 收口管理后台格式门禁：`npm run format:check` 全部通过；本次只格式化 `AdminPage/tabs/OverviewTab.tsx`、`AdminPage/tabs/RunsTab.tsx` 和 `services/adminService.ts`，提交为 `383c6d9`。
- [ ] Figma 105 张画板的汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`；本次浏览器 smoke 不改变该结论。iconfont 实体包、CSS 映射、来源和许可证仍为 `BLOCKED`。

## 47. 2026-08-22 剩余前端重构门禁复核

- [x] `npm run test`：26 个测试文件、147 个测试全部通过；Admin 定向测试：9 个测试文件、32 个测试全部通过。
- [x] `npm run typecheck`、`npm run build` 和 `npm run format:check` 通过；`git diff --check -- foodmate-ui` 通过。
- [x] 本次复核没有扩大后端真实闭环范围；Agent/SSE 真实后端仍按计划不作为本轮 fixture 完成标准。
- [ ] Figma 全量像素验收仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，需要继续修正逐页视觉差异后才能产生 `PASS`。

## 48. 2026-08-23 Planning 当前画板证据与窗口装饰检查

- [x] Planning 默认态 `640:901` `/planning?state=v2` 已修正默认目标显示为 `2,400千卡`，购物清单侧栏复选框改为独立 shadcn Checkbox 与文本行结构，保留 Figma 所需的 `14×14px` 空方框几何。
- [x] 当前浏览器证据：`1440×1024`、字体 `loaded`、页面无横向溢出；目标文案和购物清单复选框均通过运行时检查。
- [x] 前端左上角红、黄、绿窗口装饰点数量为 `0`，没有需要删除的对应前端内容；首页 `sessionDot` 是活跃会话的业务状态点，不作窗口装饰处理；设计稿未修改。
- [x] 当前 Figma/浏览器 PNG 与 diff 证据已登记到 `foodmate-ui/.qa/figma-pixel-acceptance/`：`23.8253% / MAE 3.0472 / RMSE 16.8015`。
- [ ] 当前画板仍为 `DIFF_REVIEW`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，iconfont 实体包、CSS 映射、来源和许可证仍为 `BLOCKED`。
- [ ] shadcn 基础设施和控件迁移已可用，但全量页面仍未完成逐页 Figma 对照迁移。
- [ ] iconfont 实体字体包、CSS/Unicode 映射、来源、版本、SHA-256、许可证和 glyph-Figma 映射仍未提供，继续保持 `BLOCKED`，标准命令图标继续使用 Lucide。

## 51. 2026-08-22 Admin Run 详情字段补齐

- [x] 重新读取实时 Figma 节点 `797:212` 的 `get_design_context`，确认 `admin-run::detail-fields` 为当前 Run 详情设计的一部分。
- [x] `/admin?state=run-detail` 已补齐“Run 详情字段”面板：父/接续 Run、开始时间、终止原因、事件诊断、当前阶段、结果类型、Token 用量、成本、revision、request/trace/dispatch ID 和脱敏说明。
- [x] Trace fixture 不复用 Run 专属详情字段，新增 Admin 定向测试覆盖 Run 面板存在、Trace 面板不存在以及关键字段文本。
- [x] 浏览器验收：`1440×1024`、面板 `1132×300`、时间线和详情面板无重叠，页面无横向或纵向溢出。
- [x] 最新证据已登记：`admin-run-detail-figma-current.png`、`admin-run-detail-browser-current.png`、`admin-run-detail-current-diff.json`；自动 diff 为 `29.3664% / RMSE 17.8805`，继续保持 `DIFF_REVIEW`。
- [ ] Admin Run 详情仍存在 shell、侧栏身份、间距和字体等可见差异，不能标记 `PASS`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`。

## 52. 2026-08-22 Admin Tool Calls 现状证据校正

- [x] 重新读取实时 Figma 节点 `797:359`，确认当前设计包含治理 Tab、工具筛选、调用摘要表、风险/状态、脱敏 payload 和 Tool Calls 详情字段。
- [x] 对照当前 `/admin?state=tool-calls` 浏览器实现，以上结构均已存在；本轮不重复创建已有组件和业务字段。
- [x] 浏览器验收：`1440×1024`、Tab `1132×32`、筛选区 `1132×32`、调用表 `1132×102`、payload 卡片 `1132×321.4`、详情面板 `1132×250`，页面无横向或纵向溢出。
- [x] 最新证据已登记：`admin-tool-calls-figma-current.png`、`admin-tool-calls-browser-current.png`、`admin-tool-calls-current-diff.json`；自动 diff 为 `24.4423% / RMSE 17.0488`，继续保持 `DIFF_REVIEW`。
- [x] 修正旧人工复核结论：不再记录“浏览器缺少 Tab、筛选、调用表和 source note”；剩余问题集中在 shell 身份、图标处理、间距和其它视觉差异。
- [ ] Tool Calls 画板仍不能标记 `PASS`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，iconfont 仍为 `BLOCKED`。

## 53. 2026-08-22 Admin SQL Audit 现状证据校正

- [x] 重新读取实时 Figma 节点 `797:490`，确认当前设计包含治理 Tab、`tool_name: query_usda` 和风险筛选、运行 ID 搜索、调用摘要表、脱敏 Payload，以及 `SQL Audit · 筛选与详情` 字段面板。
- [x] 对照当前 `/admin?state=sql-audit` 浏览器实现，以上结构均已存在；本轮不重复创建已有组件和业务字段。
- [x] 浏览器验收：`1440×1024`、字体加载完成、DPR 约为 `1`、页面无横向或纵向溢出。
- [x] 最新证据已登记：`admin-sql-audit-figma-current.png`、`admin-sql-audit-browser-current.png`、`admin-sql-audit-current-diff.json`；自动 diff 为 `24.6216% / RMSE 17.2610`，继续保持 `DIFF_REVIEW`。
- [x] 修正旧人工复核结论：不再记录“浏览器缺少 SQL Audit Tab、筛选、调用表和详情字段”；剩余问题集中在 shell 身份、图标处理、字体、间距和其它视觉差异。
- [ ] SQL Audit 画板仍不能标记 `PASS`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，iconfont 仍为 `BLOCKED`。

## 54. 2026-08-22 Admin Trace 现状证据校正

- [x] 重新读取实时 Figma 节点 `797:621`，确认当前设计包含 Run ID / 用户筛选、Failed / Success / 仅降级状态控件、运行表、四段事件时间线、刷新与完整日志下载动作，以及 `Trace 聚合与筛选` 字段面板。
- [x] 对照当前 `/admin?state=trace` 浏览器实现，以上结构均已存在；本轮不重复创建已有组件和业务字段。
- [x] 浏览器验收：`1440×1024`、字体加载完成、DPR 约为 `1`、页面无横向或纵向溢出。
- [x] 最新证据已登记：`admin-trace-figma-current.png`、`admin-trace-browser-current.png`、`admin-trace-current-diff.json`；自动 diff 为 `26.8524% / RMSE 17.2985`，继续保持 `DIFF_REVIEW`。
- [x] 修正旧人工复核结论：不再记录“浏览器缺少 Trace 筛选、运行表、四段时间线和下载动作”；剩余问题集中在 shell 身份、图标处理、头像、字体、间距和其它视觉差异。
- [ ] Trace 画板仍不能标记 `PASS`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，iconfont 仍为 `BLOCKED`。

## 55. 2026-08-22 Admin User Detail 说明面板对齐

- [x] 重新读取实时 Figma 节点 `801:215`，确认用户表、详情侧栏、资料 / 饮食 / 登录会话 / 历史 / 业务会话五个 Tab，以及 `用户详情 Tab` 说明面板和禁用 / 锁定影响提示属于当前设计。
- [x] `/admin?state=user-detail` 已补齐 `用户详情 Tab` 说明面板，使用 Figma 对应文字、颜色、边框、圆角和 `220px` 高度；新增 UsersTab 定向测试锁定面板和高风险影响提示。
- [x] 浏览器验收：`1440×1024`，说明面板约为 `378.4×220`、位置约为 `(1016.8,542.8)`，页面无横向或纵向溢出。
- [x] 最新证据已登记：`admin-user-detail-figma-current.png`、`admin-user-detail-browser-current.png`、`admin-user-detail-current-diff.json`；自动 diff 为 `22.3890% / RMSE 17.4507`，较补齐前的 `35.5884% / RMSE 18.5637` 降低，继续保持 `DIFF_REVIEW`。
- [ ] User Detail 仍存在 shell 身份、图标、头像、字体、表格选中态、侧栏宽度和分页等可见差异，不能标记 `PASS`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`，iconfont 仍为 `BLOCKED`。

## 56. 2026-08-22 Agent Chat 会话操作面板对齐

- [x] 重新读取 Figma 节点 `806:212`，确认会话操作遮罩从 `x=260` 开始，面板为 `760×316`、位置 `x=470,y=90`，选中会话卡为 `712×72`。
- [x] `/chat?state=session-actions` 已实现“会话管理”、当前会话 `每周饮食微调`、`RUNNING` 状态、重命名/归档/回收站三个操作按钮、操作状态标签和关闭按钮；关闭动作会真正移除面板并保留可访问状态提示。
- [x] 代码提交：`d0f8132 feat(Chat): 对齐会话管理操作面板`；Chat 定向测试 `25/25`、`npm run typecheck` 和触及文件 Prettier 均通过；浏览器已验证关闭后 `dialogCount=0`。
- [x] 最新证据：`agent-chat-session-actions-browser-current.png`、`agent-chat-session-actions-current-diff.json`；视口 `1440×1024`、DPR `1`、根节点无滚动溢出，自动 diff `40.1109% / RMSE 19.7513`，继续保持 `DIFF_REVIEW`。
- [ ] 当前画板底层 Workspace 壳层、字体渲染、图标处理、遮罩合成及对话/Trace 内容仍存在视觉差异，不能标记 `PASS`；全量汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`。
- [ ] iconfont 实体包、CSS 映射、来源、许可证和 glyph-Figma 映射仍缺失，继续保持 `BLOCKED`。

## 57. 2026-08-22 Agent Chat 会话重命名结果卡对齐

- [x] 重新读取 Figma 节点 `806:438`，确认成功卡位置 `x=540,y=300`、尺寸 `620×244`，包含 `SAVED` 状态、同步说明、关闭按钮和 `148×44` 返回会话列表按钮。
- [x] `/chat?state=renamed` 已按上述结构实现，关闭结果后保留 `role=status` 提示；返回会话列表动作和关闭动作均已在浏览器中验证。
- [x] 代码提交：`059e61e feat(Chat): 对齐会话重命名结果卡`；Chat 定向测试 `25/25`、`npm run typecheck` 和目标文件 Prettier 均通过。
- [x] 最新证据：`agent-chat-renamed-browser-current.png`、`agent-chat-renamed-current-diff.json`；视口 `1440×1024`、DPR `1`、卡片 `620×244`、根节点无滚动溢出，自动 diff `37.2384% / RMSE 16.7789`，继续保持 `DIFF_REVIEW`。
- [ ] 当前画板底层 Workspace 壳层、字体渲染、图标处理、遮罩合成及对话/Trace 内容仍存在视觉差异，不能标记 `PASS`；iconfont 继续为 `BLOCKED`。

## 58. 2026-08-22 Agent Chat 归档结果卡对齐

- [x] 重新读取 Figma 节点 `806:662`，确认归档卡位置 `x=540,y=286`、尺寸 `620×276`，包含 `ARCHIVED` 状态、归档会话条 `556×56`、保留说明、`108×44` 恢复按钮和关闭按钮。
- [x] `/chat?state=archived` 已按上述结构实现；恢复会话和关闭结果动作均已在浏览器中验证，并保留独立的可访问状态提示。
- [x] Chat 定向测试 `25/25`、`npm run typecheck` 和目标文件 Prettier 均通过。
- [x] 最新证据：`agent-chat-archived-browser-current.png`、`agent-chat-archived-current-diff.json`；视口 `1440×1024`、DPR `1`、根节点无滚动溢出，自动 diff `38.1125% / RMSE 17.1220`，继续保持 `DIFF_REVIEW`。
- [ ] 当前画板底层 Workspace 壳层、字体渲染、图标处理、遮罩合成及对话/Trace 内容仍存在视觉差异，不能标记 `PASS`；iconfont 继续为 `BLOCKED`。

## 59. 2026-08-22 Chat 历史分页当前版本验收证据

- [x] 重新读取 Figma 节点 `740:212`、`740:426`、`742:212`，并重新采集 `/chat?state=history-page-2|history-page-3|search-results` 当前代码截图。
- [x] 三页均保持 `1440×1024`、字体 `loaded`、根节点无溢出；前端左上角窗口装饰点检查结果为 `0`，Figma 设计稿未修改。
- [x] 当前 diff：历史第 2 页 `35.11997% / RMSE 21.57858`；历史第 3 页 `35.12099% / RMSE 21.57857`；搜索结果 `34.61378% / RMSE 21.26354`，均保留 `DIFF_REVIEW`。
- [x] 已修正旧证据中“缺少响应、来源、确认控件和 Trace”的过时描述；当前 Chat fixture 已包含对应结构。
- [ ] 当前 in-app 浏览器实际 DPR 为 `1.25`，DPR1 门禁未关闭；Chrome 隔离截图尝试受执行策略拦截，不能将本轮证据写成 DPR1。
- [ ] 105 张画板全量汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`；iconfont 实体包、CSS 映射、来源和许可证继续为 `BLOCKED`。

## 60. 2026-08-23 Intake Analysis 当前版本验收证据更新

- [x] `640:773` `/analysis?state=v2` 已改用当前 Figma 参考图和当前浏览器截图；当前参考包含实时画板中的会话列表和数据质量面板，旧版 `docxs/设计/figma-png/intake-analysis-v2.png` 不再作为本页最新基线。
- [x] 本页运行时证据：`1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、页面无溢出、文字越界 `0`；侧栏、账户停靠区和 Figma fixture 顶栏几何已记录到像素验收报告。
- [x] 前端左上角红、黄、绿窗口装饰点检查结果为 `0`；只保留空白占位用于布局对齐，Figma 设计稿未修改；头像、状态指示器、radio 和图表等业务圆形元素未误删。
- [x] 当前 PNG diff：差异比例 `26.8560%`、`MAE=3.2500`、`RMSE=17.4654`、最大通道差异 `234`，证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/`。
- [ ] 本页仍为 `DIFF_REVIEW`，不能标记 `PASS`；105 张画板汇总继续为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍未提供，继续保持 `BLOCKED`；shadcn 基础设施已可用，但全量页面逐页 Figma 视觉收口仍在继续。

## 49. 2026-08-23 Agent Clarification 当前画板证据复核

- [x] `687:642` `/chat?state=awaiting-clarification` 已重新按实时 Figma 画板复核，代码中的两个选项与当前设计稿一致：`补充食物和份量`、`上传照片识别`。
- [x] 当前浏览器证据为 `1440×1024`、DPR `1.0000000149`、字体 `loaded`、无页面横向溢出；两个选项均保留可访问 `aria-pressed` 状态。
- [x] 前端左上角红、黄、绿窗口装饰点数量为 `0`，没有需要删除的前端窗口装饰内容；设计稿未修改。
- [x] 当前 Figma/浏览器 PNG 与 diff 证据已登记：`15.4844% / MAE 1.8229 / RMSE 12.9095`，结论为 `DIFF_REVIEW`。
- [ ] 当前画板仍不能标记 `PASS`；头像处理和字体/图标光栅化差异仍需后续逐页收口，iconfont 仍为 `BLOCKED`。

## 68. 2026-08-23 摄入分析错误态视觉收口

- [x] `AnalysisPage` 的 `/analysis?state=error` 已按 Figma 节点 `692:2139` 收口错误态筛选集合、错误卡片高度/间距、警告图标颜色和重载按钮尺寸。
- [x] 新增测试确保错误态不渲染“自定义范围”和“全部餐次”，并修正三态测试的独立卸载边界；`typecheck` 和 `AnalysisPage` 定向测试通过。
- [x] 当前 Figma/浏览器证据与 diff JSON 已登记；自动差异为 `12.0143% / MAE 1.5468 / RMSE 12.2186`，继续保留 `DIFF_REVIEW`。
- [ ] 本项不关闭 105 张画板全量像素 PASS、shadcn 全量迁移或 iconfont 实体资源登记；左上角窗口装饰点仅核查前端为 `0`，业务状态圆点保留。

## 68. 2026-08-23 餐食规划列表操作按钮尺寸复核

- [x] 重新读取 Figma 节点 `692:2762`，确认“进入计划”按钮为 `80×32px`；三个计划卡操作组为 `132×36px`，菜单按钮为 `36×36px`。
- [x] 前端仅将计划卡“进入计划”按钮固定为 `80×32px`；浏览器实测三张卡、操作组和菜单按钮尺寸均与 Figma 几何一致，计划卡保持 `1116×133px`。
- [x] 前端 Workspace 的窗口装饰占位与左上角红黄绿点检查结果均为 `0`，仅确认和清理前端对应内容；业务状态圆点保留，Figma 设计稿未修改。
- [x] 最新 diff 为 `37.0059% / MAE 4.4041 / RMSE 20.9698 / maxChannelDelta 245`，证据已同步至 `.qa/figma-pixel-acceptance/meal-plan-list-current-diff.json` 和 `figma-105-diff-results.json#meal-plan-list`。
- [ ] 该项只完成局部按钮几何对齐；105 张画板仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 仍为 `BLOCKED`，shadcn 全量逐页迁移仍未完成。

## 71. 2026-08-23 餐食规划列表计划卡水平布局对齐

- [x] 依据 Figma `692:2749`、`692:2750`、`692:2761`，确认计划卡内容列目标宽度 `936px`，操作组从内部 `x=960` 开始，容器不使用额外 flex gap。
- [x] 前端移除 `.planListCard` 的 `gap:24px`，内容列实测由 `908.8px` 增至 `932.8px`；按钮、菜单操作组和卡片右边界保持可用。
- [x] 当前截图 diff 如实保持 `27.9293% / MAE 4.0463 / RMSE 19.8687`，本项只关闭水平布局差异，不关闭整页像素 `PASS`。
- [ ] 计划卡总高度、文字行盒、iconfont 实体资源和 105 张画板全量 PASS 仍未完成；左上角红黄绿窗口装饰点前端检查为 `0`，业务状态圆点保留。

## 72. 2026-08-23 餐食规划列表计划卡文字行盒对齐

- [x] 依据 Figma 计划卡子节点，将标题、状态、日期、详情、信息标签和更新时间行盒分别对齐为 `22px / 21px / 16px / 17px / 22px / 14px`。
- [x] 浏览器实测计划卡主列高度 `85px`，三张卡文字内容无越界，当前 diff 为 `23.5418% / MAE 3.5356 / RMSE 18.2830`。
- [ ] 计划卡总高度仍比 Figma `133px` 多约 `1.6px`，下一小点继续处理；全量 `PASS`、iconfont 登记和其余页面迁移仍未完成。

## 73. 2026-08-23 餐食规划列表计划卡高度与边框对齐

- [x] 计划卡外边框改为等效内描边，三张卡浏览器实测均为 `1116×133px`，与 Figma 计划卡节点一致。
- [x] 卡片间距保持 `16px`，主内容列保持 `85px`，最新 diff 为 `22.9677% / MAE 3.2275 / RMSE 17.1978`。
- [ ] 本项不关闭整页像素 `PASS`、105 张画板全量验收、iconfont 登记或其他页面迁移；前端左上角红黄绿窗口装饰点仍为 `0`，业务状态圆点保留。

## 69. 2026-08-23 摄入分析加载态指标骨架对齐

- [x] Figma 节点 `692:1901` 已对应 `/analysis?state=loading`，并重新登记当前 Figma、浏览器原始截图、RGBA PNG 和 diff JSON 证据。
- [x] loading 专用指标区域按 Figma 实测规格对齐：三张指标卡高度 `126px`，保留 `20px` 内边距、`12px` 行间距、`32px` 主骨架和 `16px` 详情骨架；默认态指标卡未被改变。
- [x] 浏览器 `1440×1024` 实测指标容器 `1116×126px`，三张卡均为 `126px`；页面无横向溢出，左上角红黄绿窗口装饰点数量为 `0`，业务状态圆点未删除。
- [x] 最新 diff：`30.3283% / MAE 2.2087 / RMSE 12.5071 / maxChannelDelta 230`，汇总结果更新至 `figma-105-diff-results.json#intake-analysis-loading`，结论保持 `DIFF_REVIEW`。
- [x] `AnalysisPage.test.tsx` 定向测试 `4/4`、`npm run typecheck`、`git diff --check` 通过。
- [ ] 本项不关闭 105 张画板全量 `PASS`、shadcn 全量迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`，Figma 设计稿未修改。

## 70. 2026-08-23 摄入分析空态图标资源对齐

- [x] Figma 节点 `692:2026` 已对应 `/analysis?state=empty`，并重新登记当前 Figma、浏览器原始截图、RGBA PNG 和 diff JSON 证据。
- [x] 空态图表区继续使用 Figma 的 `60px` padding、`64px` 图标容器和 `20px` 内容间距；图表图标改用真实 Figma SVG `public/assets/figma/analysis/intake-analysis-empty-chart-column.svg`，未创建虚构 iconfont glyph。
- [x] 浏览器 `1440×1024` 实测空态图表卡 `1116px` 宽，内容区域 `1066.4×320px`，页面无横向溢出；左上角红黄绿窗口装饰点为 `0`，业务状态圆点保留。
- [x] 最新 diff：`18.6046% / MAE 2.4987 / RMSE 15.8118 / maxChannelDelta 237`，汇总结果更新至 `figma-105-diff-results.json#intake-analysis-empty`，结论保持 `DIFF_REVIEW`。
- [x] `AnalysisPage.test.tsx` 定向测试 `4/4`、`npm run typecheck`、Prettier、`git diff --check` 通过。
- [ ] 本项不关闭 105 张画板全量 `PASS`、shadcn 全量迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`，Figma 设计稿未修改。

## 61. 2026-08-23 Agent Write Confirmation 当前画板证据复核

- [x] `687:773` `/chat?state=write-confirmation` 已建立当前 Figma PNG、浏览器 PNG、运行时检查和 diff JSON 的完整证据链，入口为 `/chat?state=write-confirmation`。
- [x] 写入目标、日期、食物、营养摘要、来源、估算假设和“确认写入/取消”交互均已存在；真实模式继续调用既有写入确认接口，不在前端伪造成功结果。
- [x] 当前运行时为 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、无页面横向溢出；左上角窗口装饰点为 `0`。
- [x] 当前 diff 为 `25.7218% / MAE 2.9444 / RMSE 16.2155`，结论保留 `DIFF_REVIEW`；不能因为状态 fixture 可见而标记像素级 `PASS`。
- [ ] 下一步按计划处理 `687:918` Agent Budget Limit；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍为 `BLOCKED`，shadcn 基础设施也不替代 Figma 逐画板视觉验收。

## 62. 2026-08-23 Agent Budget Limit 当前画板证据复核

- [x] `687:918` `/chat?state=budget-limit` 已建立当前 Figma PNG、浏览器 PNG、运行时检查和 diff JSON 的完整证据链。
- [x] 页面展示 `50,000 tokens`、`100%`、预计费用、追加预算与结束会话；追加动作保留当前 Run 语义，真实模式不创建伪造会话。
- [x] 当前运行时为 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、无页面横向溢出；左上角窗口装饰点为 `0`。
- [x] 当前 diff 为 `25.3484% / MAE 3.7123 / RMSE 18.7204`，结论保留 `DIFF_REVIEW`；不能标记像素级 `PASS`。
- [ ] 下一步按计划处理 `687:1439` Agent Tool Failed Retryable；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍为 `BLOCKED`，shadcn 基础设施也不替代 Figma 逐画板视觉验收。

## 63. 2026-08-23 Agent Tool Failed Retryable 当前画板证据复核

- [x] `687:1439` `/chat?state=tool-failed-retryable` 已建立当前 Figma PNG、浏览器 PNG、运行时检查和 diff JSON 的完整证据链。
- [x] 页面展示工具超时、外部知识库不可用、错误码 `TOOL_TIMEOUT_001`、失败原因，以及仅在该失败明确可重试时显示的“重试”和“跳过此步骤”。
- [x] 当前运行时为 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、无页面横向溢出；左上角窗口装饰点为 `0`。
- [x] 当前 diff 为 `21.6768% / MAE 3.0042 / RMSE 15.9168`，结论保留 `DIFF_REVIEW`；不能标记像素级 `PASS`。
- [ ] 下一步按计划处理 `687:1563` Agent Safety Degraded；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍为 `BLOCKED`，shadcn 基础设施也不替代 Figma 逐画板视觉验收。

## 64. 2026-08-23 Agent Safety Degraded 当前画板证据复核

- [x] `687:1563` `/chat?state=safety-degraded` 已建立当前 Figma PNG、浏览器原始截图、RGBA PNG、运行时检查和 diff JSON 的完整证据链。
- [x] 页面按 Figma 对齐安全降级警告文字层级、头像状态标签、`560×58` 警告卡、`560×125.1` 回答卡、灰色受限说明和 Agent 时间戳；仍展示有限数据范围、个人高血压排除条件未完整应用的提示，并保留可用追问入口。
- [x] 当前运行时为 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、无页面横向溢出；左上角窗口装饰点为 `0`。
- [x] 当前 diff 为 `19.8383925% / MAE 2.9192213 / RMSE 16.6107335`，结论保留 `DIFF_REVIEW`；不能标记像素级 `PASS`。
- [ ] 下一步按计划处理 `687:1684` Agent User Cancelled；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍为 `BLOCKED`，shadcn 基础设施也不替代 Figma 逐画板视觉验收。

## 65. 2026-08-23 Agent User Cancelled 当前画板证据复核

- [x] `687:1684` `/chat?state=user-cancelled` 已建立当前 Figma PNG、浏览器 PNG、运行时检查和 diff JSON 的完整证据链。
- [x] 页面保留已接收部分文本，显示“用户已取消此次运行”，取消状态不显示为系统失败，并提供“重新开始提问”。
- [x] 当前运行时为 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、无页面横向溢出；左上角窗口装饰点为 `0`。
- [x] 当前 diff 为 `17.9155% / MAE 2.5663 / RMSE 15.4846`，结论保留 `DIFF_REVIEW`；不能标记像素级 `PASS`。
- [ ] 下一步按计划处理 `687:1803` Agent SSE Reconnecting；iconfont 实体包、CSS/Unicode 映射、来源和许可证仍为 `BLOCKED`，shadcn 基础设施也不替代 Figma 逐画板视觉验收。

## 66. 2026-08-23 Agent SSE Reconnecting 当前画板证据复核

- [x] `687:1803` `/chat?state=sse-reconnecting` 已建立当前 Figma PNG、浏览器 PNG、运行时检查和 diff JSON 的完整证据链。
- [x] 页面展示第 `2/5` 次重连、等待重连和持续失败时刷新提示；重连期间保留已显示文本并禁用 Composer，达到上限进入稳定错误状态。
- [x] 当前运行时为 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`、无页面横向溢出；左上角窗口装饰点为 `0`。
- [x] 按 Figma 收口补齐 Agent 头像、`560px` 回答气泡、`1132×66px` 全宽重连提示带和已登记的 Figma loader SVG；浏览器实测窗口装饰点为 `0`，Figma 设计稿未修改。
- [x] 当前 diff 为 `26.3488% / MAE 2.6880 / RMSE 15.2945`，结论保留 `DIFF_REVIEW`；不能标记像素级 `PASS`。
- [ ] Agent 六个独立状态的当前 Figma 证据已全部复核；下一步转入 105 张画板的差异逐页收口和 shadcn 迁移剩余项。iconfont 实体包、CSS/Unicode 映射、来源和许可证仍为 `BLOCKED`。

## 67. 2026-08-23 摄入分析错误态视觉收口

- [x] `AnalysisPage` 的 `/analysis?state=error` 已按 Figma 节点 `692:2139` 收口错误态筛选集合、错误卡片高度/间距、警告图标颜色和重载按钮尺寸。
- [x] 新增测试确保错误态不渲染“自定义范围”和“全部餐次”，并修正三态测试的独立卸载边界；`typecheck` 和 `AnalysisPage` 定向测试通过。
- [x] 当前 Figma/浏览器证据与 diff JSON 已登记；自动差异为 `12.0143% / MAE 1.5468 / RMSE 12.2186`，继续保留 `DIFF_REVIEW`。
- [ ] 本项不关闭 105 张画板全量像素 PASS、shadcn 全量迁移或 iconfont 实体资源登记；左上角窗口装饰点仅核查前端为 `0`，业务状态圆点保留。

## 74. 2026-08-23 个人中心账号注销成功与失败状态收口

- [x] Figma `795:499` 已对应 `/profile?state=privacy-deletion-success`；成功卡桌面 `560×250`、移动 `358.4×250`，包含绿色成功语义、request ID 和关闭动作，无进度条；diff `42.1501% / MAE 3.2028 / RMSE 13.9386`。
- [x] Figma `795:642` 已对应 `/profile?state=privacy-deletion-failed`；失败卡桌面 `600×280`、移动 `358.4×280`，包含错误码、request ID 和“重新创建注销请求”动作，无进度条；diff `34.1727% / MAE 2.6096 / RMSE 14.4439`。
- [x] 两个状态已完成桌面/移动浏览器截图、RGBA 归一化和 JSON 登记；`ProfilePage.test.tsx` 定向测试 `20/20`，`typecheck`、Prettier 和 `git diff --check` 通过。
- [x] 当前前端实时检查没有左上角红黄绿窗口装饰点，业务状态圆点保留，Figma 设计稿未修改。
- [ ] 两个画板仍为 `DIFF_REVIEW`，不代表像素级 `PASS`；105 张汇总继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 实体包仍为 `BLOCKED`，shadcn 全量视觉迁移仍未完成。

## 82. 2026-08-23 摄入分析错误态侧栏图标与窗口装饰复核

- [x] 依据 Figma 节点 `692:2139`，将仅限 Figma fixture 的侧栏导航图标统一为实际图标 `16×16px`，包括 `Agent 对话` 标题图标；普通页面规则未改变。
- [x] 浏览器实测 `/analysis?state=error` 为 `1440×1024`、DPR `1.0000000149011612`、根节点无横向溢出；左上角红黄绿窗口装饰点数量为 `0`，没有删除业务状态圆点，Figma 设计稿未修改。
- [x] 当前 PNG diff 为 `12.0226% / MAE 1.2604 / RMSE 10.4082 / maxChannelDelta 230`，已同步独立 diff、105 画板汇总和映射证据。
- [x] `AnalysisPage` 定向测试 `4/4`、`npm run typecheck`、Prettier 和 `git diff --check` 通过。
- [ ] 该画板及 105 张画板汇总继续保持 `DIFF_REVIEW`，不标记像素级 `PASS`；iconfont 仍为 `BLOCKED`，shadcn 全量逐页视觉迁移仍未完成。

## 83. 2026-08-23 摄入分析空态指标卡高度对齐

- [x] Figma 节点 `692:2026` 的三张空态指标卡已从前端 fixture 的 `107px` 对齐到 Figma 的 `100px`；只作用于 `/analysis?state=empty` 的 Figma 分析区域，真实模式和其他状态未改变。
- [x] 浏览器实测三张指标卡均为 `100px`，图表卡 y 坐标为 `288px`；页面 `1440×1024` 无横向溢出，左上角红黄绿窗口装饰点为 `0`，业务状态圆点保留，Figma 设计稿未修改。
- [x] 证据已更新：`intake-analysis-empty-browser-2026-08-23.jpg`、`intake-analysis-empty-browser-current-rgba.png`、独立 diff JSON 和 `figma-105-mapping.json` / `figma-105-diff-results.json`；diff 为 `16.6444% / MAE 1.8523 / RMSE 13.0926`。
- [x] `AnalysisPage.test.tsx` `4/4`、typecheck、build、目标文件格式检查和 `git diff --check` 通过。
- [ ] 本项仍为 `DIFF_REVIEW`，图表空态区还需从 `320px` 收口到 Figma `308px`；当前 in-app DPR `1.25`，不能标记 `PASS`。shadcn 全量页面视觉迁移尚未完成，iconfont 仍为 `BLOCKED`。

## 84. 2026-08-23 摄入分析空态图表区域高度与内容几何对齐

- [x] Figma `692:2026` 的空态图表区域目标为 `1066×308px`；前端空态区域实测已从 `314px` 收口到 `308px`，图标 `64×64px`、说明组 `47px`、按钮 `118×37px`、内容间距 `20px` 与 Figma 子节点一致。
- [x] 本轮只调整空态 fixture 的正文行高和记录按钮高度，真实分析模式、错误态、Figma 设计稿和业务状态圆点未修改。
- [x] Chromium headless fallback 实测视口 `1440×1024`、DPR `1`、字体 `loaded`、无页面横向溢出；前端左上角红黄绿窗口装饰候选为 `0`。
- [x] 新证据为 `intake-analysis-empty-browser-308-final.png`；PNG diff 为 `5.6210% / MAE 1.5111 / RMSE 13.0804 / maxChannelDelta 204`，已同步独立 diff 和 105 画板映射。
- [ ] 本画板继续保持 `DIFF_REVIEW`，不代表像素级 `PASS`；105 张汇总继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全量视觉迁移尚未完成，iconfont 仍为 `BLOCKED`。

## 85. 2026-08-23 餐食规划空态实时 Figma 基线收口

- [x] 重新读取实时 Figma 节点 `692:2446`，确认空态卡片为 `520×368px`，图标 `100×100px`，文案组 `424×82px`，创建按钮 `192×42px`，提示行 `412×16px`。
- [x] 前端 `/planning?state=empty` 已按上述几何重组空态卡片，使用 `UtensilsCrossed` 和 `Info` 的现有 Lucide 封装；没有创建虚构 iconfont glyph，也没有修改 Figma 设计稿。
- [x] 浏览器实测视口 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向溢出；空态卡片为 `x=590,y=340,width=520,height=368`，前端左上角红黄绿窗口装饰点数量为 `0`，业务状态圆点保留。
- [x] 使用实时 Figma PNG `meal-planning-empty-figma-live.png` 与浏览器 PNG `meal-planning-empty-browser-current.png` 运行 `scripts/png-diff.mjs`：差异比例 `13.2758%`、`MAE=1.2768`、`RMSE=10.0411`、最大通道差异 `244`；结果保持 `DIFF_REVIEW`。
- [x] `PlanningPage.test.tsx` 定向测试 `8/8`、`npm run typecheck`、目标文件 Prettier 和运行时几何检查通过。
- [ ] 本项只关闭空态卡片局部几何和信息层级差异，不代表该画板或 105 张画板达到像素级 `PASS`；全量 shadcn 迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 86. 2026-08-26 餐食规划 Loading 小点完成记录

- [x] `/planning?state=loading` 已按实时 Figma 节点 `692:2256` 重新对齐 loading banner、日期骨架卡、餐食骨架卡、约束校验和购物清单骨架。
- [x] 只改前端 loading fixture；Figma 设计稿、真实计划接口、后端逻辑和其它页面未改动。
- [x] `1440×1024`、DPR `1.0000000149011612`、无横向溢出；左上角红黄绿窗口控制点检测为 `0`，业务状态点和复选框未误删。
- [x] 定向 PNG diff 为 `18.1852% / MAE 1.19237 / RMSE 9.65195 / maxChannelDelta 244`，证据已登记到 105 画板映射；结果保持 `DIFF_REVIEW`。
- [x] `PlanningPage.test.tsx` `8/8`、`npm run typecheck` 和 `git diff --check` 通过。

## 87. 2026-08-26 餐食规划错误态实时 Figma 几何收口

- [x] `/planning?state=error` 已重新读取实时 Figma 节点 `692:2542`；错误卡片 `520×417px`、内边距 `48px`、错误图标容器 `100×100px`、错误码 `25px` 高、操作按钮 `42px` 高均已按节点规格复核。
- [x] 前端仅调整错误态 CSS：错误图标背景对齐 `rgba(255,117,118,0.06)`，卡片使用等效 `inset` 描边，错误码和按钮文字行高对齐；Figma 设计稿、空态、真实模式和业务状态点未修改。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出；卡片实测 `x=590,y=315.5,width=520,height=417`，前端左上角红黄绿窗口控制点为 `0`。
- [x] 最新 Figma/浏览器证据已登记：`meal-planning-error-figma-live-2026-08-26.png`、`meal-planning-error-browser-current-2026-08-26-rgba.png`；PNG diff 为 `13.2237% / MAE 1.23294 / RMSE 9.91903 / maxChannelDelta 244`。
- [ ] 当前画板继续保持 `DIFF_REVIEW`，整页壳层、头像、图标和光栅化差异仍未满足像素级 `PASS`；105 张画板汇总继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 继续为 `BLOCKED`。

## 106. 2026-08-28 餐食规划列表 Tab 垂直几何收口

- [x] 按实时 Figma 节点 `692:2662` 将 `/planning?state=list` 标题行高设为 `29px`、Tab 高度设为 `33px`、Tab 底部间距设为 `11.2px`；新增 `data-figma-role` 标记仅用于稳定验收定位。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载；标题组 `x=292,y=100,h=50`，Tab 容器 `x=292,y=174,h=45`，三张计划卡 `y=243/392/541` 且均为 `1116×133px`，页面无横向溢出。
- [x] 定向测试 `PlanningPage.test.tsx 11/11`、类型检查、目标文件格式检查和 `git diff --check` 通过；前端左上角红黄绿窗口控制点与 `window-controls` 均为 `0`，业务状态圆点保留，Figma 未修改。
- [x] Figma/浏览器 PNG 与 diff 证据已登记；`differentPixels=321004`、差异比例 `21.7695%`、`MAE=2.40082`、`RMSE=13.86524`、最大通道差异 `232`，结论为 `DIFF_REVIEW`。
- [ ] 本小点只完成列表 Tab 的局部垂直几何收口，不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 继续为 `BLOCKED`。
## 88. 2026-08-26 摄入分析 Loading 实时 Figma 结构收口

- [x] `/analysis?state=loading` 已按实时 Figma 节点 `692:1901` 收口：指标区 `126px`、图表卡 `303px`、图表骨架 `1068×160px`、洞察卡 `217px`、洞察骨架列表 `1068×74px`。
- [x] 本轮只调整 Loading fixture 的专属 class 和等效内描边；真实分析模式、Figma 设计稿、后端接口和业务状态圆点未修改。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出、文字越界 `0`；前端左上角红黄绿窗口装饰点为 `0`。
- [x] 最新证据和机器记录已同步到 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/`、`figma-105-mapping.json`、`figma-105-diff-results.json` 和 `intake-analysis-loading-current-diff.json`；PNG diff 为 `29.3719% / MAE 1.38677 / RMSE 9.60788 / maxChannelDelta 230`，结论为 `DIFF_REVIEW`。
- [ ] 本项只完成 Loading 区域几何收口，不关闭该画板像素级 `PASS`、105 张画板全量 `PASS`、shadcn 全量视觉迁移或 iconfont 资源登记；iconfont 继续为 `BLOCKED`。
## 89. 2026-08-26 饮食记录 Loading 实时 Figma 结构收口

- [x] `/analysis?view=records&state=loading` 已按实时 Figma 节点 `692:1427` 收口：指标区 `1116×80px`，餐次卡分别 `1116×118px`、`1116×168px`，指标到餐次容器间距 `24px`。
- [x] 本轮只调整 Diet Records Loading fixture 的骨架 DOM 结构、尺寸、间距和 Figma 作用域颜色；真实饮食记录接口、Figma 设计稿和业务状态圆点未修改，前端左上角红黄绿窗口装饰点为 `0`。
- [x] 最新 Figma/浏览器 PNG、独立 diff、105 画板映射和汇总记录已登记；PNG diff 为 `15.5160% / MAE 1.33810 / RMSE 11.19032 / maxChannelDelta 230`，结论保持 `DIFF_REVIEW`。
- [x] Diet Records Loading 运行时实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出；指标和餐次骨架几何与 Figma 节点一致。
- [ ] 本项只完成 Loading fixture 的结构收口，不关闭该画板或 105 张画板的像素级 `PASS`、shadcn 全量视觉迁移或 iconfont 资源登记；iconfont 继续为 `BLOCKED`。
## 90. 2026-08-26 饮食记录 Empty 实时 Figma 几何收口

- [x] `/analysis?view=records&state=empty` 已按实时 Figma 节点 `692:1556` 收口：指标卡 `267×88px`，空态面板 `1116×377px`，图标容器 `80×80px`，文案组 `182×47px`，按钮 `132×42px`。
- [x] 本轮只调整 Diet Records Empty fixture 的空态面板高度和副文案行高；真实饮食记录接口、Figma 设计稿和业务状态圆点未修改，前端左上角红黄绿窗口装饰点为 `0`。
- [x] 最新 Figma/浏览器 PNG、独立 diff、105 画板映射和运行时 URL 已登记；PNG diff 为 `14.7512% / MAE 1.63114 / RMSE 12.22059 / maxChannelDelta 230`，结论保持 `DIFF_REVIEW`。
- [x] Diet Records Empty 运行时实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出、文字越界为 `0`；内部面板、图标、文案和按钮坐标与 Figma 元数据一致。
- [ ] 本项只完成 Empty fixture 的局部几何收口，不关闭该画板或 105 张画板的像素级 `PASS`、shadcn 全量视觉迁移或 iconfont 资源登记；iconfont 继续为 `BLOCKED`。
## 91. 2026-08-26 饮食记录 Error 实时 Figma 几何收口

- [x] `/analysis?view=records&state=error` 已按实时 Figma 节点 `692:1685` 收口：错误面板 `1116×457px`，垂直内边距 `120px`，图标容器 `80×80px`，文案组 `144×47px`，重载按钮 `132×42px`。
- [x] 本轮只调整 Diet Records Error fixture 的错误面板高度、垂直内边距和副文案行高；真实饮食记录接口、Figma 设计稿和业务状态圆点未修改，前端左上角红黄绿窗口装饰点为 `0`。
- [x] 最新 Figma/浏览器 PNG、独立 diff、105 画板映射和运行时 URL 已登记；PNG diff 为 `11.9399% / MAE 1.38010 / RMSE 11.40657 / maxChannelDelta 230`，结论保持 `DIFF_REVIEW`。
- [x] Diet Records Error 运行时实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出、文字越界为 `0`；错误面板、图标、文案和重载按钮坐标与 Figma 元数据一致。
- [ ] 本项只完成 Error fixture 的局部几何收口，不关闭该画板或 105 张画板的像素级 `PASS`、shadcn 全量视觉迁移或 iconfont 资源登记；iconfont 继续为 `BLOCKED`。

## 92. 2026-08-27 Meal Planning 餐食卡长标题显示收口

- [x] 依据实时 Figma 节点 `640:901`，将 `/planning?state=v2` 餐食卡长英文菜名调整为 `13px`、单行隐藏溢出并显示省略号；只修改前端，未修改 Figma 设计稿。
- [x] 浏览器实测所有餐食标题满足 `white-space: nowrap`、`overflow: hidden`、`text-overflow: ellipsis`，字体已加载；前端左上角红黄绿窗口装饰点数量为 `0`，业务状态圆点保留。
- [x] 补充 PNG diff 为 `23.6412% / MAE 2.47720 / RMSE 13.87382 / maxChannelDelta 234`，比此前 `23.8253%` 下降；证据已登记到 `foodmate-ui/.qa/figma-pixel-acceptance/`。
- [x] `PlanningPage.test.tsx` 定向测试 `8/8`、全量前端测试 `38` 个测试文件 `192/192`、格式、类型、构建和 `git diff --check` 均通过。
- [ ] 本次补充浏览器截图 DPR 为 `1.25`，不满足 DPR 1 门禁；该画板和 105 张画板汇总继续保持 `DIFF_REVIEW`，不能标记 `PASS`。

## 93. 2026-08-27 Agent Chat 顶栏品牌方块与窗口装饰复核

- [x] 依据 Figma 节点 `640:428` 区分品牌标记：侧栏为 `36×36px` 的 `F`，顶栏为 `28×28px` 的空白绿色方块；Figma 中的 `window-controls` 不在前端实现。
- [x] `BrandLogo` 增加 `showMarkLetter` 控制，Figma Chat fixture 仅隐藏顶栏 `F`，侧栏 `F` 和普通页面品牌行为保持不变。
- [x] `/chat?state=figma-v2` 浏览器实测视口 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出；顶栏品牌方块 `28×28px` 且为空，侧栏品牌标记 `36×36px` 且显示 `F`。
- [x] 前端左上角红黄绿窗口装饰候选为 `0`；会话状态圆点保留，Figma 设计稿未修改。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/agent-chat-v2-brand-mark-browser-2026-08-27.png`；同尺寸 diff 为 `43.7252% / MAE 5.20864 / RMSE 22.10895 / maxChannelDelta 255`，画板继续为 `DIFF_REVIEW`。
- [x] 105 画板映射全量刷新后为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`；WorkspaceLayout 定向测试 `5/5`、类型检查和本次文件格式检查通过。
- [ ] 本条不代表 105 张画板像素级 PASS、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 94. 2026-08-27 Meal Planning 顶栏品牌标记 fixture 边界修正

- [x] 依据 Figma 节点 `692:2662` 区分规划页品牌标记：顶栏为 `28×28px` 空白绿色方块，侧栏为 `36×36px` 的 `F`；Figma 中的 `window-controls` 不在前端实现。
- [x] `WorkspaceLayout` 同时覆盖 `sidebarFixture` 和 `designChat` 两类 Figma fixture；仅在顶部导航隐藏时隐藏顶栏 `F`，普通页面和侧栏 `F` 保持不变。
- [x] `/planning?state=list` 浏览器实测视口 `1440×1024`、DPR `1.0000000149011612`、字体已加载、无横向溢出；顶栏方块为空、侧栏显示 `F`，左上角红黄绿窗口装饰候选为 `0`。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/meal-plan-list-brand-mark-browser-2026-08-27.png`；同尺寸 diff 为 `37.0622% / MAE 4.25977 / RMSE 20.45833 / maxChannelDelta 245`，画板继续为 `DIFF_REVIEW`。
- [x] 105 画板映射已更新，汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`；WorkspaceLayout 定向测试 `5/5`、类型检查和本次文件格式检查通过。
- [ ] 本条不代表 105 张画板像素级 PASS、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 95. 2026-08-27 摄入分析洞察颜色小点收口

- [x] 依据 Figma 节点 `640:773`，将 `/analysis?state=v2` 第三条营养洞察圆点从 `#73D4DF` 修正为设计稿的 `#80E0E6`，代码使用现有语义变量 `--fm-color-info-strong`。
- [x] 浏览器运行时计算颜色为 `rgb(128, 224, 230)`，与 Figma 节点一致；字体状态为 `loaded`，页面无横向溢出。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`，没有需要删除的对应代码；Figma 设计稿未修改，业务状态圆点保留。
- [x] 已更新 `figma-105-mapping.json` 和 `figma-105-diff-results.json`；DPR1 浏览器证据 diff 为 `13.9400% / MAE 3.52169 / RMSE 19.53109 / maxChannelDelta 211`，画板继续为 `DIFF_REVIEW`。
- [x] `AnalysisPage.test.tsx` 定向测试 `4/4`、`npm run typecheck` 和 `git diff --check` 通过。
- [ ] 本小点不关闭 105 张画板全量像素 `PASS`、shadcn 全页面视觉迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 119. 2026-08-29 Meal Planning 默认页右侧栏边界收口

- [x] 按 Figma 节点 `640:901`、`640:1077` 将 `/planning?state=v2` fixture 的右侧栏边界改为 `inset` 内描边，避免 1px `border-left` 挤占画板布局；右侧栏实测为 `x=1100,width=340px`，内部内容为 `x=1124,width=292px`。
- [x] 约束行实测 `y=54/111/168/225,height=45px`，购物清单分组实测 `y=348/446,height=86px`；字体已加载、无横向溢出，前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保留，Figma 设计稿未修改。
- [x] 新增 `meal-planning-v2-right-rail-inset-browser-2026-08-29-rgba.png` 和 `meal-planning-v2-right-rail-inset-2026-08-29-diff.json`；同尺寸 diff 为 `23.1839% / MAE 2.24425 / RMSE 12.93822 / maxChannelDelta 234`，结论为 `DIFF_REVIEW`。
- [ ] 当前采集 DPR 为 `1.25`，本小点只完成右侧栏局部几何收口，不代表该画板或 105 张画板达到像素级 `PASS`；shadcn 全量页面迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 115. 2026-08-29 Intake Analysis 趋势卡内容区边界收口

- [x] 重新读取实时 Figma 节点 `640:773`，确认趋势卡目标为 `1116×303px`、图表内容区目标为 `1068×220px`、洞察卡目标为 `1116×219px`；前端仅在 Figma fixture 作用域将趋势卡和洞察卡外描边改为等效内描边，真实模式不受影响。
- [x] 浏览器实测 `/analysis?state=v2`：视口 `1440×1024`、DPR `1.0000000149`、字体已加载、页面无横向溢出；趋势卡为 `1116×303px`，图表区为 `1068×220px`，洞察卡为 `1116×219px`，左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 新增原始浏览器帧 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/intake-analysis-v2-browser-card-inset-2026-08-29.jpg`、RGBA PNG `intake-analysis-v2-browser-card-inset-2026-08-29.png` 和独立 diff `intake-analysis-v2-card-inset-2026-08-29-diff.json`；同尺寸结果为 `differentPixels=406148`、差异比例 `27.5437%`、`MAE=2.49562`、`RMSE=14.13861`、最大通道差异 `234`，结论继续为 `DIFF_REVIEW`。
- [x] `AnalysisPage` 定向测试 `5/5`、浏览器几何检查、截图格式归一化、`png-diff.mjs` 和窗口装饰源码/运行时检查均已执行；Figma 设计稿未修改。
- [ ] 本小点只收口趋势卡内容区边界，不代表该画板或 105 张画板达到像素级 `PASS`；整页字体、图标和其他视觉差异仍需复核，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 116. 2026-08-29 Intake Analysis 洞察操作按钮几何收口

- [x] 重新读取实时 Figma 节点 `640:895` 及子节点 `640:896/640:898`，确认“让 Agent 解读”目标为 `121×37px`、“基于分析制定计划”目标为 `144×37px`，两者间距为 `12px`。
- [x] 前端仅在 Figma fixture 作用域为两个按钮增加 `box-sizing: border-box` 和明确宽度；真实模式保持原有自适应按钮尺寸与交互。
- [x] 浏览器实测 `/analysis?state=v2`：第一按钮 `121×37px`、第二按钮 `144×37px`、间距 `12px`；视口 `1440×1024`、DPR `1.0000000149`、字体已加载、页面无横向溢出，左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 新增原始浏览器帧 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/intake-analysis-v2-browser-insight-buttons-2026-08-29.jpg`、PNG `intake-analysis-v2-browser-insight-buttons-2026-08-29.png` 和独立 diff `intake-analysis-v2-insight-buttons-2026-08-29-diff.json`；同尺寸结果为 `differentPixels=406120`、差异比例 `27.5418%`、`MAE=2.49371`、`RMSE=14.13255`、最大通道差异 `234`，结论继续为 `DIFF_REVIEW`。
- [x] `AnalysisPage` 定向测试 `5/5`、`npm run typecheck`、浏览器几何检查、截图格式归一化、`png-diff.mjs` 和 `git diff --check` 均通过；Figma 设计稿未修改。
- [ ] 本小点只收口洞察操作按钮几何，不代表该画板或 105 张画板达到像素级 `PASS`；整页字体、图标和其他视觉差异仍需复核，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 108. 2026-08-28 Agent 完成态引用与蛋白质指标 fixture 收口

- [x] 新增 `/chat?state=completed-with-citations` 独立前端 fixture，对应 Figma 节点 `687:1306`；完成态包含已完成响应、蛋白质指标、数据源引用和不显示 Trace 的布局。
- [x] 完成态用户气泡固定使用已登记的默认男头像；左上角红、黄、绿窗口装饰点候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] ChatPage 定向测试新增完成态内容、引用、头像和无 Trace 回归；本轮浏览器实测 `1440×1024`、字体已加载、无横向溢出，用户气泡 `186×49px`、指标卡 `528px`、引用标签 `2` 个。
- [x] 最新 PNG diff：`24.1531% / MAE 2.68472 / RMSE 14.72847 / maxChannelDelta 236`；证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-completed-with-citations-browser-2026-08-28.png` 和同目录独立 diff JSON，结论保留 `DIFF_REVIEW`。
- [ ] 本小点仅补齐完成态视觉与 fixture 证据，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 109. 2026-08-28 Agent Tool Failed Retryable 当前版本证据收口

- [x] 对应 Figma 节点 `687:1439` 的 `/chat?state=tool-failed-retryable` fixture 保留错误码、失败原因、“重试”和“跳过此步骤”两个动作；只有该明确可重试状态显示重试入口。
- [x] 前端仅移除左上角红、黄、绿窗口装饰点，保留 Figma 侧栏顶部占位；当前浏览器实测品牌、新建任务、搜索框和工作区导航起点为 `y=52/104/161/217`，窗口控制候选为 `0`，业务状态圆点保留。
- [x] 当前证据视口 `1440×1024`、字体已加载、无横向溢出；独立 diff 为 `24.4331% / MAE 3.03221 / RMSE 16.25115 / maxChannelDelta 253`，结论为 `DIFF_REVIEW`，运行时实际 DPR 为 `1.25`。
- [ ] 本小点只完成当前版本证据收口，不代表该画板或 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 110. 2026-08-28 Agent Safety Degraded 当前版本证据收口

- [x] 对应 Figma 节点 `687:1563` 的 `/chat?state=safety-degraded` fixture 保留安全降级警告、有限数据范围、个人条件未完整应用提示和可继续追问入口。
- [x] 当前浏览器证据视口为 `1440×1024`，DPR `1.0000000149`，字体已加载，`scrollWidth=clientWidth=1440`；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保留。
- [x] 最新 PNG diff 为 `25.6455% / MAE 3.35588 / RMSE 16.78875 / maxChannelDelta 255`；证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-safety-degraded-browser-2026-08-28.png` 和 `agent-safety-degraded-current-diff-2026-08-28.json`，结论保留 `DIFF_REVIEW`。
- [x] ChatPage 定向状态回归已确认降级提示、受限回答和启用追问输入框存在；Figma 设计稿未修改。
- [ ] 本小点只完成当前版本验收证据收口，不代表该画板或 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 111. 2026-08-28 Agent User Cancelled 当前版本证据收口

- [x] 对应 Figma 节点 `687:1684` 的 `/chat?state=user-cancelled` fixture 保留已接收的部分文本、用户取消原因和重新开始入口，取消态不显示为系统失败。
- [x] 当前浏览器证据视口为 `1440×1024`，DPR `1.0000000149`，字体已加载，`scrollWidth=clientWidth=1440`；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保留。
- [x] 最新 PNG diff 为 `21.1433% / MAE 2.69148 / RMSE 15.00025 / maxChannelDelta 245`；证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-user-cancelled-browser-2026-08-28.png` 和 `agent-user-cancelled-current-diff-2026-08-28.json`，结论保留 `DIFF_REVIEW`。
- [x] ChatPage 定向状态回归已确认部分文本、取消原因和重新开始输入框存在，且不显示“运行失败”；Figma 设计稿未修改。
- [ ] 本小点只完成当前版本验收证据收口，不代表该画板或 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 112. 2026-08-28 Agent SSE Reconnecting 当前版本证据收口

- [x] 对应 Figma 节点 `687:1803` 的 `/chat?state=sse-reconnecting` fixture 保留已显示文本，展示第 `2/5` 次重连、等待重连状态和持续失败后的刷新提示；输入框保持禁用。
- [x] 当前浏览器证据视口为 `1440×1024`，DPR `1.0000000149`，字体已加载，`scrollWidth=clientWidth=1440`；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保留。
- [x] 最新 PNG diff 为 `26.0581% / MAE 2.98683 / RMSE 15.58692 / maxChannelDelta 248`；证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-sse-reconnecting-browser-2026-08-28.png` 和 `agent-sse-reconnecting-current-diff-2026-08-28.json`，结论保留 `DIFF_REVIEW`。
- [x] ChatPage 定向状态回归已确认重连次数、保留文本、刷新提示和禁用输入框存在；Figma 设计稿未修改。
- [ ] 本小点只完成当前版本验收证据收口，不代表该画板或 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 104. 2026-08-28 餐食规划空态图标底色收口

- [x] 重新读取实时 Figma 节点 `692:2446`，确认空态图标容器节点 `692:2529` 的底色为 `#EBF7ED`；前端 `/planning?state=empty` 已将空态图标底色从半透明叠加色改为该精确颜色，错误态、真实模式和 Figma 设计稿未修改。
- [x] 浏览器实测视口 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向溢出；图标容器计算颜色为 `rgb(235, 247, 237)`，空态卡片为 `x=590,y=340,width=520,height=368`，前端左上角红黄绿窗口装饰候选与 `window-controls` 均为 `0`，侧栏品牌 `F` 保留且顶栏品牌方块为空。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/meal-planning-empty-icon-background-2026-08-28.png`，独立 diff 为 `meal-planning-empty-icon-background-2026-08-28-diff.json`；同尺寸 PNG diff 为 `190726` 个差异像素、`12.9344%`、`MAE=1.24123`、`RMSE=10.12835`、最大通道差异 `232`，相比上一份 `13.1101%` 有下降。
- [x] `PlanningPage.test.tsx` 定向测试 `9/9` 通过，类型检查和 `git diff --check` 通过；目标 CSS 文件的 Prettier 检查仍报告既有格式提示，本次未扩大格式化范围。
- [ ] 本小点只完成空态图标底色局部收口；该画板和 105 张画板仍为 `DIFF_REVIEW`，不代表像素级 `PASS`，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 105. 2026-08-28 餐食规划空态图标线条颜色收口

- [x] 依据实时 Figma 节点 `692:2446` 导出的 `utensils-crossed` 资产，确认图标线条为 `#89B27C`；前端 `/planning?state=empty` 已将空态图标继承色切换到现有语义 Token `--fm-color-success-strong`，错误态、真实模式和 Figma 设计稿未修改。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向溢出；图标计算颜色为 `rgb(137, 178, 124)`，背景仍为 `rgb(235, 247, 237)`，前端左上角红黄绿窗口装饰候选与 `window-controls` 均为 `0`。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/meal-planning-empty-icon-color-2026-08-28.png`，独立 diff 为 `meal-planning-empty-icon-color-2026-08-28-diff.json`；同尺寸 PNG diff 为 `190697` 个差异像素、`12.9325%`、`MAE=1.24022`、`RMSE=10.12595`、最大通道差异 `232`，相比上一份 `12.9344%` 有下降。
- [x] `PlanningPage.test.tsx` 定向测试 `9/9` 通过，类型检查、构建和 `git diff --check` 通过；目标 CSS 文件仍存在本次之前的 Prettier 格式提示，本次未扩大格式化范围。
- [ ] 本小点只完成空态图标线条颜色局部收口；该画板和 105 张画板仍为 `DIFF_REVIEW`，不代表像素级 `PASS`，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 117. 2026-08-29 Meal Planning 列表创建按钮几何收口

- [x] 依据实时 Figma 节点 `692:2662`，将 `/planning?state=list` 的“+ 新建膳食计划”前端按钮固定为 `128×37px`，内边距为 `10px 16px`，文字为 `14px / 700 / 17px`；Figma 设计稿未修改。
- [x] 浏览器实测位置为 `x=1280,y=106.5`、尺寸为 `128×37px`，视口 `1440×1024`，字体已加载且无横向溢出；前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 新增实时 Figma PNG、浏览器 RGBA PNG 和独立 diff JSON；最新同尺寸 diff 为 `21.6557% / MAE 2.38728 / RMSE 13.83342 / maxChannelDelta 232`，画板继续为 `DIFF_REVIEW`。
- [x] 本轮前端测试、类型检查和生产构建均通过；本条只证明局部按钮几何收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面视觉迁移或 iconfont 解阻塞。

## 118. 2026-08-29 Meal Planning 列表 Tab 水平间距收口

- [x] 依据实时 Figma 节点 `692:2662`，将 `/planning?state=list` 的三个状态 Tab 水平间距从 `4px` 调整为 `8px`，实测位置为 `x=292/374/456`、宽度 `74px`、高度 `33px`；Figma 设计稿未修改。
- [x] 浏览器视口为 `1440×1024`、DPR `1`、字体已加载且无横向溢出；前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 新增浏览器 JPEG、RGBA PNG 和最新独立 diff 结果；同尺寸 diff 为 `21.6817% / MAE 2.37253 / RMSE 13.77192 / maxChannelDelta 232`，画板继续为 `DIFF_REVIEW`。
- [x] `PlanningPage.test.tsx` 定向测试 `11/11`、类型检查和构建均通过；本条只证明 Tab 局部几何收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面视觉迁移或 iconfont 解阻塞。

## 97. 2026-08-27 工作台状态说明文案对齐

- [x] 依据 Figma 节点 `640:256`，将 `/?state=figma-v2` 工作台状态说明中的“待处理事项提醒”和“预算通知”修正为设计稿使用的“待处理事项覆盖”和“预算追加”。
- [x] 重新运行工作台首页定向测试 `2/2`；前端代码仅修改状态说明文案，未改变业务状态圆点或 Figma 文件。
- [x] headless Chromium 使用 `1440×1024`、DPR `1`、禁用动画；最新同尺寸 PNG diff 为 `26.7060% / MAE 3.62688 / RMSE 18.43240 / maxChannelDelta 254`，较修改前 MAE `3.62743` 有所下降。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`；顶栏品牌方块保持 Figma 要求的空白绿色方块，侧栏品牌 `F` 和业务状态圆点保留。
- [ ] 工作台画板继续为 `DIFF_REVIEW`；本小点不关闭 105 张画板像素级 `PASS`、shadcn 全页面视觉迁移或 iconfont 资源阻塞。

## 98. 2026-08-27 Agent Chat 助手正文排版收口

- [x] 依据 Figma 节点 `640:428`，将 `figma-v2` fixture 的助手正文恢复为 `Noto Sans SC Regular 14px`；真实模式、其他 Chat 状态、来源行和 Trace 数值不受影响。
- [x] headless Chromium 使用 `1440×1024`、DPR `1`、禁用动画；助手正文换行为两行，消息操作区实测 `796×150px`、`y=543`，前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留，Figma 设计稿未修改。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-assistant-font-browser-2026-08-27.png`；同尺寸 diff 为 `35.6182% / MAE 5.03448 / RMSE 23.05043 / maxChannelDelta 239`，已同步 `figma-105-mapping.json` 与 `figma-105-diff-results.json`，画板继续为 `DIFF_REVIEW`。
- [ ] 本小点不代表 Agent Chat 或 105 张画板达到像素级 `PASS`；头像、图标和浏览器渲染差异仍需逐页收口，shadcn 全页面迁移与 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 99. 2026-08-27 Agent Chat Trace 卡片状态样式收口

- [x] 依据 Figma 节点 `640:428`，仅在 `figma-v2` 的 Trace rail 中去掉 running 工具的橙色外框；`Executing ●` 状态条、Trace 文字和耗时颜色保持不变，其他运行态页面不受影响。
- [x] 浏览器实测 Trace 最后一张卡边框为透明，视口 `1440×1024`、DPR `1`、字体已加载；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保留，Figma 设计稿未修改。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-trace-background-browser-2026-08-27.png`；同尺寸 diff 为 `35.6183% / MAE 5.00323 / RMSE 22.95874 / maxChannelDelta 239`，已同步 105 画板映射和汇总结果，画板继续为 `DIFF_REVIEW`。
- [ ] 本小点不代表 Agent Chat 或 105 张画板达到像素级 `PASS`；其他视觉差异、shadcn 全页面迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 100. 2026-08-27 Agent Chat 窗口控制占位与侧栏基线收口

- [x] 依据 Figma 节点 `640:428`，前端不渲染左上角红黄绿窗口装饰点，但保留设计稿窗口控制区域的 `12px` 顶部空白；`figma-v2` 侧栏品牌、新建任务、搜索框实测 `y=52/104/161`。
- [x] 本次仅作用于 `designChat` fixture，业务状态圆点和普通页面不受影响；浏览器实测左上角红、黄、绿窗口装饰候选为 `0`，Figma 设计稿未修改。
- [x] 新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-sidebar-baseline-browser-2026-08-27.png`；同尺寸 diff 为 `34.5656% / MAE 4.61398 / RMSE 21.70300 / maxChannelDelta 239`，已同步 105 画板映射和汇总结果，画板继续为 `DIFF_REVIEW`。
- [ ] 本小点不代表 Agent Chat 或 105 张画板达到像素级 `PASS`；头像、图标和其他视觉差异、shadcn 全页面迁移及 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 96. 2026-08-27 摄入分析默认态垂直节奏收口

- [x] 依据 Figma 节点 `640:773`，仅对 `/analysis?state=v2` 的默认态摘要区使用 `107px` 高度；Empty 保持 `100px`，Loading 保持 `126px`，Error 不渲染摘要区。
- [x] 浏览器 headless fallback 使用 `1440×1024`、DPR `1`、字体已加载；默认态实测摘要卡 `y=164,h=107`，趋势卡 `y=295,h=303`，洞察卡 `y=622,h=219`，数据质量面板 `y=873,h=140`，页面无横向溢出。
- [x] `/analysis?state=v2`、`empty`、`loading`、`error` 四种状态的左上角红、黄、绿窗口装饰候选数量均为 `0`；业务状态圆点保留，Figma 设计稿未修改。
- [x] 最新 DPR1 浏览器证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/intake-analysis-v2-browser-default-height-2026-08-27-dpr1.png`；同尺寸 diff 为 `8.3475% / MAE 2.82908 / RMSE 17.57882 / maxChannelDelta 211`，相较上一份 `13.9400%` 证据有所改善。
- [x] 已同步 `figma-105-mapping.json` 与 `figma-105-diff-results.json`；该画板继续保持 `DIFF_REVIEW`，没有将局部几何改善标记为像素级 `PASS`。
- [ ] 本小点不关闭 105 张画板全量像素 `PASS`、shadcn 全页面视觉迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 106. 2026-08-28 餐食规划默认页计划横幅按钮几何收口

- [x] 依据 Figma 节点 `640:901`，将计划横幅描边调整为等效内描边，并把“重新生成”“保存计划”按钮固定为 `88×37px`；仅修改前端 `PlanningPage.module.css`，未修改 Figma 设计稿。
- [x] 浏览器实测横幅为 `776×97px`，两个按钮均为 `88×37px`；视口 `1440×1024`，字体已加载，页面无横向溢出。
- [x] 新增浏览器 PNG、JPEG 和 RGBA 归一化证据及独立 diff JSON；与最新 Figma PNG 比较为 `23.3449% / MAE 2.40089 / RMSE 13.73352 / maxChannelDelta 234`，结果继续为 `DIFF_REVIEW`。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`；没有对应内容需要删除，业务状态圆点保留，Figma 设计稿未修改。
- [ ] 当前 in-app 采集实际 DPR 为 `1.25`，本条不代表该画板或 105 张画板像素级 `PASS`；shadcn 全量逐页视觉迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 107. 2026-08-28 Profile Basic 默认入口 fixture 收口

- [x] mock 模式的 `/profile` 默认入口已切换为 Figma 节点 `806:1119` 对应的账号和资料 fixture；real 模式无 query 时仍保留真实用户资料，显式 `?state=basic` 入口继续可用。
- [x] 默认 fixture 使用 `Anddy / anddy_operator_9 / 1234567`、`180cm`、`78kg`、`精益增肌`、`2500 kcal`、`150g protein`、`花生 / 乳糖`，并继续使用已登记的男女 SVG 默认头像。
- [x] 浏览器实测 `1440×1024`、DPR `1.25`、字体已加载、无页面横向溢出；前端左上角红黄绿窗口装饰候选数量为 `0`，没有需要删除的对应代码，业务状态圆点保留，Figma 设计稿未修改。
- [x] Profile/Workspace 定向测试 `27/27` 通过；独立 PNG diff 为 `67.8905% / MAE 4.87625 / RMSE 21.40682 / maxChannelDelta 244`，证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/`，结论继续为 `DIFF_REVIEW`。
- [ ] 本小点不关闭 105 张画板全量像素 `PASS`、shadcn 全页面视觉迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 122. 2026-08-29 Knowledge 来源不可用状态视觉收口

- [x] 按 Figma 节点 `795:1151` 修复 `/knowledge?state=source-unavailable`：恢复 `6px` 橙色左侧色条、`PARTIAL ACCESS` 标签、橙色标题、`600×260` 卡片圆角和状态层背景；Figma 设计稿未修改。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149` 实测卡片 `x=550,y=300,width=600,height=260`、标签 `174×24`；前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点继续保留。
- [x] `KnowledgePage` 定向测试 `6/6` 通过；最新 RGBA 截图和独立 diff 已登记，结果为 `35.1598% / MAE 3.94340 / RMSE 16.95234 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`。
- [ ] 本小点只收口来源不可用卡片视觉层，不代表 105 张画板像素级 `PASS`、shadcn 全量迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 121. 2026-08-29 Knowledge 检索失败状态视觉收口

- [x] 按 Figma 节点 `795:968` 修复 `/knowledge?state=search-failed`：恢复 `6px` 红色左侧色条、`ERROR · RETRY AVAILABLE` 标签、红色标题、`600×260` 卡片圆角和状态层背景；Figma 设计稿未修改。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149` 实测卡片 `x=550,y=300,width=600,height=260`、标签 `174×24`；前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点继续保留。
- [x] `KnowledgePage` 定向测试 `6/6` 通过；最新 RGBA 截图和独立 diff 已登记，结果为 `35.2425% / MAE 3.88158 / RMSE 16.57742 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`。
- [ ] 本小点只收口检索失败卡片视觉层，不代表 105 张画板像素级 `PASS`、shadcn 全量迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 120. 2026-08-29 Knowledge 空态前端窗口装饰复核

- [x] 复核 Figma `795:786` 与 `/knowledge?state=empty`：Figma 中的红、黄、绿窗口装饰仅作为设计参考，前端不渲染；业务会话状态圆点继续保留。
- [x] 新增 `KnowledgePage` 回归断言，确认空态 fixture 不包含 `window-controls`、`traffic-light` 或三色窗口控制标记；Figma 设计稿未修改。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149`、字体加载完成条件下复核，空态卡片为 `x=570,y=320,width=560,height=220`，页面无横向溢出。
- [x] 最新同尺寸 PNG diff 为 `22.0938% / MAE 2.30524 / RMSE 13.80022 / maxChannelDelta 244`，证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/user-knowledge-empty-browser-dpr1-2026-08-29.png`，结论继续为 `DIFF_REVIEW`。
- [ ] 本小点只固化前端窗口装饰复核，不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 123. 2026-08-29 Admin Tool Calls 表格行高收口

- [x] `/admin?state=tool-calls` 已按实时 Figma 节点 `797:359`、`797:453-462` 将工具调用表固定为 `1132×87px`：表头 `38px`、数据行 `49px`；表格边框改为 inset 实现，不改变布局尺寸。
- [x] 浏览器在 `1440×1024 / DPR 1` 下实测表格 `1132×87px`、载荷卡片 `1132×321.4px`、说明面板 `1132×250px`，字体已加载且页面无横向/纵向溢出。
- [x] 前端左上角红黄绿窗口装饰候选数量为 `0`，侧栏权限状态圆点保留，Figma 设计稿保持只读；Admin 定向测试 `8/8` 通过。
- [x] 新增证据 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/admin-tool-calls-table-height-browser-2026-08-29-rgba.png`、实时 Figma 参考图和独立 diff；差异比例 `21.1480%`、`MAE=2.85604`、`RMSE=17.03777`、最大通道差异 `230`，映射与聚合结果已同步。
- [ ] 本项只完成工具调用表局部几何收口；画板仍为 `DIFF_REVIEW`，不代表 105 张画板像素级 `PASS`、shadcn 全页面视觉迁移或 iconfont 实体资源登记完成。iconfont 继续为 `BLOCKED`。

## 125. 2026-08-29 Admin Trace 事件面板几何收口

- [x] `/admin?state=trace` 按 Figma 节点 `797:621`、`797:732`、`986:25` 收口：工具栏 `34px`，运行表 `1132×87px`，事件面板 `1132×162px`，四张事件卡 `261×87px`，详情面板 `1132×300px at y=454`；使用内描边保持尺寸，不修改 Figma。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载；事件面板 `y=257.28`，详情面板 `y=454.08`，页面无横向/纵向溢出，左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点继续保留。
- [x] 最新证据已登记：Figma PNG `admin-trace-figma-live-2026-08-29.png`、浏览器 RGBA PNG `admin-trace-height-browser-2026-08-29-rgba.png`、独立 diff `admin-trace-height-2026-08-29-diff.json`；差异比例 `24.8562%`、`MAE=2.743037`、`RMSE=15.440114`、最大通道差异 `230`。
- [ ] 该画板仍为 `DIFF_REVIEW`，侧栏身份、图标、字体及其它整页视觉差异尚未消除；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移未完成，iconfont 继续为 `BLOCKED`。

## 126. 2026-08-29 Admin Run Detail 详情面板垂直位置收口

- [x] `/admin?state=run-detail` 按 Figma 节点 `797:212`、`986:3` 对齐详情面板：上间距由 `24px` 调整为 `34.8px`，详情面板实测 `1132×300px at y=453.998`，共享 Trace 事件面板保持 `1132×162px at y=257`；未修改 Figma，业务状态圆点继续保留。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载，页面无横向/纵向溢出，左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 最新证据已登记：Figma PNG `admin-run-detail-figma-live-2026-08-29.png`、浏览器 RGBA PNG `admin-run-detail-height-browser-2026-08-29-rgba.png`、独立 diff `admin-run-detail-height-2026-08-29-diff.json`；差异比例 `25.4806%`、`MAE=2.793519`、`RMSE=15.535898`、最大通道差异 `230`。
- [ ] 该画板仍为 `DIFF_REVIEW`，侧栏身份、图标、字体及其它整页视觉差异尚未消除；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移未完成，iconfont 继续为 `BLOCKED`。

## 124. 2026-08-29 Admin SQL Audit 详情面板垂直位置收口

- [x] `/admin?state=sql-audit` 按 Figma 节点 `797:490`、`986:18` 对齐 SQL Audit 详情面板：仅新增 `sqlAuditNotes` 前端样式，将面板从 `y=656.4` 调整至 `y=674`，未修改 Figma，业务状态圆点继续保留。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载；调用表 `1132×87px`、Payload 卡片 `1132×321.4px`、详情面板 `1132×250px at y=674`，页面无横向/纵向溢出，左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 最新证据已登记：Figma PNG `admin-sql-audit-figma-live-2026-08-29.png`、浏览器 RGBA PNG `admin-sql-audit-notes-offset-browser-2026-08-29-rgba.png`、独立 diff `admin-sql-audit-notes-offset-2026-08-29-diff.json`；差异比例 `19.6396%`、`MAE=2.520686`、`RMSE=16.064298`、最大通道差异 `230`。
- [ ] 该画板仍为 `DIFF_REVIEW`，侧栏身份、图标、字体及其它整页视觉差异尚未消除；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移未完成，iconfont 继续为 `BLOCKED`。

## 127. 2026-08-29 Admin User Detail 筛选行垂直对齐收口

- [x] `/admin?state=user-detail` 按 Figma 节点 `801:215` 的筛选行基线，将“重置筛选”按钮从继承的 `40px` 高度固定为 `32px`；搜索、角色、状态、日期和重置控件均对齐到 `y=88`，未修改 Figma 设计稿，也未删除业务状态圆点。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载；筛选行 `692×32px`，用户表 `692×278px at x=284,y=144`，详情面板 `420×912px at x=996,y=88`，页面无横向/纵向溢出，左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 最新 RGBA PNG `admin-user-detail-filter-aligned-browser-2026-08-29-rgba.png` 和独立 diff `admin-user-detail-filter-aligned-2026-08-29-diff.json` 已登记；差异比例 `22.4363%`、`MAE=3.276996`、`RMSE=17.607226`、最大通道差异 `233`，较修正前 `29.3546%` 有所改善；Admin 定向测试 `8/8` 通过。
- [ ] 本小点只完成筛选行局部几何收口，整页仍为 `DIFF_REVIEW`，不代表 105 张画板像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 129. 2026-08-29 Admin Deleted Resources 表格行高与内描边收口

- [x] 依据实时 Figma 节点 `692:4205`、`692:4215`、`692:4216`、`692:4236`、`692:4256`、`692:4276`，将删除者列内容宽度恢复为 `120px`，使 `anddy_operator_9` 按设计稿换行；四条数据行实测为 `70px / 58px / 70px / 58px`。
- [x] 仅修改前端 `AdminPage.module.css`：表格外框和行分隔线改用内描边，表格容器实测 `1116×304px`，表头实测 `1116×48px`；Figma 设计稿、真实后端和业务状态圆点未修改。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向溢出；前端左上角红黄绿窗口装饰候选数量为 `0`，删除者内容宽度为 `120px`。
- [x] 新增浏览器 JPEG、RGBA PNG 和独立 diff 证据；`png-diff.mjs` 结果为 `26.0396% / MAE 3.10932 / RMSE 17.05253 / maxChannelDelta 230`，相较前一份 `27.2033%` 的 MAE/RMSE 有所改善，画板继续为 `DIFF_REVIEW`。
- [x] Deleted Resources 定向测试 `11/11` 和 `npm run typecheck` 通过；目标 CSS Prettier 检查仍有本次之前的格式提示，`git diff --check` 通过。
- [ ] 本小点只完成删除资源表格局部几何收口，不代表 105 张画板像素级 `PASS` 或 shadcn 全页面迁移完成；iconfont 继续为 `BLOCKED`。

## 131. 2026-08-29 Admin Trace 下载链接样式收口

- [x] `/admin?state=trace` 按 Figma 节点 `797:621`、`797:737` 对齐下载链接：`#5da9b2`、Noto Sans SC Regular、`13px`、下划线；仅修改前端 `AdminPage.module.css`，未修改 Figma，业务权限状态圆点继续保留。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载；链接实测 `78×16px at x=1318,y=278.5`，页面无横向/纵向溢出，前端左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 新增浏览器原始 JPEG `admin-trace-download-link-browser-2026-08-29.jpg`、RGBA PNG 和独立 diff 证据；同尺寸 `png-diff.mjs` 结果为 `24.8855% / MAE 2.733262 / RMSE 15.432517 / maxChannelDelta 230`，画板继续为 `DIFF_REVIEW`。
- [x] Admin 定向测试 `8/8` 通过；运行时 computed style、截图格式、JSON 解析和 `git diff --check` 已验证。
- [ ] 本小点只完成下载链接局部样式收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 132. 2026-08-29 Admin Trace 状态与时间线颜色收口

- [x] `/admin?state=trace` 按 Figma 节点 `797:704`、`797:705`、`797:706`、`797:707`、`797:741`、`797:747`、`797:753`、`797:759` 对齐失败/成功筛选颜色、`11px` 字号、`Failed ✖` 文案和四个时间线标题颜色；仅修改前端 `AdminPage.tsx`、`AdminPage.module.css`，未修改 Figma，业务权限状态圆点继续保留。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体已加载；工具栏 `1132×34px`，搜索框 `663.375×32px at x=476,y=89`，失败/成功筛选 `64.1875×25px`、`72.4375×25px`，页面无横向/纵向溢出，前端左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 新增浏览器原始 JPEG、RGBA PNG 和独立 diff 证据；同尺寸 `png-diff.mjs` 结果为 `24.79397% / MAE 2.713447 / RMSE 15.377519 / maxChannelDelta 230`，较前项 `24.8855%` 有所改善，画板继续为 `DIFF_REVIEW`。
- [x] Admin 定向测试 `8/8`、`npm run typecheck`、截图格式转换、JSON 解析和 `git diff --check` 已通过。
- [ ] 本小点只完成 Trace 状态和时间线颜色局部收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 133. 2026-08-29 Admin Trace 运行记录表列轨道收口

- [x] 依据实时 Figma 节点 `797:712-731`，将 `/admin?state=trace` 的 Run 记录表表头与数据行统一为 `120px 120px minmax(0, 1fr) 110px 120px 90px 80px 80px`，列间距统一为 `12px`；仅修改前端 `AdminPage.module.css`，未修改 Figma 设计稿，也未删除业务权限状态圆点。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149011612`、字体加载完成条件下实测表格为 `1132×87px at x=284,y=146`；状态列为 `110px at x=876`，阶段列为 `120px at x=998`，页面无横向或纵向溢出，左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 最新浏览器原始 JPEG `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/admin-trace-table-columns-browser-2026-08-29.jpg`、RGBA PNG `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/admin-trace-table-columns-browser-2026-08-29-rgba.png` 和独立 diff `foodmate-ui/.qa/figma-pixel-acceptance/admin-trace-table-columns-2026-08-29-diff.json` 已登记；同尺寸结果为 `differentPixels=364747`、差异比例 `24.7360%`、`MAE=2.652009`、`RMSE=15.174374`、最大通道差异 `230`，结论继续为 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json` 已保留 105 个画板的完整字段，并将 `admin-trace` 指向本次最新浏览器证据；聚合结果仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] Admin Trace 定向测试、JSON 解析、`npm run typecheck`、`npm run build` 和 `git diff --check` 已验证；Figma 设计稿未修改。
- [ ] 本小点只完成 Run 记录表列轨道局部几何收口，不代表该画板或 105 张画板达到像素级 `PASS`；侧栏身份、其它字体/图标和整页组合差异仍需继续处理，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 134. 2026-08-29 Token 无效页背景边界收口

- [x] 按最新 Figma 节点 `680:738` 对 `/token-status?state=invalid` 的斜向背景边界进行像素测量；前端仅增加 Token 页面 `+8px` 顶部交点修正，左上角红黄绿窗口装饰仍不存在，业务状态圆点未删除。
- [x] 浏览器 `1440×900`、DPR `1.0000000149011612`、字体加载完成；卡片 `460×416.4px`，页面无横向溢出。
- [x] 新增最新 Figma PNG、浏览器 RGBA PNG 和独立 diff JSON；结果为 `differentPixels=661829`、差异比例 `51.0671%`、`MAE=0.437377`、`RMSE=2.339229`、最大通道差异 `154`，较旧证据的 `99.9988% / RMSE 25.36159` 已改善，结论继续为 `DIFF_REVIEW`。
- [x] 105 画板映射中的 `token-invalid` 已指向本轮证据；聚合结果仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] Token/Auth/Workspace/Knowledge 定向测试 `38/38`、`npm run typecheck`、`npm run build` 和 `git diff --check` 已验证。
- [ ] 本小点只完成 Token 无效页背景边界的局部视觉收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 135. 2026-08-29 Token 过期页当前证据收口

- [x] 按实时 Figma 节点 `680:757` 复核 `/token-status?state=expired`；页面复用已验证的 Token 认证壳层和斜线背景边界，不需要新增源码改动，未修改 Figma 设计稿。
- [x] 浏览器在 `1440×900`、DPR `1.0000000149011612`、字体加载完成条件下实测卡片为 `460×416.4px at x=490,y=241.8`，页面无横向溢出；重新发送和返回登录入口保持可用。
- [x] 新增当前 Figma PNG、浏览器截图转换后的 RGBA PNG 和独立 diff JSON；结果为 `differentPixels=661762`、差异比例 `51.0619%`、`MAE=0.446812`、`RMSE=2.399694`、最大通道差异 `154`，较旧证据的 `99.9988% / RMSE 25.47945` 已改善，结论继续为 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json` 已将 `token-expired` 指向本轮证据；105 张画板聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] Token/Auth 定向行为测试、类型检查和生产构建已在本轮共享样式修复后通过；本轮证据 JSON 已重新解析，`git diff --check` 已验证。
- [ ] 本小点只完成 Token 过期页当前证据收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 136. 2026-08-29 Token 已使用页当前证据收口

- [x] 按实时 Figma 节点 `680:776` 复核 `/token-status?state=used`；品牌、信息状态图标、重新发送、联系客服和返回登录均与设计稿层级一致，未修改 Figma 设计稿。
- [x] 浏览器在 `1440×900`、DPR `1.0000000149011612`、字体加载完成条件下实测卡片为 `460×506.8px at x=490,y=196.6`；两个操作按钮均为 `380×52px`，间距为 `16px`，页面无横向溢出。
- [x] 新增当前 Figma PNG、浏览器截图转换后的 RGBA PNG 和独立 diff JSON；结果为 `differentPixels=674374`、差异比例 `52.0350%`、`MAE=0.551200`、`RMSE=3.350627`、最大通道差异 `149`，结论继续为 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json` 已将 `token-used` 指向本轮证据；105 张画板聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] Token 页面行为测试、JSON 解析和 `git diff --check` 已验证；状态按钮导航逻辑未改变。
- [ ] 本小点只完成 Token 已使用页当前证据收口，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 137. 2026-08-29 Agent Write Confirmation 最新证据

- [x] `687:773` `/chat?state=write-confirmation` 已登记最新 Figma PNG、浏览器 RGBA PNG、运行时检查和独立 diff JSON；映射字段保持完整。
- [x] 写入确认卡局部几何已按 Figma 收口：卡片 `305×319px`、详情区 `265×168px`、元信息单行 `265×13px`、操作按钮 `84×32px` / `58×32px`；确认/取消按钮不包含设计稿不存在的图标，真实写入确认和取消接口未改变。
- [x] 运行时 `1440×1024`、字体已加载、无页面横向溢出、文字越界 `0`；前端左上角红黄绿窗口装饰点数量为 `0`，没有删除业务状态圆点。
- [x] 最新 PNG diff 为 `18.6870% / MAE 2.020594 / RMSE 13.143764 / maxChannelDelta 232`，结论保持 `DIFF_REVIEW`；定向测试 `27/27`、`npm run typecheck` 和 `git diff --check` 已通过。
- [ ] 本小点不代表该画板或 105 张画板达到像素级 `PASS`；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移未完成，iconfont 继续为 `BLOCKED`。

## 138. 2026-08-29 Agent Tool Failed Retryable 最新视觉收口

- [x] 按实时 Figma 节点 `687:1439` 收口 `/chat?state=tool-failed-retryable` 的失败卡片：`560×160px`、`#ffe9e9` 背景、`#ff7576` 描边、`16px` 圆角、`16px` 内容间距；说明区域按 Figma 的 `8px` 子间距分组。
- [x] 标题、错误详情和说明分别使用 Figma 的 `14px/#d67676`、`13px/#333` 和 `12px/#6b7280`；重试按钮为 `58×32px`、`#ffda93` 白字，跳过按钮为 `97×32px` 白底深色字。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149011612`、字体加载完成条件下实测目标卡片 `560×160px`，页面无横向溢出，前端左上角红黄绿窗口装饰点为 `0`；业务状态点未删除。
- [x] `ChatPage` 定向测试 `27/27`、`agentRunService` 定向测试 `3/3`、相关文件 Prettier 检查和 `git diff --check` 已通过；最新同尺寸 PNG diff 为 `20.1341% / MAE 2.241953 / RMSE 14.730809 / maxChannelDelta 245`，画板继续标记 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json` 已将该画板指向本轮 Figma 与浏览器 RGBA 证据；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成失败卡片局部视觉收口，不代表该画板或 105 张画板达到像素级 `PASS`、shadcn 全页面迁移或 iconfont 解阻塞；iconfont 继续为 `BLOCKED`。

## 139. 2026-08-29 Agent Budget Limit 最新视觉收口

- [x] 按实时 Figma 节点 `687:918` 收口 `/chat?state=budget-limit`：预算卡 `286×289px`、选择说明区 `246×60px`、Token 计量区 `246×27px`，与设计稿的层级和垂直节奏一致。
- [x] 追加预算和结束会话按钮分别固定为 `150×32px`、`84×32px`；预算说明、选择项、进度条颜色、字号和圆角按 Figma 当前节点校准；真实模式预算追加和取消接口逻辑保持不变。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149011612`、字体加载完成条件下复核，目标卡片几何门禁通过，页面无横向/纵向溢出，前端左上角红黄绿窗口装饰点为 `0`；业务状态点未删除，Figma 设计稿未修改。
- [x] `ChatPage` 定向测试 `27/27`、`npm run typecheck`、目标文件 Prettier 检查和 `git diff --check` 已通过；最新同尺寸 PNG diff 为 `19.0439% / MAE 2.626395 / RMSE 15.964083 / maxChannelDelta 237`，画板继续标记 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json`、`figma-105-diff-results.json` 和独立 diff JSON 已同步本轮 Figma/浏览器证据；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成预算卡局部视觉收口，不代表该画板或 105 张画板达到像素级 `PASS`、shadcn 全页面迁移或 iconfont 解阻塞；iconfont 继续为 `BLOCKED`。

## 140. 2026-08-29 Agent Chat 消息操作说明补充收口

- [x] 依据 Figma 节点 `640:428`，补齐消息操作面板的第四行“右侧面板：运行 · 工具 · 引用；原始 JSON 默认折叠并隐藏敏感参数”，并同步测试断言；仅修改前端，未修改 Figma。
- [x] 独立补充证据使用 `/chat?state=redesign-default`、`1440×1024` 视口；消息操作面板实测为 `796×150px`，Figma 与浏览器截图同尺寸，独立结果为 `foodmate-ui/.qa/figma-pixel-acceptance/agent-chat-v2-message-actions-2026-08-29-diff.json`，diff 为 `23.5613% / MAE 2.557491 / RMSE 14.877245 / maxChannelDelta 234`，结论保持 `DIFF_REVIEW`。
- [x] 105 画板映射保留主入口 `/chat?state=figma-v2`，并新增明确的局部 `supplementalEvidence`，避免将不同 query fixture 错误登记为同一主画板结果。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`；设计稿中的三色点保持不变，业务会话状态圆点不删除。
- [ ] 本小点只完成消息操作说明和局部证据收口，不代表 `agent-chat-v2` 或 105 张画板达到像素级 `PASS`；当前汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 143. 2026-08-29 Admin Overview 顶栏横向内边距收口

- [x] 依据实时 Figma 节点 `995:1044`、`995:1045`、`995:1049`，将前端 Admin Overview 桌面 `.topbar` 左右内边距从 `24px` 调整为 `32px`；移动端媒体查询保持 `16px`，未修改 Figma 设计稿。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体状态 `loaded`；顶栏 `1180×64px`，标题 `160×24px at x=292,y=19.6`，右侧操作区 `192.6×32px at x=1215.4,y=15.6`，刷新按钮 `85.6×32px at x=1322.4,y=15.6`，页面无横向/纵向溢出。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`；本次仅核验/保持前端无三色窗口点，业务权限状态圆点保留，Figma 设计稿未修改。
- [x] 新增真实 RGBA PNG `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/admin-overview-topbar-padding-browser-2026-08-29.png` 和独立 diff `foodmate-ui/.qa/figma-pixel-acceptance/admin-overview-topbar-padding-2026-08-29-diff.json`；同尺寸结果为 `differentPixels=493936`、差异比例 `33.4972%`、`MAE=3.988667`、`RMSE=19.207800`、最大通道差异 `230`，保持 `DIFF_REVIEW`。
- [x] 已同步 `figma-105-mapping.json`、`figma-105-diff-results.json`；Admin 定向测试 `9/9`、`npm run typecheck` 和 `git diff --check` 通过。
- [ ] 本小点只完成 Admin Overview 顶栏局部几何收口，不代表 105 张画板像素级 `PASS`；整页视觉差异、shadcn 全页面迁移和 iconfont 实体登记仍未完成，iconfont 继续为 `BLOCKED`。

## 144. 2026-08-29 Admin Overview 分析卡内卡高度与裁切收口

- [x] 依据实时 Figma 节点 `1005:2`、`1005:3`、`1005:7`、`1005:11`，将底部三个分析内卡高度从 `148px` 调整为 `180px`，并为外层分析卡补齐 `hidden` 裁切；仅修改前端，Figma 设计稿和业务权限状态圆点未修改。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载；外框为 `1116×180px at x=292,y=766`，三个内卡高度均为 `180px`，页面无横向或纵向溢出，前端左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 实时 Figma/浏览器同尺寸 diff 为 `33.6643% / MAE 3.932245 / RMSE 19.178615 / maxChannelDelta 230`，证据和聚合结果已同步，画板保持 `DIFF_REVIEW`；Admin 定向测试 `9/9` 通过。
- [ ] 本小点不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 继续为 `BLOCKED`。

## 145. 2026-08-29 Admin Overview 主表列轨道收口

- [x] 依据实时 Figma 节点 `995:1053`、`995:1085`，将 Admin Overview 主表列轨道收口为 `196px 96px 116px 116px 116px 156px 84px 84px 64px 88px`；表格目标尺寸为 `1116×396px`，表头 `48px`，六条数据行各 `58px`，未修改 Figma 设计稿。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149011612`、字体加载完成条件下实测表格 `1116×396px at x=292,y=346`；页面无横向或纵向溢出，前端左上角红、黄、绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 已登记 JPEG、RGBA PNG 和独立 diff JSON；同尺寸 `png-diff.mjs` 结果为 `differentPixels=495686`、差异比例 `33.6159%`、`MAE=4.139174`、`RMSE=19.694850`、最大通道差异 `230`，结论继续为 `DIFF_REVIEW`。
- [x] 105 张画板映射已指向最新主表证据，分析卡高度和顶栏内边距证据仍保留为补充记录；聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成主表列轨道局部几何收口，不代表 Admin Overview 或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 146. 2026-08-29 Admin Overview 桌面表格滚动条收口

- [x] 依据实时 Figma 节点 `995:1085`，将 Admin Overview 表格卡片占用内容宽度的 1px 边框改为等效内描边，保持 `#f4f6f5` 视觉边界和 `1116×396px` 外框尺寸，未修改 Figma 设计稿。
- [x] 严格 `1440×1024`、DPR `1.0000000149011612`、字体已加载条件下，表格可视区和内容区均为 `1116px`，`scrollWidth === clientWidth`，桌面横向滚动条消失；`390×844` 下仍保留表格内部横向滚动，页面级宽度为 `390px`。
- [x] 已登记原始截图、RGBA PNG 和独立 diff JSON；同尺寸结果为 `differentPixels=486020`、差异比例 `32.9603%`、`MAE=3.566176`、`RMSE=18.049821`、最大通道差异 `230`，结论继续为 `DIFF_REVIEW`。
- [x] 105 张画板映射已指向本次最新 Admin Overview 证据，聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只消除桌面表格非设计滚动条，不代表 Admin Overview 或 105 张画板达到像素级 `PASS`；表格行高、shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 147. 2026-08-29 Admin Overview 主表数据行高度收口

- [x] 按 Figma 节点 `995:1053`、`995:1085` 将 Admin Overview 主表六条数据行固定为 `58px`，表头保持 `48px`，桌面主表总高为 `396px`；只修改前端，未修改 Figma。
- [x] `1440×1024`、DPR `1.0000000149011612`、字体已加载条件下，六条数据行实际均为 `58px`，表格/卡片为 `1116×396px`，页面无溢出；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除。
- [x] `390×844` 下可视区约 `343px`、表格内容区 `1116px`，内部横向滚动保留且页面宽度仍为 `390px`。
- [x] 新增原始浏览器截图、RGBA PNG 和独立 diff：`472888` 个差异像素、差异比例 `32.0698%`、`MAE=2.946841`、`RMSE=15.766627`、最大通道差异 `230`；整页结论保持 `DIFF_REVIEW`。
- [x] Admin 定向测试 `9/9`、`npm run typecheck`、`npm run build`、`git diff --check` 已验证；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点不代表 Admin Overview 或 105 张画板像素级 `PASS`，也不代表 shadcn 全页面迁移完成；iconfont 实体资源继续为 `BLOCKED`。

## 148. 2026-08-29 Admin Overview 侧栏导航图标资产收口

- [x] 重新读取 Figma 节点 `995:977` 的 Admin Overview 侧栏结构，确认导航图标使用 9 个唯一的 `18×18` SVG 资产；前端新增 `public/assets/figma/admin/navigation/` 资源目录，并按 Figma 的复用关系覆盖 11 个导航项。
- [x] `adminNavItems` 改为保存可追溯的 Figma 资产路径，导航图标通过 CSS mask 保留真实 SVG 轮廓并按当前菜单状态着色；标准操作图标仍继续使用 Lucide，未创建 iconfont 字体、glyph 或 Unicode 映射。
- [x] 浏览器实测 `1440×1024` 与 `390×844`、DPR `1.0000000149011612`：11 个导航图标桌面和移动端均为 `18×18`，移动端仅折叠文字、不隐藏图标；页面无横向溢出、资源加载失败数为 `0`，前端左上角红黄绿窗口装饰候选数量为 `0`，业务权限状态圆点保留，Figma 设计稿未修改。
- [x] 最新同尺寸 PNG diff 使用 `admin-overview-navigation-browser-2026-08-29.png`，结果为 `differentPixels=472595`、差异比例 `32.0499%`、`MAE=2.965224`、`RMSE=15.923452`、最大通道差异 `230`；独立结果为 `admin-overview-navigation-2026-08-29-diff.json`，结论继续为 `DIFF_REVIEW`。
- [x] Admin 定向测试 `10/10`、`npm run typecheck`、`npm run build`、`git diff --check` 已通过；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成 Admin 侧栏导航图标资产和移动端可见性收口，不代表 Admin Overview 或 105 张画板达到像素级 `PASS`；整页字体、侧栏身份、其它组合差异、shadcn 全页面迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。
## 149. 2026-08-29 Admin Overview 筛选栏图标资产收口

- [x] 依据实时 Figma 节点 `995:1054`，将时间、结果、降级三个筛选 Select 的箭头和搜索框图标替换为已登记的 Figma SVG 资产；Select 仍使用 shadcn/Radix 行为，未修改 Figma 设计稿。
- [x] 三个下拉箭头实际为 `12×12`，搜索图标实际为 `16×16`；浏览器在 `1440×1024`、DPR `1.0000000149011612`、字体已加载条件下无页面横向溢出。移动截图实际尺寸为 `390×844`，页面宽度保持 `390px`，内部筛选交互可打开并选择 `failed`，返回两条失败记录。
- [x] 前端左上角红、黄、绿窗口装饰候选数量在桌面和移动检查中均为 `0`；业务状态点未删除。资源文件为 `public/assets/figma/admin/overview/dropdown-arrow.svg` 与 `search.svg`。
- [x] 独立 PNG diff 为 `differentPixels=862029`、差异比例 `58.4601%`、`MAE=3.634867`、`RMSE=17.215182`、最大通道差异 `230`，证据为 `admin-overview-filter-icons-browser-1440x1024-2026-08-29-rgba.png` 与 `admin-overview-filter-icons-2026-08-29-diff.json`，结论保持 `DIFF_REVIEW`。
- [x] Admin 定向测试 `11/11`、目标文件 Prettier、`npm run typecheck`、`npm run build` 和本次改动 `git diff --check` 已通过；仓库级 `format:check` 仍受既有无关文件阻断。
- [ ] 本小点只完成筛选栏图标资产与交互核验，不代表 Admin Overview 或 105 张画板达到像素级 `PASS`；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 150. 2026-08-30 Agent Safety Degraded 内容边界对齐

- [x] 依据 Figma 节点 `687:1563`，将 `/chat?state=safety-degraded` 的安全内容容器从 `608px` 调整为 `612px`，正文右移 `4px`；移动端清除该偏移，避免横向溢出。
- [x] 浏览器 `1440×1024` 实测安全内容容器 `x=292,width=612`，警告带和回答区均为 `x=344,width=560`；`390×844` 内容区为 `x=64,width=295.2`，字体已加载，页面无横向溢出。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`，业务状态圆点保留，Figma 设计稿保持只读。
- [x] 最新同尺寸 `png-diff.mjs` 结果为 `376896` 个差异像素、差异比例 `25.5599%`、`MAE=3.223878`、`RMSE=16.379168`、最大通道差异 `255`；独立证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-safety-degraded-horizontal-boundary-browser-1440x1024-2026-08-30-rgba.png`，独立 diff 为 `agent-safety-degraded-horizontal-boundary-2026-08-30-diff.json`，结论保持 `DIFF_REVIEW`。
- [x] ChatPage 与 WorkspaceLayout 定向测试共 `34/34` 通过；该小点只完成局部边界对齐，不代表该画板或 105 张画板达到像素级 `PASS`，shadcn 全页面迁移仍未完成，iconfont 继续为 `BLOCKED`。

## 151. 2026-08-30 Agent SSE Reconnecting 时间文案收口

- [x] 依据 Figma 节点 `687:1803`，将 `/chat?state=sse-reconnecting` 的用户消息时间从共享默认时间修正为 `Anddy · 03:00 PM`；仅修改前端，未修改 Figma 设计稿。
- [x] 浏览器 `1440×1024` 实测字体已加载、页面无横向溢出；助手行 `608×52px`、助手气泡 `560×52px`、重连提示带 `1132×66px`、输入区 `1180×112px` 均有运行时证据。前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 已登记最新浏览器 PNG、Figma PNG 和独立 diff JSON；`png-diff.mjs` 结果为 `373405` 个差异像素、差异比例 `25.3231%`、`MAE=2.19232`、`RMSE=13.47360`、最大通道差异 `234`，结论继续为 `DIFF_REVIEW`；当前采集实际 DPR 为 `1.25`，不满足严格 DPR 1 门禁。
- [x] 重连态定向测试、类型检查、生产构建和 `git diff --check` 将在本小点提交前重新验证；Figma 设计稿保持只读。
- [ ] 本小点只完成重连态时间文案和证据收口，不代表该画板或 105 张画板达到像素级 `PASS`；整页工作台、头像、字体和图标差异仍需继续处理，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 152. 2026-08-30 Agent SSE Reconnecting 告警颜色收口

- [x] 依据 Figma 节点 `687:1803`，将重连提示带背景、描边和文字颜色对齐为 `#FFEDD5`、`#F97316`、`#C2410C`；仅修改前端，未修改 Figma 设计稿。
- [x] 浏览器实测提示带 `x=284,y=820,width=1132,height=66`，计算样式与 Figma 颜色一致；视口 `1440×1024`、字体已加载、页面无横向溢出，前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 已登记最新浏览器 PNG、Figma PNG 和独立 diff JSON；`png-diff.mjs` 结果为 `339487` 个差异像素、差异比例 `23.0229%`、`MAE=2.77881`、`RMSE=15.46246`、最大通道差异 `249`，相较上一个重连态证据 `25.3231%` 有下降，结论继续为 `DIFF_REVIEW`。
- [x] 重连态测试、类型检查、生产构建和 `git diff --check` 将在本小点提交前重新验证；Figma 设计稿保持只读。
- [ ] 本小点只完成重连提示带颜色收口，不代表该画板或 105 张画板达到像素级 `PASS`；工作台、头像、字体和图标差异仍需继续处理，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 153. 2026-08-30 Agent Chat 侧栏选中态背景色收口

- [x] 依据 Figma 节点 `640:428`，将 `/chat?state=figma-v2` 侧栏 `Agent 对话` 选中态背景对齐为 `#FBF7F2`，当前会话行背景对齐为 `#FFFCF9`；仅修改前端 `.designChat` 语义变量，未修改 Figma 设计稿。
- [x] 浏览器实测视口 `1440×1024`、字体状态 `loaded`、侧栏选中态实际为 `rgb(251, 247, 242)`、当前会话行实际为 `rgb(255, 252, 249)`；页面无横向/纵向溢出，前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保留。
- [x] 已登记浏览器 RGBA PNG、Figma 参考和独立 diff；同尺寸 `png-diff.mjs` 结果为 `351778` 个差异像素、差异比例 `23.8565%`、`MAE=2.74033`、`RMSE=15.61032`、最大通道差异 `234`，结论继续为 `DIFF_REVIEW`。
- [x] `WorkspaceLayout.test.tsx` 定向测试 `8/8`、`npm run typecheck`、`npm run build` 和 `git diff --check` 已通过；105 张画板映射和聚合结果已同步，最新采集实际 DPR 为 `1.25`，不满足严格 DPR 1 的像素 `PASS` 门禁。
- [ ] 本小点只完成 Agent Chat 侧栏两个选中态背景色的局部收口，不代表该画板或 105 张画板达到像素级 `PASS`；整页头像、字体、图标和光栅化差异仍需继续处理，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 154. 2026-08-30 Agent Chat 顶部导航选中态颜色收口

- [x] 依据 Figma 节点 `640:428`，将 `/chat?state=figma-v2` 顶部“工作台”选中态背景从旧的 `#FFFEFA` 收口为目标 `#FFFEFC`；同时保留侧栏和会话行的独立语义 Token，未修改 Figma 设计稿。
- [x] 浏览器实测视口 `1440×1024`、字体状态 `loaded`、顶部导航选中态实际为 `rgb(255, 254, 252)`；页面无横向溢出，前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 已登记原始 JPEG、转换后的 PNG 和独立 diff：`foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-top-nav-active-surface-browser-2026-08-30.jpg`、`foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-top-nav-active-surface-browser-2026-08-30-rgba.png`、`foodmate-ui/.qa/figma-pixel-acceptance/agent-chat-v2-top-nav-active-surface-2026-08-30-diff.json`；`png-diff.mjs` 结果为 `differentPixels=598653`、差异比例 `40.5988%`、`MAE=4.608837`、`RMSE=20.508797`、最大通道差异 `255`，结论继续为 `DIFF_REVIEW`。
- [x] `WorkspaceLayout.test.tsx` 定向测试 `8/8` 通过；顶部导航和页面 DOM 均确认不存在 `window-controls`、`traffic-light` 或红黄绿窗口装饰候选。
- [ ] 本小点只完成顶部导航选中态颜色收口，不代表该画板或 105 张画板达到像素级 `PASS`；DPR 为 `1.25`，整页头像、字体、图标和光栅化差异仍需继续处理，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 155. 2026-08-30 前端左上角窗口装饰核验

- [x] 按用户要求只检查前端，不修改 Figma 设计稿；Figma 参考图中的红、黄、绿三色窗口点保持为设计来源内容，前端不复制该装饰。
- [x] 对 `foodmate-ui` 生产源码与静态资源中的 `window-controls`、`traffic-light`、macOS 三色值和等价 RGB 值执行全量扫描，结果为 `NO_FRONTEND_PRODUCTION_WINDOW_DECORATION_MARKERS`。
- [x] `/chat?state=figma-v2` 在 `1440×1024` 运行时检查 `windowControls=0`、`redYellowGreen=0`；现有 `WorkspaceLayout.test.tsx` 也断言不渲染窗口控制节点，业务状态圆点保持不变。
- [ ] 当前没有可删除的前端左上角红黄绿装饰代码；本小点不影响 105 张画板 `DIFF_REVIEW`、shadcn 全页面迁移或 iconfont `BLOCKED` 状态。

## 156. 2026-08-30 Diet Records 默认态操作栏结构收口

- [x] `/analysis?view=records&state=v2` 已按 Figma 节点 `640:588` 展示餐次卡下方“记录一餐”和“分析这一天”按钮，并隐藏 Figma 不包含的额外“记录详情”大面板；仅修改前端，未修改 Figma。
- [x] 浏览器运行时确认操作栏 `1116×48px`、两个按钮分别为 `140×40px` 和 `164×40px`；详情面板未渲染、字体已加载、页面无横向溢出，左上角红黄绿窗口装饰候选为 `0`，业务内容未误删。
- [x] `DietRecordsPage` 定向测试 `10/10` 通过；PNG diff 为 `35.7843% / MAE 2.681890 / RMSE 15.511048 / maxChannelDelta 234`，证据和映射已登记在 `foodmate-ui/.qa/figma-pixel-acceptance/`。
- [ ] 实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过；整页继续为 `DIFF_REVIEW`，不代表该画板、105 张画板、shadcn 全页面迁移或 iconfont 实体资源登记完成。

## 157. 2026-08-30 注册页 Figma 色彩 Token 收口

- [x] `/register` 已依据 Figma 节点 `680:216` 将斜切背景收口为 `#dfeedb`，品牌标记和主按钮收口为 `#a6d997`；颜色通过 `AuthShell` 语义变量传递，仅修改前端，未修改 Figma 设计稿。
- [x] 浏览器验收为 `1440×900`、字体 `loaded`、根节点无横向或纵向溢出，认证卡片为 `460×831.2px`，内容宽 `380px`，背景为 `rgb(223,238,219)`、主按钮为 `rgb(166,217,151)`，前端左上角红黄绿窗口装饰候选为 `0`。
- [x] `AuthPages.test.tsx` 先验证色彩断言红灯（`1 failed / 22 passed`），再完成实现后定向测试 `23/23` 通过；Figma、浏览器 RGBA、独立 diff 和运行时几何证据已登记到 `foodmate-ui/.qa/figma-pixel-acceptance/`，PNG diff 为 `53.9498% / MAE 0.813297 / RMSE 5.644032 / maxChannelDelta 204`。
- [ ] 当前浏览器实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过；画板仍为 `DIFF_REVIEW`，不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 158. 2026-08-30 Agent Budget Limit 当前前端证据复核

- [x] 已重新采集 `/chat?state=budget-limit` 当前前端截图；预算卡 `286×289px`、选择说明区 `246×60px`、Token 计量区 `246×27px`、追加按钮 `150×32px`、结束按钮 `84×32px`，页面仍展示预算用量和当前 Run 操作。
- [x] 已登记原始 JPEG、RGBA PNG 和独立 diff：`agent-budget-limit-current-browser-2026-08-30.jpg`、`agent-budget-limit-current-browser-2026-08-30-rgba.png`、`agent-budget-limit-current-browser-2026-08-30-diff.json`；diff 为 `23.7866% / MAE 3.437230 / RMSE 17.809802 / maxChannelDelta 254`，结论保持 `DIFF_REVIEW`。
- [x] 当前视口为 `1440×1024`，字体已加载，页面无溢出；实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 105 张画板映射、diff 汇总和运行时检查已同步，汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成预算上限页当前证据复核，不代表该画板或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 159. 2026-08-30 Agent Tool Failed Retryable 当前前端证据复核

- [x] 已重新采集 `/chat?state=tool-failed-retryable` 当前前端截图；失败卡 `560×160px`，说明区域 `518.4×44px`，重试按钮 `58×32px`，跳过按钮 `97×32px`，错误码 `TOOL_TIMEOUT_001` 和失败原因均可见。
- [x] 已登记原始 JPEG、RGBA PNG 和独立 diff：`agent-tool-failed-retryable-current-browser-2026-08-30.jpg`、`agent-tool-failed-retryable-current-browser-2026-08-30-rgba.png`、`agent-tool-failed-retryable-current-browser-2026-08-30-diff.json`；diff 为 `24.3021% / MAE 3.098139 / RMSE 16.594073 / maxChannelDelta 253`，结论保持 `DIFF_REVIEW`。
- [x] 当前视口为 `1440×1024`，字体已加载，页面无溢出；实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 105 张画板映射、diff 汇总和运行时检查已同步，汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成工具失败可重试页当前证据复核，不代表该画板或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 160. 2026-08-30 Agent Safety Degraded 当前前端证据复核

- [x] 已重新采集 `/chat?state=safety-degraded` 当前前端截图；安全内容容器 `612×211.1px`，警告带 `560×58px`，回答区域 `560×125.1px`，有限数据和个人条件未完整应用提示均可见。
- [x] 已登记原始 JPEG、RGBA PNG 和独立 diff：`agent-safety-degraded-current-browser-2026-08-30.jpg`、`agent-safety-degraded-current-browser-2026-08-30-rgba.png`、`agent-safety-degraded-current-browser-2026-08-30-diff.json`；diff 为 `25.4949% / MAE 3.213981 / RMSE 16.377636 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`。
- [x] 当前视口为 `1440×1024`，字体已加载，页面无溢出；实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 105 张画板映射、diff 汇总和运行时检查已同步，汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成安全降级页当前证据复核，不代表该画板或 105 张画板达到像素级 `PASS`；降级回答、shadcn 全页面迁移和 iconfont 实体资源仍未完成。

## 161. 2026-08-30 Agent User Cancelled 当前运行证据复核

- [x] 已重新采集 `687:1684` `/chat?state=user-cancelled` 当前前端截图；已接收部分文本、用户取消原因和重新开始入口均可见，取消状态不显示为系统失败。
- [x] 已登记原始 JPEG、RGBA PNG 和独立 diff：`agent-user-cancelled-current-browser-2026-08-30.jpg`、`agent-user-cancelled-current-browser-2026-08-30-rgba.png`、`agent-user-cancelled-current-browser-2026-08-30-diff.json`；diff 为 `20.5035% / MAE 2.421603 / RMSE 14.507177 / maxChannelDelta 249`，结论保持 `DIFF_REVIEW`。
- [x] 当前视口为 `1440×1024`，实际 DPR 为 `1.0000000149`，字体已加载且页面无溢出；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 105 张画板映射和 diff 汇总已切换到当前 RGBA 证据，汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成用户取消态当前证据复核，不代表该画板或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 162. 2026-08-30 Agent SSE Reconnecting 当前运行证据复核

- [x] 已重新采集 `687:1803` `/chat?state=sse-reconnecting` 当前前端截图；已显示文本、第 `2/5` 次重连、等待重连状态、刷新提示和禁用 Composer 均可见。
- [x] 已登记原始 JPEG、RGBA PNG 和独立 diff：`agent-sse-reconnecting-current-browser-2026-08-30.jpg`、`agent-sse-reconnecting-current-browser-2026-08-30-rgba.png`、`agent-sse-reconnecting-current-browser-2026-08-30-diff.json`；diff 为 `22.9499% / MAE 2.769102 / RMSE 15.461383 / maxChannelDelta 249`，结论保持 `DIFF_REVIEW`。
- [x] 当前视口为 `1440×1024`，实际 DPR 为 `1.0000000149`，字体已加载且页面无溢出；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 105 张画板映射和 diff 汇总已切换到当前 RGBA 证据，汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成 SSE 重连态当前证据复核，不代表该画板或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 163. 2026-08-30 前端全量质量门禁复核

- [x] `npm run test` 全量通过：`38/38` 个测试文件、`215/215` 个用例通过，包含 Chat 六状态和 Agent SSE 重连/去重/终态关闭测试。
- [x] `npm run typecheck`、`npm run build` 和 `git diff --check` 均通过；生产构建完成，Vite 转换 `2010` 个模块。
- [x] 前端源码仍未发现左上角红、黄、绿窗口装饰；该结论只针对前端，Figma 设计稿和业务状态圆点均保持不变。
- [ ] 本门禁只确认当前前端代码质量，不代表 105 张画板像素级 `PASS`、shadcn 全量逐页迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 164. 2026-08-30 Admin Knowledge 六种流程态实现与验收证据

- [x] 已实现 Admin 知识库 6 个独立前端 fixture：`/admin?state=knowledge-uploading`、`knowledge-indexing`、`knowledge-upload-failed`、`knowledge-upload-success`、`knowledge-format-error`、`knowledge-size-error`；覆盖上传中、索引中、部分失败、成功、格式校验失败和大小限制失败。
- [x] 已对齐 Figma 节点 `782:212`、`782:366`、`782:520`、`806:1737`、`997:2`、`997:160`；浏览器视口均为 `1440×1024`，上传中弹层为 `560×260px`，索引中为 `560×280px`，失败态为 `560×286px`，成功态不显示额外弹层并保留知识库页面。
- [x] 已登记 6 组真实浏览器 JPEG、PNG 和独立 diff JSON；差异比例分别为 `35.9383%`、`36.3896%`、`36.5374%`、`49.4558%`、`63.2992%`、`63.3012%`，均保持 `DIFF_REVIEW`，未将局部实现包装为像素级 `PASS`。
- [x] 已补充 Admin 知识库状态测试；当前前端全量 `npm run test` 为 `38/38` 个测试文件、`221/221` 个用例通过，`npm run typecheck`、`npm run build` 和 `git diff --check` 通过；本次提交为 `6891ecb8`（`feat(管理端): 完善知识库上传流程态`）。
- [x] 已按用户要求复核前端左上角 Apple 红、黄、绿窗口装饰：生产源码和资源未发现对应节点、选择器或颜色标记；工作台 `sessionDot` 等业务状态圆点保留，Figma 设计稿未修改。
- [ ] 本小点不代表 105 张画板像素级验收、shadcn 全量逐页迁移或 iconfont 实体资源登记完成；当前 105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 继续为 `BLOCKED`。

## 165. 2026-08-30 Chat Figma Fixture 移除额外消息操作面板

- [x] 依据 Figma 节点 `640:428`、`1013:653`，移除 `/chat?state=figma-v2` 和 `/chat?state=running-stop` 中不属于画板的“消息操作”说明面板；非 Figma fixture 的真实模式仍保留该操作说明。
- [x] Chat 定向测试由断言面板存在改为断言 Figma fixture 不渲染面板，`ChatPage.test.tsx` 为 `27/27` 通过；运行时两个页面均确认 `messageActions=0`、字体已加载、无页面溢出。
- [x] 已登记当前浏览器 PNG 和独立 diff：`agent-chat-v2-no-message-actions-browser-2026-08-30.png`、`agent-chat-v2-no-message-actions-2026-08-30-diff.json`、`agent-chat-running-stop-no-message-actions-browser-2026-08-30.png`、`agent-chat-running-stop-no-message-actions-2026-08-30-diff.json`；差异比例分别为 `32.2509%` 和 `30.6371%`，结论均为 `DIFF_REVIEW`。
- [x] 前端左上角窗口控制点继续为 `0`；本次只删除 Figma fixture 的额外说明面板，未删除运行状态点、会话状态点或 Figma 设计稿内容。
- [ ] 本小点不代表 `agent-chat-v2`、`agent-chat-running-stop` 或 105 张画板达到像素级 `PASS`；DPR 为 `1.25`，字体、头像、图标和其他布局差异仍需复核，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 166. 2026-08-30 Agent Chat Figma 助手正文宽度收口

- [x] `/chat?state=figma-v2` 仅对 Figma fixture 的 `MessageBubble` 启用宽助手内容区；桌面端助手正文实测为 `x=340,width=764px`，真实模式继续使用原有 `560px` 宽度。
- [x] 新增 `ChatPage.test.tsx` 回归断言，锁定 Figma fixture 必须使用 `assistantBodyWide`；定向测试 `28/28` 通过。
- [x] 浏览器在 `1440×1024` 视口完成复核：字体状态为 `loaded`、页面无横向溢出、左上角红黄绿窗口装饰候选为 `0`；Figma 设计稿未修改。
- [x] 新增浏览器 PNG 和独立 diff：`foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-assistant-wide-browser-2026-08-30.png`、`foodmate-ui/.qa/figma-pixel-acceptance/agent-chat-v2-assistant-wide-2026-08-30-diff.json`；同尺寸结果为 `differentPixels=457274`、差异比例 `31.0109%`、`MAE=4.079167`、`RMSE=19.384585`、最大通道差异 `255`，较前版 `32.2509%` 有改善，结论继续为 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json` 与 `figma-105-diff-results.json` 已切换至本次浏览器证据；汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成助手正文宽度局部收口，不代表 `agent-chat-v2` 或 105 张画板达到像素级 `PASS`；整体字体、头像、图标、DPR 和其它页面差异、shadcn 全量逐页迁移仍需继续，iconfont 继续为 `BLOCKED`。

## 167. 2026-08-30 Agent Chat Figma 绿色 Token 收口

- [x] 依据 Figma 节点 `640:428` 的已登记 PNG，确认品牌标记和 Agent 方块的主绿色为 `#4caf50`；前端仅在 `.designChat` fixture 作用域覆盖 `--fm-green`，全局页面颜色不变。
- [x] `WorkspaceLayout` 定向测试已按 TDD 先验证缺少 Token 时失败，再在补充 CSS 后通过 `9/9`；浏览器 `1440×1024` 实测品牌标记和 Agent 方块均为 `rgb(76,175,80)`，字体已加载、无页面溢出、左上角红黄绿窗口控制点为 `0`。
- [x] 新增 RGBA 浏览器证据 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-design-green-browser-2026-08-30-rgba.png` 和独立 diff `foodmate-ui/.qa/figma-pixel-acceptance/agent-chat-v2-design-green-2026-08-30-diff.json`；同尺寸结果为 `31.0184% / MAE 4.049774 / RMSE 19.528353 / maxChannelDelta 255`，结论继续为 `DIFF_REVIEW`。
- [ ] 本小点只收口 Figma fixture 绿色 Token，不代表 `agent-chat-v2` 或 105 张画板达到像素级 `PASS`；头像、图标、DPR 和其它整页渲染差异、shadcn 全量逐页迁移仍需继续，iconfont 继续为 `BLOCKED`。Figma 设计稿未修改。

## 168. 2026-08-30 Agent Chat Figma 助手消息背景收口

- [x] 依据 Figma 节点 `640:428`，将 Figma fixture 的助手消息外层背景对齐为 `#f9fafb`；新增 `.designChatPage .assistant .messageBubble` 回归断言，Chat 与 Workspace 定向测试共 `38/38` 通过。
- [x] 浏览器 `1440×1024` 实测字体状态为 `loaded`、页面无横向溢出、助手气泡计算背景为 `rgb(249, 250, 251)`，前端左上角红黄绿窗口控制点为 `0`，业务状态圆点保留，Figma 设计稿未修改。
- [x] 已登记 RGBA 浏览器证据和独立 diff：`agent-chat-v2-assistant-surface-browser-2026-08-30-rgba.png`、`agent-chat-v2-assistant-surface-2026-08-30-diff.json`；同尺寸结果为 `30.9938% / MAE 3.868814 / RMSE 19.521027 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成助手消息背景的局部收口，不代表 `agent-chat-v2` 或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 169. 2026-08-30 Agent Chat Figma Composer 输入行背景收口

- [x] 依据 Figma 节点 `640:428`，将 Figma fixture 的 Composer 输入行背景对齐为 `#fcfcfc`；共享 Composer 使用 `--fm-fixture-composer-input-surface`，真实模式保持原有 fallback。
- [x] 新增背景 Token 回归断言；Chat 与 Composer 定向测试共 `35/35` 通过。浏览器 `1440×1024` 实测输入行背景为 `rgb(252, 252, 252)`、字体已加载、页面无横向溢出，前端左上角红黄绿窗口控制点为 `0`，业务状态圆点保留，Figma 设计稿未修改。
- [x] 已登记 RGBA 浏览器证据和独立 diff：`agent-chat-v2-composer-surface-browser-2026-08-30-rgba.png`、`agent-chat-v2-composer-surface-2026-08-30-diff.json`；同尺寸结果为 `27.8977% / MAE 3.693984 / RMSE 19.488887 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成 Composer 输入行背景的局部收口，不代表 `agent-chat-v2` 或 105 张画板达到像素级 `PASS`；shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 170. 2026-08-30 Agent Chat Figma 助手正文宽度与背景基准校正

- [x] 依据已登记的 Figma 节点 `640:428` PNG 像素证据，确认助手消息外层基准色为 `#f4f6f5`，正文容器为 `560px`；前端移除 `state=figma-v2` 的错误 `assistantBodyWide` 标记，内嵌确认卡实测为 `528×142px`，未修改 Figma 设计稿。
- [x] 按 TDD 先验证两个回归断言失败，再完成最小 CSS/渲染修正；`ChatPage.test.tsx` 定向测试 `30/30` 通过，`npm run typecheck` 通过。
- [x] 新增浏览器 JPEG/PNG 证据 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-chat-v2-assistant-layout-browser-2026-08-30.jpg`、`agent-chat-v2-assistant-layout-browser-2026-08-30-rgba.png` 和独立 diff `foodmate-ui/.qa/figma-pixel-acceptance/agent-chat-v2-assistant-layout-2026-08-30-diff.json`；同尺寸结果为 `differentPixels=452126`、差异比例 `30.6618%`、`MAE=2.944767`、`RMSE=15.908241`、最大通道差异 `234`。
- [x] 浏览器运行时为 `1440×1024`、字体 `loaded`、页面无溢出、左上角红黄绿窗口控制点候选为 `0`；实际 DPR 为 `1.25`，因此严格像素 `PASS` 门禁未通过。
- [x] `figma-105-mapping.json` 主证据已切换到本次浏览器 PNG，`figma-105-diff-results.json` 已同步本次指标；全量汇总继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只校正 Agent Chat fixture 的助手正文结构和背景基准，不代表 `agent-chat-v2` 或 105 张画板达到像素级 `PASS`；头像、图标光栅化、DPR 和其它页面差异、shadcn 全量逐页迁移仍需继续，iconfont 继续为 `BLOCKED`。

## 177. 2026-08-30 Chat 助手基准提交后的前端质量门禁复核

- [x] 提交 `df092098` 后重新运行前端全量测试：`38/38` 个测试文件、`225/225` 个用例通过；覆盖 Chat 六状态、Agent SSE 连接状态/重连/去重/终态关闭及工作区窗口装饰门禁。
- [x] `npm run typecheck`、`npm run build` 和目标文件 `git diff --check` 均已通过；构建产物由 Vite 正常生成。
- [x] 本次提交范围仅为 Chat 助手正文宽度/背景校正、回归测试、Figma 映射和验收证据；未修改 Figma，未混入工作区已有的后端测试与脚本修改。
- [ ] 本条只确认前端质量门禁，不代表 105 张画板像素级 `PASS`、shadcn 全量逐页迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 178. 2026-08-30 Profile 基本资料 Figma fixture 布局证据复核

- [x] 依据 Figma 节点 `806:1119`，修正 `/profile?state=basic` fixture 被通用表单规则覆盖的问题；真实模式的完整资料字段和提交逻辑未修改。
- [x] 浏览器实测 `1440×1024`、字体 `loaded`、页面无横向溢出；目标卡片为 `692×366px`，蛋白质输入框为内容区全宽 `644.4×40px`，过敏原区和底部操作栏均保持在卡片内部；窗口装饰候选为 `0`，业务圆点未删除，Figma 设计稿未修改。
- [x] 已登记加载完成后的浏览器 JPEG/PNG、独立 diff JSON，并同步 `figma-105-mapping.json`、`figma-105-diff-results.json` 和运行时检查；diff 为 `81.6477% / MAE 5.044907 / RMSE 20.363129 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`。
- [x] `ProfilePage.test.tsx` 定向测试 `21/21` 通过，`npm run typecheck` 和目标文件 `git diff --check` 通过；当前 in-app 浏览器实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过。
- [ ] 本小点只完成 Profile fixture 的布局规则和证据复核，不代表该画板或 105 张画板达到像素级 `PASS`；工作区壳层、头像、图标、字体光栅化和全量 shadcn 迁移仍需继续，iconfont 实体资源登记继续为 `BLOCKED`。

## 179. 2026-08-30 Profile Figma 侧栏壳层收口

- [x] 依据 Figma 节点 `806:1119`，为 Profile fixture 增加“在线代理”标签，隐藏 Figma 画板未包含的侧栏搜索框和“收起导航”；真实模式与其他页面 fixture 默认行为不变。
- [x] 浏览器实测 `1440×1024`、字体 `loaded`、页面无横向溢出：在线状态 `x=24,y=84,width=60,height=20`，新建任务 `y=128`，工作区导航 `y=193`；搜索框与收起按钮均未渲染，窗口控制点候选为 `0`，Figma 设计稿保持不变。
- [x] 已登记浏览器 JPEG/PNG、独立 diff JSON，并同步 105 画板映射、diff 汇总和运行时检查；diff 为 `80.5026% / MAE 4.861440 / RMSE 20.069718 / maxChannelDelta 255`，较上一组 Profile fixture 证据下降，结论保持 `DIFF_REVIEW`。
- [x] `WorkspaceLayout.test.tsx` 与 `ProfilePage.test.tsx` 定向测试 `31/31` 通过；前端红黄绿窗口控制点静态扫描无命中。
- [ ] 本小点只完成 Profile 侧栏 fixture 收口，不代表该画板或 105 张画板达到像素级 `PASS`；其余页面视觉差异、全量 shadcn 迁移和 iconfont 实体资源登记仍需继续，iconfont 保持 `BLOCKED`。

## 180. 2026-08-30 Profile 记忆与偏好 Figma 主体几何收口

- [x] 依据 Figma 节点 `806:1281`，仅对 mock/Figma fixture 增加专用布局契约：介绍卡 `96px`，普通记忆行 `124px`，待确认记忆行 `130px`，底部管理说明区与列表保持 `8px` 补偿间距；真实模式不改变。
- [x] 浏览器实测 `1440×1024`、字体 `loaded`、无页面横向溢出：介绍卡 `x=292,y=100,width=1116,height=96`，三条记忆行分别为 `124px/130px/124px`，底部说明区 `x=292,y=726,width=1116,height=240`，窗口控制点候选为 `0`。
- [x] 已登记浏览器 JPEG、真实 RGBA PNG 和独立 diff JSON，并同步 105 画板映射、diff 汇总和运行时检查；同尺寸 diff 为 `47.7057% / MAE 5.497678 / RMSE 23.473287 / maxChannelDelta 255`，结论保持 `DIFF_REVIEW`。
- [x] `ProfilePage.test.tsx` 定向测试 `22/22` 通过；TDD 红灯曾确认布局契约缺失，补充实现后转绿。
- [ ] 本小点只完成 Profile 记忆页 fixture 主体几何收口，不代表该画板或 105 张画板达到像素级 `PASS`；其余页面视觉差异、全量 shadcn 迁移和 iconfont 实体资源登记仍需继续，iconfont 保持 `BLOCKED`。

## 181. 2026-08-30 Profile 安全与设备 Figma fixture 结构收口

- [x] 依据 Figma 节点 `806:1445`，仅为 `/profile?state=security` 增加两卡片 Figma fixture：密码卡和活跃工作区会话卡；Figma 不包含的最近安全活动、`SECURE`、`2 ACTIVE DEVICES`、设备状态说明和顶部装饰线不再渲染，普通安全页和真实模式不变。
- [x] 当前浏览器证据已登记：视口 `1440×1024`、字体 `loaded`、密码卡 `546×464`、会话卡 `546×378`、页面无溢出、窗口控制点 `0`；Figma 设计稿保持只读。
- [x] `ProfilePage.test.tsx` 定向测试 `23/23` 通过；RGBA PNG 与独立 diff 已登记，差异比例 `26.4509%`、`MAE=3.965800`、`RMSE=19.438791`、`maxChannelDelta=255`，继续为 `DIFF_REVIEW`；实际 DPR `1.25`，严格 DPR 1 门禁未通过。
- [ ] 本小点只关闭 Profile 安全页的前端结构边界，不代表该画板或 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 182. 2026-08-30 Workspace Home 侧栏与状态面板前端收口

- [x] `/?state=figma-v2` 已按 Figma 节点 `640:256` 恢复会话搜索、9 条 Agent 会话历史、会话分页和“任务入口与状态”说明面板；真实模式不改变，Figma 设计稿未修改。
- [x] `HomePage.test.tsx` 与 `WorkspaceLayout.test.tsx` 定向测试 `14/14` 通过；前端源码扫描与运行时检查均确认左上角红、黄、绿窗口装饰数量为 `0`，业务状态圆点未删除。
- [x] `1440×1024` 浏览器证据已登记，字体加载完成且页面无溢出；PNG diff 为 `37.7674% / MAE 3.651656 / RMSE 17.505058 / maxChannelDelta 253`，继续记录为 `DIFF_REVIEW`。
- [x] 实际 DPR 为 `1.25`，映射已如实登记 `dprPass=false`；不能据此宣称严格 DPR 1 或像素级 `PASS`。
- [ ] 本小点只完成 Workspace Home 前端结构与验收证据，不代表 105 张画板像素级 `PASS`、shadcn 全量迁移、后端闭环或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 184. 2026-08-30 Agent Chat Figma 助手宽布局复采集

- [x] 依据 Figma 节点 `640:428` 的原始 `1440×1024` PNG，确认助手内容区从 `x=340` 延伸至主内容边界，目标宽度为 `780px`；内嵌饮食记录确认卡为 `x=356,y=313,width=764,height=143px`，仅调整前端 Figma fixture，未修改 Figma 设计稿。
- [x] `/chat?state=figma-v2` 当前浏览器实测视口 `1440×1024`、DPR `1.0000000149`、字体 `loaded`、页面无横向溢出；助手正文、来源和确认卡均在目标边界内，前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保持不变。
- [x] `ChatPage.test.tsx` 定向测试为 `30/30`；已登记 JPEG、RGBA PNG 和 `scripts/png-diff.mjs` 独立结果，`differentPixels=378986`、差异比例 `25.7016%`、`MAE=3.162904`、`RMSE=17.558625`、`maxChannelDelta=255`，画板继续为 `DIFF_REVIEW`。
- [x] `figma-105-mapping.json`、`figma-105-diff-results.json` 和 `figma-105-runtime-checks.json` 已同步本次证据；运行时汇总为 `viewportPass=105/105`、`geometryPass=105/105`、`textPass=105/105`、`dprPass=95/105`，全量画板仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只收口 `agent-chat-v2` 助手宽布局证据，不代表整页或 105 张画板达到像素级 `PASS`；shadcn 全量逐页迁移尚未完成，iconfont 实体包、CSS 映射、来源和许可证继续为 `BLOCKED`。

## 185. 2026-08-31 Diet Records 会话历史与记录详情结构纠正

- [x] 依据 Figma 节点 `640:588`，`/analysis?view=records&state=v2` fixture 已恢复会话搜索、Agent 会话历史、分页和底部“记录详情”面板，并隐藏 Figma 中不存在的“记录一餐 / 分析这一天”额外操作栏；普通默认态和真实模式保持原有行为，Figma 设计稿未修改。
- [x] `DietRecordsPage.test.tsx` 按 TDD 先确认目标契约红灯（`1 failed / 9 passed`），实现后定向测试为 `10/10`；浏览器运行时会话搜索 `1` 个、记录详情 `1` 个、额外操作栏 `0` 个。
- [x] 已登记浏览器 JPEG、RGBA PNG 和独立 diff：`diet-records-v2-session-history-browser-2026-08-31.jpg`、`diet-records-v2-session-history-browser-2026-08-31-rgba.png`、`diet-records-v2-session-history-2026-08-31-diff.json`；差异比例 `36.4149% / MAE 2.622175 / RMSE 15.524117 / maxChannelDelta 234`，结论保持 `DIFF_REVIEW`。
- [x] `1440×1024` 视口、字体加载、无溢出和前端左上角红黄绿窗口控制点 `0` 均已复核；实际 DPR `1.25`，严格 DPR 1 门禁未通过；105 张画板仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成 Diet Records Figma fixture 结构边界和验收证据，不代表像素级 PASS、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 191. 2026-08-31 摄入分析会话历史结构纠正

- [x] 依据实时 Figma 节点 `640:773`，`/analysis?state=v2` 已恢复会话搜索、活动会话和分页；真实模式保持原有会话历史行为，Figma 设计稿未修改。
- [x] 按 TDD 目标契约确认 Analysis fixture 显示搜索框、`每周饮食微调` 和分页；实现后 `AnalysisPage` 定向测试为 `5/5`。
- [x] 浏览器运行时证据为 `1440×1024`、字体 `loaded`、无横向/纵向溢出、搜索框 `1` 个、活动会话 `1` 个、分页可见，分页区域为 `x=24,y=598,width=212,height=22`；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保持不变。
- [x] 已登记原始浏览器 JPEG `intake-analysis-v2-session-history-browser-2026-08-31.jpg`、RGBA PNG 和独立 diff；`differentPixels=405974`、差异比例 `27.5319%`、`MAE=2.493448`、`RMSE=14.1324168`、最大通道差异 `234`，结论保持 `DIFF_REVIEW`。
- [x] 当前浏览器实际 DPR 为 `1.25`，严格 DPR 1 门禁为 `false`；映射、diff 汇总和运行时检查已同步，运行时汇总为 `dprPass=94/105`，105 张画板仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成 Analysis fixture 会话历史结构和验收证据，不代表该画板或 105 张画板达到像素级 `PASS`；图标、字体光栅化、主体视觉差异、shadcn 全量逐页迁移和 iconfont 实体资源登记仍需继续，iconfont 保持 `BLOCKED`。

## 192. 2026-08-31 Admin Overview 分析卡固定网格收口

- [x] 依据实时 Figma 节点 `995:977` 下的 `1005:2`、`1005:3`、`1005:7`、`1005:11`，将 Figma fixture 的三张分析卡由弹性三等分改为 `344px` 固定网格，卡片位置实测为 `x=308/668/1028,y=782`；外层分析容器为 `1116×180px`、`x=292,y=766`。
- [x] 外层与内层分析卡的边界改为等效 `inset` 描边，保留 `#e8edeb` 视觉边界并消除描边参与布局造成的 `0.8px` 偏移；真实模式不改变。
- [x] `AdminPage.test.tsx` 按 TDD 先确认目标契约红灯（`1 failed / 17 passed`），实现后定向测试为 `18/18`；三张卡均登记 Figma 节点和 `344px` 宽度契约。
- [x] 已登记 Figma 原始 PNG、浏览器原始 JPEG、RGBA PNG 和独立 diff `foodmate-ui/.qa/figma-pixel-acceptance/admin-overview-analytics-grid-2026-08-31-diff.json`；同尺寸结果为 `differentPixels=466371`、差异比例 `31.6278%`、`MAE=2.613027`、`RMSE=14.310728`、最大通道差异 `230`，结论保持 `DIFF_REVIEW`。
- [x] 浏览器运行时为 `1440×1024`、DPR `1.0000000149`、字体 `loaded`、页面无横向/纵向溢出；前端左上角红黄绿窗口装饰候选为 `0`，业务状态圆点保持不变；Figma 设计稿未修改。
- [x] `figma-105-mapping.json`、`figma-105-diff-results.json` 和 `figma-105-runtime-checks.json` 已同步本次证据；105 张画板聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本小点只完成 Admin Overview 分析卡几何与证据收口，不代表该画板或 105 张画板达到像素级 `PASS`；头像、图标和浏览器光栅化差异、shadcn 全量逐页迁移及 iconfont 实体资源登记仍需继续，iconfont 保持 `BLOCKED`。

## 193. 2026-09-02 Admin 操作审计页 Figma 对齐与验收

依据实时 Figma 节点 `995:1499`，本轮完成操作审计页的前端 fixture 收口；Figma 设计稿保持只读，左上角红黄绿装饰点只在前端检查和移除。

- [x] `/admin?view=audit` 已补齐生产环境标记、结果/目标类型/动作筛选、`request_id / trace_id` 搜索、三张统计卡、十列表格、四页分页、底部分析卡和只读详情 Dialog。
- [x] 按 TDD 补充“生产环境”与表头背景契约，先确认测试红灯，再完成最小实现；`OperationAuditTab.test.tsx` 定向测试为 `4/4`。
- [x] 浏览器使用 `1440×1024`、DPR 1 和字体加载完成条件采集；页面根节点 `1440×1024`、无页面横向溢出、文字溢出检查通过。
- [x] 已实际验证结果筛选、request_id 搜索和详情查看；详情保留动作、操作者、目标、请求摘要、前后状态、错误码、`trace_id` 和客户端信息。
- [x] 前端左上角红、黄、绿窗口装饰点候选数量为 `0`，业务状态点未删除，Figma 设计稿未修改。
- [x] 当前浏览器证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/admin-operation-audit-browser-2026-09-02.png`；独立 diff 为 `10.6629% / MAE 3.015004 / RMSE 17.033025 / maxChannelDelta 241`，已同步 105 画板映射、diff 汇总和验收报告。
- [ ] 该画板仍存在头像素材、文字光栅化和局部视觉像素差异，结论保持 `DIFF_REVIEW`，不能标记为像素级 `PASS`；iconfont 仍为 `BLOCKED`，shadcn 全量逐页迁移仍未完成。

## 194. 2026-09-05 Admin Trace fixture 加载状态修复与前端质量门禁

- [x] 修复 `RunsTab` 详情选择逻辑：mock 模式打开 Trace 详情时直接展示静态 `planner -> food_log_writer -> plan_validator` 链路，不再错误停留在“正在加载链路明细”；real 模式仍仅在接口请求场景显示加载状态，并继续调用既有 Trace 详情接口。
- [x] `RunsTab.test.tsx` 的 Trace 详情链路断言已恢复，前端全量测试为 `38/38` 个测试文件、`237/237` 个用例通过。
- [x] `npm run typecheck` 通过；`npm run build` 通过，Vite 完成 `2011 modules transformed` 并生成生产构建产物。
- [ ] 测试输出仍有既有 React/Radix `act(...)` 警告；本轮未扩大范围处理，不影响测试退出码。
- [ ] 本轮只完成 Admin Trace fixture 状态修复和质量门禁，不代表 105 张 Figma 画板达到像素级 `PASS`；当前汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全量逐页视觉迁移尚未完成，iconfont 实体资源继续为 `BLOCKED`。

## 195. 2026-09-05 Figma 视觉验收环境固定

- [x] 新增 `?visual-qa=1` 采集模式：通过 `data-visual-qa` 标记统一关闭动画、过渡、光标闪烁和滚动行为；普通访问和真实模式不受影响。
- [x] 登录页在视觉验收模式下跳过 GSAP 入场时间线，避免采集到不稳定的中间帧；mock Agent 回放时间固定为 `2024-03-14 12:46`，不改变真实模式时间。
- [x] 新增 `foodmate-ui/scripts/figma-acceptance-config.mjs`，集中登记 DPR 1、`zh-CN`、浅色主题、字体等待、动画冻结和 `1440x1024`、`1440x900`、`1366x768`、`1024x768`、`390x844` 视口。
- [x] 新增 `npm run qa:figma:validate`，校验 105 条映射的必填字段、登记视口、Figma/浏览器 PNG 文件头与尺寸、viewport/geometry/text 运行时结果和严格 DPR 门禁；`--strict` 仅在全部条件满足时返回成功。
- [x] 新增视觉验收模式最小契约测试；本大点完成后统一质量门禁为 `39/39` 个测试文件、`239/239` 个用例通过，typecheck、build 和 `git diff --check` 通过。
- [ ] 现有证据尚未重新采集，当前校验结果应保持 `DPR_RECAPTURE_REQUIRED`；105 张画板仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，不能提前标记 PASS。

## 196. 2026-09-05 Auth 页面品牌色与主操作色收口

依据 Figma 节点 `647:214`、`680:216`、`680:275`、`680:307` 和 `680:738`，本轮只修正认证页面语义色分离，Figma 设计稿保持只读。

- [x] `AuthShell` 为注册、找回密码、重置密码和 Token 状态页分别注入 `diagonal`、`brand`、`primary` 三类 Token，避免品牌标记错误复用主按钮颜色。
- [x] 注册页使用 `#dfeedb / #a6d997 / #a6d997`；找回密码和重置密码页使用 `#c5f0d6 / #a6d997 / #48c78e`；Token 状态页使用 `#dfeedb / #a6d997 / #a6d997`。登录页继续使用其 Figma 专属全局背景和状态 Token。
- [x] 已通过本地浏览器打开并检查 `/login`、`/register`、`/forgot-password`、`/reset-password` 和 `/token-status?state=invalid` 的 `visual-qa=1` 页面；Figma SVG 资产、字体、表单控件和各页主要操作均可见，未观察到页面级溢出。
- [x] `AuthPages.test.tsx`、全量 Vitest、`npm run typecheck`、`npm run build` 和 `git diff --check` 均通过；全量结果为 `39/39` 个测试文件、`239/239` 个用例。
- [ ] 本轮只完成认证页面 Token 语义和浏览器复核，不代表 Auth 画板或 105 张画板达到像素级 `PASS`；现有 105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，DPR 仍为 `94/105`，shadcn 全量逐页迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 198. 2026-09-05 Workspace Home 与 Agent Chat 共享壳层收口

本大点依据实时 Figma 节点 `640:256`（Workspace Home）和 `640:428`（Agent Chat）完成共享壳层资源与结构收口；Figma 文件保持只读，未修改设计稿。

- [x] `WorkspaceLayout` 的 Figma Home/Chat fixture 已统一登记 `260px` 侧栏、`24px` 侧栏内边距和 `52×12px` 窗口控制点；窗口控制点仅作为设计稿装饰，不进入普通业务壳层、导航或键盘交互。
- [x] Home fixture 已拆分顶部头像和底部工作区头像；Chat fixture 已接入侧栏头像、顶栏头像和用户消息头像，真实模式继续使用既有用户资料资源。
- [x] Home fixture 已保留 Figma 节点 `989:3` 的“任务入口与状态”面板；Chat fixture 已保留 `780px` 助手内容边界和 `764×143px` 内嵌写入确认卡结构。
- [x] 本大点新增或更新的 Figma 导出资源已登记 SHA-256：`window-controls.svg` 为 `BC9E16DF4389D80CEC6750588A5757C4AFABED75E887F2E6A01D7B2DF8D127FC`；`home-sidebar-avatar.png` 为 `ABCC3C903904A879EC46D61C58C335A83958AA04FCC2D236F2AF9AC6B0AC1F05`；`home-topbar-avatar.png` 为 `6ED858016A534DAAB6F19A97F197AAC3D0297EEE478E6F36FEE4C8133E94D394`；`figma-v2-topbar-avatar.png` 为 `0F23997FEC22B7C5787E790A1142D21C952644FA11336D35F7CC03F107A2F75B`；`figma-v2-message-avatar.png` 为 `EBDF5997593DCAED42F943E5D297579B2FAE50612047F1D406DD3C590432E3B7`。
- [x] 已使用 Chrome CDP 在 `1440×1024`、DPR `1`、字体 `loaded` 条件下重新采集 `workspace-home-v2` 和 `agent-chat-v2`；两页根节点尺寸正确，页面无横向溢出，文字边界检查通过。
- [x] 已重新运行 `scripts/generate-figma-105-diff.mjs`；Home diff 为 `23.7111% / MAE 3.486692 / RMSE 18.329592 / maxChannelDelta 254`，Chat diff 为 `14.5983% / MAE 3.046648 / RMSE 18.422228 / maxChannelDelta 236`。
- [x] 本大点完成后集中执行质量门禁：`npm run test` 为 `39/39` 个测试文件、`240/240` 个用例通过，`npm run typecheck`、`npm run build`、`npm run format:check`、`npm run lint` 和 `git diff --check` 均通过；测试输出中的既有 React/Radix `act(...)` 提示不影响退出码。
- [ ] 两页自动 diff 与人工视觉复核仍存在图标、字体光栅化和局部组合差异，因此本大点及 105 张画板汇总继续保持 `DIFF_REVIEW`，不得标记像素级 `PASS`；iconfont 实体包和授权资料仍为 `BLOCKED`。

## 199. 2026-09-05 Auth 页面组 DPR 1 证据同步

本大点完成 Auth 页面组 13 个画板的最新浏览器证据同步，范围包括登录默认态、登录 6 个反馈态、注册、找回密码、重置密码和 Token 3 个状态。Figma 设计稿保持只读；本次只更新前端资源、映射和验收证据。

- [x] 13 张浏览器 PNG 均使用 Chrome CDP、`1440×900`、DPR `1`、字体 `loaded` 和 `visual-qa=1` 条件；页面根节点尺寸一致，运行时无横向或纵向溢出。
- [x] `figma-105-mapping.json` 已将上述 13 项统一指向 `recaptured/dpr1-*-browser-2026-09-05.png`，并登记截图工具、浏览器版本、视口、DPR、字体和 URL。
- [x] `figma-105-runtime-checks.json` 已同步 Auth 13 项运行时证据；全量运行时汇总为 `viewportPass=105/105`、`geometryPass=105/105`、`textPass=105/105`、`dprPass=105/105`、`errors=0`。
- [x] 已使用 `scripts/png-diff.mjs` 重新计算 13 项同尺寸 diff，并同步 `figma-105-diff-results.json`。结果范围为：登录默认态 `3.5494%`，注册 `53.4744%`，找回密码 `3.1839%`，重置密码 `4.0651%`，登录反馈态 `5.9847%~8.3975%`，Token 状态 `1.8195%~1.9593%`；具体 MAE、RMSE 和最大通道差异以聚合 JSON 为准。
- [x] Auth 代码和测试改动继续使用 Figma 导出的品牌、字段、分隔线、密码可见性和 Token 状态 SVG 资源；标准控件仍使用 shadcn/Radix，标准命令图标不引入虚构 iconfont。
- [x] 本大点完成后统一质量门禁：Vitest `39/39` 个测试文件、`240/240` 个用例通过，`npm run typecheck`、`npm run build` 和 `git diff --check` 通过。
- [ ] Auth 13 项自动 diff 均为非零 `COMPARED`，且本次代码/截图同步后仍需逐项人工视觉复核，因此全部保持 `DIFF_REVIEW`；不能把 DPR、几何和文字检查通过替代像素级 `PASS`。
- [ ] 本大点不关闭 105 张画板全量像素验收、shadcn 全量逐页视觉迁移或 iconfont 资源阻塞；全量汇总继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，iconfont 继续为 `BLOCKED`。

## 200. 2026-09-05 Workspace 业务 fixture 证据同步

本大点收口 Workspace 共享壳层在 Diet Records、Intake Analysis 和 Meal Planning 三组 Figma fixture 中的窗口控制点边界，并重新采集 19 张 `1440×1024 / DPR 1` 浏览器证据。Figma 文件保持只读；窗口控制点仅属于 Figma fixture 装饰，不进入普通业务壳层、导航或键盘交互。

- [x] `WorkspaceLayout` 现对 `records`、`analysis` 和 `planning` fixture 统一渲染 Figma 窗口控制点；新增 `WorkspaceLayout.test.tsx` 回归覆盖三条入口，真实模式和 Knowledge/Profile 壳层不受影响。
- [x] 重新采集 Diet Records 4 张、Intake Analysis 4 张和 Meal Planning 11 张浏览器 PNG，视口均为 `1440×1024`、DPR `1`、字体加载完成；证据目录为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/`。
- [x] `figma-105-mapping.json` 已将本大点 19 个画板切换到 `dpr1-*-browser-2026-09-05.png`；`figma-105-diff-results.json` 已重新计算，19/19 项均完成同尺寸比较，未产生 `UNMAPPED` 或 `SIZE_MISMATCH`。
- [x] 当前前端质量门禁：Vitest `39/39` 个测试文件、`243/243` 个用例通过；`npm run typecheck`、`npm run build`、`git diff --check` 通过。
- [x] `npm run qa:figma:validate` 返回 `structuralPass=true`、`strictDprPass=true`、`errors=[]`；105 项运行时视口、几何、文字和 DPR 证据完整。
- [ ] 本大点 19 项自动 diff 均有非零差异，差异比例约为 `5.53%~38.11%`，因此全部保持 `DIFF_REVIEW`；这批证据补齐了验收输入，不代表三页或 105 张画板达到像素级 `PASS`。
- [ ] shadcn 基础设施已存在且 `AdminPrimitives` 无直接引用，但领域布局仍由页面 CSS Modules 承载，逐页视觉迁移尚未结束；iconfont 实体字体包、CSS 映射、来源和许可证仍为 `BLOCKED`。

## 197. 2026-09-05 DPR 1 视觉证据复采集

本轮针对历史运行时 `actualDpr=1.25` 的 11 张画板使用 Chrome CDP 重新采集；不修改 Figma 文件，不覆盖原有证据。

- [x] 新增 `foodmate-ui/scripts/capture-figma-dpr1.mjs` 和 `npm run qa:figma:dpr1`，通过 `--force-device-scale-factor=1` 与 CDP `Emulation.setDeviceMetricsOverride` 固定 DPR 和页面 viewport，并在截图前等待字体加载完成。
- [x] 复采集 `workspace-home-v2`、`diet-records-v2`、`intake-analysis-v2`、`register-page`、`agent-budget-limit`、`agent-tool-failed-retryable`、`agent-safety-degraded`、`agent-chat-history-page-2`、`agent-chat-history-page-3`、`agent-chat-search-results` 和 `profile-security`；Chrome 返回 `dpr=1`、字体 `loaded`、无页面横向溢出，PNG 尺寸分别与目标 `1440×1024` 或 `1440×900` 一致。
- [x] 最新浏览器 PNG 已登记在 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/dpr1-*-browser-2026-09-05.png`，并同步 `figma-105-mapping.json`、`figma-105-runtime-checks.json` 和 `figma-105-diff-results.json`；全量 diff 输入仍为 `105/105`。
- [x] 最新运行时汇总为 `viewportPass=105/105`、`geometryPass=105/105`、`textPass=105/105`、`dprPass=105/105`；`npm run qa:figma:validate` 的结构校验通过。
- [ ] 11 张复采集画板的自动 diff 均仍为 `COMPARED`，人工视觉结论和全量汇总仍保持 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`；DPR 门禁已关闭，但字体、头像、图标、布局和浏览器光栅化差异仍需逐页处理，不能标记像素级 `PASS`。iconfont 继续为 `BLOCKED`，shadcn 全量逐页迁移仍未完成。

## 201. 2026-09-05 Auth 页面实时 Figma 证据与 Motion 契约收口

本大点仅处理认证页面的实时 Figma 参考、DPR 1 浏览器证据、Login 动画契约和验收元数据；Figma 文件保持只读，未修改用户已有的 Admin 或本地 E2E 文件。

- [x] 重新获取 Figma 节点 `647:214`、`680:216`、`680:275`、`680:307` 和 `680:738` 的原始尺寸 PNG；五张参考图均为 `1440×900`，分别登记为 `recaptured-figma/auth-login-647-214-live-2026-09-05.png`、`auth-register-680-216-live-2026-09-05.png`、`auth-forgot-680-275-live-2026-09-05.png`、`auth-reset-680-307-live-2026-09-05.png` 和 `auth-token-invalid-680-738-live-2026-09-05.png`。
- [x] 重新采集 Login、Register、Forgot Password、Reset Password 和 Token Invalid 五个入口；Chrome `152.0.7977.77`、DPR `1`、字体 `loaded`、页面无横向溢出，五张浏览器 PNG 均为 `1440×900`。
- [x] 五组自动 diff 已使用实时 Figma PNG 和有效浏览器页面截图重新生成：Login `3.7962% / MAE 0.566035 / RMSE 7.555450 / maxChannelDelta 213`；Register `4.2780% / MAE 0.567688 / RMSE 6.677208 / maxChannelDelta 198`；Forgot `3.0128% / MAE 0.648710 / RMSE 7.311507 / maxChannelDelta 188`；Reset `3.8650% / MAE 1.093539 / RMSE 10.593789 / maxChannelDelta 213`；Token Invalid `1.8195% / MAE 0.101208 / RMSE 2.509056 / maxChannelDelta 204`。
- [x] 修正 `capture-figma-dpr1.mjs` 的页面就绪门禁：除等待字体外，必须等待页面主 `main` 节点出现；同时忽略空 query 的字符串 `null`，避免生成 `/register?null&visual-qa=1` 一类无效证据 URL。
- [x] 依据 Figma `get_motion_context` 对 Login 节点 `647:214` 的实时返回，使用现有 GSAP 对齐 `4.5s` 无限循环、背景弹性收拢、品牌标记旋转/缩放和各内容层的 `2.8s` 至 `3.9s` 分段显现；`visual-qa=1` 和 `prefers-reduced-motion` 继续关闭动画，异常登录状态不启动装饰循环。
- [x] `figma-105-mapping.json` 已切换五个认证主画板的 Figma 参考路径和浏览器证据，`figma-105-diff-results.json` 已重新生成；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 五个画板仍存在非零像素差异，且本次实时参考图尚未全部完成独立人工视觉复核，因此不能标记 `PASS`；这次只完成证据和 Motion 契约收口，不代表 Auth 或 105 张画板像素级验收完成。
- [ ] shadcn 全量逐页视觉迁移、iconfont 实体包和授权登记仍未完成；标准命令图标继续使用 Lucide，iconfont 继续为 `BLOCKED`。

## 202. 2026-09-05 Workspace Home 与 Agent Chat 视觉收口

本大点依据实时 Figma 节点 `640:256`（Workspace Home）和 `640:428`（Agent Chat）完成当前工作区前端改动的代码收口；Figma 文件保持只读，未纳入后端或 Admin 并行改动。

- [x] `WorkspaceLayout` 的 Figma fixture 保持 `260px` 侧栏、`24px` 侧栏内边距和中等桌面下的导航优先响应式规则；带右侧轨道的中等桌面宽度隐藏工作区搜索框，避免顶栏操作区发生挤压。
- [x] Chat fixture 的助手正文宽度收口为 `560px`，助手气泡背景收口为 `#f4f6f5`，正文允许按设计稿自然换行；来源和运行轨迹继续使用等宽字体边界。
- [x] Figma Chat 消息操作区的文案、间距和“写入确认 / 预算追加”提示已按设计稿收口；真实模式的消息、确认、预算和 SSE 业务逻辑未改写。
- [x] `WorkspaceLayout.test.tsx` 与 `ChatPage.test.tsx` 已同步覆盖绿色 Token、`560px` 正文宽度、助手背景和消息操作区；标准命令图标继续使用 Lucide，未创建虚构 iconfont glyph。
- [x] 当前大点的前端改动仅包含 WorkspaceLayout、ChatPage 及对应测试；后端 Mapper、Admin 额外改动和临时 SQL 文件保持未纳入本次提交。
- [ ] 本大点的自动 diff 和人工视觉复核仍存在字体、头像、图标及局部组合差异，Workspace Home、Agent Chat 和 105 张画板继续保持 `DIFF_REVIEW`；shadcn 全量逐页视觉迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 203. 2026-09-05 Diet Records、Intake Analysis、Meal Planning 头像资源收口

本大点依据实时 Figma 节点 `640:588`、`640:773` 和 `640:901`，完成三个业务页面 Figma fixture 的默认头像资源替换和当前批次验收证据同步。Figma 文件保持只读；真实模式的性别默认头像逻辑未修改。

- [x] 新增 `foodmate-ui/src/lib/avatar.ts` 中的 `FIGMA_WORKSPACE_AVATARS` 常量，三个 Figma fixture 的侧栏和顶栏分别使用 `/assets/figma/workspace/home-sidebar-avatar.png` 与 `/assets/figma/workspace/home-topbar-avatar.png`；未创建新的头像字形或虚构 iconfont 资源。
- [x] Diet Records、Intake Analysis、Meal Planning 的页面测试已补充头像资源映射断言，页面代码仍复用现有 `WorkspaceLayout` 和 FoodMate 语义 Token。
- [x] 三页均已按 `1440×1024`、DPR `1`、字体加载完成和无横向溢出条件登记浏览器 PNG；`figma-105-mapping.json` 已同步主证据路径、DPR 1 采集信息和人工复核结论。
- [x] 当前主画板 diff 已重新计算：Diet Records `7.9734% / MAE 2.546144 / RMSE 16.834996 / maxChannelDelta 227`；Intake Analysis `7.5430% / MAE 2.004188 / RMSE 14.243366 / maxChannelDelta 211`；Meal Planning `10.8512% / MAE 1.887898 / RMSE 12.687881 / maxChannelDelta 204`。
- [x] 三项均为同尺寸 `COMPARED`，但仍存在非零像素差异，结论保持 `DIFF_REVIEW`；105 张画板汇总保持 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本大点完成后集中执行四个直接相关测试文件，共 `29/29` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] 本大点只完成头像资源和证据同步，不代表三个页面或 105 张画板达到像素级 `PASS`；Knowledge、Profile、Admin 的后续视觉收口、shadcn 全量逐页审计和 iconfont 资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 204. 2026-09-05 Knowledge 与 Profile 头像资源及证据收口

本大点依据实时 Figma 节点 `795:838`（Knowledge）和 `806:1119`（Profile）完成前端 fixture 头像资源收口；Figma 文件保持只读，真实模式的性别默认头像逻辑不变。

- [x] Knowledge fixture 继续使用已登记的 Figma 示例头像；Profile fixture 新增侧栏、顶栏和资料卡三份真实 Figma PNG，资源路径统一由 `foodmate-ui/src/lib/avatar.ts` 管理。
- [x] 三份 Profile 资源已登记 SHA-256：`sidebar-avatar.png` 为 `ABCC3C903904A879EC46D61C58C335A83958AA04FCC2D236F2AF9AC6B0AC1F05`；`topbar-avatar.png` 为 `65A34D58804F9A493A7A2A92B56CD60418F0B9404E0249C787D8102894511345`；`main-avatar.png` 为 `DEBED9DB285B7FCD2040BFEF6B9AB68C1E9A529F5E20140415C0BAF4BE538C73`。
- [x] Profile 19 个状态和 Knowledge 3 个映射状态已重新采集浏览器 PNG；统一使用 `1440×1024`、DPR `1`、字体 `loaded` 和 Chrome `152.0.7977.77` 条件。
- [x] 修正 Profile fixture 头像为空字符串时不回退到 Figma 头像的问题，并补充 Knowledge/Profile 头像映射测试。
- [x] `figma-105-mapping.json` 与 `figma-105-diff-results.json` 已同步当前证据；105 项仍为同尺寸 `COMPARED`，无 `UNMAPPED` 和 `SIZE_MISMATCH`。
- [ ] 当前 105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`；Knowledge 默认内容区独立 diff 为 `38.7247% / MAE 3.447377 / RMSE 17.079127 / maxChannelDelta 240`，Profile 与 Knowledge 状态不能标记像素级 `PASS`。
- [ ] 本大点不关闭 shadcn 全量逐页迁移或 iconfont 阻塞；iconfont 继续为 `BLOCKED`。

## 206. 2026-09-05 Auth 页面状态视觉收口

本大点完成认证页面当前未提交改动的代码、Figma 状态上下文回读和 DPR1 证据同步。Figma 文件保持只读；本次只处理 Auth 页面及其视觉采集脚本，不吸收用户并行的 Admin、后端和本地 E2E 修改。

- [x] `LoginPage` 使用 `visualState` 统一驱动默认态、提交态、字段错误、凭据错误、账号锁定、账号禁用和服务不可用状态；真实登录提交仍只调用既有认证接口，不伪造成功结果。
- [x] 登录动画依据 Figma 节点 `647:214` 的 `get_motion_context` 实现 `4500ms` 无限循环、斜切背景、品牌标记缩放/旋转和内容分段显现；`visual-qa=1`、减少动态模式和异常状态均关闭装饰动画。
- [x] 读取实时 Figma 状态节点 `680:408`、`680:445`、`680:483`、`680:524`、`680:564`、`680:606`，确认提交态、错误态、警告态和信息态的颜色、透明度、间距和 `460px` 状态卡布局；状态资源继续使用已登记的 Figma SVG。
- [x] 重新采集 13 个 Auth 画板，统一使用 Chrome `152.0.7977.77`、`1440×900`、DPR `1`、字体 `loaded`、`visual-qa=1` 和无页面溢出条件；浏览器证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/dpr1-*-browser-2026-09-05.png`。
- [x] `capture-figma-dpr1.mjs` 增加 Windows Chrome profile 锁的限定次数清理重试，避免已结束的采集因临时目录释放时序失败；重试只作用于本次采集创建的临时目录。
- [x] 最新 13 项自动 diff 已写入 `figma-105-diff-results.json`：登录默认态 `3.7948%`、注册 `4.2780%`、找回密码 `3.0128%`、重置密码 `3.3946%`、登录反馈态 `6.0791%~8.4825%`、Token 状态 `1.8195%~1.9593%`；全部为同尺寸 `COMPARED`。
- [x] 本大点集中执行 `npm test -- --run`，结果为 `39/39` 个测试文件、`245/245` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] 13 项自动 diff 均为非零差异，不能标记为像素级 `PASS`；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。shadcn 全量逐页视觉收口和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 207. 2026-09-05 Admin Overview 分页总量与视觉证据收口

本大点依据实时 Figma 节点 `995:977` 完成 Admin Overview mock 分页总量和数字格式的视觉收口。Figma 文件保持只读；本次不吸收 Admin RunsTab、后端和本地 E2E 并行修改。

- [x] `OverviewTab` 将 mock 首屏六条记录与系统总量分离，分页总量固定为设计稿要求的 `12,480`；分页文案使用千位分隔格式，真实模式仍使用后端返回的 `total`。
- [x] 新增 Admin Overview 回归断言，确认页面显示 `显示第 1 到 6 条，共 12,480 条结果`；本次改动保留 shadcn Select、Table、Card 和 Button 的交互边界。
- [x] 使用 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded` 和无页面横向溢出条件采集 `dpr1-admin-overview-browser-2026-09-05.png`，并更新 `figma-105-mapping.json` 与 `figma-105-diff-results.json`。
- [x] Admin Overview 最新同尺寸 diff 为 `157481/1474560` 个差异像素，差异比例 `10.6799%`，`MAE=2.591621`，`RMSE=15.781021`，最大通道差异 `238`；自动状态保持 `DIFF_REVIEW`。
- [x] 本大点完成后执行 Admin 定向测试 `23/23`、`npm run typecheck`、`npm run build`、`npm run format:check`、`npm run lint`、`npm run qa:figma:validate` 和 `git diff --check`，均通过；Figma 结构校验为 `structuralPass=true`、`strictDprPass=true`、`errors=[]`。
- [ ] 本大点只完成 Admin Overview 分页内容和当前证据收口；默认男性头像按用户要求继续使用项目默认资产，字体、头像和浏览器光栅化仍有差异，不能标记该画板或 105 张画板为像素级 `PASS`。Admin 其它页面、shadcn 全量逐页视觉审计和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 208. 2026-09-05 Admin 工具注册表及六种操作状态视觉收口

本大点依据实时 Figma 节点 `692:3847`、`692:4319`、`692:4539`、`692:4766`、`692:4995` 和 `692:5207`，完成工具注册表及六种管理操作状态的前端视觉收口。Figma 文件保持只读，本批次未纳入后端、Agent Runtime 或本地 E2E 并行改动。

- [x] 工具注册表表格数据行统一为 `58px`，末四列宽度按 Figma 收口为 `68px / 68px / 48px / 72px`；行内操作按钮为 `26px`，图标为 `14px`，保留 shadcn `DataTable`、`Select`、`Card` 和 `Button` 行为。
- [x] 确认、提交中、成功、失败和无权限状态已分别对齐 Figma 的弹窗、遮罩、进度说明、错误调试信息和权限提示；操作按钮高度统一为 `37px`，失败态操作按钮使用“关闭”，不与系统失败语义混淆。
- [x] `op-no-permission` fixture 显示 `管理员：Operator（无写权限）`，停用按钮全部禁用；`op-success` fixture 保留已停用工具行，并显示启用入口和其它工具的注意态停用入口。
- [x] 采集脚本在截图前移除焦点描边，遮罩按 Figma 使用纯黑 `40%`；该处理只作用于视觉验收采集，不改变生产页面样式。
- [x] 已登记 6 张最新 Admin 浏览器证据：`dpr1-admin-tool-registry-browser-2026-09-05.png`、`dpr1-admin-op-no-permission-browser-2026-09-05.png`、`dpr1-admin-op-confirm-browser-2026-09-05.png`、`dpr1-admin-op-submitting-browser-2026-09-05.png`、`dpr1-admin-op-success-browser-2026-09-05.png`、`dpr1-admin-op-failed-browser-2026-09-05.png`，以及对应的 Figma PNG 和 diff JSON。
- [x] 本批次集中执行 `npm run test`，结果为 `39/39` 个测试文件、`247/247` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [x] 当前 Figma 验收结构结果为 `total=105`、`structuralPass=true`、`strictDprPass=true`、`errors=[]`；105 个画板均有自动比较输入，无 `UNMAPPED` 和 `SIZE_MISMATCH`。
- [ ] 六个 Admin 目标画板仍存在非零自动 diff，当前结论保持 `DIFF_REVIEW`；本大点不把结构门禁、构建通过或截图存在替代像素级 `PASS`。shadcn 全量逐页迁移仍未完成，iconfont 实体包、CSS 映射、来源和许可证仍缺失，继续为 `BLOCKED`。

## 209. 2026-09-05 Workspace Home 与 Agent Chat 当前 Figma 证据校正

本大点在实时重新读取 Figma 节点 `640:256` 和 `640:428` 后，校正 Workspace Home 与 Agent Chat 的 fixture 内容及主验收参考图。Figma 文件保持只读；本次不吸收后端、Admin 或本地 SQL 并行改动。

- [x] Home fixture 的快捷操作、输入框、发送按钮、指标进度环和待确认队列文案继续按 `640:256` 的颜色、尺寸和内容收口，调整均限制在 `state=figma-v2`。
- [x] Chat fixture 已确认实时画板包含 `983:3 agent::message-actions` 说明面板，已恢复对应的面板结构、文案、边框、间距和移动端适配；真实模式消息、确认、预算和 SSE 逻辑不受影响。
- [x] 主映射已将 `agent-chat-v2` 的 Figma PNG 切换为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured-figma/agent-chat-v2-figma-2026-09-05.png`，不再使用缺少说明面板的历史参考图；历史 PNG 保留，不覆盖或删除。
- [x] Home 与 Chat 均已在 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集；对应浏览器证据为 `dpr1-workspace-home-v2-browser-2026-09-05.png` 和 `dpr1-agent-chat-v2-browser-2026-09-05.png`。
- [x] 同版本自动 diff：Workspace Home 为 `24.4219% / MAE 3.798675 / RMSE 19.280955 / maxChannelDelta 248`；Agent Chat 为 `11.7176% / MAE 2.906090 / RMSE 17.246569 / maxChannelDelta 211`。
- [x] 本大点完成后集中执行 `npm run test`，结果为 `39/39` 个测试文件、`247/247` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] 两页自动 diff 仍为非零，结论继续保持 `DIFF_REVIEW`；105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。这次校正不关闭 shadcn 全量逐页视觉迁移或 iconfont 阻塞，iconfont 继续为 `BLOCKED`。

## 210. 2026-09-05 饮食业务三页批次交付范围调整

本大点将当前前端执行范围收敛为 Diet Records、Intake Analysis 和 Meal Planning 三页的代码质量与交付收口，不把 105 个画板的全量复验作为本阶段完成条件。Figma 文件保持只读，后端、Agent Runtime、Admin 并行变更和临时 SQL 不纳入本批次。

- [x] 三页已完成对应 Figma 节点 `640:588`、`640:773`、`640:901` 的页面结构、指标环资源、餐次图标、Meal Planning 边框和 `data-figma-node-id` 标识收口。
- [x] 三页主画板当前同尺寸 diff 已登记：Diet Records `7.9260% / MAE 2.544206 / RMSE 16.833673 / maxChannelDelta 219`；Intake Analysis `7.5430% / MAE 2.004188 / RMSE 14.243366 / maxChannelDelta 211`；Meal Planning `10.8815% / MAE 1.939608 / RMSE 12.962410 / maxChannelDelta 204`，三项均保持 `DIFF_REVIEW`。
- [x] 本批次仅保留现有 Figma 证据和三页相关资源，不新增虚构 iconfont 字体、Unicode 映射或未经登记的图标资源；iconfont 继续为 `BLOCKED`。
- [ ] 本阶段不执行 105 个画板的全量重新截图和全量人工验收；既有 105 项映射仅作为历史证据索引，不得被解释为全量像素级完成。
- [x] 三页批次完成后集中执行三页定向测试：3 个测试文件、27 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check` 和 `git diff --check` 均通过。

## 211. 2026-09-06 Knowledge 页面 Figma 壳层与状态证据收口

本大点依据实时 Figma 节点 `795:838` 及状态节点 `795:786`、`795:968`、`795:1151`，完成 Knowledge 页面 Figma fixture 壳层收口。Figma 文件保持只读；本批次不重新验收全部 105 个画板，也不纳入后端、数据库和其它并行变更。

- [x] Knowledge fixture 恢复 Figma 画板要求的会话搜索、会话历史和分页，并仅在 fixture 中显示桌面窗口控制点；真实模式不渲染装饰性窗口控制点。
- [x] Knowledge 主内容 fixture 使用白色 Figma 画布背景；真实模式的知识检索、引用详情、来源不可用和检索失败交互逻辑未改变。
- [x] 已登记 3 组最新 Figma 与浏览器证据：`user-knowledge-empty`、`user-knowledge-search-failed`、`user-knowledge-source-unavailable`；浏览器采集条件为 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出。
- [x] 当前同尺寸自动 diff 分别为 Empty `40.8765%`、Search Failed `39.9119%`、Source Unavailable `39.8908%`；三项均存在非零差异，继续保持 `DIFF_REVIEW`。
- [x] Knowledge 与 WorkspaceLayout 定向测试为 `2` 个测试文件、`19/19` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] 本大点不关闭 105 张画板的像素级 `PASS`；当前全量结构校验仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。shadcn 全量逐页视觉审计及 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 212. 2026-09-06 Profile 页面 Figma 视觉收口与证据更新

本大点依据实时 Figma 节点 `806:1119`、`806:1281`、`806:1445`、`806:1585` 及 Profile 状态节点 `792:212`、`794:212`、`794:380`、`794:548`、`794:693`、`794:838`、`794:984`、`794:1127`、`795:212`、`795:356`、`795:499`、`795:642`、`1013:2`、`1013:235`、`1013:465`，完成 Profile fixture 的当前代码收口和证据更新。Figma 文件保持只读；本批次只复核 Profile 的 19 个状态，不重新验收其它画板。

- [x] `state=basic` fixture 恢复完整八字段资料表单：展示名称、性别、身高、体重、活动水平、饮食目标、每日热量目标和每日蛋白质目标；饮食与身体目标卡按 Figma 采用双列布局和 `520px` 高度。
- [x] Profile fixture 恢复 Figma 要求的会话搜索、会话历史、分页、收起导航和桌面窗口控制点；顶栏绿色品牌方块按设计稿不显示 `F` 字母，真实模式行为保持不变。
- [x] Profile 的基本资料、记忆与偏好、安全与设备、隐私与数据，以及头像上传、未保存离开、密码提交、导出和注销相关状态均保留独立 query fixture；真实模式的资料、密码、导出和注销请求逻辑未改写。
- [x] 19 项浏览器 PNG 已统一更新到 `2026-09-06`，采集条件为 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded` 和页面无横向溢出；运行时证据和 105 画板映射已同步到本批次路径。
- [x] 19 项 Figma 与浏览器 PNG 尺寸一致，自动 diff 已由 `scripts/generate-figma-105-diff.mjs` 重新生成；Profile 差异指标和节点映射详见 [Figma 前端像素级验收报告](./Figma前端像素级验收报告.md) 第 26 节。
- [x] ProfilePage 与 WorkspaceLayout 定向测试为 `2` 个测试文件、`37/37` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] 19 项自动 diff 均为非零差异，仍全部保持 `DIFF_REVIEW`，本批次不标记任何 Profile 画板为像素级 `PASS`；105 项聚合继续保持 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本大点不关闭 shadcn 全量逐页视觉迁移或 iconfont 资源阻塞；标准命令图标继续使用 Lucide，iconfont 实体包、CSS 映射、来源和许可证缺失时继续保持 `BLOCKED`。

## 213. 2026-09-06 Auth 页面组 shadcn 控件迁移审计

本大点完成 Auth 页面组的控件迁移审计，范围包括 Login、Register、Forgot Password、Reset Password 和 Token Status。审计以当前代码和既有 Figma 证据为准，不重新采集 105 个画板，也不改变已经登记的 Auth 浏览器 PNG 与 diff。

- [x] `AuthVisual` 统一使用 `src/components/ui/button.tsx` 和 `src/components/ui/input.tsx`；密码可见性、返回登录、提交、辅助操作和状态操作均通过 shadcn Button 承载。
- [x] Login 页面直接使用 shadcn `Input`/`Button`；Register、Forgot Password、Reset Password 和 Token Status 通过共享 Auth 组件使用相同基础控件，未发现可见原生 `button`、`input`、`select` 或 `textarea` 控件。
- [x] Figma 导出的品牌、字段图标、密码可见性图标、分隔线和 Token 状态 SVG 仍由页面资源映射使用；本审计没有创建虚构 iconfont glyph 或 Unicode 映射。
- [x] Auth 13 个状态的既有证据仍统一为 `1440×900`、DPR `1`、字体加载完成和无页面溢出，全部保持 `DIFF_REVIEW`。
- [x] 本大点未修改 Auth 业务提交逻辑、动画契约、页面 CSS 或后端接口；共享壳层的 Figma 语义 Token 继续由 `authVisualTokens.ts` 提供。
- [ ] Auth 13 个画板仍存在非零自动 diff，人工视觉复核和像素级 `PASS` 尚未完成；本审计只关闭 Auth 的 shadcn 控件迁移审计，不关闭 105 画板验收、其它页面迁移或 iconfont `BLOCKED`。

## 214. 2026-09-06 Workspace/Home 与 Agent Chat shadcn 控件迁移审计

本大点完成 Workspace/Home、Agent Chat 及共享 `WorkspaceLayout`、`Composer`、会话列表和 Agent 卡片的控件迁移审计。审计以当前代码和既有 Figma 证据为准，不重新采集全部 105 个画板。

- [x] WorkspaceLayout 的新建任务、会话搜索、分页、通知、账户菜单、重命名、删除和回收站操作使用 shadcn `Button`、`Input`、`DropdownMenu` 和 `Dialog`。
- [x] Home 的任务输入和发送操作使用 shadcn `Input`/`Button`；隐藏文件输入只承担浏览器文件选择，不作为可见设计控件。
- [x] Agent Chat 的运行轨迹使用 shadcn `Tabs`，确认、预算、重试、取消、跳过、反馈和结果操作使用 shadcn `Button`，选项确认使用 shadcn `RadioGroup`，内容容器使用 shadcn `Card`/`Alert`。
- [x] Composer、SidebarSessionList、ConfirmationCard、ClarificationCard、ResultCard 和 AgentFeedback 未发现直接依赖 `AdminPrimitives` 或可见原生交互控件。
- [x] 本审计不改变 Agent 状态 fixture、真实 SSE 连接、会话操作和后端请求边界；标准命令图标继续使用 Lucide，iconfont 不新增虚构映射。
- [ ] Workspace Home 与 Agent Chat 的自动 diff 仍为非零差异，页面 CSS 的 Figma 视觉收口和人工像素复核仍需继续；本审计只确认控件迁移边界，不标记画板为 `PASS`。

## 215. 2026-09-06 Diet Records、Intake Analysis、Meal Planning shadcn 控件迁移审计

本大点完成 Diet Records、Intake Analysis、Meal Planning 及餐食规划向导、购物清单和共享表格组件的控件迁移审计。审计以当前代码和既有 Figma 证据为准，不重新采集全部 105 个画板。

- [x] Diet Records 使用 shadcn `Button`、`Dialog` 和 `Input` 承载记录操作、食物编辑、添加食物和状态操作；指标环与记录内容继续由页面视觉结构渲染。
- [x] Intake Analysis 使用 shadcn `Button` 和 `Skeleton` 承载操作与加载状态，未发现可见原生表单控件或 `AdminPrimitives` 依赖。
- [x] Meal Planning 和 `MealPlanningFlow` 使用 shadcn `Button`、`Checkbox`、`Input`、`RadioGroup`、`Select`；`MealPlanTable` 使用共享 shadcn Table，购物清单使用共享 Badge/Checkbox 语义。
- [x] 三页真实模式的分析、饮食记录、计划创建、购物清单和删除/保存请求边界未因控件审计改变；Figma fixture 继续使用独立 query 状态。
- [ ] 三页及其状态画板仍有非零自动 diff，页面 CSS 的 Figma 视觉收口和人工像素复核仍需继续；控件迁移审计不标记任何画板为 `PASS`。

## 216. 2026-09-06 Knowledge 与 Profile shadcn 控件迁移审计

本大点完成 Knowledge 和 Profile 页面及其状态、弹窗、资料表单、记忆编辑、密码修改、数据导出和注销操作的控件迁移审计。审计以当前代码和已提交的 Figma 证据为准，不重新采集全部 105 个画板。

- [x] Knowledge 使用 shadcn `Input`、`Button`、`Card` 和 `Badge`；搜索、引用详情、来源状态和重试操作均通过共享 UI 组件承载。
- [x] Profile 使用 shadcn `Button`、`Card`、`Dialog`、`Input`、`Progress`、`Select`、`Textarea` 和 `Tooltip`；基本资料、记忆、安全、隐私和确认状态均复用这些组件。
- [x] Profile 的原生 `file input` 使用隐藏样式，仅承担头像文件选择；可见的“选择图片”和删除操作均使用 shadcn Button。
- [x] Knowledge/Profile 代码未发现 `AdminPrimitives` 直接引用；真实模式的知识检索、账户资料、记忆、密码、导出和注销服务边界未因审计改变。
- [ ] Knowledge 与 Profile 的已登记画板仍有非零自动 diff，人工视觉复核和像素级 `PASS` 尚未完成；本审计不关闭 Figma 视觉验收或 iconfont `BLOCKED`。

## 217. 2026-09-06 Admin 页面组 shadcn 控件迁移审计

本大点完成 Admin 概览、工具注册表、删除资源、知识库、用户详情、Run/Tool/SQL/Trace、模型治理、用量和操作审计页面的控件迁移审计。审计以当前代码和已登记的 Figma 证据为准，不重新采集全部 105 个画板，也不改变 Admin 现有真实服务边界。

- [x] Admin 页面及各 Tab 已使用 `src/components/ui` 下的 shadcn/Radix 组件：`Button`、`Input`、`Select`、`Card`、`Table`、`DataTable`、`Tabs`、`DropdownMenu`、`Dialog`、`Alert`、`Textarea`、`Sheet`、`Badge` 和 `Progress`。
- [x] 概览、工具注册表、删除资源、用户详情、治理审计、知识库上传、模型治理和用量页面的筛选、分页、详情、确认、恢复、重试和状态反馈均由共享 UI 组件承载；没有发现 `AdminPrimitives` 直接依赖。
- [x] 知识库上传保留一个隐藏的原生 `file input`，仅连接浏览器文件选择和拖拽 API；该入口尺寸为 `1px`、透明且不可见，不替代页面可见的 shadcn 控件。
- [x] Admin 页面没有新增未经登记的 iconfont 字体、glyph 或 Unicode 映射；标准命令图标继续使用 Lucide，真实模式请求、权限判断、确认和操作审计边界未因本审计改变。
- [ ] Admin 已登记画板的自动 diff 仍存在非零差异，视觉 Token、布局差异和人工复核仍需继续；本审计只关闭 Admin 的控件迁移审计，不将任何画板标记为像素级 `PASS`。
- [ ] iconfont 实体字体包、CSS 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`；本大点也不执行 105 个画板的全量重新验收。

## 218. 2026-09-06 Diet Records、Intake Analysis、Meal Planning Figma 资源收口

本大点完成 Diet Records、Intake Analysis 和 Meal Planning 三页工作区壳层资源的独立登记与当前浏览器证据同步。Figma 文件保持只读；本批次只处理三个对应画板，不重新验收全部 105 个画板，也不吸收工作区中已有的后端并行改动。

- [x] 三页分别使用 `diet-records`、`analysis` 和 `planning` 目录下的真实 Figma SVG 资源；窗口控制点、导航图标、会话状态点、搜索、通知和分页图标不再复用 Home fixture 变体。
- [x] Diet Records、Intake Analysis 和 Meal Planning 的页面入口分别保持 `/analysis?view=records&state=v2`、`/analysis?state=v2` 和 `/planning?state=v2`；真实模式的饮食记录、摄入分析、餐食规划请求边界未改变。
- [x] 三项最新浏览器证据均为 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded`、页面无横向溢出；证据文件分别为 `dpr1-diet-records-v2-browser-2026-09-06.png`、`dpr1-intake-analysis-v2-browser-2026-09-06.png` 和 `dpr1-meal-planning-v2-browser-2026-09-06.png`。
- [x] 最新同尺寸 PNG diff 已同步到 `figma-105-diff-results.json`：Diet Records `7.9036% / MAE 2.529741 / RMSE 16.802579 / maxChannelDelta 219`；Intake Analysis `7.5198% / MAE 1.990366 / RMSE 14.208778 / maxChannelDelta 211`；Meal Planning `10.8603% / MAE 1.925624 / RMSE 12.924167 / maxChannelDelta 204`。
- [ ] 三项自动 diff 均为非零，当前结论继续为 `DIFF_REVIEW`；本批次不将资源登记、结构检查或截图存在替代像素级 `PASS`，也不改变 105 项历史聚合状态。
- [x] 本批次没有创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 实体包、CSS 映射、来源和许可证仍缺失，继续保持 `BLOCKED`。

## 219. 2026-09-06 Admin 页面组 Figma 资源与证据收口

本大点只收口 Admin 页面组的 Figma 资源和当前浏览器证据，不重新验收全部 105 个画板。Admin 代码仍保持真实模式服务、权限判断、操作确认和审计边界；Figma 设计稿保持只读。

| 画板 | Figma 节点 | 前端入口 | 浏览器截图 | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---:|---:|---:|---:|---|
| Admin Tool Registry | `692:3847` | `/admin?state=tool-registry` | `dpr1-admin-tool-registry-browser-2026-09-06.png` | 15.8782% | 3.999545 | 20.969931 | 224 | `DIFF_REVIEW` |
| Admin Deleted Resources | `692:4104` | `/admin?state=deleted-resources` | `dpr1-admin-deleted-resources-browser-2026-09-06.png` | 17.6843% | 2.682758 | 16.687796 | 224 | `DIFF_REVIEW` |
| Admin Operation No Permission | `692:4319` | `/admin?state=op-no-permission` | `dpr1-admin-op-no-permission-browser-2026-09-06.png` | 15.4554% | 3.857794 | 20.380618 | 239 | `DIFF_REVIEW` |
| Admin Operation Confirm | `692:4539` | `/admin?state=op-confirm` | `dpr1-admin-op-confirm-browser-2026-09-06.png` | 17.4894% | 2.173829 | 11.940769 | 224 | `DIFF_REVIEW` |
| Admin Operation Submitting | `692:4766` | `/admin?state=op-submitting` | `dpr1-admin-op-submitting-browser-2026-09-06.png` | 15.9030% | 2.180962 | 11.956242 | 224 | `DIFF_REVIEW` |
| Admin Operation Success | `692:4995` | `/admin?state=op-success` | `dpr1-admin-op-success-browser-2026-09-06.png` | 14.9661% | 3.819746 | 20.499449 | 231 | `DIFF_REVIEW` |
| Admin Operation Failed | `692:5207` | `/admin?state=op-failed` | `dpr1-admin-op-failed-browser-2026-09-06.png` | 17.1870% | 2.490200 | 13.230959 | 204 | `DIFF_REVIEW` |
| Admin Knowledge Uploading | `782:212` | `/admin?state=knowledge-uploading` | `dpr1-admin-knowledge-uploading-browser-2026-09-06.png` | 30.3681% | 1.976585 | 12.259095 | 250 | `DIFF_REVIEW` |
| Admin Knowledge Indexing | `782:366` | `/admin?state=knowledge-indexing` | `dpr1-admin-knowledge-indexing-browser-2026-09-06.png` | 30.8948% | 2.110329 | 12.663239 | 250 | `DIFF_REVIEW` |
| Admin Knowledge Upload Failed | `782:520` | `/admin?state=knowledge-upload-failed` | `dpr1-admin-knowledge-upload-failed-browser-2026-09-06.png` | 31.0393% | 2.431026 | 14.149767 | 250 | `DIFF_REVIEW` |
| Admin Run Detail | `797:212` | `/admin?state=run-detail` | `dpr1-admin-run-detail-browser-2026-09-06.png` | 12.0080% | 2.499439 | 15.990066 | 211 | `DIFF_REVIEW` |
| Admin Tool Calls | `797:359` | `/admin?state=tool-calls` | `dpr1-admin-tool-calls-browser-2026-09-06.png` | 6.5448% | 2.357275 | 16.839382 | 211 | `DIFF_REVIEW` |
| Admin SQL Audit | `797:490` | `/admin?state=sql-audit` | `dpr1-admin-sql-audit-browser-2026-09-06.png` | 6.5533% | 2.401219 | 17.082409 | 211 | `DIFF_REVIEW` |
| Admin Trace | `797:621` | `/admin?state=trace` | `dpr1-admin-trace-browser-2026-09-06.png` | 11.8608% | 2.428496 | 15.748137 | 211 | `DIFF_REVIEW` |
| Admin User Detail | `801:215` | `/admin?state=user-detail` | `dpr1-admin-user-detail-browser-2026-09-06.png` | 23.4795% | 4.031070 | 21.611936 | 227 | `DIFF_REVIEW` |
| Admin Knowledge Upload Success | `806:1737` | `/admin?state=knowledge-upload-success` | `dpr1-admin-knowledge-upload-success-browser-2026-09-06.png` | 46.4221% | 5.066889 | 23.039189 | 242 | `DIFF_REVIEW` |
| Admin Overview | `995:977` | `/admin?state=overview` | `dpr1-admin-overview-browser-2026-09-06.png` | 10.6503% | 2.553537 | 15.653056 | 204 | `DIFF_REVIEW` |
| Admin Model Usage | `995:1238` | `/admin/usage` | `dpr1-admin-model-usage-browser-2026-09-06.png` | 11.9490% | 2.789790 | 16.413194 | 238 | `DIFF_REVIEW` |
| Admin Operation Audit | `995:1499` | `/admin?view=audit` | `dpr1-admin-operation-audit-browser-2026-09-06.png` | 9.0025% | 3.000415 | 18.332441 | 241 | `DIFF_REVIEW` |
| Admin Knowledge Format Error | `997:2` | `/admin?state=knowledge-format-error` | `dpr1-admin-knowledge-format-error-browser-2026-09-06.png` | 59.9796% | 2.542161 | 12.922332 | 204 | `DIFF_REVIEW` |
| Admin Knowledge Size Error | `997:160` | `/admin?state=knowledge-size-error` | `dpr1-admin-knowledge-size-error-browser-2026-09-06.png` | 59.9767% | 2.552692 | 13.012652 | 204 | `DIFF_REVIEW` |

- [x] Admin 21 项浏览器证据已由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和页面无横向溢出条件下采集；映射、运行时检查和自动 diff JSON 已同步。
- [x] 新增 Figma Admin 侧栏头像 `foodmate-ui/public/assets/figma/admin/admin-sidebar-avatar.png`，SHA-256 为 `EC63D2CDB253B2E58A36075163731E98F7CD3032BB50C0DC94F22BBC339F45AF`。
- [x] 新增 Figma Overview 复制图标 `foodmate-ui/public/assets/figma/admin/overview/copy.svg`，SHA-256 为 `C23E71416B45E9528A605ECD8CA8D2029CB2D0DE09AEBBD09285C78792CB6F5E`；User Detail 继续使用已登记的 Figma 用户头像资源。
- [x] Admin 侧栏和 User Detail 已改用登记的 Figma 资源，Overview 复制操作已改用登记的 `copy.svg`；真实模式业务请求和权限边界未改变。
- [ ] 21 项自动 diff 均为非零差异，全部保持 `DIFF_REVIEW`；本批次不将截图、几何检查、构建或控件迁移审计替代像素级 `PASS`。
- [ ] 本批次只复核 Admin 21 项，不代表其它画板重新采集，也不关闭 105 项全量人工视觉复核；当前聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。
- [x] 本大点完成后集中执行三页定向测试：4 个测试文件、28 个用例通过；`npm run format:check`、`npm run lint`、`npm run typecheck`、`npm run build`、`npm run qa:figma:validate`、JSON 解析和 `git diff --check` 均通过。证据结构校验为 `structuralPass=true`、`strictDprPass=true`、`errors=[]`，全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。

## 220. 2026-09-06 Admin 知识库状态页视觉收口

本大点只处理 Admin 知识库上传中、索引中、批次失败、上传成功、格式错误和大小错误 6 个 Figma 状态，不重新采集全部 105 个画板，不修改真实模式的后端上传契约。

- [x] AdminPage 为知识库 Figma fixture 增加独立页面边距、顶部“批量上传”入口和上传 Dialog 打开请求；真实模式的刷新、上传接口和权限边界保持不变。
- [x] KnowledgeSection 增加 `figmaFixture` 边界：fixture 使用 Figma 的 `Max file size: 50MB. Allowed formats: PDF, CSV, XLSX, TXT.` 文案、上传区尺寸、表格行高、向量洞察背景和 Figma 资源视觉；真实模式仍使用原有 `20` 个文件、`20 MB`、`PDF/DOCX/Markdown/TXT` 校验。
- [x] 6 项浏览器证据已由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集；映射、diff JSON 和截图路径已同步。
- [x] 本批次进一步收口知识库 fixture：Figma 专用侧栏合并为 8 项、表格状态改为“已索引/索引中/失败”、隐藏 Figma 未展示的管理操作和错误徽章，同时保留可访问的关闭操作；状态关闭后返回知识库页面，真实模式路径不变。
- [x] 最新 diff：上传中 `29.5154% / MAE 1.887562 / RMSE 12.209293`；索引中 `30.2598% / MAE 2.022699 / RMSE 12.614875`；批次失败 `30.4384% / MAE 2.327844 / RMSE 14.066926`；上传成功 `38.0500% / MAE 3.629768 / RMSE 19.628179`；格式错误 `57.4451% / MAE 2.482137 / RMSE 12.951715`；大小错误 `57.4422% / MAE 2.492672 / RMSE 13.041847`。
- [x] Admin 定向测试 `28/28` 通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 通过；Figma 结构校验保持 `structuralPass=true`、`strictDprPass=true`、`errors=[]`。
- [ ] 6 项仍存在字体光栅化、导航图标、头像和局部内容差异，继续保持 `DIFF_REVIEW`；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，不能宣称像素级 `PASS`。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，继续保持 `BLOCKED`。

## 221. 2026-09-06 Workspace Home 与 Agent Chat 共享 Figma 视觉基线收口

本节记录 Workspace Home 节点 `640:256` 与 Agent Chat 节点 `640:428` 的共享壳层视觉基线收口。本批次只处理两个受影响画板，Figma 文件保持只读，不重新验收其余 103 个画板，也不纳入后端改动。

- [x] Workspace Figma 语义 Token 已集中登记背景、表面、控件表面、边框、侧栏宽度 `260px`、顶栏高度 `68px`、右侧轨道 `320px/340px` 和页面内边距 `24px`；真实业务模式不读取这些 fixture 专属 Token。
- [x] WorkspaceLayout 已使用上述 Token 收口 Figma fixture 的侧栏、顶栏、主表面、右侧 Trace rail 和响应式列宽；Home 与 Chat 继续保留真实模式的业务结构和服务边界。
- [x] Home fixture 已按 Figma 收口任务输入器 `80px`、指标卡 `88px`、面板圆角、边框、表面和控件背景；Chat fixture 已统一消息区、助手消息、确认卡、Composer 和 Trace rail 的中性表面与边框。
- [x] 已重新采集 `dpr1-workspace-home-v2-browser-2026-09-06.png` 和 `dpr1-agent-chat-v2-browser-2026-09-06.png`；采集条件为 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded` 和页面无横向溢出。
- [x] Workspace Home 最新同尺寸 diff 为 `11.8524% / MAE 2.977588 / RMSE 17.700409 / maxChannelDelta 244`；Agent Chat 最新同尺寸 diff 为 `12.0520% / MAE 2.929536 / RMSE 17.357318 / maxChannelDelta 211`。
- [x] `figma-105-mapping.json`、`figma-105-diff-results.json` 和对应浏览器 PNG 已同步到最新证据路径；105 个画板映射数量、视口、几何、文字和结构校验保持有效。
- [x] 本批次集中验证：40 个前端测试文件、`249/249` 个用例通过；`npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run build`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。
- [ ] Workspace Home 与 Agent Chat 仍存在字体光栅化、头像、图标和局部组合差异，两个画板继续为 `DIFF_REVIEW`，不能标记为像素级 `PASS`。
- [ ] 本批次只重新验收两个受影响画板，不代表其余 103 个画板重新采集或完成全量人工视觉复核；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] shadcn 基础设施和控件迁移审计不等于全页面视觉收口；iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 222. 2026-09-06 Agent 六个状态 DPR1 增量验收

本节只更新 Agent 写入确认、预算上限、可重试工具失败、安全降级、用户取消和 SSE 重连六个状态的最新浏览器证据。Figma 文件保持只读；本批次不重新验收其余 99 个 Agent 以外画板，也不修改后端协议或真实模式业务逻辑。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---:|---:|---:|---:|---|
| Agent Write Confirmation | `687:773` | `/chat?state=write-confirmation` | `dpr1-agent-write-confirmation-browser-2026-09-06.png` | 10.0311% | 2.024202 | 14.170644 | 251 | `DIFF_REVIEW` |
| Agent Budget Limit | `687:918` | `/chat?state=budget-limit` | `dpr1-agent-budget-limit-browser-2026-09-06.png` | 11.7330% | 3.070085 | 18.411157 | 255 | `DIFF_REVIEW` |
| Agent Tool Failed Retryable | `687:1439` | `/chat?state=tool-failed-retryable` | `dpr1-agent-tool-failed-retryable-browser-2026-09-06.png` | 14.3805% | 2.825468 | 17.330696 | 254 | `DIFF_REVIEW` |
| Agent Safety Degraded | `687:1563` | `/chat?state=safety-degraded` | `dpr1-agent-safety-degraded-browser-2026-09-06.png` | 15.4136% | 3.030788 | 17.578041 | 255 | `DIFF_REVIEW` |
| Agent User Cancelled | `687:1684` | `/chat?state=user-cancelled` | `dpr1-agent-user-cancelled-browser-2026-09-06.png` | 10.5565% | 2.181197 | 15.279190 | 250 | `DIFF_REVIEW` |
| Agent SSE Reconnecting | `687:1803` | `/chat?state=sse-reconnecting` | `dpr1-agent-sse-reconnecting-browser-2026-09-06.png` | 15.1592% | 2.427713 | 16.289603 | 255 | `DIFF_REVIEW` |

- [x] 六项均由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和页面无横向溢出条件下重新采集；浏览器 PNG、映射、运行时记录和 diff JSON 已同步到 `2026-09-06`。
- [x] 已复核六个状态的内容和交互边界：写入确认、预算追加、工具重试/跳过、安全降级追问、用户取消重启和 SSE 重连提示均保留既有真实模式请求边界；fixture 操作不伪造后端成功事件。
- [x] 六项状态的尺寸和结构检查通过，自动 diff 均可复现；没有将头像、图标、字体光栅化和局部组合差异隐藏为 PASS。
- [ ] 六项自动 diff 均为非零，且人工复核仍确认共享工作台、头像、图标和浏览器字体光栅化差异，继续保持 `DIFF_REVIEW`。
- [ ] 本批次只重新采集上述六个受影响画板，不代表其余画板重新采集或完成全量人工视觉复核；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，继续保持 `BLOCKED`。

## 223. 2026-09-06 Admin 核心列表页视觉增量收口

本大点只处理 Admin Overview、Tool Registry 和 Deleted Resources 三个核心列表页的 Figma 视觉差异与增量证据，不重新验收全部 105 个画板。Figma 文件保持只读；真实模式服务、权限判断、操作确认和审计边界未改变。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---:|---:|---:|---:|---|
| Admin Overview | `995:977` | `/admin?state=overview` | `dpr1-admin-overview-browser-2026-09-06.png` | `1440×1024 / 1` | 7.3124% | 2.300200 | 15.565838 | 204 | `DIFF_REVIEW` |
| Admin Tool Registry | `692:3847` | `/admin?state=tool-registry` | `dpr1-admin-tool-registry-browser-2026-09-06.png` | `1440×1024 / 1` | 13.7887% | 2.906208 | 17.849120 | 224 | `DIFF_REVIEW` |
| Admin Deleted Resources | `692:4104` | `/admin?state=deleted-resources` | `dpr1-admin-deleted-resources-browser-2026-09-06.png` | `1440×1024 / 1` | 12.6503% | 3.186895 | 19.126565 | 224 | `DIFF_REVIEW` |

- [x] Overview 表头、Tool Registry 表头和列表高度已按当前 Figma PNG 复核；Deleted Resources 合规通告背景已校正为 Figma 证据中的 `#FFF5F5`，正文和标题层级保持可读性。
- [x] 三项浏览器 PNG 已由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集；三项同尺寸 diff 已同步到 `figma-105-diff-results.json`。
- [ ] 三项自动 diff 均为非零，仍存在字体光栅化、导航图标、头像和局部内容差异，不能标记为像素级 `PASS`；全量聚合继续保持 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次集中执行 4 个 Admin 测试文件、`38/38` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。证据校验返回 `structuralPass=true`、`strictDprPass=true`、`errors=[]`，全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 224. 2026-09-06 Admin 操作状态组视觉收口

本大点只处理 Admin 无权限、确认、提交中、成功和失败五个操作状态画板，不重新验收全部 105 个画板。Figma 文件保持只读；真实模式的权限判断、写操作确认、请求执行和审计边界未改变。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---:|---:|---:|---:|---|
| Admin Operation No Permission | `692:4319` | `/admin?state=op-no-permission` | `dpr1-admin-op-no-permission-browser-2026-09-06.png` | `1440×1024 / 1` | 14.8073% | 3.037330 | 18.268024 | 239 | `DIFF_REVIEW` |
| Admin Operation Confirm | `692:4539` | `/admin?state=op-confirm` | `dpr1-admin-op-confirm-browser-2026-09-06.png` | `1440×1024 / 1` | 17.2768% | 1.723395 | 10.732244 | 224 | `DIFF_REVIEW` |
| Admin Operation Submitting | `692:4766` | `/admin?state=op-submitting` | `dpr1-admin-op-submitting-browser-2026-09-06.png` | `1440×1024 / 1` | 15.5569% | 1.710165 | 10.673565 | 224 | `DIFF_REVIEW` |
| Admin Operation Success | `692:4995` | `/admin?state=op-success` | `dpr1-admin-op-success-browser-2026-09-06.png` | `1440×1024 / 1` | 14.5965% | 3.070683 | 18.550601 | 231 | `DIFF_REVIEW` |
| Admin Operation Failed | `692:5207` | `/admin?state=op-failed` | `dpr1-admin-op-failed-browser-2026-09-06.png` | `1440×1024 / 1` | 16.9732% | 2.039585 | 12.151475 | 204 | `DIFF_REVIEW` |

- [x] 操作态 fixture 在 Figma 专用路径下使用统一文件夹导航图标；无权限状态的操作列改为锁图标并保留按钮无障碍名称，真实模式操作列不变。
- [x] 五项浏览器 PNG 已由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集；五项同尺寸 diff 已同步到 `figma-105-diff-results.json`。
- [ ] 五项自动 diff 均为非零，仍存在共享头像、字体光栅化和局部内容差异，继续保持 `DIFF_REVIEW`；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次集中执行 3 个 Admin 操作态测试文件、`33/33` 个用例通过；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。证据校验返回 `structuralPass=true`、`strictDprPass=true`、`errors=[]`，全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次没有创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 资源登记继续保持 `BLOCKED`。

## 225. 2026-09-06 Admin User Detail 视觉收口

本大点只处理 Admin User Detail 节点 `801:215`，不重新验收全部 105 个画板。Figma 文件保持只读；真实模式用户加载、状态变更、会话撤销和权限边界未改变。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---:|---:|---:|---:|---|
| Admin User Detail | `801:215` | `/admin?state=user-detail` | `dpr1-admin-user-detail-browser-2026-09-06.png` | `1440×1024 / 1` | 21.0313% | 3.914682 | 21.375078 | 227 | `DIFF_REVIEW` |

- [x] Figma fixture 的角色和状态筛选值改为完整单行显示，注册日期控件按 Figma 目标字号显示；筛选行仍保持 `692×32`、`y=88`。
- [x] 仅 fixture 首行移除额外选中底色，真实模式仍保留选中反馈；详情操作区和说明卡的垂直节奏按 Figma 参考图调整。
- [x] 浏览器 PNG 由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下采集；运行时几何和文字检查通过。
- [ ] 自动 diff 仍为非零，侧栏身份、图标、字体、头像和浏览器光栅化仍存在可见差异，继续保持 `DIFF_REVIEW`，不能标记像素级 `PASS`。
- [ ] 本批次只复核 User Detail 一个画板，不代表其它画板重新采集或完成 105 项全量人工视觉复核；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 资源登记继续保持 `BLOCKED`。

## 226. 2026-09-06 Admin 运行详情组顶栏视觉收口

本大点只处理 Admin Run Detail、Tool Calls、SQL Audit、Trace 四个共享详情画板，不重新验收全部 105 个画板。当前 diff 使用 `figma-105-mapping.json` 指向的既有 Figma 参考图；Figma 文件保持只读，真实模式运行查询和详情接口未改变。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---:|---:|---:|---:|---|
| Admin Run Detail | `797:212` | `/admin?state=run-detail` | `dpr1-admin-run-detail-browser-2026-09-06.png` | 11.8894% | 2.361519 | 15.235328 | 211 | `DIFF_REVIEW` |
| Admin Tool Calls | `797:359` | `/admin?state=tool-calls` | `dpr1-admin-tool-calls-browser-2026-09-06.png` | 6.4040% | 2.187168 | 15.930855 | 211 | `DIFF_REVIEW` |
| Admin SQL Audit | `797:490` | `/admin?state=sql-audit` | `dpr1-admin-sql-audit-browser-2026-09-06.png` | 6.4125% | 2.231111 | 16.187527 | 211 | `DIFF_REVIEW` |
| Admin Trace | `797:621` | `/admin?state=trace` | `dpr1-admin-trace-browser-2026-09-06.png` | 11.7421% | 2.290575 | 14.981216 | 211 | `DIFF_REVIEW` |

- [x] 四个详情 fixture 的顶栏左右内边距按当前映射中的 Figma 参考图从通用 `32px` 调整为 `24px`，标题和刷新按钮分别完成 `8px` 水平校正。
- [x] 四张浏览器 PNG 均由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集。
- [x] 四项均为同尺寸 `COMPARED`；差异比例均较本批次前记录下降，仍保留 `DIFF_REVIEW`，没有将非零 diff 标记为 `PASS`。
- [ ] 本批次只复核四个 Admin 详情画板，不代表其余画板重新采集或完成 105 项全量人工视觉复核；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 资源登记继续保持 `BLOCKED`。
- [x] 本批次集中执行 AdminPage 定向测试 `25/25`；`npm run typecheck`、`npm run build`、`npm run lint`、`npm run format:check`、`npm run qa:figma:validate` 和 `git diff --check` 均通过。证据校验返回 `structuralPass=true`、`strictDprPass=true`、`errors=[]`。

## 227. 2026-09-06 Admin 模型用量与操作审计视觉收口

本大点只处理 Admin Model Usage 节点 `995:1238` 和 Operation Audit 节点 `995:1499`，不重新验收全部 105 个画板。Figma 文件保持只读；真实模式的模型治理、操作审计、导出和权限边界未改变。

| 画板 | 前端入口 | 浏览器 PNG | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---:|---:|---:|---:|---|
| Admin Model Usage | `/admin/usage` | `dpr1-admin-model-usage-browser-2026-09-06.png` | 11.4438% | 2.749786 | 16.293722 | 238 | `DIFF_REVIEW` |
| Admin Operation Audit | `/admin?view=audit` | `dpr1-admin-operation-audit-browser-2026-09-06.png` | 8.4880% | 2.966338 | 18.254483 | 241 | `DIFF_REVIEW` |

- [x] 修正验收截图 URL 中 `visual-qa=1` 导致的导航高亮误判；该参数现在不会改变实际业务 query 的导航匹配。
- [x] 两项 Figma fixture 的导航选中态已恢复为参考图中的背景、文字和图标颜色；真实路由和真实模式服务边界未改变。
- [x] 两张浏览器 PNG 均由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集，diff JSON 和映射已同步。
- [ ] 两项仍存在头像素材、字体光栅化和局部像素差异，继续保持 `DIFF_REVIEW`；本大点不能宣称像素级 `PASS`。
- [ ] 本大点只复核两个 Admin 画板，不代表其它画板重新采集或完成 105 项全量人工视觉复核；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本大点未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 228. 2026-09-06 Auth 认证页面证据收口

本大点只处理 Login、Register、Forgot Password、Reset Password、登录五个异常/提交状态和 Token 三个状态，共 13 个 Auth 画板，不重新验收全部 105 个画板。Figma 文件保持只读；未根据无法区分字体光栅化与布局错误的像素差异修改 CSS。

| 画板 | 前端入口 | 浏览器 PNG | 差异比例 | 结论 |
|---|---|---|---:|---|
| Login | `/login?state=v2` | `dpr1-login-v2-browser-2026-09-06.png` | 3.7948% | `DIFF_REVIEW` |
| Register | `/register` | `dpr1-register-page-browser-2026-09-06.png` | 4.2780% | `DIFF_REVIEW` |
| Forgot Password | `/forgot-password` | `dpr1-forgot-password-page-browser-2026-09-06.png` | 3.0128% | `DIFF_REVIEW` |
| Reset Password | `/reset-password` | `dpr1-reset-password-page-browser-2026-09-06.png` | 3.3946% | `DIFF_REVIEW` |
| Login 状态 6 项 | `/login?state=...` | `dpr1-login-*-browser-2026-09-06.png` | 6.0791% - 8.4825% | `DIFF_REVIEW` |
| Token 状态 3 项 | `/token-status?state=...` | `dpr1-token-*-browser-2026-09-06.png` | 1.8195% - 1.9593% | `DIFF_REVIEW` |

- [x] 13 项浏览器证据已使用 Chrome `152.0.7977.77`、`1440×900`、DPR `1`、字体 `loaded` 和无横向溢出条件重新采集。
- [x] 13 项映射、diff JSON 和运行时截图日期已同步到 `2026-09-06`；认证服务、真实模式请求和状态语义未改变。
- [ ] 13 项仍存在字体、图标和局部像素差异，不能标记为像素级 `PASS`；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] 本大点只复核 13 个 Auth 画板，不代表其它画板重新采集或完成 105 项全量人工视觉复核。
- [x] 本大点未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 资源登记继续保持 `BLOCKED`。

## 229. 2026-09-06 Diet Records 状态侧栏视觉收口

本大点只处理 Diet Records 的 Loading、Empty、Error 三个状态画板，不重新采集或验收全部 105 个画板。Figma 文件保持只读；Default 状态和真实模式侧栏不受本次 Fixture 配置影响。

| 画板 | Figma 节点 | 前端入口 | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---:|---:|---:|---:|---|
| Diet Records Loading | `692:1427` | `/analysis?view=records&state=loading` | `dpr1-diet-records-loading-browser-2026-09-06.png` | `1440×1024 / 1` | 17.4040% | 1.889433 | 12.030390 | 243 | `DIFF_REVIEW` |
| Diet Records Empty | `692:1556` | `/analysis?view=records&state=empty` | `dpr1-diet-records-empty-browser-2026-09-06.png` | `1440×1024 / 1` | 8.2707% | 1.688111 | 13.265539 | 243 | `DIFF_REVIEW` |
| Diet Records Error | `692:1685` | `/analysis?view=records&state=error` | `dpr1-diet-records-error-browser-2026-09-06.png` | `1440×1024 / 1` | 6.9955% | 1.417403 | 12.250139 | 243 | `DIFF_REVIEW` |

- [x] 状态 Fixture 侧栏按 Figma 收口：隐藏会话搜索、保留三条会话、显示“共 15 条会话”、隐藏分页和饮食工具侧栏，并关闭状态画板不包含的窗口装饰点；Default Fixture 保留完整搜索、八条会话、分页和饮食工具侧栏。
- [x] `WorkspaceLayout` 和 `SidebarSessionList` 通过显式 Fixture 配置实现上述差异，真实模式和其它页面未改变；新增中文注释说明状态列表收缩原因。
- [x] 三张浏览器 PNG 均由 Chrome `152.0.7977.77` 在 `1440×1024`、DPR `1`、字体 `loaded` 和无横向溢出条件下重新采集；mapping、diff JSON 和 runtime recapture 证据已同步到 `2026-09-06`。
- [x] 三项差异比例较 2026-09-05 记录分别下降 `1.8605`、`1.8604` 和 `1.8604` 个百分点；仍存在头像、字体、图标光栅化和局部像素差异，继续保持 `DIFF_REVIEW`。
- [x] Diet Records、WorkspaceLayout、SidebarSessionList 定向测试共 `24/24` 个用例通过；typecheck、lint、format:check 已通过。
- [ ] 本批次只复核三个状态画板，不代表其余画板重新采集或完成 105 项全量人工视觉复核；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本批次没有创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 实体包、CSS 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 230. 2026-09-06 Intake Analysis 状态筛选层级与当前证据收口

本大点只处理 Intake Analysis 的 Loading、Empty、Error 三个状态画板，不重新验收全部 105 个画板。Figma 文件保持只读；本次依据当前 Figma 节点重新采集参考图，并将浏览器截图统一到 DPR1 条件。

| 画板 | Figma 节点 | 前端入口 | Figma PNG | 浏览器 PNG | 视口 / DPR | 差异比例 | MAE | RMSE | 最大通道差异 | 结论 |
|---|---|---|---|---|---|---:|---:|---:|---:|---|
| Intake Analysis Loading | `692:1901` | `/analysis?state=loading` | `recaptured-figma/intake-analysis-loading-live-2026-09-06.png` | `recaptured/dpr1-intake-analysis-loading-browser-2026-09-06.png` | `1440×1024 / 1` | 10.9146% | 0.939062 | 9.493559 | 204 | `DIFF_REVIEW` |
| Intake Analysis Empty | `692:2026` | `/analysis?state=empty` | `recaptured-figma/intake-analysis-empty-live-2026-09-06.png` | `recaptured/dpr1-intake-analysis-empty-browser-2026-09-06.png` | `1440×1024 / 1` | 4.5144% | 1.216185 | 11.820950 | 204 | `DIFF_REVIEW` |
| Intake Analysis Error | `692:2139` | `/analysis?state=error` | `recaptured-figma/intake-analysis-error-live-2026-09-06.png` | `recaptured/dpr1-intake-analysis-error-browser-2026-09-06.png` | `1440×1024 / 1` | 4.0445% | 0.927725 | 9.860458 | 204 | `DIFF_REVIEW` |

- [x] 状态筛选组维持 Figma 约 `60%` 不透明度，导出操作单独调整为 `50%` 不透明度，避免将两个层级错误合并为同一视觉值。
- [x] 三项映射、Figma 参考图、浏览器截图、diff JSON 和运行时检查已同步到 `2026-09-06`；浏览器截图使用 Chrome `152.0.7977.77`，字体已加载，页面无横向溢出。
- [x] 三项几何检查和文字溢出检查均通过；`scripts/png-diff.mjs` 已生成差异比例、MAE、RMSE 和最大通道差异。
- [ ] 三项仍存在头像、图标处理、字体光栅化和局部像素差异，继续保持 `DIFF_REVIEW`，不能标记为像素级 `PASS`。
- [ ] 本大点只复核三个 Intake Analysis 状态画板，不代表其它 102 个画板重新采集或完成全量人工视觉复核；全量聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本大点未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 实体包、CSS 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 231. 2026-09-06 Agent SSE 终态与页面卸载订阅收口

本大点只补充 Agent SSE 客户端连接生命周期的回归证据，不修改后端 SSE 协议，不重新验收 105 个画板。Figma 文件保持只读；六个 Agent query fixture 的视觉实现和既有真实服务边界不变。

- [x] `agentRunService.test.ts` 覆盖 SSE 消息 ID 优先作为恢复游标、相同 ID 事件去重，以及 `run.failed`、`run.cancelled`、`run.superseded` 三种终态关闭连接并禁止后续重连。
- [x] `ChatPage.real.test.tsx` 覆盖真实会话页面卸载时关闭当前 SSE 订阅，避免组件卸载后继续接收事件或重新建立连接。
- [x] 本大点定向验证共 `9/9` 个用例通过；`npm run typecheck`、`npm run lint` 和 `npm run format:check` 通过。
- [x] 未修改 Java/Python 后端、SSE 事件字段或业务状态；前端继续使用 `Last-Event-ID`/`lastEventId` 续接兼容路径和 `sse_event_id` 去重。
- [ ] 本大点不代表真实后端故障注入、断网恢复矩阵或 105 个画板全量验收已完成；全量 Figma 聚合继续为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [x] 本大点未创建虚构 iconfont 字体、glyph 或 Unicode 映射；iconfont 资源登记继续保持 `BLOCKED`。

## 232. 2026-09-06 Figma 验收证据路径基线修复

本大点只修复验收映射中的文件路径，不重新采集 Figma 或浏览器截图，不重新判定全部 105 个画板，也不修改前端页面代码。

- [x] 修正 `intake-analysis-loading`、`intake-analysis-empty` 和 `intake-analysis-error` 三项 `figmaPng` 路径，使其与仓库根目录下的 `foodmate-ui/.qa/figma-pixel-acceptance/` 实体文件一致。
- [x] 三项 Figma PNG 和三项 DPR1 浏览器 PNG 均通过 PNG 文件头和尺寸读取；本次只修复映射路径，没有覆盖或删除已有证据。
- [x] `npm run qa:figma:validate` 结果为 `total=105`、`structuralPass=true`、`strictDprPass=true`、`errors=[]`；`mappedPass=0` 和 `diffReview=105` 保持不变。
- [ ] 本次只恢复结构校验门禁，不代表任何画板达到像素级 `PASS`；105 项聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 233. 2026-09-06 Meal Planning 三态视觉收口

本大点只处理 Meal Planning 的 Loading、Empty、Error 三个状态画板，不重新验收其它 102 个画板。Figma 文件保持只读；真实餐食规划接口和生产模式服务边界未改变。

- [x] Error fixture 按 Figma 画板隐藏会话搜索、历史列表和分页，仅保留 Agent 对话入口及业务导航；Loading、Empty 的既有会话壳层不受影响。
- [x] Error 状态错误图标背景/描边和重新加载按钮颜色已按 Figma 收口；Empty 状态继续保留已对齐的绿色图标背景和主按钮样式。
- [x] 三项均重新采集 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded`、无横向溢出的浏览器 PNG；映射、diff 和 runtime JSON 仅更新这三项。
- [x] 最新 PNG diff：Loading `7.1500% / MAE 0.910384 / RMSE 9.423521 / maxChannelDelta 204`；Empty `5.1309% / MAE 1.018983 / RMSE 10.102858 / maxChannelDelta 204`；Error `8.5620% / MAE 1.678509 / RMSE 13.648686 / maxChannelDelta 229`。
- [x] PlanningPage 定向测试、PNG diff、JSON 解析、Figma 证据结构校验和 `git diff --check` 在本大点完成后统一执行；视觉证据均可复现。
- [ ] 三项仍存在头像、图标轮廓、字体光栅化和局部组合差异，继续保持 `DIFF_REVIEW`，不能标记为像素级 `PASS`；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、CSS/Unicode 映射、来源和许可证仍缺失，资源登记继续保持 `BLOCKED`。

## 234. 2026-09-06 Profile Security / Privacy 状态组人工复核与证据收口

本大点处理 Profile Security / Privacy 的 16 个实时 Figma 状态画板，不重新采集或验收其它画板。Figma 文件保持只读；真实模式的密码修改、会话退出、数据导出和账号注销请求边界未改变。

| 状态组 | 画板数量 | 实时节点范围 | 浏览器视口 / DPR | 证据结论 |
|---|---:|---|---|---|
| Profile Basic 头像与离开确认 | 3 | `792:212`、`794:212`、`794:380` | `1440×1024 / 1` | `DIFF_REVIEW` |
| Profile Security 密码与设备 | 5 | `794:548`、`794:693`、`794:838`、`806:1445`、`1013:235` | `1440×1024 / 1` | `DIFF_REVIEW` |
| Profile Privacy 导出与注销 | 8 | `794:984`、`794:1127`、`795:212`、`795:356`、`795:499`、`795:642`、`806:1585`、`1013:465` | `1440×1024 / 1` | `DIFF_REVIEW` |

- [x] Security fixture 已恢复实时 Figma 中的密码卡、活跃工作区会话卡、`2 ACTIVE DEVICES`、设备状态提示和“最近安全活动”卡；卡片顶线和 `#fcfcfc` 背景限定在 Figma fixture。
- [x] Privacy fixture 保留导出表、导出状态弹窗、危险区域和“注销影响与二次确认”说明；Figma fixture 的导出/下载/重新创建按钮按实时画板隐藏额外 Lucide 图标，真实模式图标和请求行为不变。
- [x] 16 个实时 Figma PNG 已保存到 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured-figma/`，浏览器证据均为 Chrome `152.0.7977.77`、`1440×1024`、DPR `1`、字体 `loaded`、无横向溢出。
- [x] 16 项已使用 `scripts/png-diff.mjs` 生成同尺寸差异结果，并同步 `figma-105-mapping.json` 与 `figma-105-diff-results.json`；自动 diff、几何检查、文字检查和人工视觉复核均有记录。
- [x] 人工复核确认主要结构和状态语义与实时 Figma 一致，但弹层位置/尺寸、字体光栅化、头像素材、遮罩和局部颜色仍有可见差异；16 项全部保持 `DIFF_REVIEW`，未提前标记 `PASS`。
- [ ] 本大点只复核上述 16 个 Profile 状态，不代表其它画板重新采集或完成 105 项全量人工视觉复核；全量聚合仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`。
- [ ] iconfont 实体包、完整 CSS/Unicode 映射、来源和许可证仍未提供，资源登记继续保持 `BLOCKED`。
