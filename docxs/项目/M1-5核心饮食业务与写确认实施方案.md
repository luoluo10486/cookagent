# M1-5 核心饮食业务与写确认实施方案

更新时间：2026-09-06
状态：M1-5 核心范围已实现并完成本地验收，营养目录已完成 V32/V33 真实数据重建，后续扩展仍在开发中
适用阶段：本地优先的 M1-5

本轮全量 Java 验证：历史 `mvnw.cmd verify` 已成功完成，当前根 POM 实际包含 5 个 Reactor Java 模块，Spotless 全部通过。启用本地依赖开关后，HTTP 与 RocketMQ writer 回归各 11/11 通过；当前 1,518 条 USDA FoodData Central foodPortions 换算规则的 matched/pending 路径均有数据库断言。V32/V33 seed/validation 已在本地 PostgreSQL 通过；2026-09-05 已取得真实 Embedding、`food_log_writer` 和餐食计划云业务验收证据，性能/长稳与生产门禁仍后置。

## 1. 文档目的

本文把 M1-5 的已确认决策固化为实现前的单一实施口径。它约束饮食记录、营养目录、营养分析、餐食计划、购物清单和写操作确认的范围、数据模型、接口语义、事务边界与验收门槛。

本文同时作为 M1-5 的实施口径和当前状态记录。饮食记录和餐食计划 API 已完成 Java application、infra、api、迁移脚本和本地 HTTP E2E；`food_log_writer` 已完成 application/Tool Gateway/Runtime Proposal 的完整 Java 写入扩展，并通过真实 PostgreSQL HTTP/RocketMQ 跨进程回归。HTTP 与 RocketMQ 各 11 个用例覆盖拒绝、失败回滚与失败审计、superseded、update/delete/restore、revision 冲突、幂等重放，以及官方 foodPortion 换算 matched/pending。Java 仍不在启动时自动建表、迁移或回滚。V13/V14/V15 的数据库结构已在当前本地 `FoodMate` 库中经只读校验确认，其中 V15 已人工执行并通过校验；营养 seed V1 和 portion seed V2 已人工导入并通过校验；原始执行人和历史执行时间若未记录不得补写。

发生冲突时，按 [文档索引](../文档索引.md) 的优先级判断当前事实；本文顶部状态和“当前实现状态”小节描述已验证事实，后续章节描述目标和剩余实现。历史归档中的页面或 mock 完成记录只代表视觉/交互交付，不代表真实业务闭环。

## 当前实现状态（2026-08-15）

| 能力 | 状态 | 事实边界 |
|---|---|---|
| 饮食记录 | 已实现第一切片 | 创建、查询、编辑、删除、恢复、幂等和 `revision` 已通过 application/API 定向测试及本地 PostgreSQL HTTP 回归；当前 1,000 条 USDA seed 已验证 matched 计算。 |
| 营养分析 | 已实现第一切片 | today/7d/30d、覆盖率、未匹配提示和免责声明已验证；当前库已有 1,000 条 approved/official 食材、1,518 条 approved 食材级换算规则和历史精确质量换算，无法安全换算的单位继续保留 `pending`。 |
| 餐食计划 | 已实现第一切片 | 创建、查询、修改、校验、保存、软删除、恢复和购物清单已通过定向测试及本地 HTTP 回归；`meal_plan.save_plan` 已接入确认执行，更广泛 Agent 写工具仍待后续阶段。 |
| 写操作确认 | 本地跨进程回归已完成 | `meal_plan.save_plan` 和 `food_log_writer` 的 create/update/delete/restore 支持 Proposal -> Confirm -> Execute；新增 reject、failed、superseded 状态，校验工具类型、工具名、confirmation_ref、AgentRun/用户归属、参数摘要、revision 和幂等键，失败时业务事务回滚并在独立事务写入 failed 审计。HTTP 与 RocketMQ 各 11 个真实跨进程用例均通过，包含数据库状态、审计、revision、软删除、营养快照、换算 pending 和 Inbox 重放断言。 |
| 营养目录 | 已完成当前本地重建 | `nutrition_foods`/`nutrition_unit_conversions` 表和查询适配器已存在；当前 1,000 条 USDA `SR Legacy` 食材和 1,518 条 USDA `foodPortions` 食材级换算均为 `approved` 并通过 V32/V33 校验，历史精确质量换算继续保留；未覆盖或无证据的单位仍不换算。 |
| B4-2 持久化映射 | 核心认证路径已接入 | 用户、资料、会话和消息已通过本地 PostgreSQL 真实读写回归；刷新令牌已接入轮换/撤销业务路径并通过 Java/API 业务测试，头像 PO/Mapper 已补齐但写路径仍待独立验收。 |
| M1-6 本地门禁 | 子项已验证，整体未完成 | Actuator/metrics 配置回归、双 JVM 有界读取、Java 重启回读、Python readiness、Redis AOF 探针恢复和 RocketMQ 重启恢复已验证；PostgreSQL 进程重启、完整故障注入和生产门禁仍未完成。 |

## 2. 范围和明确不做项

### 2.1 本轮范围

1. 手工饮食记录真实闭环：Java API -> PostgreSQL -> 营养目录匹配与确定性计算 -> 查询和日报汇总；已覆盖当前 1,000 条食材 seed、1,518 条官方 portion 换算规则和历史精确质量换算的 matched 分支，以及无规则/未知食材的 `pending` 分支。
2. 饮食记录的创建、编辑、软删除、恢复、查询，以及幂等、并发版本和操作审计。
3. 营养目录、别名、分类、单位换算、来源版本和营养快照。
4. 当天、最近 7 天、最近 30 天的营养分析，包含目标对比、数据覆盖率和不完整提示。
5. 单用户、1-7 天、早餐/午餐/晚餐的餐食计划；支持人数、预算、热量目标、蛋白质目标、忌口和过敏原。
6. 由已保存计划生成购物清单。
7. Agent 提案和自然语言写操作的确认事实、过期、拒绝、执行、失败、superseded、幂等和审计；`meal_plan.save_plan` 以及 `food_log_writer` 的 create/update/delete/restore 均经同一 Java 确认入口执行，并已通过 HTTP 与 RocketMQ 跨进程回归。
8. 本地双 Java JVM 压测、吞吐基线和 Java/Python/PostgreSQL/Redis/RocketMQ 重启恢复验证。
9. Java Actuator liveness/readiness 和本地基础 metrics；日志至少关联 `request_id`、`trace_id`、`run_id`。

### 2.2 当前明确不做

- 不建立 Kubernetes、staging/production 两套环境、云资源或发布平台。
- 不把本地验证结果当作生产容量、生产可用性或云模型长期稳定性结论。
- 不在本轮做真实云模型长期稳定性、供应商价格/账单审计；这些在本地链路稳定后单独测试。
- 不在本轮做 Prometheus、Grafana、OpenTelemetry、ELK 等完整生产监控栈。
- 不在本轮做 `pg_dump`、独立恢复库、灾备切换、数据损坏恢复或备份保留策略；数据库备份暂不纳入。
- 不允许 Python、模型或检索内容直接写业务表、直接确认或直接执行 Tool。
- 不使用模型临时估算营养数值，也不凭空生成 USDA 数值或许可证结论。
- 暂不规范化 `meal_plans`、`shopping_lists` 的 JSON 细节；对应功能实现前只保持当前基线结构，并以本文状态约束为准。
- 不支持多人复杂营养目标、周期循环、实时价格库存、医疗诊断或治疗建议。

## 3. 实现顺序

```text
文档统一
  -> 手工记录 schema/API/事务
  -> 营养目录和确定性计算
  -> 查询与日报分析
  -> 餐食计划校验/保存
  -> 购物清单
  -> Agent 提案和确认接入同一写入服务
  -> 本地压测与故障恢复
```

手工录入和 Agent 写入必须共用同一套 Java application 用例、领域校验、Repository、事务、幂等和审计逻辑，不创建第二套 Agent 专用写入逻辑。

## 4. 饮食记录数据模型

当前没有历史数据，因此直接修改基线表，不保留旧 JSON 兼容字段。

### 4.1 `food_logs` 餐次主表

| 字段 | 类型 | 说明 |
|---|---|---|
| `food_log_id` | `BIGINT` | 主键 |
| `user_id` | `BIGINT` | 所属用户 |
| `session_id` | `BIGINT` nullable | 来源会话 |
| `agent_run_id` | `BIGINT` nullable | 来源 AgentRun |
| `meal_time` | `TIMESTAMPTZ` | 用餐时间 |
| `meal_type` | `VARCHAR(32)` | breakfast/lunch/dinner/snack |
| `notes` | `TEXT` nullable | 备注 |
| `source` | `VARCHAR(32)` | manual/agent/import |
| `idempotency_key` | `VARCHAR(128)` nullable | 写入幂等键 |
| `revision` | `BIGINT` | 乐观并发版本，初始为 0 或 1，项目实现统一一种起始值 |
| `created_at` / `updated_at` | `TIMESTAMPTZ` | 通用时间字段 |
| `created_by` / `updated_by` | `BIGINT` | 操作者 |
| `is_deleted` | `BOOLEAN` | 软删除标志 |
| `deleted_at` / `deleted_by` | `TIMESTAMPTZ` / `BIGINT` | 删除信息 |

删除 `items_json`、`nutrition_json`。食材逐项保存到明细表，营养汇总由明细快照确定性计算并在查询/报告层聚合，不再由调用方传入可信汇总。

### 4.2 `food_log_items` 食材明细表

| 字段 | 类型 | 说明 |
|---|---|---|
| `food_log_item_id` | `BIGINT` | 主键 |
| `food_log_id` | `BIGINT` | 所属餐次 |
| `item_order` | `INT` | 明细顺序 |
| `raw_name` | `VARCHAR(255)` | 用户原始名称 |
| `nutrition_food_id` | `BIGINT` nullable | 匹配的营养目录项 |
| `amount` | `NUMERIC(12,3)` | 用户原始份量 |
| `unit` | `VARCHAR(32)` | 用户原始单位 |
| `normalized_amount` | `NUMERIC(12,3)` nullable | 可换算后的标准份量 |
| `normalized_unit` | `VARCHAR(32)` nullable | 当前首批为 `g/ml` |
| `conversion_id` | `BIGINT` nullable | 使用的换算规则 |
| `calories_kcal` | `NUMERIC(12,4)` nullable | 营养快照 |
| `protein_g` / `fat_g` / `carbs_g` | `NUMERIC(12,4)` nullable | 营养快照 |
| `nutrition_status` | `VARCHAR(32)` | matched/pending/invalid |
| `nutrition_source` | `VARCHAR(64)` nullable | USDA/人工复核等来源标识 |
| `nutrition_version` | `VARCHAR(64)` nullable | 数据版本 |
| 通用审计/软删除字段 | 与主表一致 | 用于追溯和恢复 |

`pending` 明细保留原始名称和份量，但其营养字段为空，不计入热量、蛋白质、脂肪、碳水汇总。后续补齐目录匹配后重新计算快照。

## 5. 营养目录和单位换算

### 5.1 数据落点

营养目录使用 PostgreSQL 表，不写死在 Java 代码中，也不由模型直接返回热量、蛋白质、脂肪和碳水数值。建议表为：

- `nutrition_foods`：食材标准名称、中文名称、别名、分类、来源、版本、每 100g/100ml 的营养基准值、审核状态。
- `nutrition_unit_conversions`：食材级单位换算，例如某食材“1 个”对应多少克，包含规则来源和版本。

基础来源采用 USDA FoodData Central Foundation Foods / SR Legacy。FoodMate 负责中文名称、别名、分类、单位换算、来源版本和人工复核。当前 seed 由 V1 营养基准和 V2 `foodPortions` 规则组成；每条规则保留 FDC ID、portion 序号和来源版本，不能从名称或模型推断数值。

### 5.2 首批目录

当前目录已引入 1,000 个可追溯 USDA 食材和 1,518 条食材级 portion 规则，历史精确质量换算继续保留。系统优先支持 `g/ml`；`cup`、`large`、`medium`、`oz` 等单位只有存在明确的食材级换算规则时才转换；中文单位只做明确的词法别名映射，不改变数值证据。

种子数据采用版本化人工 SQL，例如 `script/sql/FoodMate/seed/V1__nutrition_usda_seed.sql`。Java 不在启动时自动执行种子 SQL，SQL 必须人工执行、校验并记录版本。

### 5.3 计算规则

```text
每项营养 = 标准化份量 / 100 * 目录中每 100g（或 100ml）的营养值
餐次汇总 = 所有 nutrition_status = matched 的明细快照求和
```

无法匹配名称或无法安全换算单位时，保存原始记录，标记 `pending`，不使用模型估算补值。报告必须显示覆盖率，例如 `3/4 项已估算`，并显示“数据不完整”。

## 6. 数值精度

- PostgreSQL 营养和份量字段使用 `NUMERIC`，避免浮点二进制误差。
- Java 使用 `BigDecimal`，禁止使用 `double` 参与营养计算。
- 份量使用 `NUMERIC(12,3)`；每 100g/100ml 基准值和营养快照使用 `NUMERIC(12,4)`。
- 计算过程保留 4 位小数；API 和报告展示时四舍五入到 2 位。
- `NUMERIC` 是 PostgreSQL 的精确十进制定点数类型；括号内的两个数字分别表示最大精度和小数位数，例如 `NUMERIC(12,4)` 最多 12 位数字，其中 4 位在小数点后。

## 7. 写操作确认

### 7.1 确认范围

Agent/自然语言提案产生的新增、修改、删除、恢复和保存计划都必须经过确认。用户在页面中主动填写并点击保存，视为明确意图，可以直接提交；删除和恢复仍需二次确认。

Python、模型输出和检索内容不能自行确认。确认事实绑定用户、资源、操作类型、参数摘要、`request_id`/`trace_id` 和过期时间；参数变化后旧确认立即失效。

### 7.2 `approval_requests`

新增独立确认事实表 `approval_requests`，建议字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `approval_request_id` | `BIGINT` | 主键 |
| `user_id` | `BIGINT` | 确认用户 |
| `session_id` / `agent_run_id` | `BIGINT` nullable | 来源上下文 |
| `resource_type` / `resource_id` | `VARCHAR` / `BIGINT` nullable | 资源绑定 |
| `operation` | `VARCHAR(64)` | create/update/delete/restore/save_plan |
| `parameters_digest` | `VARCHAR(128)` | 参数摘要 |
| `status` | `VARCHAR(32)` | pending/confirmed/rejected/expired/executed/failed/superseded |
| `expires_at` | `TIMESTAMPTZ` | 过期时间 |
| `confirmed_at` / `executed_at` | `TIMESTAMPTZ` nullable | 状态时间 |
| `idempotency_key` | `VARCHAR(128)` | 确认幂等键 |
| 审计字段 |  | request/trace/操作者等 |

确认、业务写入、工具结果和 `operation_audits` 必须在同一事务内完成。相同确认/幂等键不能重复执行；已执行确认重放返回第一次结果。

### 7.3 幂等和并发

- 所有创建、编辑、删除、恢复写接口统一要求 `Idempotency-Key`。
- 编辑、删除、恢复必须携带 `revision`；不一致返回冲突，不覆盖他人修改。
- 相同幂等键和相同参数重复请求返回第一次结果；相同幂等键但参数不同返回幂等冲突。
- 网络重试必须复用原幂等键。

### 7.4 状态机与 Tool Gateway 结果

确认状态的合法方向如下：

```text
pending -> confirmed -> executed
pending -> rejected
pending/confirmed -> expired
pending/confirmed -> superseded
confirmed -> failed  (业务事务失败后由独立失败事务记录)
```

`executed`、`rejected`、`failed`、`superseded` 和 `expired` 都是终态；重放只能返回原状态，不得重新执行写副作用。创建同一用户、同一资源、同一操作的新 Proposal 时，旧的 `pending`/`confirmed` 确认标记为 `superseded`。`food_log_writer` 仅接受 `proposal_type=tool` 且 `tool_name=food_log_writer`，并把状态映射为：

| Java 确认事实 | Tool Gateway Result | 错误码 |
|---|---|---|
| 未确认 | `confirmation_required` | `TOOL_CONFIRMATION_REQUIRED` |
| 过期 | `confirmation_required` | `TOOL_CONFIRMATION_EXPIRED` |
| 拒绝 | `rejected` | `TOOL_CONFIRMATION_REJECTED` |
| superseded | `superseded` | `TOOL_CONFIRMATION_SUPERSEDED` |
| 执行失败或幂等冲突 | `failed` | `TOOL_EXECUTION_FAILED` 或 `TOOL_IDEMPOTENCY_CONFLICT` |

上述 Gateway 状态是 M1-5 Java Tool Gateway 的结果语义；双运行时 V1 持久化映射仍须保留完整 Result JSON，不能从 V1 的粗粒度状态反推原始确认状态。

## 8. 营养分析

第一版支持当天、最近 7 天、最近 30 天；指标为热量、蛋白质、脂肪和碳水。目标来自用户 Profile 的 `calorie_target`、`protein_target`；目标未设置时只展示实际摄入，不判断是否达标。

分析结果必须包含：统计范围、已纳入的明细数/总明细数、各营养值、目标对比（若有目标）、未匹配项提示和非医疗建议免责声明。报告可继续暂存于现有 `analysis_reports.report_json`，待分析功能真正实现时再决定是否规范化。

## 9. 餐食计划和购物清单

第一版限制为单用户、1-7 天、每天早餐/午餐/晚餐。输入支持人数、预算、热量目标、蛋白质目标、忌口和过敏原。Java 确定性生成器负责生成和校验；Python 只做自然语言理解和参数提取，生成后必须经过 Java 校验。

状态机：

```text
draft -> validated -> saved
```

计划修改后回到 `draft`，必须重新校验和确认；只有 `saved` 计划才能生成正式购物清单。计划修改会软删除旧购物清单，避免旧内容继续被读取。`meal_plans` 和 `shopping_lists` 当前继续保留 JSON 结构，字段语义和状态以本节为准。`meal_plans.revision` 从 1 开始，修改、校验、保存、删除和恢复均使用乐观并发校验；写接口使用 `Idempotency-Key`，重复请求返回第一次成功结果。

本地真实 API（`/api/meal-plans`）当前覆盖：

- `POST /api/meal-plans`：创建计划。
- `GET /api/meal-plans/{id}`：查询未删除计划，并返回 `revision`/`deleted`。
- `PATCH /api/meal-plans/{id}?revision=...`：整条计划内容替换，成功后回到 `draft`。
- `POST /api/meal-plans/{id}/validate?revision=...`：校验并递增版本。
- `POST /api/meal-plans/{id}/save?revision=...`：保存并递增版本。
- `DELETE /api/meal-plans/{id}?revision=...`：软删除并递增版本。
- `POST /api/meal-plans/{id}/restore?revision=...`：恢复并递增版本。
- `POST`/`GET /api/meal-plans/{id}/shopping-list`：生成或读取当前有效购物清单。

## 10. 本地可观测性、压测和故障恢复

### 10.1 本地可观测性

- Java 保留 Actuator `/actuator/health/liveness` 和 `/actuator/health/readiness`。
- 增加本地可访问的基础 metrics，用于请求数、错误数、延迟和核心异步积压的最小观察；暂不承诺 Prometheus/Grafana 格式和生产告警。
- Java/Python 日志至少包含 `request_id`、`trace_id`、`run_id`，不得记录密钥、完整 Prompt 或敏感营养/账户信息。

### 10.2 本地压测

已执行的第一轮脚本使用两个 Java JVM，共享 PostgreSQL，测量的是认证会话读取，不包含饮食记录、营养查询、计划确认、Agent 业务流量或 SSE；历史 160 请求和本轮依赖重启后的 20 请求均为 0% 错误率。本结果只作为本机读取基线，不外推生产容量。M1-5 计划生命周期目前已做单 JVM 本地 HTTP 回归，但这不是生产吞吐测试；完整业务链路吞吐、数据库连接池、队列积压和重复执行数仍待单独设计并执行。

### 10.3 本地故障恢复

目标矩阵覆盖 Java、Python、PostgreSQL、Redis、RocketMQ 逐个重启，以及 Outbox/Inbox 重试、ACK 丢失、租约接管、重复消息和 SSE 恢复。当前已实际验证 Java 重启回读、Python Runtime 重启后的 readiness、Redis AOF 探针恢复、RocketMQ 组件重启/Topic 初始化，以及 MQ Outbox/Proposal/Result、DLQ、continuation 和 Java checkpoint 对账回归；尚未完成 PostgreSQL 进程重启和完整 ACK/租约/SSE 业务故障注入。允许当前请求失败，但必须满足：

- 不丢失已提交业务数据；
- 不重复执行写副作用；
- 不伪造成功；
- 恢复后可重新查询饮食记录、营养分析、餐食计划、确认事实和 SSE 事件。

数据库备份恢复不属于本轮验收。

## 11. 文档和实现验收门槛

进入代码改造前，本文及相关索引、路线图、TODO、数据库、契约、架构、测试、实现、产品和设计状态文档必须统一。进入 SQL/Java 实现后，至少满足：

1. SQL 基线、迁移、seed 和校验脚本版本化，人工执行，不由应用启动自动执行；V13/V14/V15 当前库结构已复核，营养 seed V1 和 portion seed V2 已人工导入并通过校验。
2. Java application 不依赖表结构细节；infra 承担 Repository/Mapper，api 只做参数转换和响应包装。
3. 手工录入与 Agent 提案复用同一业务写入用例。
4. 测试覆盖匹配、官方单位换算、单位不可换算 pending、精度、幂等、revision 冲突、确认过期/参数变更、软删除/恢复和事务回滚。
5. 本地真实依赖 E2E、双 JVM 压测和进程重启恢复完成后，才进入真实云模型稳定性和成本审计工作。
6. 未实际运行和验证的能力不得标记为完成；页面视觉验收不得替代后端业务验收。

## 12. 下一步

1. 扩展营养目录前继续按官方来源、版本和许可证逐条评审；家庭单位换算必须有食材级证据，不能从名称或模型推断。
2. 继续扩展有官方证据的食材和 portion 规则；当前 V33 已完成 1,000 条食材、1,518 条 approved 食材级换算和无规则 pending 保护，后续新增仍需独立来源评审。
3. 继续执行 M1-6 本地压测、队列积压/重复执行统计和 PostgreSQL/Outbox/Inbox/SSE 故障恢复矩阵；数据库备份恢复、生产部署和发布回滚仍是后置范围。
