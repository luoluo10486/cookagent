# 非生产业务能力完善执行计划

## 1. 目标与边界

本计划用于完成 FoodMate 在本地开发环境和秋招演示场景下的业务能力完善。目标是让真实业务数据、Agent 编排、知识库、记忆、营养检索、SQL Agent、管理端和前端体验形成一致、可复现、可解释的闭环。

本计划不把以下内容作为完成门槛：

- 生产级吞吐、P95/P99、容量规划和长时间压测。
- Java、Python、PostgreSQL、Redis、RocketMQ 的完整故障注入矩阵。
- Kubernetes、staging/production、CI/CD、灰度发布和发布回滚。
- 数据库备份恢复、跨节点容灾和生产级监控平台。
- 多实例扩容、队列积压 SLO、账单对账和供应商长期稳定性。

测试只服务于业务正确性。每完成一个大功能点，集中执行受影响模块的定向业务测试，再提交 Git；不为小改动重复运行全量测试。

## 2. 当前基线

已具备的能力：

- 用户、会话、消息、AgentRun、SSE、Java/Python 双运行时和 RocketMQ 主链路。
- 饮食记录、营养分析、餐食计划、购物清单和写确认。
- `calculator`、`time_parser`、`knowledge_search`、`database_query`、`food_log_writer`、`plan_validator`、`meal_plan.save_plan` 七个正式工具。
- 公共知识库批量上传、PDF/DOCX/Markdown/TXT 解析、Redis Stub/Milvus 索引、显式发布和引用。
- PostgreSQL 中 1,000 条 USDA 食材和 1,518 条单位换算；独立营养 Milvus 集合保留 1,000 条向量。
- SQL Agent 只读查询、权限过滤、AST Guard、SQL 审计和真实 Chat 单次业务闭环。
- 短期消息上下文、Session 摘要、长期记忆候选和用户记忆管理接口。

已知缺口：长期记忆候选仍采用可解释的本地规则，不做模型推断；营养目录缺少更完整的中文业务映射；部分管理页面仍需真实接口核对。K1 已重新导入 9 份正式公共资料并完成 local-stub 业务闭环；K2 已更新知识切分和关键词检索代码，但现有正式批次未为本轮重复重索引。

## 3. 实施顺序

实施顺序固定为：

```text
真实业务数据
  -> 知识库内容与检索质量
  -> 记忆与上下文质量
  -> 营养语义候选与饮食业务
  -> SQL Agent 业务覆盖
  -> 管理端和前端真实接口收口
  -> 全链路业务回归与文档收尾
```

依赖关系：知识库内容先于 RAG 质量；记忆结构先于记忆检索优化；营养候选先于饮食记录中的模糊食材匹配；后端契约核对先于前端收口。

## 4. K1 真实公共知识库内容建设

### 4.1 内容范围

建立首批可演示的公共知识目录，至少包含：

- 平衡膳食和宏量营养素基础。
- 常见食材的营养特点、适用烹饪方式和保存方法。
- 早餐、午餐、晚餐和加餐的搭配原则。
- 控盐、控糖、增加蛋白质、增加膳食纤维等通用饮食主题。
- 食品安全、熟食保存和备餐卫生基础。

每份文档必须有标题、来源名称、来源版本、授权说明、适用范围和更新时间。数据只进入公共知识库，不把用户饮食记录复制为知识文档。

### 4.2 导入与质量门禁

- 使用正式 PDF、DOCX、Markdown 或 TXT 文件导入。
- 每份文档先做 UTF-8、文件签名、空文档、外链、宏、脚本和基础 PII 校验。
- 文档导入后保持 `draft`，索引成功后仍需要管理员显式发布。
- 发现重复文档时按来源、版本和内容摘要去重；新版本不覆盖旧版本，发布后再切换当前版本。
- 不在本阶段重新调用真实 Embedding，除非用户明确确认；先允许 stub/local deterministic 运行验证业务链路。

### 4.3 完成标准

- 至少导入 8 至 12 份内容不同的正式知识文档。
- 每份文档均完成索引、状态查询和显式发布。
- 检索结果能返回标题、版本、章节路径、chunk ID 和安全片段。
- 下线后不可被检索，新版本发布后旧版本不再成为当前版本结果。

## 5. K2 知识切分和检索质量完善

当前 `chunk_markdown` 已实现标题识别和最多 900 字符切分。本阶段在保持现有稳定 `embedding_id` 规则的前提下完善切分策略。

### 5.1 切分策略

- Markdown：按标题层级保留 section path，再按段落切分。
- PDF/DOCX：先恢复段落和标题，再进入同一切分器。
- 优先按段落边界切分，避免在句子中间截断。
- 使用 Token 或字符双重上限，默认目标 500 至 800 字符，硬上限 1,000 字符。
- 相邻 chunk 保留少量语义重叠，避免跨 chunk 丢失上下文。
- 每个 chunk 保存文档版本、章节路径、序号、ACL 和来源元数据。
- 表格先按“表名/列名/行语义”转成文本；复杂扫描表格仍保留为后续能力，不作为本阶段门槛。

### 5.2 检索策略

- 先过滤 `tenant_id=0`、`public_published`、`published`、`indexed`、当前版本和未删除。
- 召回最多 12 个候选，重排最多 6 个，最终最多 4 条引用。
- 每个文档最多贡献 2 条引用。
- 关键词检索用于标题、别名和精确术语；向量检索用于语义扩展。
- 无证据或证据不足时明确返回无命中，不生成伪引用。
- 引用只保留安全片段，不返回对象存储地址、对象键和完整原文。

### 5.3 完成标准

- 增加切分单元测试和 5 至 10 个固定检索样例。
- 检查标题命中、同义词命中、跨段落语义命中、无命中和下线过滤。
- 检查重复索引不重复产生 chunk/vector。
- 检查 stub 与 local deterministic 返回相同的 ACL 和引用约束。

### 5.4 K2 当前交付（2026-09-06）

- `chunk_markdown` 默认目标长度为 `700` 字符，硬上限为 `1000` 字符，默认重叠为 `80` 字符；同一章节的段落优先合并，长段落优先在句末、标点或空白处拆分。
- Markdown 标题按层级维护为 `section_path`，关键词检索同时计算标题、章节和正文命中；stub、Redis stub 均保留最多 12 个候选、最多 6 个重排项和最多 4 条最终引用的边界，Milvus 继续保留同一 ACL 过滤。
- 业务测试覆盖稳定 `embedding_id`、章节层级、重叠和硬上限、标题/章节命中、中文单字主题、5 个固定公共主题和无命中；`test_knowledge_rag.py` 与 `test_knowledge_worker.py` 合计 `70 passed`、`4` 个子断言通过。
- 本轮没有重建 D144 正式批次，数据库中既有 `58` 个 chunk 仍是 K1 索引快照；后续显式重试或新批次会使用 K2 切分策略，不调用真实 Embedding。

## 6. K3 结构化记忆与上下文完善

截图中的数据库记忆思路适用于 `user_memories`，但不能替代饮食记录、营养目录和餐食计划等领域表。

### 6.1 数据边界

- `food_logs`：饮食事实。
- `meal_plans`：餐食计划事实。
- `nutrition_foods`：营养目录事实。
- `session_summaries`：当前会话的压缩视图。
- `user_memories`：跨会话仍有价值的偏好、习惯和交互规则。

### 6.2 记忆候选

- 保留 Python 只生成候选、Java 负责校验和写入的边界。
- 增加明确表达样例：喜欢、不喜欢、忌口、烹饪能力、预算和用餐时间偏好。
- 一次性参数、完整计划、营养目标、医疗事实和工具审批不进入普通长期记忆。
- 继续执行来源消息、用户归属、置信度、冲突、TTL 和敏感信息校验。
- 相同 `memory_type + memory_key` 进入更新或冲突流程，不无限追加。

### 6.3 上下文装配

- 最近 8 条有效消息继续保留。
- 摘要固定保存 goals、constraints、decisions、open_questions 和来源消息 ID。
- 已确认、未过期、未删除的长期记忆才可进入 AgentRun。
- 按意图筛选记忆类型：planning、cooking、record、nutrition 使用不同白名单。
- Trace 只保存来源 ID、digest 和安全摘要，不复制完整记忆正文。

### 6.4 完成标准

- 通过喜欢、不喜欢、预算、烹饪能力、一次性请求、冲突和删除场景的业务测试。
- 用户删除或修改记忆后，下一次 AgentRun 不再使用旧内容。
- 过期记忆不参与冲突判断和 Context 注入。
- 更新记忆后关联摘要失效并重建。

### 6.5 K3 当前交付（2026-09-06）

- Python 使用确定性规则将喜欢、不喜欢/忌口、预算、烹饪能力、用餐时间和回答偏好转换为短结构化候选；候选只携带来源消息 ID，不保存整句请求、回答或 Prompt。
- 一次性请求、完整计划、营养目标和医疗/高影响健康事实不会生成或通过 Java 校验；`plan` 不再属于新的长期记忆白名单。
- Java 对同一 `memory_type + memory_key` 做事务内读取和 PostgreSQL advisory lock：相同 JSON 视为重复事实不再新增；不同值进入 `conflict`，用户确认后同 key 其他活动值转为 `rejected`。
- 手动修改要求 JSON 对象且重新执行长期记忆安全校验；修改、删除和冲突确认都会使用户会话摘要失效。ContextBuilder 继续执行意图类型白名单、确认状态、最多 8 条和重复 key 防护。
- K3 业务验证：Python `test_memory_context.py` 与 `test_runtime_server.py` 共 `57 passed`；Java `MemoryCandidateServiceImplTest`、`SessionSummaryServiceImplTest` 和 `AgentRunCommandServiceImplTest` 共 `16/16` 通过；未执行真实云服务、性能压测、依赖重启或故障矩阵。

## 7. K4 营养目录与饮食业务完善

### 7.1 营养数据

- 保留 1,000 条 USDA 食材和 1,518 条单位换算作为当前基线。
- 补充中文名称、别名、食物形态和常见份量映射。
- 对重复规范键、空名称、异常营养值和无效换算继续执行 validation。
- 增加复合菜/菜谱与基础食材的关系设计，先作为业务表数据，不直接混入营养食材原子目录。

### 7.2 语义候选

- 营养向量只负责候选召回。
- Java 回源 PostgreSQL 做精确匹配和营养值计算。
- 为低置信度候选增加 `pending_confirmation` 分支。
- 禁止模型直接决定营养事实、过敏事实或医疗建议。

### 7.3 完成标准

- “鸡胸肉、鸡胸、煮鸡胸肉、米饭、燕麦”等中文查询均能返回可解释候选。
- raw/cooked/不同食物形态不会被错误合并。
- 候选缺失、候选歧义和低置信度时进入明确人工确认路径。
- 饮食记录写入后营养值与 PostgreSQL 权威值一致。

### 7.4 K4 当前交付：营养候选确认（2026-09-06）

- 新增营养目录候选查询应用服务和 `GET /api/nutrition-foods/search?query=...&limit=...` 接口；查询结果只返回目录候选、营养摘要和来源信息，不返回对象存储字段。
- 中文烹饪前缀会在查询前归一化；多个同等级的生熟、部位或食物形态候选不会自动猜测，调用方必须提交明确的 `nutrition_food_id`。
- 饮食记录 create/update 支持显式目录 ID；无法安全选择目录时保存原始明细并标记 `pending_confirmation`，不写入推断营养快照；已有 `pending`、`matched` 和 `invalid` 语义保持兼容。
- V34 已在本地 `FoodMate` PostgreSQL 执行；只扩展状态约束和候选查询索引，不改写既有饮食数据。只读 validation 结果为 `pending_confirmation_rows=0`、`invalid_catalog_candidate_rows=0`，候选索引存在。
- 业务验证已通过：Application `19/19`、Infrastructure `2/2`、API `NutritionFoodControllerTest` `1/1`；候选查询“鸡胸肉”实际返回多个 USDA 生熟/部位候选，证明歧义保护生效。
- 本切片未执行真实 Embedding、性能压测、依赖重启、故障注入或生产治理；复合菜/菜谱关系仍属于后续业务数据扩展，不混入原子营养目录。

## 8. K5 SQL Agent 业务覆盖

- 增加饮食记录统计：今日热量、近 7 天蛋白质、某食材出现次数、计划完成度和购物清单缺项。
- 增加时间范围缺失、食材名称歧义、空结果和字段不支持时的澄清流程。
- 保持 Python 只生成结构化 SQL Proposal，Java 执行 Schema、用户范围、AST、LIMIT、超时和审计校验。
- 结果中展示时间范围、统计口径和数据为空的原因，不暴露原始 SQL。
- 增加 SQL Agent 业务样例集，覆盖有数据、无数据、歧义、非法字段和越权查询。

完成标准是上述主问题能够完成“理解 -> 澄清/规划 -> Java 查询 -> 审计 -> 回答 -> SSE”链路，重复 Proposal 不重复执行。

### 8.1 K5 当前交付（2026-09-06）

- SQL Planner 已补齐今日热量、近 7 天蛋白质、指定食材出现次数、餐食计划生命周期完成度和待确认购物清单统计；食材次数查询会把早餐、午餐或晚餐过滤条件带入 SQL。
- 缺少时间范围、食材名称歧义和不支持的营养字段均返回结构化澄清；Composer 对空结果说明数据为空原因，不输出原始 SQL，也不编造统计值。
- 读取餐食计划和购物清单进入 SQL Agent；生成、保存餐食计划以及饮食记录写入仍使用原有 Proposal -> Confirm -> Execute 流程。
- Java 继续负责 SQL Schema、当前用户范围、软删除、AST 只读、LIMIT、超时和 SQL 专用审计；本轮只新增允许意图和时间标签校验，不放宽业务边界。
- 业务验证命令及结果：在 `agent-runtime` 使用项目 `.venv` 执行 `python.exe -B -m pytest -q -p no:cacheprovider tests/test_sql_planner.py tests/test_runtime_server.py tests/test_tool_protocol.py`，`87 passed`；Java 执行 `mvnw.cmd -pl foodmate-application -am test "-Dtest=SqlQueryPlanValidatorTest,JSqlParserQueryGuardTest" "-Dsurefire.failIfNoSpecifiedTests=false"`，`12/12` 通过。
- 本切片未执行性能压测、组件重启、Outbox/Inbox ACK 丢失、重复投递、SSE 故障恢复、生产环境或备份恢复验证。

## 9. K6 工具与管理端真实业务收口

- 以数据库正式注册表为准复核七个工具的名称、版本、风险、权限、确认和幂等策略。
- 工具注册表页面展示真实 API 返回；没有真实接口的数据使用明确空态。
- 核对工具启用/停用、版本查看和权限失败的真实响应。
- 管理端知识库接入真实批次上传、进度、重试、发布、下线和恢复。
- 管理端 Run、Tool Call、SQL、Trace、操作审计页面核对真实字段和分页。
- 删除、恢复、失败和无权限状态均必须能在 UI 中看到稳定错误码和 request ID。

完成标准是管理员能从页面追踪：批次 -> 文档 -> 索引 -> 发布 -> AgentRun -> 引用 -> 工具/SQL/审计事实。

### 9.1 K6 当前交付：知识库管理查询真实接口（2026-09-06）

- 管理端知识库列表已从 dashboard 概览响应切换为 `GET /api/admin/queries/knowledge?page=1&size=100` 专用分页查询，列表字段与 Java 管理查询契约保持一致。
- 批次上传继续使用 `POST /api/admin/knowledge-documents/upload-batches`；批次详情、SSE、失败条目重试以及发布、下线、恢复和软删除继续使用现有真实接口。
- 真实模式保留加载中、空列表、接口失败和重试状态；mock/Figma 展示数据不参与真实接口路径。
- 业务验证：`foodmate-ui` 执行 `npm.cmd run test -- --run src/pages/AdminPage/tabs/KnowledgeTab.real.test.tsx src/pages/AdminPage/tabs/KnowledgeTab.test.tsx`，`4/4` 通过；执行 `npm.cmd run typecheck` 通过；`git diff --check` 通过。
- 本切片未执行 Docker 重启、性能测试、故障注入、真实付费服务调用或数据库清理。

### 9.2 K6 当前交付：管理员用户详情真实读取（2026-09-06）

- 新增 `GET /api/admin/users/{id}/detail`，管理员和操作员可读取脱敏资料、登录会话、业务会话分页结果及与该用户相关的操作审计摘要。
- 操作审计查询只匹配 `operator_id=用户 ID` 或 `target_type=user` 且 `target_id=用户 ID` 的记录，不把其他用户的审计事实混入详情。
- 管理端选中真实用户后独立加载详情，资料、饮食画像、登录会话、业务会话和历史 Tab 均使用真实响应；加载中、失败和无记录状态分别展示。
- 不返回密码哈希、令牌、完整审计参数或业务原文；重置凭证仍明确保持未接入，不执行伪操作。
- 业务验证：Java `AdminOperationalQueryServiceImplTest` `8/8`、API `AdminUserControllerRbacTest` `3/3` 通过；前端 `UsersTab.test.tsx` `4/4`、`adminToolRegistry.test.ts` `1/1` 通过；`npm.cmd run typecheck` 通过；本轮涉及 Java 文件 Spotless 检查通过。
- 本切片未执行 Docker 重启、性能测试、故障注入、真实付费服务调用或数据库清理。

## 10. K7 前端业务体验收口

- Chat 页面展示公共知识引用、无命中、失败和历史 Run 恢复。
- 饮食记录页面展示营养候选歧义和确认结果。
- 餐食计划页面展示校验问题、确认、保存和购物清单。
- 记忆页面展示候选、已确认、冲突、过期和删除状态。
- 管理页面统一 loading、empty、error、permission denied 和 retry 状态。
- 只验证业务主路径、关键错误态和移动端不溢出，不做浏览器性能矩阵。

## 11. K8 集中业务回归与文档收尾

每个大功能点完成后执行受影响模块定向测试；全部业务能力收口后集中执行一次：

```powershell
.\mvnw.cmd -pl foodmate-application,foodmate-infra,foodmate-api -am test
cd agent-runtime
.venv\Scripts\python.exe -B -m pytest -q -p no:cacheprovider
cd ..\foodmate-ui
npm.cmd run typecheck
npm.cmd run build
```

同时更新：

- `README.md`
- `docxs/项目/路线图.md`
- `docxs/项目/完整功能实施TODO.md`
- `docxs/架构/Agent运行架构.md`
- `docxs/实现/后端建设现状.md`
- `docxs/测试/测试策略.md`
- `script/sql/FoodMate/EXECUTION_RECORD.md`

文档只记录真实执行证据。没有执行的真实云调用、性能测试、故障测试和生产能力不写成完成。

## 12. Git 与代码约定

- 每完成一个 K 大点创建一次符合规范的中文或中英混合提交信息，例如 `feat(rag): 完善公共知识切分与检索`。
- 每次提交前运行受影响模块的业务测试和 `git diff --check`。
- Java 使用 Java 21、构造函数注入、模块边界和项目格式化规则。
- 新增代码注释使用中文，注释解释业务原因和边界。
- 不修改用户已有未提交改动，不提交 `.env`、密钥、Python 虚拟环境、QA 截图和临时缓存。
- 不为小改动单独扩展测试体系；在一个大功能点完成后集中验证。

## 13. 最终完成定义

满足以下条件后，才能把本计划标记为完成：

- 公共知识库拥有可追溯的正式内容，索引、发布、检索、引用和下线闭环可复现。
- 知识切分、ACL、版本、引用和无证据回答行为通过业务验证。
- 结构化记忆能够候选生成、Java 校验、用户确认、读取、修改、删除、过期和失效。
- 营养候选、PostgreSQL 权威营养值和饮食记录写入行为一致。
- SQL Agent 覆盖核心饮食分析问题、澄清、空结果、拒绝和审计。
- 七个正式工具和管理端页面使用一致的真实注册信息与执行事实。
- Chat、饮食、餐食计划、记忆和管理页面与后端真实接口一致。
- Java、Python、前端业务主路径验证通过，文档、路线图、README 和执行记录同步。

生产可靠性、性能容量、运维部署和灾备能力继续由独立计划负责，不影响本计划的业务完成判定。
