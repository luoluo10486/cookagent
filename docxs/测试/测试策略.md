# FoodMate 测试策略

> 当前测试口径（2026-09-05）：M1-5 饮食记录、营养分析、餐食计划、写确认和 `food_log_writer` 的 HTTP/RocketMQ 跨进程回归各 11/11 通过；M2-1 公共知识库、M2-2 Tool/SQL、M2-3 管理核心切片和 M3 可审计任务均以业务正确性作为当前门禁。R1/R2/R3/R4 已分别取得一次真实云业务闭环证据，包含真实 Embedding/Milvus、`food_log_writer` 确认写入、餐食计划审批保存和 SQL Agent 只读查询。性能、长稳、重启、ACK 丢失、重复投递、生产容量和备份恢复不属于当前业务门禁。

## 1. 原则与当前基线

测试结论必须来自实际命令、容器或浏览器结果。当前代码、业务闭环和发布级目标分开验收：业务测试或本地 Docker 通过不等于真实云服务、生产容量或完整故障恢复已完成。

| 范围 | 当前命令 | 2026-08-28 复核 | 结论 |
|---|---|---|---|
| 前端构建 | `cd foodmate-ui; npm.cmd run build` | 2026-09-02：通过；Vite 转换 2011 个模块 | TypeScript 检查和 Vite 生产构建通过 |
| 前端 lint | `cd foodmate-ui; npm.cmd run lint` | 通过；退出码 0，无 ESLint 错误或未使用禁用指令 | 当前前端质量门禁通过 |
| 前端测试 | `cd foodmate-ui; npm.cmd test -- --run` | 2026-09-02：38 个测试文件、236/237 通过；D137 对 `RunsTab.test.tsx` 与 `RunsTab.real.test.tsx` 定向验证 4/4 通过，完整套件未在本轮重跑 | 已消除已知 `RunsTab` 定向失败证据；完整套件需在下一个前端大功能点集中验证，不替代真实依赖联调 |
| Java | `.\mvnw.cmd clean verify` | 2026-08-28 复核通过；Shared 12/12、Application 201/201、Infrastructure 83/83（19 skipped）、API 65/65、Bootstrap 58/58（37 skipped） | Spotless、ArchUnit 和 Spring Boot repackage 通过；环境依赖型测试按开关跳过 |
| PostgreSQL/Flyway | 无当前容器测试命令 | 未执行 | 现有测试只读取 SQL 文本，不证明真实 PostgreSQL 可迁移 |
| Python Runtime | `cd agent-runtime; .\.venv\Scripts\python.exe -m pytest -q` | 2026-09-04：206 passed、2 skipped、6 subtests passed | 覆盖 deterministic Runtime、Eval、CAS/recovery、MQ/Redis 适配、知识库 RAG、SQL Planner 与契约回归；不等同于当前供应商认证或生产云模型稳定性 |

## 2. 前端最小测试矩阵

前端测试文件补齐后，最小矩阵应使用 Vitest、React Testing Library 和 MemoryRouter，覆盖以下行为：

| 场景 | 最小断言 | 当前状态 |
|---|---|---|
| 路由与 mock 登录 | mock 状态下的预期页面与受限入口行为明确 | 未实现真实认证或路由守卫 |
| 会话切换 | 从 `/chat/:session_id` 的会话 A 切到 B 时，A 的消息、Run、卡片和 seed 不泄漏到 B | 待补测试；审计已识别风险 |
| Agent replay | seed 只执行一次，完成、失败和取消的 mock 显示一致 | 待补测试 |
| 无行为控件 | 未实现的 Tools、Agents、附件、搜索和菜单项应禁用、移除或有明确可见状态 | 待实现/测试 |
| 管理 mock | 禁用用户、工具切换、删除恢复会刷新 UI，且每个用例重置 fixture | 待补测试；当前共享可变 mock 有污染风险 |
| 构建与风格 | `npm run build`、`npm run lint`、`npm run test -- --run` 均退出 0 | 已通过（2026-08-27）；本段其他行保留为历史 Phase 1-2 矩阵 |

本段描述 Phase 1-2 的历史最小矩阵。当前真实 HTTP、Cookie/CSRF、会话、消息、AgentRun SSE 和断线续传已经接入；后续测试需以真实模式为主，mock 结果不能替代 M1-4 治理交互、Tool/SQL 或 RAG 验收。

## 3. Java 单元、集成与 Testcontainers

保持当前 `.\mvnw.cmd clean verify` 作为骨架基线。后续数据库和 V2 工作必须新增真实 PostgreSQL Testcontainers，而不是只断言迁移 SQL 文本。Testcontainers 验收至少包含：

- 空库从 V1 升级到 V2、带代表性 V1 数据升级到 V2，以及 Flyway `migrate`/`validate`。
- V2 阶段 1-4 的可空新增列与上一兼容 Java 版本的读写 smoke test。
- dispatch 的 partial unique 单 active、active/admission epoch、fencing token、旧 dispatch 拒绝和事务回滚。
- `runtime_dispatch_outbox` 的 `outbox_id/dispatch_id/FK` 唯一、状态/lease CHECK、领取索引，以及 AgentRun/dispatch/immutable RunCommand outbox 同事务原子性；提交后不得从可变业务状态重建 payload。
- dispatch publisher 故障注入：提交后发送前崩溃、发送后 ACK 前崩溃、lease owner 崩溃、多实例竞争、deadline 过期、同 dispatch hash 冲突和 superseded 不发送；每次重放 payload/hash 不变，Python 业务执行计数始终为 1。
- `runtime_event_inbox` 只接受 canonical event：`(agent_run_id,event_id)` 与 active `(dispatch_id,event_seq)` 唯一；同 seq 不同 event、旧/superseded dispatch、乱序和 gap 只写无冲突唯一键的 `runtime_event_rejections`，并返回稳定 RuntimeError。
- 取消/admission epoch 与完成/失败终态竞争，首个 Java 合法终态不回退；旧 epoch/fence invocation 在副作用提交前被拒绝。
- Tool/SQL 的 `invocation_id` 与 `(agent_run_id,idempotency_key)` 幂等，`pending/leased/succeeded/failed/unknown` CHECK、owner token CAS、lease 过期安全接管，以及恶意 SQL、越权 schema、限行、超时和审计断言。
- SSE transactional outbox 的 `source_event_key/sse_event_id/(agent_run_id,stream_seq)` 唯一、业务事务原子性、publisher lease/retry/dead-letter、崩溃接管和 `Last-Event-ID` 恢复。
- `model_call_id`、provider attempt 去重和 V1 `request_id` 兼容。

当前 POM 没有 Testcontainers 依赖；在依赖、容器可用性和测试 profile 落地前，不能声称上述集成测试已运行。V2 阶段 4 启用 Runtime/Tool/SQL/SSE 主链路前，上述表、字段、索引、约束和 fixtures 必须全部通过，不能留作后续增量。

## 4. 双运行时契约与故障注入

V1 内部契约落地后，以 JSON Schema 或等价严格 DTO 校验覆盖 RunCommand、RunEvent、CancelCommand、ToolProposal/Result、SqlProposal/Result、RuntimeError 与 PreRunProtocolError。每个消息验证 `v1`、snake_case、字符串 ID、UTC 时间、未知顶层字段拒绝、RFC 8785 JCS + SHA-256 摘要、幂等键和错误码映射。

Java Runtime Client 的 MockWebServer 或统一 WireMock 测试应注入并验证：connect/read/run timeout、503、连接重置、重复 dispatch、取消重试、401/403/400/409 不重试、426 版本不兼容、PreRun 协议错误与流内 `runtime.error`。RunCommand 网络重试必须发送 outbox 中完全相同的完整 envelope（包括 body 内 `request_id/trace_id`），只刷新短期 Service JWT 等 HTTP Header；其他消息仍按契约复用业务幂等标识并可刷新 request ID。所有重试不得越过 `deadline_at`。

dispatch outbox 的 PostgreSQL Testcontainers + Runtime stub 测试必须逐项断言：只有可验证的接受/已接受 2xx 才转 `delivered`；传输不确定恢复为原 outbox 重试；deadline 和 superseded 在 POST 前转 `expired`；`RUNTIME_DISPATCH_IDEMPOTENCY_CONFLICT` 转 `failed` 且不自动创建 dispatch；显式重新派发先 supersede 旧 dispatch，再创建新 `dispatch_id/attempt/outbox`。启动恢复和并发 publisher 必须证明 lease/CAS 单领取，ACK 丢失重放由 Python `dispatch_id/request_hash` 去重，不能只断言 HTTP 调用次数。

## 5. Python Runtime 测试

当前 `agent-runtime/` 已有 `.venv`、`pyproject.toml`、知识索引 Worker 和 Runtime 回归，可执行 `cd agent-runtime; .\.venv\Scripts\python.exe -m pytest -q`。2026-09-04 实测结果为 `206 passed, 2 skipped, 6 subtests passed`；deterministic RAG、四类文档解析、Redis/Milvus 适配、SQL Planner 共享路由和 Runtime 业务契约均有回归。这仍不能证明当前供应商认证、生产云模型稳定性、生产容量或完整业务故障矩阵。

local 模式的索引与可见性消费者启动前会等待 Milvus `/healthz` 就绪；默认从 `FOODMATE_RAG_MILVUS_URI` 的 `19530` 推导到 `9091/healthz`，也可用 `FOODMATE_RAG_MILVUS_HEALTH_URL` 覆盖。stub 模式不探测、不连接 Milvus，因此不因 Milvus 不可用而阻塞 Redis 关键词索引。

M1-4 测试必须新增并覆盖：

- 本地 RocketMQ 单 NameServer/Broker 启动、Topic 初始化、消息持久化和 Broker 重启恢复。
- Java PostgreSQL Outbox 提交后崩溃、Relay 发布重试、Broker ACK 丢失和相同 message ID/hash 重发。
- Python Redis Inbox/Event Outbox、checkpoint 原子写入、消费重复、Redis 中断停止消费和恢复。
- Java 消费事务提交后 ACK 前崩溃、MQ 重投、PostgreSQL Inbox 去重和 AgentRun 不重复推进。
- `run_id` 局部顺序、不同 Run 并行、同 Run `event_seq` gap/rejection，以及 MQ 顺序不能绕过状态机。
- retry/DLQ、不可重试 rejection 和 DLQ Reconciler 对账；DLQ 不得自动把 AgentRun 标为 failed。
- LangGraph 每条白名单边、循环预算、总节点数、模型调用数和预算耗尽终止。
- 用户 2 Session、全局 20 Run、队列 100 的可配置边界，以及 lease 过期、取消释放、priority burst、aging 和 Redis 故障关闭。
- queue、execution、node、waiting_user、cancel drain 超时各自独立计时和 `TimeoutSnapshot` 恢复。
- 70%/85%/100% Token 与成本降级、模型路由策略、用户预算追加 revision 和幂等确认。
- 最近 8 条原始消息、第 9 条触发摘要、Token 裁剪和摘要失败降级。
- Redis checkpoint 的 CAS 冲突、TTL、大小限制、加密、崩溃恢复及恢复前 Java 对账；不得重复 Tool/SQL 副作用。
- 确定性 Eval、复杂/高风险强制 LLM Judge、低风险抽样、Eval 前正文不可见、重写/重规划上限和无人审核安全降级。
- Eval 通过后的 150ms/2048 字节分片、非逐 Token 发布、稳定 event ID/seq 和 SSE 断线恢复。
- continuation 新 Run 与工具/预算恢复原 Run 的分流，以及 `superseded` 契约升级后的状态和事务测试。
- Trace 脱敏、禁止 Chain-of-Thought/完整 Prompt/默认原始响应落盘，以及结构化反馈只保存稳定原因、关联 ID 和摘要，不保存回答正文、Prompt 或原始业务请求。

Python 测试还必须覆盖模型/RAG adapter、Prompt/评测离线 golden、Schema fixture、Service JWT、事件序列和 Tool/SQL proposal，并证明 Runtime 不持有业务数据库凭据、不能直接执行 Tool 或 SQL。

目标 Runtime 的路由契约测试还必须覆盖 `GET /foodmate/internal/health/live` 和 `GET /foodmate/internal/health/ready`，并拒绝任何缺少 `/foodmate/internal` 前缀的 Python health 路由。readiness 只断言实例可安全认证、接收和持久化请求；单个 run gap、duplicate conflict、lease backlog 与 outbox lag 另作业务告警和发布/灰度门禁测试，不能通过摘除恢复事件入口来“恢复” readiness。

当前缺少的是上述生产级依赖故障、容量和真实云服务证据，不是 Python 业务编排或 M2-1 deterministic RAG 代码本身。

## 6. E2E 与质量门禁

M1-3 已具备登录、会话、发送消息、确定性 Runtime stub、AgentRun 流式事件、取消和断线恢复的真实主链路；M1-4 已增加协调、预算、continuation、Eval、安全降级和 Proposal/Result 业务回归。M2-1 的 RAG、M2-2 的 Tool/SQL 和 M2-3 的管理业务以 deterministic 本地路径作为当前门禁，不能把它们的业务通过扩展为生产稳定性结论。

M1-5 业务 E2E 当前已通过：手工创建饮食记录 -> 目录匹配或未知食材 `pending` -> 官方 foodPortions 换算 matched/pending -> 查询 -> 当天/7 天/30 天分析 -> 餐食计划创建/查询/修改 -> 校验/保存 -> 购物清单 -> 修改后清单失效 -> 删除/恢复 -> `meal_plan.save_plan` Proposal/Confirm/Execute；数据库中已确认 `approval.propose`、`approval.confirm`、`approval.execute` 成功审计。`food_log_writer` 的真实 HTTP 和 RocketMQ Proposal/Result 回归各 11/11 通过，覆盖确认绑定、真实营养匹配、资源 ID 回填、rejected 无写入、failed stale revision 回滚与 `approval.failed` 审计、superseded 禁止旧 Proposal 执行、update/delete/restore、revision 冲突和幂等重放，以及换算快照和 Inbox 完成事实。计划回归同时覆盖创建幂等重放、stale `revision` 返回 409 和恢复后的 revision 递增。后续仅扩展更广的官方营养目录和单位规则。

跨进程 writer 回归命令：

- HTTP：`.\mvnw.cmd -pl foodmate-bootstrap -am -Dfoodmate.local-http-e2e=true -Dtest=M15FoodLogWriterHttpE2ETest -Dsurefire.failIfNoSpecifiedTests=false test`
- RocketMQ：`.\mvnw.cmd -pl foodmate-bootstrap -am -Dfoodmate.local-mq-e2e=true -Dtest=M15FoodLogWriterProposalResultE2ETest -Dsurefire.failIfNoSpecifiedTests=false test`

两条命令均使用本地 PostgreSQL/RocketMQ 和随机测试数据；本轮不新增表、不执行迁移、不清理既有本地数据、不执行数据库备份。

M1-6 当前已完成两个独立 Java JVM 共享 PostgreSQL 的有界认证会话读取基线和 Java 重启后的数据回读，并完成 Python readiness、Redis AOF 探针恢复和 RocketMQ 组件重启恢复；该子项不包含 Agent 业务流量。完整故障测试仍要求逐个重启 Java、Python、PostgreSQL、Redis、RocketMQ，并注入 Outbox/Inbox ACK 丢失、租约接管、重复消息和 SSE 恢复；当前尚未完成 PostgreSQL 进程重启和完整业务故障注入。数据库备份恢复当前明确不在范围内。

M1-6 统一审计已改由 `OperationAuditPort` 与单一 PostgreSQL 适配器处理；业务审计仅保存操作者、目标、动作、结果、错误码、关联标识、幂等键、参数摘要和安全结果摘要，不保存密码、令牌、Prompt、完整回答、饮食备注或完整业务载荷。饮食/餐食计划幂等回放从业务表重新构建结果，审计响应不保留食材、备注或计划内容。低基数指标仅允许 `transport`、`operation`、`result`、`reason` 标签，覆盖 dispatch、proposal、result、event、SSE replay、重投/重复与 pending 队列；Python readiness 同时返回有界运行时统计。

2026-08-23 已补齐结构化 Agent 反馈业务入口：仅允许用户对自己已完成 Run 的 assistant message 提交一次反馈；负面反馈至少选择一个稳定原因，重复幂等键重放既有事实，同一回答的并发重复提交不产生第二条记录；高风险原因进入可配置的高优先级审计标记。Application、PostgreSQL Mapper、API 和前端反馈组件均有定向业务测试。V26 迁移只完成脚本、validation/rollback precheck 和静态校验，未执行目标数据库迁移。

共享依赖可用时，执行 `.\script\local\m1-6-traffic-recovery.ps1` 进行安全预检；真实业务流量使用既有 M1-5 HTTP/RocketMQ harness，固定 30 秒预热、120 秒稳态和 16 worker，并记录吞吐、P50/P95/P99、错误率、队列峰值/清空、重投与重复副作用。只有显式 `-EnableFaultInjection` 才会注入单项 Redis 重启；Java、Python、PostgreSQL、RocketMQ、ACK 丢失、重复消息和 SSE `Last-Event-ID` 恢复必须逐项执行并记录恢复时间、审计数量、业务写入数量、SSE 缺口/重复与终态，未取得实际依赖证据不得标为通过。

生产级压测、多实例吞吐、云模型长期稳定性、账单审计、完整监控和发布回滚属于后置阶段，不能从本地测试结果推导完成。

建议门禁分层如下：

1. 每次前端变更：typecheck/build、lint、单元测试均为 0 退出码。
2. 每次 Java 变更：`.\mvnw.cmd clean verify`，并对迁移/持久化改动运行 PostgreSQL Testcontainers。
3. 每次契约改动：Java/Python 两端 Schema fixture、canonical digest、JWT 和错误映射互测；所有文档 JSON 示例程序化解析并按字段集重算摘要。
4. 每次发布候选：兼容矩阵、包含 dispatch outbox 的 PostgreSQL V2 reliability fixtures、上述七类派发故障注入、双运行时无双执行 smoke、迁移回滚/drain 演练和浏览器 E2E。

在 lint/test 失败、没有容器验证或目标 Runtime 能力尚未实现时，相应门禁状态必须是失败或不可执行，而非豁免通过。当前前端业务门禁已在 2026-08-27 复核通过；真实依赖和生产门禁仍按各自证据单独判断。

## 7. M2 功能版业务门禁（2026-08-28）

M2 当前只把业务正确性作为完成门槛，不投入性能压测、长时间稳定性、队列容量、组件重启、ACK 丢失、重复投递或生产环境演练。上述项目继续保持未执行，不得用 mock、静态检查或单机基线替代真实证据。

M2-3 管理后台已按资源域拆分验证：后端分页查询与 RBAC、管理写操作的确认/revision/幂等/审计、模型治理安全字段，以及前端 real/fixture 隔离、加载错误态、空态、用户状态操作、AgentRun 分页查询和概览真实数据路径。当前已通过的定向命令包括：

- `foodmate-ui`: `npm run typecheck`、UsersTab 4 tests、RunsTab 3 tests、AdminPage 8 tests。
- Java API: `AdminUserControllerRbacTest`、`AdminManagementControllerTest`，共 4 tests 通过；用户列表响应包含 `revision`，管理写请求包含 `Idempotency-Key` 和版本/确认字段。

M2-1 已有 Java 索引状态/Outbox/结果处理、Python 四类文档解析与 stub/local RAG 定向测试；知识库用户检索、管理端批次上传/进度/失败重试、聊天可展开引用和餐食规划 real 页面均已接入并有前端业务测试。2026-08-23 已补齐 deterministic 上传 -> 索引 -> 发布 -> AgentRun -> SSE 的真实业务闭环及 Chat SSE 兼容入口回放证据。local 模式另有 Milvus readiness 启动门禁，避免在向量服务未就绪时提前消费索引消息。M2-2 已有 Tool Registry/Policy、SQL Catalog/AST Guard、双模式 Planner 和分析 AgentRun 代码，并完成本地 PostgreSQL/RocketMQ/宿主 Java/Python 的 `time_parser -> database_query -> Composer` 多轮业务闭环，SQL 审计、空数据语义和事件连续性均已验证。M2-3 的真实 Trace 明细在后端没有对应资源契约，real 模式只显示空态，不伪造 fixture 数据。前端当前门禁为 38 个测试文件、201 passed，lint、typecheck 和 build 均通过（2026-08-28）。

2026-08-23 M2-1 SSE 业务收尾：`RunStreamControllerTest` 与既有 `ChatControllerTest` 共 3/3 通过；数值型 V1 Run 已由 `/api/chat/runs/{runId}/stream` 分流到持久化事件服务。宿主 Java deterministic Run 携带 `Last-Event-ID: 5` 的实际请求成功回放唯一 `run.completed` 终态，证明 Chat 兼容入口可以使用稳定 SSE 游标。该项是业务回放验证，不等同于组件重启、ACK 丢失、重复消息或性能故障矩阵；这些仍按当前决策暂缓。

2026-08-23 业务门禁复核结果：Python 项目 `.venv` 全量 `pytest` 为 113 passed、1 skipped、1 warning；前端 36 个测试文件、170 passed，typecheck 通过；全量 `mvnw.cmd clean verify` 通过，Shared 12/12、Application 155/155、Infrastructure 71/71（11 skipped）、API 59/59、Bootstrap 58/58（37 skipped），Spotless 和 Spring Boot repackage 均通过。M2-1 deterministic 知识库跨运行时和 M2-2 SQL Agent 本地真实业务闭环均已取得证据；真实云模型/embedding、性能压测、组件重启、ACK 丢失、重复投递和生产恢复门禁仍未执行。

## 8. M3 业务治理门禁（2026-08-23）

M3 当前只验收可审计的业务契约和受控任务编排，不把不可逆删除或生产运维能力混入本地业务门禁。

| 能力 | 当前业务门禁 | 未纳入本轮 |
|---|---|---|
| 运营审计报告 | 聚合查询只返回安全计数、时间和稳定原因码；不返回 Prompt、回答、原文、消息载荷或凭据 | 历史报告归档、生产告警 |
| DLQ 人工重放 | superadmin、确认摘要、幂等键、Outbox 发布和原 DLQ 状态收敛 | 真实 Broker 重放、组件故障恢复 |
| 保留策略与 legal hold | 申请 -> superadmin 审批 -> 任务分解；active hold 阻止审批/领取 | 真实对象/向量清理演练 |
| 受控数据库清理 | 仅允许已软删除知识文档按依赖顺序清理；对象/向量任务完成后才可领取；默认策略关闭 | 本地真实硬删除、备份恢复 |

M3 业务测试最低断言：角色和确认不可绕过；重复请求不产生重复任务；任务失败可进入可解释状态；active hold 不会被领取；清理顺序和目标类型固定；默认配置不会执行不可逆操作。未取得真实依赖和删除演练证据，不得将 M3 标记为整体完成。

## 9. 当前后置验证

以下项目继续暂缓，不因本轮文档更新而改变：吞吐、延迟、积压、长稳、Java/Python/PostgreSQL/Redis/RocketMQ 重启、Outbox/Inbox ACK 丢失、重复投递、SSE 故障恢复、真实云模型/Embedding、Docker 应用镜像、备份恢复、发布回滚和生产环境验证。
