# FoodMate 前端架构文档

版本：v1.1
状态：Figma 重构代码迁移进行中 / 前端实现入口文档
日期：2026-09-02
详细来源：[UI 设计规范](../设计/UI设计规范.md)、[Figma 前端重构设计需求规格](../设计/Figma前端重构设计需求规格.md)、[接口与数据规范](../契约/接口与数据规范.md)、[实现附录](../参考/实现附录.md)

## 1. 定位

本文是 FoodMate 前端实现入口。Phase 1 静态原型和 Phase 2 Mock 交互是历史建设阶段；当前 M1-2/M1-3 已接入真实认证、会话、消息、AgentRun SSE、取消和断线续传，M1-4 已接入预算确认、continuation、协调故障提示和安全降级展示。M1-5 的饮食记录、营养分析和餐食规划页面已完成视觉迁移和交互展示，后端已接入饮食记录创建/查询/编辑/删除/恢复、分析、餐食计划完整资源生命周期和 `meal_plan.save_plan` 确认；`food_log_writer` 的 create/update/delete/restore 及拒绝、失败、superseded、revision 冲突和幂等重放已通过真实 PostgreSQL HTTP/RocketMQ 各 11/11 跨进程回归。M2-1 公共知识库批次上传、进度/重试、检索和聊天引用，M2-2 deterministic SQL Agent 和 M2-3 管理核心真实查询/写操作均已接入当前业务路径。不能把页面验收或 mock 数据当作完整生产完成。默认模型仍为 `deterministic:local`；真实云长稳、生产 RAG 质量和生产质量治理仍是后续工作。

### 当前 M1-5 联调边界（2026-09-02）

- 饮食记录、摄入分析和餐食规划页面表达目标交互和状态；当前 `food_logs`、`food_log_items`、营养目录和 `approval_requests` 表已落地，营养目录已有 1,000 条 approved/official USDA 食材和 1,518 条 approved foodPortions 换算规则；没有官方证据的单位仍不推断。
- 当前本地真实路径覆盖手工记录创建/查询/编辑/删除/恢复、分析、餐食计划创建/查询/修改/校验/保存/删除/恢复/购物清单和 `meal_plan.save_plan` Proposal/Confirm/Execute；`food_log_writer` 的 create/update/delete/restore、拒绝/失败/superseded、revision 冲突和幂等重放已通过真实 PostgreSQL HTTP/RocketMQ 各 11/11 writer 回归。M2-1 公共知识库和 M2-2 deterministic Tool/SQL 已形成业务闭环；真实云服务、性能和故障矩阵仍后置。
- 当前只做本地优先；不以页面配置或文档中的 `dev/prod` profile 推断 staging/production、Kubernetes、完整生产监控、数据库备份恢复或发布回滚已经建立。

前端第一轮目标：

- 让首页、会话页、分析页、规划页、个人资料页和管理后台入口具备可运行的静态或 mock 原型。
- 让会话页能在无后端情况下播放一轮 Agent mock 任务。
- 让追问卡、确认卡、工具轨迹、引用、失败和取消状态先在前端闭环。
- 为后续接入 `/foodmate/agent-runs/{agent_run_id}/events` SSE 事件保留数据结构和状态映射。

## 2. 技术栈决策

当前代码基线：

- React
- Vite
- TypeScript
- Arco Design（迁移前存量依赖，不作为新页面目标方案）
- CSS Modules

目标重构基线：

- React
- Vite
- TypeScript
- shadcn/ui（基于 Radix 的可组合基础组件）
- Tailwind CSS（shadcn/ui 的目标样式编译层，使用 Vite 集成）
- CSS Variables + FoodMate 语义化 design tokens
- iconfont（仅用于已有准确的品牌或领域矢量资产）
- 标准命令图标使用统一的代码图标组件；与 shadcn/ui 生态一致的图标实现可作为默认选择

文档前置阶段已完成。自 2026-08-09 起进入代码迁移：Figma 设计稿、Token、组件 Variant 和图标资源清单作为实现约束，代码按 G1-G6 边界逐步替换；当前已推进到 G5-6 Run / Tool Call / Trace，不能视为 G5 或 G6 全部完成。

### 2.1 启动迁移前置条件

当前 `foodmate-ui` 已具备 Tailwind、shadcn/ui、`lucide-react`、`@/*` 路径别名、FoodMate 语义 Token、`cn` 工具和基础组件目录。G1、G2、G3 已完成，G4 已完成 `DietRecordsPage`、`AnalysisPage`、`PlanningPage`、`KnowledgePage` 和 `ProfilePage`；G5-G6 仍必须逐边界验证，不得把用户业务页迁移完成等同于管理端和 Arco 清理全部完成。

G2 基础组件验收记录（2026-08-09）：

- `components/ui/components.test.tsx` 已覆盖 15 个测试，验证 Button 的 variant、size 和 disabled，Input/Textarea 的可访问名称，Dialog/Sheet 的 Escape 关闭和触发器焦点归还，Tooltip Provider 的显示/隐藏，Tabs 键盘切换，Select 选项提交，Badge variant，Alert `role="alert"`，以及 Table 原生语义和窄屏滚动容器。
- `npm run test`（15/15）、`npm run typecheck`、本次测试文件的 ESLint 和 Prettier 检查，以及 `npm run build` 均通过。
- 390x844 登录页实际浏览器检查未发现裁切、重叠或水平溢出；工作台首页的 `.quickActions` 问题已移交并在 G3 记录中关闭。
- 项目级 `lint`/`format:check` 当前仍会报告既有页面、服务和类型文件的格式问题；本阶段不扩大范围格式化无关文件，G3 之前需单独清理或建立基线豁免。

### 2.2 Admin Operation Status 实现约束

- Figma 状态来源为 `692:4319`、`692:4539`、`692:4766`、`692:4995`、`692:5207`；状态枚举必须保持 `idle`、`no-permission`、`confirm`、`submitting`、`success`、`failed`。
- `no-permission` 和 `success` 是工作区顶层 `48px` 信息横幅，不应插入内容页间距；`operator` 的写按钮必须带锁并禁用，不能通过前端 mock 绕过服务端权限。
- `confirm` 弹窗使用 shadcn `Dialog`，目标宽度 `480px`、内边距 `32px`、圆角 `16px`；`submitting` 保留确认标题和警告图标，显示 `4px` 红色进度条与禁用按钮；`failed` 必须保留稳定错误码和 `request_id`，并明确配置未改变。
- 成功只在执行函数和审计写入成功后展示；失败不能替换为成功数据，重试只能由用户显式触发。新增状态时先补充组件定向测试，再进行浏览器截图和 Figma diff。

G3 工作台迁移验收记录（2026-08-09）：

- `WorkspaceLayout` 增加可选右栏契约；Chat 的 Trace 从 fixed 浮层迁移到工作台壳层的 `246px` rail，1366x768 下聊天区实测 `860px`，顶部栏 `68px`。
- `AgentStatusStrip` 与 Chat grid 行统一为 `45px`；消息区实测 `567px`；Composer 按 Figma 目标收敛为 `88px`，保留发送、停止、附件、真实 API/SSE 回调语义。
- Composer 的 Enter 发送排除了 `isComposing` 和 `keyCode=229`，覆盖 IME 组合输入、Shift+Enter、运行中停止和 disabled 状态；新增 5 个 Composer 回归测试。
- 首页快捷任务改为先带入 Composer，附件按钮打开文件选择器并通过无障碍状态反馈已选择文件；390x844 下快捷按钮改为两列换行，容器与内容宽度均为 `343px`。
- 浏览器实测视口：1366x768 Chat、1440x900 首页、1024x768 Chat、390x844 首页和 Chat；文档/正文无横向溢出，控制台无 error。
- `npm run test`（20/20）、`npm run typecheck`、本轮文件 Prettier 检查和 `npm run build` 通过。定向 ESLint 仍受 ChatPage 既有 `react-hooks/set-state-in-effect` 两条 warning 阻断，未扩大范围修改其业务状态逻辑。

G4 用户业务页迁移记录（2026-08-09，首个页面）：

- `DietRecordsPage` 已按 Figma `diet-records-v2`（节点 `640:588`）完成首轮视觉迁移；页面主体、日期导航、四项营养指标、Breakfast/Lunch 餐次卡和记录详情均按 Figma 的颜色、字体、间距、圆角和尺寸实现。
- 已对照 `records-body`（`640:660`）、日期栏（`640:661`）、指标区（`640:674`）和详情卡（`974:3`）的设计上下文；桌面几何实测为主体 `1180x541`、内容内边距 `32px`、指标区 `1116x88`、餐次区 `1116x301`、详情卡从主内容 `y=609` 开始。
- 详情卡保留自然语言记录、编辑、复制、分析和软删除操作；新增食物继续通过 shadcn `Dialog`、`Input`、`Button` 完成，状态仍区分已确认、待确认和待估算。
- 浏览器验收视口：`1440x900`、`1366x768`、`1024x768`、`390x844`。四个视口均未发现文档、body 或 root 横向溢出；1024px 主内容收敛为两列营养指标，390px 详情卡与主体统一 `16px` 对齐。
- 新增 `DietRecordsPage.test.tsx` 两条回归测试；本次 `npm run test` 为 `22/22`、`npm run typecheck` 和 `npm run build` 通过。全局 `lint` 与 `format:check` 的既有基线问题未在本页扩大处理范围。
- 本记录只关闭 `DietRecordsPage` 的页面边界；`AnalysisPage`、`PlanningPage` 的独立验收记录见下文。记录当时 `KnowledgePage`、`ProfilePage` 仍未完成 G4 验收。

`AnalysisPage` 页面迁移记录（2026-08-09，第二个页面）：

- `AnalysisPage` 已按 Figma `intake-analysis-v2`（节点 `640:773`）完成首轮视觉验收；筛选条、三项摘要指标、能量趋势图、Agent 洞察和数据质量面板均保留真实交互与状态文本。
- 已对照 `analysis-body`（`640:845`）、趋势图卡（`640:872`）、洞察卡（`640:883`）和质量面板（`975:3`）；1440px 主体实测 `1180x799`，筛选条 `384x40`，摘要区 `1116x101`，趋势卡 `1116x303`，洞察卡 `1116x219`，质量面板 `1116x140`。
- 筛选条继续使用可访问的 tab 语义；导出 CSV、让 Agent 解读和基于分析制定计划的操作未改变路由或状态语义。移动端保留筛选条横向滚动能力并隐藏原生滚动条，避免出现设计稿中不存在的滚动箭头。
- 浏览器验收视口：`1440x900`、`1366x768`、`1024x768`、`390x844`。四个视口均未发现文档、body 或 root 横向溢出；390px 图表与筛选条保持在内容滚动容器内。
- 新增 `AnalysisPage.test.tsx` 两条回归测试，覆盖范围切换和导出状态；本次 `npm run test` 为 `24/24`、`npm run typecheck` 和 `npm run build` 通过。

`PlanningPage` 页面迁移记录（2026-08-09，第三个页面）：

- `PlanningPage` 已按 Figma `meal-planning-v2`（节点 `640:901`）完成默认态视觉迁移；主计划区保持 `840px`，右侧约束校验与购物清单保持 `340px`，内容区使用 `32px` 主内边距、侧栏 `24px` 内边距和 Figma 的 `#fcfcfc`、`#ffffff`、`#f4f6f5`、`#333333`、`#6b7280`、`#a6d997`、`#c79654`、`#ff7576` 语义颜色。
- 已对照 `main-content`（`640:949`）、`planning-core`（`640:950`）、默认计划主体（`640:974`）和 `plan-sidebar`（`640:1077`）；1440px 下计划主体 `840px`、侧栏 `340px`，计划横向网格使用 `100px` 餐次标签列与 5 列日期卡，日期卡 `40px` 高、餐次卡 `74px` 高。
- 默认态保留日期 tab、重新生成、保存计划、空计划入口、约束校验和购物清单勾选；计划列表按进行中/草稿箱/已归档真实筛选，购物清单采购总数由实际条目计算，不再使用固定数量。
- 已覆盖加载中、空态、错误态、计划列表、三步创建向导、冲突解决、购物清单和生成中状态；状态之间继续通过查询参数路由，保留取消、返回、重试、保存草稿、冲突应用和导出反馈语义。
- 浏览器验收视口：`1440x900`、`1366x768`、`1024x768`、`390x844`。四个默认视口均无文档横向溢出；修复 `1024px` 下主计划区被 Grid 压缩导致的日程/侧栏重叠；移动端冲突态保留日程横向滚动且只保留单一纵向滚动层。
- 新增 `PlanningPage.test.tsx` 五条回归测试，覆盖默认日程与购物清单、加载/空/错态恢复、计划列表筛选、向导生成取消、冲突应用和购物清单导出；本次全量 `npm run test` 为 `29/29`，`npm run typecheck` 和 `npm run build` 均通过。
- 本记录只关闭 `PlanningPage` 的页面边界。记录当时 G4 尚剩 `KnowledgePage`、`ProfilePage`；G5 管理端和 G6 Arco 清理不因本页完成而提前关闭。

`KnowledgePage` 页面迁移记录（2026-08-09，第四个页面）：

- `KnowledgePage` 已按 Figma `KnowledgePage` 默认画板（节点 `795:838`）完成首轮视觉迁移；空态（`795:963`）、检索失败（`795:1145`）和来源不可用（`795:1328`）均保留为可操作状态，并按设计稿使用 shadcn `Button`、`Input`、`Badge`、`Card` 与 Lucide 图标。
- 已按 Figma 结构移除设计稿中不存在的搜索按钮；引用切换改为基于引用条目标识绑定，避免按数组索引切换导致内容错位；移动端移除固定 `760px` 高度，改为自然纵向流布局。
- 桌面端结果卡实测为 `776x155`；结果区/引用区在 `1440px`、`1366px`、`1024px` 下分别实测为 `840px/340px`、`766px/340px`、`464px/300px`。空态卡实测为 `570,320,560x220`，检索失败和来源不可用卡实测为 `550,300,600x260`；移动端无水平溢出。
- 浏览器验收视口：`1440x900`、`1366x768`、`1024x768`、`390x844`。四个视口均未发现文档、body 或 root 横向溢出，页面状态卡、结果区和引用区未发现重叠或裁切。
- 新增 `KnowledgePage.test.tsx` 四条回归测试，覆盖搜索、筛选、引用切换、空态恢复、检索失败重试、来源不可用和“就此提问”；本次全量 `npm run test` 为 `33/33`，`npm run typecheck` 和 `npm run build` 均通过。定向 ESLint 与 Prettier 检查通过；全仓 lint 仍受其他既有文件的 254 个 Prettier 告警阻断，本页未扩大范围修复无关文件。
- 本页继续使用 Lucide 作为标准命令图标；iconfont 资源登记仍为 `BLOCKED`，不在本次页面迁移中虚构或导入未确认授权的字体资源。
- 本记录只关闭 `KnowledgePage` 的页面边界；当时的 G5 管理端和 G6 Arco 清理尚未开始，当前状态以本文后续 G5-7/G6 记录为准。

`ProfilePage` 页面迁移记录（2026-08-09，第五个页面）：

- `ProfilePage` 已按 Figma 个人中心四个主节点完成实现与回归：基本资料 `806:1119`、记忆与偏好 `806:1281`、安全与设备 `806:1445`、数据与隐私 `806:1585`。
- 已纳入设计稿状态节点：记忆空态 `1013:2`，退出其他设备确认 `1013:235`，注销账号确认 `1013:465`，以及头像上传中/失败、未保存离开、密码提交中/成功/失败、导出排队/运行/过期、注销提交中/成功/失败状态节点（`792:212`、`794:212`、`794:380`、`794:548`、`794:693`、`794:838`、`794:984`、`794:1127`、`795:212`、`795:356`、`795:499`、`795:642`）。
- 视觉实现严格沿用 Figma 的 `#fcfcfc`、`#f4f6f5`、`#a6d997`、`#c79654`、`#ff7576`、`#d67676`、`#5da9b2`、`#333333`、`#6b7280` 颜色契约；页面控件统一复用 shadcn `Button`、`Input`、`Card`、`Dialog`、`Badge`、`Progress`、`Select`、`Textarea`，标准命令图标继续使用 Lucide。
- 基本资料桌面几何与 Figma 对照：1440x900 下内容区起点 `x=260,y=68`，主表单 `681.6x520`，左侧资料卡 `400x245`；1366x768 下主表单宽度收敛为 `608px`；1024x768 下两栏自然堆叠，主表单 `689.6x520`；390x844 下卡片统一 `16px` 边距，资料卡 `343.2x395.3`，表单 `343.2x857.9`。
- 浏览器验收覆盖 `1440x900`、`1366x768`、`1024x768`、`390x844` 四个视口和 `/profile`、`/profile/memories`、`/profile/security`、`/profile/data` 四个路由；文档、body、root 均无横向溢出，未发现文字裁切或非设计稿重叠。数据与隐私页的“注销影响与二次确认”层叠关系已用 Figma `806:1585` 截图核对，属于设计稿定义的视觉层级。
- 记忆空态按 Figma `1014:2` 修正为居中的 `680x320` 卡片，使用 `#fcfffc`、`#d1e8d9`、`#549e6b`、`#1f3629`、`#526359`、`#7d8c82` 和 `#9ed685`；移动端实测为 `343.2x320`，内容自然收缩且没有水平溢出。
- 新增 `ProfilePage.test.tsx` 五条回归测试，覆盖资料字段和过敏原、记忆筛选与空态、密码失败校验、退出其他设备确认、数据导出排队和注销确认；定向测试 `5/5`，全量测试 `38/38`，`npm run typecheck`、`npm run build` 和本次变更文件的 ESLint/Prettier 检查通过。
- 本页继续不引入未经确认的 iconfont 字体资源，iconfont 登记仍为 `BLOCKED`。本记录只关闭 `ProfilePage` 页面边界；G5-7、G6 Arco 清理和 Figma 实际流程画板的当前状态以本文后续记录为准。

`AdminPage` G5-1 管理概览迁移记录（2026-08-09，第一个管理端页面）：

- 目标 Figma 节点为 `admin-overview`（`995:977`）。管理壳层严格沿用设计稿的 `260px` 侧栏、`64px` 顶栏、`32px` 内容留白、`#fcfcfc` 页面底色、`#ffffff` 容器、`#f4f6f5` 边框、`#ff7576` 主强调色、`#333333` 正文色和 `#6b7280` 辅助色；标题使用 Noto Sans SC，ID、耗时、指标和错误码使用 Space Mono。
- 概览页已完成 3 项指标、时间/结果/降级筛选、运行/用户搜索、10 列运行表格、6 行首屏数据、分页和 3 张分析卡；交互控件使用 shadcn `Select`、`Input`、`Button`、`Card` 和语义 `Table`，标准命令图标使用 Lucide。
- 表格按 Figma 固定桌面密度实现：表格卡片为 `1116x396`，正文行使用约 `57px` 的稳定行高；错误码和操作列重新分配固定宽度，shadcn Table 内层容器禁止纵向滚动，保证 1440 视口 6 行和“查看详情”按钮完整可见。1366、1024、390 视口仅在表格卡片内部保留横向滚动，不产生 document/body/root 横向溢出。
- 保留真实 `loadAdminDashboard` 分支、mock fallback、admin/operator/普通用户权限判断、admin 写操作二次确认和审计记录写入；本页没有改变 API、权限、错误码或审计语义。
- 新增 `AdminPage.test.tsx`，覆盖 Figma 结构、结果筛选与搜索、pathname/query 精确导航高亮；定向测试为 `3/3`。本轮浏览器验收覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`，未发现页面级横向溢出、表格纵向裁切或 1440 视口操作列裁切。
- G5-1 只关闭管理端概览页边界；工具注册表、删除资源、管理操作状态、用户详情、Run / Tool Call / Trace 和操作审计随后已分别在 G5-2 至 G5-7 记录中关闭代码页面边界。G5 前端权限矩阵与 G6 Arco 清理已完成本地代码核验；Figma 实际流程画板、像素级对照、实体 iconfont 登记和真实后端授权仍是独立未完成项。

`ToolsSection` G5-2 工具注册表迁移记录（2026-08-09）：

- 工具注册表已接入 `/admin/tools?tab=registry`，保留 `/admin/tools` 原工具调用页；页面按 Figma `692:3847` 使用固定 10 列表格、状态/风险/权限筛选、搜索、指标、分页和配置详情只读 Dialog。
- 视觉契约沿用管理端 `260px` 侧栏、`64px` 顶栏、`#fcfcfc` 页面底色、`#ffffff` 容器、`#f4f6f5` 边框、`#ff7576` 强调色、`#333333` 正文色、`#6b7280` 辅助色；ID、版本、超时、失败率和策略使用 Space Mono，标准命令图标使用 Lucide。
- 1440x1024 下表格卡片实测 `1116x396`，6 行稳定显示，错误码、操作列和内层滚动均无裁切；1366、1024、390 视口仅保留卡片内横向滚动，document/body/root 无页面级横向溢出。
- 新增 `ToolsRegistryTab.test.tsx` 三条回归测试，覆盖 Figma 结构、状态/搜索筛选和配置详情 Dialog；全量前端测试已扩展到 44 条，typecheck/build 通过，本次测试文件定向 ESLint/Prettier 通过。

`DeletedSection` G5-3 删除资源迁移记录（2026-08-09）：

- 删除资源已接入 `/admin/deleted`，按 Figma `692:4104` 完成 `删除资源管理` 顶栏、`审计存档区` 徽标、合规提示、资源类型/删除者/删除时间筛选、存档搜索和 8 列归档表格。
- 表格严格映射 Figma 的 `1116px` 桌面内容宽度与 8 列比例：资源 ID、类型、摘要、原所有者、删除者、删除时间、可恢复和操作；颜色使用文档/会话/计划/记录对应的 `#5da9b2`、`#d67676`、`#c79654`、`#89b27c` 语义色，ID 与时间使用 Space Mono。
- 4 条首屏样例、19 条结果分页、复制存档 ID、查看详情 Dialog、不可恢复项禁用和恢复按钮均已实现；恢复继续调用真实 `restoreAdminResource`，mock 模式走统一 admin 二次确认、审计写入和局部移除。
- 浏览器验收覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`：1440 桌面无非设计稿滚动条，其他视口只在表格内部横向滚动；document/body/root 均无横向溢出，行高实测 `71/59/71/59px`，与删除者换行状态一致。
- 新增 `DeletedResourcesTab.test.tsx` 三条回归测试，当前全量测试目标为 47 条，typecheck、build、本次变更文件定向 ESLint/Prettier 通过。
- G5-3 只关闭删除资源页面边界；G5-4 管理操作状态页、G5-5 用户详情、G5-6 Run / Tool Call / Trace、G5-7 权限矩阵和操作审计已完成各自代码边界，但真实后端授权和 Figma 实际画板仍需独立验收；iconfont 实体资源登记仍为 `BLOCKED`。

| 前置项 | 目标状态 | 通过条件 |
|---|---|---|
| TypeScript/Vite 别名 | `@/*` 指向 `src/*`，并在 `tsconfig.json` 和 `vite.config.ts` 同步配置 | 类型检查和 Vite 解析同一别名 |
| Tailwind | 使用与 Vite 匹配的 Tailwind 集成，扫描 `src/**/*.{ts,tsx}` | 生成的工具类可在最小测试组件生效 |
| shadcn/ui 基础 | 初始化组件配置、`cn` 工具、全局 CSS 变量和基础组件目录 | `Button`、`Dialog`、`Tooltip` 可独立渲染和键盘操作 |
| 图标边界 | 标准命令图标组件与 iconfont 封装均有明确入口 | 没有页面直接引用 iconfont 字码 |
| 基线留档 | 当前主要路由在目标视口的截图、交互和测试结果已保存 | 出现回归时可比较与回退 |

目标依赖由 shadcn/ui 初始化和所选组件按需引入；至少需要 Tailwind/Vite 集成、`clsx`、`tailwind-merge`、`class-variance-authority`、`lucide-react`、Radix primitives 和通知组件。具体版本不在文档中锁死，安装时以 shadcn/ui 当前生成配置和项目 Node 版本兼容性为准。

选择 React 而不是 Vue 的原因：

- FoodMate 更像 Agent 工作台，不是普通表单后台；核心复杂度在消息流、工具轨迹、Agent 状态和事件回放。
- React 的组件组合和 TypeScript 契约适合承载 `AgentRun`、`ToolCall`、`SSEEvent` 这类状态驱动 UI。
- 后续接入 SSE、mock event replay、局部状态机和复杂业务卡片时，React 生态和 AI Coding Agent 生成稳定性更好。
- Vue 3 也可实现本项目，但当前文档、系统设计和执行路线已偏向 React，切换会增加文档和实现同步成本。

选择 shadcn/ui 的原因：

- shadcn/ui 提供可复制、可组合、可审查的基础组件代码，适合将 Figma 的组件变体和状态逐项映射到项目中。
- Radix 的可访问行为覆盖 Dialog、Sheet、Tooltip、Tabs、Select 等复杂交互，减少业务组件重复处理焦点、键盘和弹层关闭逻辑。
- 组件源码归项目所有，能够在不套用后台模板的前提下保留 FoodMate 的工作台布局、色彩、卡片层级和动效。
- iconfont 仅承载 Figma 或品牌资产中没有合适通用图标替代的领域矢量，避免把业务图标和普通控件图标混在同一套不可追踪资源中。

## 3. Phase 边界

Phase 1：静态 UI 原型

- 创建前端应用骨架。
- 实现首页、会话页、分析页、规划页的静态页面。
- 抽取共享组件，组件通过 props 接收 mock 数据。
- 覆盖空态、加载中、错误态、完成态等静态状态。

Phase 2：Mock 交互

- 使用前端本地 mock event sequence 模拟 AgentRun 生命周期。
- 支持消息发送后的状态推进、工具轨迹出现、答案逐步输出。
- 支持 `waiting_user` 追问流程和确认流程。
- 展示 `failed`、`cancelled` 和停止按钮；当前只有错误展示，重试入口待实现，见 `当前实现审计与完善计划.md` 的对应任务。

不属于 Phase 1-2：

- 不接真实后端。
- 不创建真实数据库、SSE 服务或认证系统。
- 不实现真实 RAG、Tool、SQL Agent、ModelService。
- 不引入大型全局状态方案，除非 Phase 2 事件回放复杂度证明必要。

## 4. 路由与页面

推荐路由：

| 路由 | 页面 | Phase |
|---|---|---|
| `/` | 首页 | Phase 1 |
| `/login` | Mock 登录入口、用户资料和鉴权状态占位 | Phase 1 |
| `/profile` | 个人资料页：昵称、头像、邮箱、营养目标、忌口、单位偏好、修改密码入口；Phase 2 先做 mock，真实接口接入属于后端认证/个人资料阶段 | Phase 2 mock / 后续真实接入 |
| `/admin/*` | 独立管理后台，仅 `admin/operator` 可见，包含概览与治理子页面；Phase 2 先做 mock，真实接口接入属于管理接口阶段 | Phase 2 mock / 后续真实接入 |
| `/chat/:session_id?` | 会话页 | Phase 1-2 |
| `/analysis` | 分析页 | Phase 1 |
| `/planning` | 规划页 | Phase 1 |
| `/knowledge` | 知识库检索占位 | Phase 1 |

路由行为：

- Phase 1 可以先使用前端路由，不依赖后端 session。
- 首页快捷任务可跳转到会话页并带入 mock prompt。
- 会话页如果没有 `session_id`，使用 mock 当前会话。
- 分析页和规划页第一版使用静态 mock 数据。
- `/login` 第一版提供 mock 登录、注册入口、忘记密码入口、用户资料、营养目标、权限范围和鉴权状态说明，不写入真实 token。
- `/profile` 第一版展示 mock 个人资料，后续接 `GET/PATCH /foodmate/users/me`、头像上传和修改密码接口。
- `/admin` 第一版展示 mock 管理概览，普通用户不可见；真实接入后根据 `/foodmate/auth/me` 的 `role` 控制入口和路由访问。
- `/admin/users`、`/admin/runs`、`/admin/tools`、`/admin/usage`、`/admin/knowledge`、`/admin/deleted` 为管理后台独立子路由，左侧后台导航必须真实跳转。
- `/knowledge` 第一版只展示知识库检索与 ACL 过滤占位，不接真实 RAG 接口。

真实认证接入后的路由守卫：

- 未登录访问受保护页面跳转 `/login?redirect=当前路径`。
- 登录过期时先调用 `/api/auth/refresh`，刷新失败再跳转登录页；Refresh Token 只由 HttpOnly Cookie 承载。
- 普通用户访问 `/admin` 展示 403 权限不足状态。
- `operator` 可进入管理后台只读视图，涉及用户状态、工具启停、恢复删除资源等操作必须隐藏或禁用。
- `admin` 可看到完整管理入口和高风险操作确认。

## 5. 目录结构

推荐前端目录：

```text
foodmate-ui/
  src/
    app/
      router.tsx
      App.tsx
    layouts/
      WorkspaceLayout/
    pages/
      HomePage/
      ChatPage/
      AnalysisPage/
      PlanningPage/
      ProfilePage/
      AdminPage/
    components/
      common/
      feedback/
      workspace/
    features/
      agent/
      auth/
      profile/
      admin/
      session/
      analysis/
      planning/
    mock/
      sessions.ts
      auth.ts
      admin.ts
      agentEvents.ts
      analysis.ts
      mealPlans.ts
    styles/
      tokens.css
      iconfont.css
      motion.css
    types/
      agent.ts
      tool.ts
      session.ts
      ui.ts
    components/ui/
      button.tsx
      input.tsx
      dialog.tsx
      sheet.tsx
    lib/
      utils.ts
```

分层规则：

- `pages/` 只负责页面组合，不直接写复杂业务状态。
- `components/` 放通用 UI 组件，不绑定具体业务数据来源。
- `features/agent/` 放 AgentRun、ToolCall、mock event replay、状态映射。
- `features/auth/` 放登录态、token 刷新、路由守卫、角色权限映射。
- `features/profile/` 放个人资料、头像上传、修改密码相关状态。
- `features/admin/` 放管理后台概览、用户管理、运行审计、知识库和工具管理状态。
- `features/session/` 放会话列表、消息、Composer 相关逻辑。
- `mock/` 放静态数据和事件序列，Phase 2 不写死在组件里。
- `styles/` 放 FoodMate 语义化 design tokens、iconfont 注册和轻量动效；迁移完成前保留现有主题文件，不在本轮文档改造中删除。
- `components/ui/` 放 shadcn/ui 基础组件源码，只负责通用交互和可访问性。
- `lib/utils.ts` 放基础 `cn` 等无业务语义的小工具，不能承载页面编排。

### 5.1 样式共存与退场规则

迁移期间允许 Tailwind、CSS Variables、CSS Modules 和遗留 Arco 样式并存，但职责必须清晰：

| 样式层 | 可承担内容 | 禁止内容 |
|---|---|---|
| `tokens.css` | FoodMate 语义 Token、主题基础变量、字体和基础刻度 | 页面专用颜色、任意像素值和业务选择器 |
| Tailwind | `components/ui` 的 Variant、间距、尺寸、可访问状态和通用布局 | 复制业务页面的大段状态编排 |
| CSS Modules | Workspace、消息流、表格等复杂领域布局及局部响应式规则 | 覆盖 shadcn 基础组件内部样式或重建通用 Button/Input |
| `iconfont.css` | 字体声明和语义图标 class 的底层注册 | 在页面中直接暴露资源字码 |
| `arco-theme.css` 与 Arco 全局 CSS | 当前源码无运行时引用；历史兼容策略仅作为迁移记录保留 | 不再新增 Arco 依赖，继续使用 shadcn/Radix primitives |

同一 DOM 元素的颜色、间距、边框、圆角和交互状态只允许一个样式层拥有最终决定权。迁移某个页面时，先移除该页面对 Arco 组件和主题覆盖的依赖，再引入对应的 shadcn/ui 组件；禁止通过全局优先级覆盖让两套组件表面上同时生效。

## 6. shadcn/ui 使用策略

shadcn/ui 只负责可组合的基础控件和可访问交互，必须服务 FoodMate 的餐饮 Agent 工作台定位。

目标组件映射：

| shadcn/ui 组件 | FoodMate 用法 |
|---|---|
| `Button` | 发送、停止、确认、重试、快捷任务操作 |
| `Input` / `Textarea` | Composer 输入、搜索会话、追问回复 |
| `Dialog` | 高风险确认、保存计划、删除或恢复确认 |
| `Sheet` | 工具轨迹详情、引用详情、移动端侧栏 |
| `Tooltip` | 工具按钮、状态图标、不熟悉的操作 |
| `Tabs` | Tools / Agents 展开面板或管理视图占位 |
| `Table` | 餐食计划、分析明细、后续管理列表 |
| `Card` | 指标卡、结果卡、推荐任务卡的基础容器 |
| `Badge` | Agent 状态、Tool 状态、风险等级 |
| `Progress` | Agent 阶段推进、计划校验进度 |
| `Skeleton` | 页面加载和局部加载 |
| `Alert` / `Sonner` | 操作反馈、错误提示和非阻塞通知 |

必须自定义的场景：

- 工作台整体布局。
- 左侧会话栏。
- 底部 Composer。
- Tools / Agents 状态区。
- 消息流和 Agent 运行轨迹。
- ResultCard、CitationBlock、ClarificationCard、ConfirmationCard。
- FoodMate 的背景、卡片层级、餐饮相关色彩和轻量动效。

图标与矢量资产：

- 发送、停止、搜索、编辑、删除、复制、下载、刷新和展开等标准命令使用统一的代码图标组件，并提供 `aria-label` 和 Tooltip。
- Figma 或品牌资产存在准确的食物、营养、状态和品牌矢量时，使用 iconfont 的语义 class 或 React 封装；不在组件里直接写字码、Unicode 或随机内联 SVG。
- iconfont 没有对应资源时，使用许可明确的 Figma SVG 或代码图标，并登记资源来源、名称、尺寸和适配背景。

禁止事项：

- 不直接套用 shadcn 示例页面或其他后台模板。
- 不把所有内容都做成默认 Card。
- 不使用默认蓝色作为唯一主视觉。
- 不让组件库默认间距破坏 `1440 x 760` 单屏工作台结构。

### 6.1 现有组件迁移矩阵

| 当前代码区域 | 当前状态 | 目标处理 | 迁移依赖 |
|---|---|---|---|
| `styles/tokens.css`、`styles/global.css` | FoodMate Token 与 shadcn 语义变量已建立；当前源码无 Arco 运行时样式引用 | 继续以语义变量驱动基础组件和页面，避免回退到页面级硬编码 | Foundations、实际画板截图基线 |
| `layouts/WorkspaceLayout` | 工作台壳层已使用 Button/Input/Dialog/DropdownMenu/Tooltip | 保留领域布局，继续按 Figma 状态矩阵验收 | Sheet、Tooltip、导航 Token |
| `components/workspace/Composer`、`SidebarSessionList` | 高频用户操作已使用 Button/Input/DropdownMenu | 继续核对移动端触控、键盘和真实会话语义 | 图标边界、键盘交互 |
| `components/agent/*` | 领域状态组件 | 保留业务语义和数据 props，替换 Card/Badge/Alert/Progress 等呈现层 | 状态 Token、长文本规则 |
| `components/common/*`、`components/planning/*` | 通用展示和表格 | 按复用度拆分，先抽基础样式再替换页面引用 | Card/Table/Skeleton |
| `pages/HomePage`、`ChatPage` | 最高优先级用户工作流 | 在工作台壳层和 Composer 迁移后逐页重构 | SSE、会话、视觉基线 |
| `pages/ProfilePage`、`AnalysisPage`、`PlanningPage`、`KnowledgePage` | 用户业务页 | 复用已迁移基础组件后按路由推进 | Form、Table、Dialog |
| `pages/AdminPage` 及 `tabs/*` | 密集表格与高风险操作 | 最后迁移，先确保权限、确认和审计交互不变 | Table、Dialog、Badge、权限测试 |

每次只迁移一个可验证边界：基础组件、一个领域组件或一个页面。迁移完成前不得删除对应 Arco 依赖；迁移完成后先全仓搜索该边界的 Arco import，再进入下一边界。

## 7. 视觉与动效策略

视觉方向：

- shadcn/ui 控件的清晰度 + FoodMate 的温暖餐饮任务感。
- 浅色中性背景，搭配绿色、陶土橙、信息蓝和少量危险红。
- 页面应像一个每天可用的 Agent 工作台，而不是普通后台管理系统。

动效允许范围：

- 页面进入时的轻量 fade/slide。
- 首页任务卡 hover/focus 的轻微抬升和阴影变化。
- Agent 状态推进时的进度条和状态 Tag 过渡。
- 工具轨迹展开/收起时的高度或透明度过渡。
- 追问卡、确认卡出现时的轻量弹入。
- answer stream mock 输出时的文本逐步出现。

性能边界：

- Phase 1-2 优先使用 CSS transition / animation。
- 不引入 Framer Motion 等额外动画库，除非后续明确需要。
- 动效不阻塞首屏渲染。
- 底部输入区高度必须稳定。
- 消息流、工具轨迹和表格只允许局部滚动。
- 必须支持 `prefers-reduced-motion`，用户减少动态时关闭非必要动效。

## 8. Mock 数据与事件

Phase 1 mock 数据：

- sessions：会话列表、置顶、最近会话、当前会话。
- tasks：快捷任务、推荐问题。
- messages：用户消息、助手消息、结果卡。
- analysis：指标、趋势、目标、异常提示。
- mealPlans：约束、多日菜单、购物清单、预算、校验结果。

Phase 2 mock event sequence 必须模拟：

- `run.created`
- `run.routed`
- `run.clarification_requested`
- `run.planned`
- `run.retrieval_started`
- `run.retrieval_finished`
- `run.tool_started`
- `run.tool_finished`
- `run.answer_stream`
- `run.completed`
- `run.failed`
- `run.cancelled`

状态规则：

- AgentRun 状态和 UI 状态必须遵循[UI 设计规范](../设计/UI设计规范.md)的映射表。
- ToolCall 状态至少覆盖 `pending`、`running`、`success`、`failed`、`timeout`、`cancelled`。
- 停止按钮触发后进入 `cancelled`，停止追加 mock 工具和答案事件。
- 追问流程进入 `waiting_user`，用户补充后回到 `planning`。
- 确认流程未确认前不能展示“已保存”。

## 9. 状态管理

Phase 1：

- 页面使用静态 mock 数据和组件 props。
- 不引入全局状态库。

Phase 2：

- 优先使用 React state / reducer 管理 mock event replay。
- 建议把事件回放封装为 `useMockAgentRun` 或等价 hook。
- 如果后续 mock 交互跨页面共享明显变复杂，再评估 Zustand 等轻量状态库。
- 不提前引入 Redux、MobX 或复杂状态框架。

真实认证接入阶段：

- Access Token 优先保存在内存状态，不写入 localStorage。
- Refresh Token 由 HttpOnly Cookie 承载，前端不读取明文 refresh token。
- `auth/me` 结果作为用户菜单、路由守卫、管理入口和权限展示的数据源。
- 头像上传、资料保存、修改密码等操作都使用真实接口反馈，不伪造已保存状态。

个人资料页规划：

- 展示并编辑昵称、头像、邮箱、身高、体重、活动水平、营养目标、忌口、过敏原和单位偏好。
- 头像上传使用图片预览、替换确认和删除入口。
- 修改密码需要输入旧密码和新密码。
- 用户不能修改自己的角色和账号状态。

管理后台页面规划：

- 概览：AgentRun 数量、失败率、工具调用量、模型用量、知识库索引状态。
- 用户管理 `/admin/users`：用户列表、详情、启用、禁用、锁定、重置登录会话；该页按后端契约仅 `admin` 可访问。
- Agent 运行 `/admin/runs`：AgentRun、ToolCall、SQLAudit、Trace 查询，ModelUsage 单独放在 `/admin/usage`。
- 工具调用 `/admin/tools`：工具注册表、版本、入参 schema、启停、风险等级和权限范围。
- 模型用量 `/admin/usage`：供应商、模型、场景、token、成本和耗时。
- 知识库 `/admin/knowledge`：文档列表、详情、上传、下线、恢复、索引状态。
- 软删除资源 `/admin/deleted`：查看软删除资源并恢复，仅 `admin` 可操作。

管理后台接口与页面对照：

| 后端能力 | 前端体现 |
|---|---|
| `/foodmate/admin/overview` | `/admin` 概览指标、运行审计摘要、治理资源卡片 |
| `/foodmate/admin/users`、`/foodmate/admin/users/{user_id}` | `/admin/users` 用户列表和用户详情侧栏，仅 `admin` |
| `/foodmate/admin/users/{user_id}/status` | `/admin/users` 行内锁定、禁用、启用类按钮，二次确认和审计提示 |
| `/foodmate/admin/users/{user_id}/sessions/reset` | `/admin/users` 重置会话按钮和登录会话表 |
| `/foodmate/admin/agent-runs` | `/admin/runs` 的 AgentRun 标签页 |
| `/foodmate/admin/tool-calls` | `/admin/runs` 的 ToolCall 标签页 |
| `/foodmate/admin/sql-audits` | `/admin/runs` 的 SQLAudit 标签页 |
| `/foodmate/admin/model-usage` | `/admin/usage` 模型调用明细表 |
| `/foodmate/admin/knowledge-documents` | `/admin/knowledge` 文档列表、详情和索引状态 |
| `/foodmate/admin/knowledge-documents` 写入接口 | `/admin/knowledge` 上传弹窗、下线/恢复二次确认 |
| `/foodmate/admin/tools`、`/foodmate/admin/tools/{name}/status` | `/admin/tools` 工具列表、详情、启停二次确认 |
| `/foodmate/admin/deleted-resources`、restore | `/admin/deleted` 软删除列表和恢复二次确认，仅 `admin` |
| 管理写操作审计 | `/admin`、`/admin/users`、`/admin/tools`、`/admin/knowledge`、`/admin/deleted` 展示 mock 审计表或审计提示 |

## 10. 验收标准

Phase 1 完成时：

- 首页、会话页、分析页、规划页可本地访问。
- 页面结构符合 SVG 原型的信息层级。
- shadcn/ui 基础组件和 FoodMate 自定义样式融合，不呈现通用后台模板感。
- 桌面端首屏能看到侧栏、主内容、Tools/Agents、Composer。
- 移动端不横向溢出。

Phase 2 完成时：

- 无后端时可演示一轮完整 Agent mock 任务。
- Agent 状态、工具轨迹、引用、答案流同步变化。
- 追问、确认、失败、取消都有明确 UI。
- mock 事件名与[接口与数据规范](../契约/接口与数据规范.md)中的 `run.*` SSE 事件一致。
- 后续接真实 SSE 时，不需要重写核心 UI 组件。

认证与管理页面接入完成时：

- `/profile` 能展示和编辑当前用户资料，头像上传路径与后端接口一致。
- `/admin` 只对 `admin/operator` 可见，普通用户访问时展示 403。
- `/admin` 使用独立后台布局，不复用 Agent 工作台会话侧栏。
- `/admin` 左侧管理导航点击后能切换到用户管理、Agent 运行、工具调用、模型用量、知识库和软删除资源子路由。
- `operator` 访问用户管理和软删除资源时展示 admin-only 权限状态。
- 管理写操作按钮必须打开二次确认，提交后展示审计写入反馈。
- 顶部用户菜单包含个人资料、管理后台和退出登录；普通用户不显示管理后台入口。
- 401 触发 refresh，refresh 失败回到 `/login?redirect=...`。
- 403 展示权限不足，不自动重试。

## 11. 当前前端对照审计

已覆盖：

- 首页、会话页、分析页、规划页、知识库占位页和登录入口可访问。
- 首页快捷任务和推荐任务可带入 mock prompt 并进入会话页。
- 会话页覆盖计算、记录、分析、规划、知识问答、追问、确认、失败和取消状态。
- 工具轨迹支持状态、耗时、错误和输入输出摘要展开。
- 分析页蛋白质目标采用 `体重 kg × 1.5-2.0` 的 mock 配置，并展示推荐区间。
- 规划页保留约束摘要、菜单、购物清单、预算和保存确认。
- 登录页展示 mock 用户资料、营养目标、权限范围、登录状态、注册入口和忘记密码入口。
- `/profile` 已实现 mock 个人资料页，覆盖昵称、头像、邮箱、营养目标、忌口、过敏原、单位偏好和修改密码入口。
- `/admin` 已实现 mock 管理后台，覆盖概览、运行审计、用户管理、治理资源、筛选和分页占位。
- `/admin/users` 已补充用户详情、登录会话、状态变更和重置会话 mock 操作。
- `/admin/runs` 已补充 AgentRun、ToolCall、SQLAudit、Trace 四个查询标签页。
- `/admin/tools` 已补充工具详情、入参 schema、风险等级和启停二次确认。
- `/admin/knowledge` 已补充文档详情、索引进度、上传弹窗和下线/恢复二次确认。
- `/admin/deleted` 已补充软删除资源恢复二次确认。
- 管理写操作已补充 mock 二次确认和审计反馈；`operator` 保持只读。
- 顶部用户菜单已包含个人资料、管理后台和退出登录占位；管理后台入口仅对 `admin/operator` 显示。
- 移动端已避免横向溢出，内部滚动条默认隐藏。

仍待后端或后续 Phase 完成：

- `/login`、真实认证路由守卫、Cookie/CSRF 和会话撤销已在 M1-2 接入；密码找回等非核心账户流程按账户实施范围继续补齐。
- `/profile` 的基础真实资料读写已接入；头像对象存储和尚未实现的个人数据扩展继续按 M1-1 后续范围建设。
- `/admin` 管理壳层、概览、工具注册表、软删除资源、权限矩阵和操作审计页面已完成 G5-1 至 G5-7 的 UI 代码迁移；概览、工具和软删除资源在 `VITE_AGENT_MODE=real` 下复用管理面板接口或恢复接口，其他用户管理、运行审计、知识库和模型用量页面仍保留 mock fallback、可选字段或明确空态。
- `/knowledge` 目前是知识库占位页，真实文档导入、检索、ACL 过滤和引用详情需等待 RAG/知识库接口。
- Tools / Agents 底部状态目前显示计数，完整展开面板可在真实工具注册接口接入后完善。
- 会话搜索、归档、恢复、删除和历史分页已在 M1-2 接入真实 API；后续重点是大数据量、竞态和浏览器回归覆盖。
- M1-3 已接入真实 AgentRun SSE、取消和续传；M1-4 的预算追加、continuation、`superseded`、协调故障友好提示、Eval 延迟正文和安全降级展示已完成本地验证，生产级浏览器回归和真实云长稳仍待完成，风险清单见[当前实现审计与完善计划](./当前实现审计与完善计划.md)。

## 12. M1-4 Agent 治理交互

本节定义后续目标交互，当前前端尚未全部实现。

### 12.1 Runtime 协调异常

- 后端返回 HTTP 503、`error_code=RUNTIME_COORDINATION_UNAVAILABLE` 时，显示“系统暂时异常，请稍后重试”，不得显示 Redis、permit、lease 等内部实现细节。
- 已有 `run_id` 时先查询权威 Run 状态，不自动创建第二个 Run；只有服务端明确尚未创建 Run 时才允许自动重试一次。
- 登录、历史会话和消息查询仍可使用时，不得把整个页面伪装成离线。

### 12.2 预算提示与追加确认

- 70%：显示非阻塞预算提示，不打断当前回答。
- 85%：显示已进入节省模式及可能减少检索、Reflection 或改用经济模型。
- 100%：暂停新模型调用，展示当前已完成内容、可信部分结果、追加 Token/成本和确认按钮。
- 用户确认必须展示本次追加上限；提交时携带服务端确认令牌或摘要，按钮防重复提交，不自动重放。
- 用户拒绝、确认过期或追加达到服务端上限时，展示已有可信结果或明确终止原因，不伪造完整答案。

### 12.3 补参、审批与 continuation

- 普通缺参时，用户补充消息创建新的 continuation Run；UI 保留父 Run 追问和新 Run 的关联，但只订阅当前 active Run。
- 旧 Run 的目标状态 `superseded` 显示为“已由后续任务接续”，不显示为失败或取消；在契约和前端映射落地前不得提前解析该状态。
- 工具审批和预算追加恢复原 Run，并产生新的 dispatch attempt；UI 仍展示同一个业务任务，不创建第二张用户任务卡。
- 用户明显改变目标时按普通新任务处理，不能把旧任务的已确认动作自动带入。

### 12.4 Eval 后交付与安全降级

- Final Eval 通过前只展示业务进度，不渲染候选答案正文。
- `result_type=safety_degraded` 时展示安全说明和“请咨询医生或注册营养师”，不得展示被拦截的高风险候选答案。
- 当前没有人工审核人员，不展示“等待审核人员处理”或无限等待状态。
- 取消结果使用确定性摘要展示已完成、未完成和状态未知步骤，不在取消后继续追加模型生成内容。

## 13. Figma 重构前置执行顺序

本阶段的完成标准是文档和设计交付约定清晰；代码迁移已经按可验证边界开始推进。后续按以下顺序推进：

1. 在 Figma `01 Foundations` 固化颜色、文字、间距、圆角、阴影和状态 Token，并确认 CSS 变量命名。
2. 在 Figma `02 Components - User`、`03 Components - Admin` 完成 shadcn/ui 基础组件和 FoodMate 领域组件的 Variant、状态与代码路径映射。
3. 完成 iconfont 资源清单，确认标准命令图标、领域图标和 Figma SVG 的使用边界。
4. 先迁移 `components/ui` 和 Token 层，再按页面逐步替换布局与业务组件；每个页面以 Figma 画板和状态矩阵做视觉验收。当前管理端已完成 G5-1 至 G5-7 的代码边界和本地权限测试，但不能据此关闭真实后端授权或 Figma 像素级验收。
5. 迁移期间保留真实接口、SSE 事件和业务状态契约，不因 UI 重构改变 API、权限或错误码语义。

## 14. 分阶段迁移与回退门槛

| 阶段 | 交付边界 | 进入下一阶段的条件 | 回退方式 |
|---|---|---|---|
| G0 基线 | 记录当前路由、关键状态、测试结果和目标视口截图 | 基线可重复生成，已标记真实接口与 mock 页面 | 不改代码，仅补齐基线 |
| G1 基础设施 | 别名、Tailwind、shadcn 配置、Token、`cn`、图标封装 | 构建、类型检查、基础组件键盘交互通过 | 移除新入口，保留 Arco 页面 |
| G2 基础组件 | Button/Input/Textarea/Dialog/Sheet/Tooltip/Tabs 等 | 组件状态、焦点和移动端触控规则通过 | 保留旧组件引用，不扩散到页面 |
| G3 工作台 | WorkspaceLayout、Composer、会话列表、Agent 基础卡片 | 首页和会话的真实 SSE/取消/续传语义不变 | 以路由或组件边界恢复旧工作台 |
| G4 用户业务页 | 分析、规划、知识库、个人资料 | 真实接口、错误态、空态和响应式通过 | 按页面独立回退 |
| G5 管理端 | 管理壳层、表格、确认、权限视图和操作审计 | 前端 admin/operator/普通用户矩阵与本地测试通过；真实接口授权仍需验收 | 按管理路由回退 |
| G5-4 操作状态页 | 管理写操作的无权限、确认、提交中、成功、失败状态 | 状态机、错误码、request_id、审计结果和四视口通过 | 只回退 G5-4 状态组件，不回退已完成的管理表格 |
| G5-5 用户详情页 | 用户资料、饮食画像、登录会话、业务会话、操作历史五个 Tab | 详情数据契约、operator 只读、admin 操作状态和四视口通过 | 只回退用户详情组件，不回退用户列表和 G5-4 状态组件 |
| G5-6 Run / Tool Call / Trace | 四个治理 Tab、统一筛选、详情 Sheet、跨记录追踪和真实模式空态 | 数据字段、Tab URL、详情联动、mock/real 边界、测试和四视口通过 | 只回退 `RunsTab` 与治理数据契约，不回退 G5-1 至 G5-5 |
| G6 清理 | 删除 Arco import、主题覆盖和无引用依赖 | `rg`、typecheck、test、build 已通过；不等同于 Figma 全量视觉重构完成 | 不再依赖 Arco，必要时使用版本控制回退 |

G6 清理门槛已于 2026-08-10 完成：`foodmate-ui/package.json` 不包含 `@arco-design/web-react`，`src/main.tsx` 无 Arco 样式入口，源码搜索未发现 Arco 组件 import 或 `arco-theme` 引用。后续不得重新引入 Arco；本状态不代表所有页面已完成 Figma 实际画板对照。

## 16. 2026-08-12 生产字体、Figma 像素验收和 iconfont 资产门槛

### 16.1 生产字体包

文字字体已经进入生产构建链：

- `@fontsource/noto-sans-sc@5.3.0`：400/500/700/900，用于中文界面和正文。
- `@fontsource/space-mono@5.3.0`：400/italic/700，用于 ID、数值、时间、引用和技术状态。
- `@fontsource/montserrat@5.3.0`：900，用于 Figma Login 品牌字标。

入口文件为 `foodmate-ui/src/main.tsx`。字体包不等于 iconfont 领域资产；两者必须分开登记、分开验收。生产构建必须检查 `dist/assets` 中存在对应 woff2 文件，并在字体加载前后检查布局不能发生关键控件位移。

### 16.2 Figma 全量回读与前端映射

Figma 文件内部验收已经完成：Design 页 105 张顶层画板、19,985 个递归节点、1,940 条 Prototype reaction、无效目标 0、截图 105/105；Foundations 有 5 组 Variables，User/Admin 分别有 24/14 个 Component Set。

前端自动化像素 diff 不是对 105 张画板的自动补全。当前 105 个页面/状态均已具备独立前端截图映射，汇总为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`；所有结果都必须继续保留 `DIFF_REVIEW`，没有达到证据门槛时不得写成 PASS。页面级无横向溢出通过不能替代像素验收。

完整数字、映射表和截图目录见 [Figma前端像素级验收报告](../功能实现说明/Figma前端像素级验收报告.md)。

### 16.3 iconfont 实体资源门槛

当前 [iconfont实体资源登记](../设计/iconfont实体资源登记.md) 为 `BLOCKED`。在收到真实字体包、CSS 映射、来源 URL、许可证、授权范围和 Figma 字形映射前：

- 不创建 `@font-face`。
- 不猜测 `.icon-*` 类名或 Unicode codepoint。
- 标准命令图标继续使用 Lucide。
- FoodMate 领域图标可使用来源和授权可追溯的 SVG 作为回退。

实体资源收到后，必须通过一个语义 icon wrapper 接入，并完成字体哈希、构建产物、16/20/24px、浅色/深色背景、加载失败、Tooltip 和 `aria-label` 验收。

### 16.4 已有后端的前端闭环边界

真实模式允许继续接入已有接口，当前已确认的闭环为：

| 前端能力 | 真实接口边界 |
|---|---|
| 登录、注册、登出、当前用户 | `/api/auth/*`、`/api/users/me` |
| 个人资料、密码、会话、导出、注销、头像 | `/api/users/me/*` |
| 记忆列表、确认、编辑、逻辑删除 | `/api/memories/*` |
| Session、消息、搜索、归档、恢复、删除 | `/api/sessions/*` |
| Chat Run、AgentRun SSE、取消、预算追加、checkpoint 恢复 | `/api/chat/runs/*`、`/api/agent-runs/*` |
| Admin 概览、用户状态/会话撤销、工具状态、知识库状态、资源恢复 | `/api/admin/*`、`/api/admin/knowledge/*` |

饮食记录、摄入分析和餐食规划目前未发现对应的后端 Controller；前端不得把静态数据改写成真实成功结果。真实模式应使用空态或明确的“接口尚未接入”状态，待后端 Controller、DTO、权限和测试具备后再接入。

## 15. 前端重构验收与回归

每个迁移边界至少执行下列检查；文档阶段只定义标准，不宣称这些检查已经完成。

| 类别 | 最低检查 |
|---|---|
| 静态质量 | `npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run test`、`npm run build` |
| 视觉 | 1440x900、1366x768、1024x768、390x844 截图与 Figma 对照；检查溢出、遮挡、固定 Composer 和局部滚动 |
| 关键流程 | 登录、发起任务、SSE 执行、停止、追问、确认、错误恢复、会话归档/恢复、管理写操作确认 |
| 权限与安全 | 普通用户、operator、admin 的路由和操作矩阵；401 refresh、403、请求失败和敏感信息不泄露 |
| 可访问性 | Tab 顺序、focus-visible、Esc 关闭 Dialog/Sheet、焦点归还、图标按钮名称、错误文本和 reduced motion |
| 资产 | iconfont 字码不直接出现在页面、字体加载失败有可用后备、SVG 来源和授权可追溯 |

视觉验收以 Figma 的指定画板和状态为准，不要求逐像素复制浏览器字体渲染差异；但不接受结构缺失、内容遮挡、文字截断、状态不一致、不可操作按钮或关键焦点不可达。

### 14.1 G5-4 管理操作状态页实现记录

- Figma 依据为 G5-4 五个状态节点：无权限 `692:4319`、确认操作 `692:4539`、提交中 `692:4766`、成功 `692:4995`、失败 `692:5207`；页面基准为 `1440x1024`，侧栏 `260px`、顶栏 `64px`、内容边距 `32px`。
- 状态契约集中在 `pages/AdminPage/tabs/types.ts`：`idle`、`no-permission`、`confirm`、`submitting`、`success`、`failed`。状态转换由 `AdminPage.tsx` 持有，所有管理写操作继续经过原有确认和审计入口。
- 状态呈现集中在 `pages/AdminPage/tabs/AdminOperationStatus.tsx`，复用 shadcn `Dialog`、`Alert`、`Card`、`Button` 和 Lucide 图标；遮罩固定为 `rgba(51,51,51,0.4)`，弹窗尺寸分别约束为确认 `480x344`、提交中 `480x288`、失败 `480x338`。
- 工具注册表配置详情打开后可以发起启停操作。管理员依次看到确认、提交中和成功状态；operator 触发写操作时展示无权限横幅；真实接口异常进入失败弹窗，并展示 `ERROR_CODE` 与 `REQUEST_ID`。工具注册表失败兜底值保留 Figma 样例 `REGISTRY_TIMEOUT_504` / `req-foodmate-9082ac918`，不等同于真实后端已经稳定返回该错误。
- 失败操作会写入 `result=failed` 的操作审计记录；成功操作保持原有 `result=success`、`request_id` 和 `trace_id` 记录。重试不会自动重放，只有用户点击“重新尝试”才会再次提交。
- 回归入口为 `AdminOperationStatus.test.tsx`、`ToolsRegistryTab.test.tsx` 和既有 `AdminPage.test.tsx`。本边界定向状态测试为 `9/9`，全量前端测试为 `50/50`，`npm run typecheck`、`npm run build`、本次改动文件的定向 ESLint/Prettier 和 `git diff --check` 均通过。浏览器已检查 `1440x1024`、`1366x768`、`1024x768`、`390x844` 的页面宽度与弹窗边界；全仓 `format:check` 仍被 10 个既有文件阻塞，全仓 lint 仍有既有 Prettier/React Hook 警告，不能写成全仓静态检查已清零。
- 本边界未导入未经确认的 iconfont 字体包；标准命令图标继续使用 Lucide。iconfont 实体资源登记和 Figma 实际流程画板仍是独立后续任务。
- [x] G5-1 管理概览、G5-2 工具注册表、G5-3 删除资源、G5-4 管理操作状态页、G5-5 用户详情、G5-6 Run / Tool Call / Trace 和 G5-7 操作审计均已形成独立代码边界；前端普通用户/operator/admin/superadmin 权限矩阵已通过本地测试。

### 14.6 Figma Admin 实际画板验收（2026-08-10）

此前本节中关于 `1064:*` Admin Flow 占位卡的记录属于历史检查结果，当前以 Figma `🎨 :: Design` 页面中的实际画板为准。已完成截图和 Plugin API 回读的核心节点为：`995:977` Admin Overview、`801:215` Admin User Detail、`692:4539` Admin Operation Confirmation、`995:1499` Admin Operation Audit、`797:212` Admin Run Detail、`797:359` Admin Tool Calls、`797:490` Admin SQL Audit、`797:621` Admin Trace。

- 8 张画板尺寸均为 `1440x1024`，Admin 壳层保持 `260px` 侧栏、`1180px` 主区；筛选、表格、详情、弹窗和追踪区域均有实际内容，不再使用 `260x112` 流程占位卡作为像素验收依据。
- Figma 回读的字体集合已统一为 `Noto Sans SC` 和 `Space Mono`；Admin 画板中原有的 `Inter` 片段已按文件可用字重映射为 Noto Sans SC，Prototype 中完全重复的 reactions 已清理。
- 核心颜色以 Figma 实际值为准：`#FCFCFC`、`#FFFFFF`、`#F4F6F5`、`#333333`、`#6B7280`、`#FF7576`、`#D67676`、`#C79654`、`#5DA9B2`、`#89B27C`。弹窗遮罩、上传进度填充等覆盖关系属于设计定义的叠层，不视为非预期重叠。
- iconfont 实体字体包、字形类名、来源和授权仍未确认，继续保持 `BLOCKED`；标准命令图标使用 Lucide，禁止在代码中写入未登记的字体字码。
- Figma 画板验收完成不代表真实管理 API、服务端授权、并发冲突和生产失败重试完成；这些仍需接口级和生产环境验收。

### 14.7 Figma 用户业务首页实际画板验收（2026-08-10）

本轮开始按实际用户业务画板推进 Figma 对照验收，首个节点为 `workspace-home-v2`（`640:256`）。该画板为 `1440x1024`，采用 `260px` 侧栏和 `1180px` 主区，实际展示问候、主输入器、高频任务、今日营养摘要、近期任务、待确认队列和任务入口/状态说明。

- [x] Plugin API 回读共 255 个节点；页面为横向布局，画板尺寸和主区边界稳定，可见文本无零尺寸、越界或裁切。
- [x] 截图复核通过：侧栏、顶栏、输入器、摘要卡、近期任务和待确认卡均有实际内容，未发现非预期的可见同级重叠。
- [x] 字体已统一为 Figma 文件中实际可用的 `Noto Sans SC` 与 `Space Mono`；`Inter` 遗留为 0。中文与界面文本使用 Noto Sans SC，营养数字和百分比使用 `Space Mono Bold`。
- [x] 颜色和布局以 Figma 实际值为准，核心回读包含 `#FCFCFC`、`#FFFFFF`、`#F4F6F5`、`#333333`、`#6B7280`、`#C79654`、`#A6D997`、`#ECCCFF`、`#80E0E6` 和 `#FF7576`；没有以现有前端样式反推 Figma。
- [x] Prototype 目标回读有效，加载、空态、错误态、知识库空态、会话搜索、Agent 空态/对话、历史分页、饮食记录、摄入分析、餐食规划和输入器状态目标均存在。
- [ ] 本记录只关闭 `workspace-home-v2` 的 Figma 画板边界，不代表用户业务页面全部完成，也不改变 iconfont `BLOCKED`、真实后端授权、并发冲突和生产验收状态；下一张画板按计划检查 `agent-chat-v2`。

### 14.8 Figma Agent 对话页实际画板验收（2026-08-10）

本轮完成 `agent-chat-v2`（`640:428`）的实际画板对照。画板为 `1440x1024`，采用 `260px` 侧栏、`860px` 对话区和 `320px` 运行轨迹区，包含确认卡、引用、运行步骤和停止运行入口。

- [x] Plugin API 回读共 229 个节点；11 个 Agent 状态、对话操作、确认操作、会话搜索、侧栏分页和跨页面入口均可解析。
- [x] 字体已统一为 `Noto Sans SC`、`Space Mono Italic` 和 `Space Mono Bold`，`Inter` 遗留为 0；引用保留斜体，RUN ID/毫秒耗时保留等宽数据语义。
- [x] 修复回复气泡内部内容层错误使用 `HUG` 导致的英文回答裁切：内容层改为填充父气泡，正文设置 `528px` 固定宽度和高度换行；截图确认正文、引用、确认卡完整显示。
- [x] 可见文本无零尺寸、越界或裁切；隐藏 `structure::screen-map` 的区域交叠不计为页面缺陷。
- [x] Prototype 目标全部存在，覆盖 11 个 Agent 状态以及工作台、会话搜索、历史分页、饮食记录、摄入分析、餐食规划和停止运行画板。
- [ ] 本记录只关闭 `agent-chat-v2` Figma 画板边界，不代表 Agent 真实 SSE、工具执行或后端状态闭环完成；下一张按计划检查 `diet-records-v2`。

### 14.9 Figma 饮食记录页实际画板验收（2026-08-10）

`diet-records-v2`（`640:588`）已完成实际画板对照。画板为 `1440x1024`，覆盖日期切换、日/周视图、四项营养目标、早餐/午餐记录、已确认/待确认状态、记录详情和自然语言记录入口。

- [x] Plugin API 回读共 273 个节点，核心内容和操作入口均有实际节点；加载、空态、错误态以及跨用户页面 Prototype 目标全部存在。
- [x] `Inter` 遗留为 0；界面文本使用 Noto Sans SC，营养大数/百分比使用 `Space Mono Bold`，`/ 目标单位` 与 `C/P/F` 宏量字段使用 `Space Mono Regular`。
- [x] 截图复核通过，四项营养卡、早餐/午餐记录、待确认项和详情卡无可见文本裁切、越界或零尺寸。
- [x] 记录详情卡和 `records-actions` 覆盖层的同区域叠层属于交互结构，截图中没有重复可见控件；营养环与百分比的覆盖属于定义叠层。
- [ ] 本记录只关闭 `diet-records-v2` Figma 画板边界；真实饮食记录接口、估算状态和软删除恢复仍需按后端契约独立验收，下一张检查 `intake-analysis-v2`。

### 14.10 Figma 摄入分析页实际画板验收（2026-08-10）

`intake-analysis-v2`（`640:773`）已完成实际画板对照。画板为 `1440x1024`，覆盖时间范围/餐次筛选、日均能量与蛋白质、活跃记录天数、目标对比图、Agent 洞察、制定计划和导出质量说明。

- [x] Plugin API 回读共 218 个节点，筛选、指标、图表、洞察、数据质量和导出入口均有实际节点；加载、空态、错误态以及跨页面 Prototype 目标全部存在。
- [x] `Inter` 遗留为 0；摘要数值 `1,940 kcal`、`114 g`、`6 / 7 Days` 使用 `Space Mono Bold`，其余界面文本使用 Noto Sans SC。
- [x] 截图复核通过，筛选、摘要、目标对比图、洞察卡和质量说明区无可见文本裁切、越界、零尺寸或非预期重叠。
- [ ] 本记录只关闭 `intake-analysis-v2` Figma 画板边界；真实分析统计、导出任务和 Agent 解读接口仍需独立验收，下一张检查 `meal-planning-v2`。

### 14.11 Figma 餐食规划页实际画板验收（2026-08-10）

`meal-planning-v2`（`640:901`）已完成实际画板对照。画板为 `1440x1024`，采用 `840px` 计划区和 `340px` 约束/购物侧栏，覆盖计划头部、五日餐表、空槽、约束校验、Pass/Review 状态和购物清单预览。

- [x] Plugin API 回读共 293 个节点，核心计划内容和跨页入口均有实际节点；计划列表、加载、空态、错误态和购物清单目标全部存在。
- [x] `Inter` 遗留为 0；界面文本使用 Noto Sans SC，`420 kcal` 营养单位使用 `Space Mono Regular`。
- [x] 餐卡长名称的 `ENDING` 省略为节点明确配置，截图无意外文本裁切；页面无可见文本零尺寸、越界或非预期重叠。
- [ ] 本记录只关闭 `meal-planning-v2` Figma 画板边界；计划生成、冲突解决、购物清单和真实后端任务仍需独立验收，下一阶段进入知识库与个人中心画板。

### 14.2 G5-5 用户详情页实现记录

- [x] `/admin/users` 已扩展为用户列表 + 用户详情侧栏；详情使用项目现有 shadcn `Tabs`、`Card`、`Table`、`Badge` 和 Lucide 图标，包含 `资料`、`饮食画像`、`登录会话`、`业务会话`、`操作历史` 五个业务 Tab。
- [x] 用户资料和饮食画像补齐用户 ID、账号资料、角色/状态、注册/登录信息、失败次数、锁定时间、身高、体重、活动水平、饮食目标、热量/蛋白质目标、过敏原、忌口和单位等字段；登录会话、业务会话和操作历史均有明确的数据表或空态。
- [x] `operator` 可进入并查看用户详情；状态变更和撤销会话按钮保持禁用。`admin/superadmin` 的锁定、禁用、启用和撤销全部会话继续通过 `AdminPage` 已有的 `confirm -> submitting -> success/failed` 状态机和审计记录，不在详情组件内复制写操作逻辑。
- [x] 已按 Figma `801:215` 收敛到 `1440×1024` 画板几何：主内容内边距 `24px`，用户列表 `692px`，详情卡 `420px`，列表行 `60px`，详情卡内边距 `20px`；mock 数据与 Figma 的 `usr_098a1`、`usr_112b9`、`usr_774x2`、`usr_889d4` 对齐。
- [x] `/admin/users` 的独立像素证据已登记：`admin-user-detail-figma.png`、`admin-user-detail-browser.png`、`admin-user-detail-browser-rgba.png`；同尺寸 diff 为 `22.36% / RMSE 17.62`，保持 `DIFF_REVIEW`。
- [x] Figma 的“重置凭证”尚无已有后端接口，前端只显示未接入提示，不伪造成功；“撤销所有会话”继续调用既有真实接口并经过统一确认与审计。
- [x] mock 模式提供详情 Tab 的可重复样例数据；`VITE_AGENT_MODE=real` 下只展示真实用户列表返回的数据，未接入真实详情接口的会话和历史数据展示明确空态，不伪造后端成功结果。
- [x] 新增 `UsersTab.test.tsx` 三条回归测试，覆盖五个 Tab、Tab 切换数据和禁用操作确认入口；全量前端测试为 `53/53`，`npm run typecheck`、`npm run build`、本次变更文件的定向 ESLint/Prettier 均通过。
- [x] 浏览器验收覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`：`document/body/root` 无页面级横向溢出；移动端业务会话 Tab 可切换；确认禁用弹窗实测 `480x344`。
- [x] 历史检查曾将 `1064:21` 读取为 `260x112` 的 `Admin Flow / User Detail` 流程占位卡；当前已由实际画板 `801:215` 替代，并完成五 Tab 详情页的截图和 Plugin API 回读，具体见 14.6。
- [ ] iconfont 实体资源登记和真实后端授权继续保持未完成；shadcn 基础设施与 G6 Arco 清理的代码边界已完成。

### 14.3 G5-6 Run / Tool Call / Trace 实现记录

- [x] `/admin/runs` 已拆分为 `AgentRun`、`ToolCall`、`SQLAudit`、`Trace` 四个治理 Tab；统一筛选支持 Run ID、用户、Session ID、Trace ID、状态、结果类型和错误码，Tool Call、SQL Audit、Trace 复用同一追踪关键词和状态筛选。
- [x] Run、Tool Call、SQL Audit、Trace 的列表字段已扩展到当前契约可确认的范围：阶段、结果类型、错误码、模型、请求 ID、输入/输出脱敏摘要、SQL 策略/查询哈希/返回行数、Trace 根服务/Span 数量/链路节点等。真实接口未返回的字段统一显示 `-`，没有新增未经确认的后端 endpoint。
- [x] 所有治理记录均可打开 shadcn `Sheet` 详情；Run 详情可进入关联 Tool Call，Tool Call/SQL Audit 可进入关联 Trace，Trace 可回到关联 Run；ID 复制使用 Lucide `Copy`，标准命令图标没有引入未经登记的 iconfont。
- [x] `?tab=sql`、`?tab=trace` 和 `?tab=tool` 均能直接定位对应 Tab。真实模式的 Trace 由现有 dashboard 契约的可选 `traces` 字段承载，未接入时显示明确空态；mock 模式仅使用本地可追溯样例数据。
- [x] 新增 `RunsTab.test.tsx` 三条测试，覆盖筛选重置、Tab URL、Run -> Tool Call -> Trace、SQL Audit 详情；本次全量前端测试为 `56/56`，`npm run typecheck`、`npm run build` 和治理改动文件定向 ESLint/Prettier 通过。
- [x] 浏览器验收已覆盖 `1440x1024`、`1366x768`、`1024x768`、`390x844`；四个视口 `document.body.scrollWidth === innerWidth`，移动端筛选区改为单列，宽表只在表格容器内横向滚动；详情 Sheet 在移动端宽度不超过视口。
- [x] 历史记录曾将 `1064:*` Admin Flow 作为流程占位卡；该结论已由 2026-08-10 的实际画板验收替代，当前 8 张 Admin 核心画板的节点、截图和回读记录见 14.6。

### 14.4 G5-7 权限矩阵与操作审计实现记录

- [x] `resolveAdminAccess` 已明确区分 `user`、`operator`、`admin` 和 `superadmin`：普通用户不可进入管理后台，operator 只读，admin/superadmin 可管理。
- [x] operator 可查看用户详情、运行、工具、知识库和模型用量；用户状态、会话撤销、工具启停、知识库写操作、软删除资源恢复和操作审计入口保持禁用或锁定。
- [x] admin/superadmin 的管理写操作、资源恢复和操作审计查看继续经过既有 `confirm -> submitting -> success/failed` 状态机；成功、失败均保留 request/trace 链路和审计结果。
- [x] `/admin?view=audit` 已独立渲染操作审计页面，提供动作、目标、结果、时间和关键词筛选、分页、真实模式空态和只读详情 Dialog；详情包含请求摘要、前后状态、错误码和客户端信息。
- [x] 新增 `OperationAuditTab.test.tsx`，管理端定向测试 `7/7`，全量前端测试 `60/60`；`npm run typecheck` 和 `npm run build` 通过。
- [x] 浏览器实际检查桌面 `1440x1000` 与移动 `390x844`：筛选、关键词查询、详情 Dialog 可操作；页面无横向溢出，表格仅在自身容器内滚动，详情 Dialog 未超出移动视口，控制台无 error/warning。

### 14.5 G6 Arco 清理与 shadcn 基础设施边界

- [x] `foodmate-ui/package.json` 不包含 `@arco-design/web-react`；`src/main.tsx` 不包含 Arco 主题入口；源码搜索未发现 Arco 组件 import 或 `arco-theme` 引用。
- [x] shadcn/Radix 风格组件集中在 `src/components/ui`，FoodMate Token 集中在 `src/styles/tokens.css`，标准命令图标使用 Lucide；此边界不虚构 iconfont 资源。
- [ ] iconfont 实体字体包、最终字形类名、资源来源、授权范围和实际 Figma 资产仍缺失，登记状态保持 `BLOCKED`。
- [x] Figma Admin 实际画板首轮验收已完成；旧 `1064:*` 流程占位卡不再作为当前像素级验收依据。iconfont 登记和真实管理数据闭环仍保持未完成。
- [ ] “Arco 已清理”只代表运行时依赖和主题入口已清理；用户业务页、管理页逐页视觉重构以及真实管理接口收口仍按独立边界推进。

### 14.12 Figma 用户知识库页实际画板验收（2026-08-10）

本轮完成知识库默认内容、空态、检索失败和来源不可用四种状态的 Figma 实际验收。默认内容节点为 `795:838`，默认/空态容器为 `795:786`，检索失败节点为 `795:968`/`795:1145`，来源不可用节点为 `795:1151`/`795:1328`；三张主画板均为 `1440x1024`，状态层为 `1180x1024`。

- [x] Plugin API 回读文本节点数为 96/97/97；三张主画板及其状态层的 `Inter` 遗留均为 0。
- [x] 普通界面文本按实际字重使用 `Noto Sans SC`；匹配百分比使用 `Space Mono Bold`；更新时间、引用数量、DOC ID、访问缓存信息、错误码、request ID、来源状态和英文引用使用 `Space Mono Regular`/`Space Mono Italic`。
- [x] 可见文本边界检查无越界、零尺寸或非预期裁切；结果引用摘要和会话列表的 `ENDING` 省略均为节点明确配置。
- [x] 截图复核确认空态、检索失败和来源不可用状态卡的色条、标签、正文、错误/来源状态和操作入口层级清晰，无非设计重叠。
- [x] Prototype 目标回读为 `18/18`、`17/17`、`17/17` 有效，目标节点全部存在，包含搜索失败返回、来源重试和知识库状态返回路径。
- [ ] 本记录只关闭知识库 Figma 画板的视觉和交互结构验收，不代表真实 RAG、文档导入、ACL、引用详情和生产数据接口完成；iconfont 实体资源登记仍为 `BLOCKED`。

### 14.15 Knowledge 前端状态差异证据补充（2026-08-12）

本轮将 Knowledge 默认态、检索失败和来源不可用三种已有前端状态纳入可重复的浏览器截图映射。Figma 唯一来源节点为 `795:838`、`795:968`、`795:1145`、`795:1151` 和 `795:1328`。

- [x] 设计上下文回读确认主区域为 `1180×1024`，状态层覆盖主区域；完整画板中的失败态和来源不可用态卡片绝对位置为 `x=550,y=300`、尺寸 `600×260`，前端 DOM 实测一致。空态保持 Figma 定义的 `560×220`。
- [x] 失败态使用 Figma `#d67676/#ffedeb`，来源不可用态使用 `#c79654/#fff8e5`；半透明遮罩、技术字段和重试入口均已通过固定视口截图复核。
- [x] `1440×1024` 下运行 `png-diff.mjs`：检索失败 `22.41% / RMSE 12.76`，来源不可用 `22.62% / RMSE 12.83`；默认态按 Figma `1180×1024` 主区域裁剪比较并保留差异结果。三者均为 `DIFF_REVIEW`。
- [x] 证据已登记到 `foodmate-ui/.qa/figma-pixel-acceptance/`，包含 Figma PNG、稳定渲染后的浏览器 PNG 和 RGBA 归一化副本。
- [ ] 本项只补齐 Knowledge 状态的前端截图映射和差异证据，不代表真实 RAG、文档导入、ACL、引用详情接口或 iconfont 实体资源完成；iconfont 继续保持 `BLOCKED`。

### 14.13 Figma 用户个人中心页实际画板验收（2026-08-10）

本轮完成个人中心四个主画板和 15 张状态画板的实际回读与截图检查：基本资料 `806:1119`、记忆与偏好 `806:1281`、安全与设备 `806:1445`、数据与隐私 `806:1585`；记忆空态 `1013:2`、退出设备确认 `1013:235`、注销确认 `1013:465`；以及头像、未保存离开、密码提交/成功/失败、导出排队/运行/过期、注销提交/成功/失败状态节点。

- [x] 19 张画板均为 `1440x1024`；全部可见文本 `Inter` 遗留为 0，普通界面文本统一为 `Noto Sans SC`，技术字段和记忆引用使用对应的 `Space Mono` 字重。
- [x] 主页面文本节点数为 `103/75/88/70`，核心状态为 `80/94/78`，剩余异步状态为 `89/90/90/76/76/78/70/70/71/70/70/72`；所有画板可见文本边界检查无越界、零尺寸或非预期裁切。
- [x] 截图复核覆盖基本资料字段和按钮、记忆引用、密码规则与设备列表、导出表格、危险区域，以及全部上传/密码/导出/注销状态卡；没有发现字体替换造成的布局破坏。
- [x] Figma 中确认框、状态卡和底层页面的遮罩关系属于设计定义的有意叠层；数据隐私页的注销说明覆盖危险区域不作为非预期重叠。
- [x] 19 张画板 Prototype 目标均有效，包含主 Tab、状态关闭/返回、密码提交反馈、导出重试和注销重试路径。
- [ ] 本记录只关闭个人中心 Figma 画板视觉和交互结构验收；真实资料读写扩展、头像对象存储、导出/注销异步任务、后端授权和 iconfont 登记仍需独立完成。

### 14.14 Figma 用户流程状态画板补充验收（2026-08-11）

本轮补齐 19 张此前尚未完成截图检查的用户流程状态画板。该记录属于 Figma 设计交付验收，不代表这些状态已经全部迁移为前端代码，也不改变真实接口、异步任务和生产环境的验收边界。

| 范围 | 节点 | 画板 | 文本节点 | Prototype 反应 |
|---|---|---|---:|---:|
| 工作台输入器 | `1015:4` | `workspace-home-input-states` | 101 | 19 |
| 餐食规划冲突 | `692:3375` | `meal-plan-conflict` | 93 | 17 |
| 会话操作 | `806:212` | `agent-chat-session-actions` | 85 | 20 |
| 购物清单 | `692:3569` | `meal-plan-shopping-list` | 82 | 17 |
| 会话归档/回收站 | `806:662` / `806:887` | `agent-chat-archived` / `agent-chat-trash` | 82 / 82 | 18 / 18 |
| Agent 停止运行 | `1013:653` | `agent-chat-running-stop` | 82 | 20 |
| 会话重命名 | `806:438` | `agent-chat-renamed` | 81 | 18 |
| 历史分页 | `740:212` / `740:426` | `agent-chat-history-page-2` / `agent-chat-history-page-3` | 75 / 75 | 16 / 17 |
| 会话检索 | `742:212` | `agent-chat-search-results` | 75 | 16 |
| 摄入分析三态 | `692:2026` / `692:1901` / `692:2139` | `intake-analysis-empty` / `intake-analysis-loading` / `intake-analysis-error` | 61 / 56 / 54 | 16 / 16 / 16 |
| 餐食规划生成中 | `692:3746` | `meal-plan-generating` | 53 | 18 |
| 餐食规划向导 | `692:2801` / `692:2934` / `692:3078` | `meal-plan-wizard-step1` / `step2` / `step3` | 79 / 78 / 79 | 18 / 18 / 19 |
| 餐食规划列表 | `692:2662` | `meal-plan-list` | 79 | 20 |

- [x] 19 张画板均为 `1440x1024`；Plugin API 汇总为 `1452` 个文本节点、`337` 条 Prototype 反应，`Inter=0`。
- [x] 根画板边界内的可见文本越界和零尺寸检查均为 `0`；截图复核未发现输入器、会话操作、三态分析、规划向导和计划列表中的非预期裁切或可见同级重叠。
- [x] Prototype 目标全部有效，覆盖输入器状态、会话归档/回收站/重命名、停止运行、历史分页/检索、分析空态/加载/错误态、规划冲突/生成中/向导和购物清单。
- [ ] 后续代码迁移仍须逐画板对照 Figma 的颜色、字体、Token、间距、状态层和交互语义；已有 shadcn/Radix 基础设施与 G6 Arco 清理不能视为全量页面重构完成。iconfont 仍需等实体字体包、字形类名、来源、授权和 Figma 资产齐全后再解除 `BLOCKED`。

### 14.15 Figma 认证页面与 Token 状态迁移（2026-08-13）

认证页面按实际 Figma 节点迁移到独立路由，避免在单一登录组件中叠加注册、找回和重置流程：

| 画板 | 路由 | 代码入口 | 状态边界 |
|---|---|---|---|
| `647:214` | `/login` | `pages/LoginPage/LoginPage.tsx` | 默认登录；真实提交仍调用 `authService.login` |
| `680:216` | `/register` | `pages/RegisterPage/RegisterPage.tsx` | 注册、密码规则、确认密码 |
| `680:275` | `/forgot-password` | `pages/ForgotPasswordPage/ForgotPasswordPage.tsx` | 邮箱提交、邮件已发送双卡 |
| `680:307` | `/reset-password` | `pages/ResetPasswordPage/ResetPasswordPage.tsx` | 双密码、强度、token 防护 |
| `680:738/757/776` | `/token-status?state=...` | `pages/TokenStatusPage/TokenStatusPage.tsx` | invalid、expired、used |

登录状态画板通过 query state 复现：`submitting`、`field-error`、`credential-error`、`account-locked`、`account-disabled`、`service-unavailable`，分别对应 Figma `680:408`、`680:445`、`680:483`、`680:524`、`680:564`、`680:606`。query state 只用于前端视觉/交互验收；正常 `/login` 的真实认证请求、注册请求和密码找回请求仍由现有 service 层负责。

- [x] 认证页面统一使用 Figma 的粉色背景、薄荷色斜切层、Noto Sans SC、Montserrat Black、Figma 状态色和控件尺寸。
- [x] 认证状态使用 shadcn `Button`/`Input` 和 Lucide 标准图标；未收到真实 iconfont 字体包、CSS 映射、许可证和 glyph 表前，不写入虚构字体类名或 codepoint。
- [x] 浏览器检查覆盖 `1440x900` 的 10 个状态以及 `390x844` 注册页；移动注册输入框均在视口内，登录状态按钮可用性与 Figma 状态契约一致。
- [x] 本轮认证回归为 18 个测试文件、74/74；认证 PNG 对照统一保留 `DIFF_REVIEW`，截图和指标见《Figma前端像素级验收报告》。
- [ ] 本节不代表全量 shadcn 页面迁移、真实后端异常码联调、生产环境浏览器矩阵或 iconfont 资源登记完成。下一步继续按剩余 Figma 状态画板逐页迁移，先读取节点再编码。

## 39. 2026-08-13 Figma 餐食规划流程状态验收

本轮补齐餐食规划流程的七个 Figma 状态映射。所有视觉值仍以对应 Figma 节点为唯一来源，状态 query 只用于前端验收 fixture。

| 状态 | Figma 节点 | 前端入口 | 差异比例 | RMSE | 结果 |
|---|---|---|---:|---:|---|
| 向导步骤 1 | `692:2801` | `/planning?state=wizard-step1` | 40.49% | 21.93 | `DIFF_REVIEW` |
| 向导步骤 2 | `692:2934` | `/planning?state=wizard-step2` | 42.86% | 22.56 | `DIFF_REVIEW` |
| 向导步骤 3 | `692:3078` | `/planning?state=wizard-step3` | 43.15% | 23.92 | `DIFF_REVIEW` |
| 冲突解决 | `692:3375` | `/planning?state=conflict` | 37.28% | 25.12 | `DIFF_REVIEW` |
| 购物清单 | `692:3569` | `/planning?state=shopping-list` | 24.35% | 17.23 | `DIFF_REVIEW` |
| 生成中 | `692:3746` | `/planning?state=generating` | 13.69% | 16.84 | `DIFF_REVIEW` |
| 计划列表 | `692:2662` | `/planning?state=list` | 26.1032% | 19.40 | `DIFF_REVIEW` |

- [x] 浏览器实际确认向导 1→2→3、开始/取消生成、冲突方案应用、购物清单初始数量和导出反馈；七个状态在 `1440×1024` 下均无页面级横向溢出。
- [x] Figma PNG、浏览器截图和 RGBA 归一化截图已登记到 `foodmate-ui/.qa/figma-pixel-acceptance/`。
- [ ] 所有 diff 继续保留 `DIFF_REVIEW`，不以人工接近或 Figma 内部 Prototype 有效替代像素级 PASS；真实计划生成、冲突、购物清单持久化、任务接口和 iconfont 实体资源仍未关闭。

## 38. 2026-08-13 Figma 餐食规划状态验收

本轮补齐餐食规划 Loading、Empty、Error 三种独立状态的 Figma 映射和浏览器复核。实现继续以 Figma 为颜色、字体、尺寸、间距、样式和状态唯一来源；query fixture 仅用于前端验收，不替代真实餐食规划接口。

- [x] Loading：Figma `692:2256` -> `/planning?state=loading`，主计划区和右侧校验/购物骨架保持固定网格；`1440×1024` 下无页面级横向溢出。
- [x] Empty：Figma `692:2446` -> `/planning?state=empty`，空态图标、标题、说明、创建入口和提示文案已对应；创建按钮进入 `/chat?prompt=请为我创建本周餐食规划`。
- [x] Error：Figma `692:2542` -> `/planning?state=error`，错误图标、错误码、重新加载和返回工作台入口已对应；重试恢复 `/planning` 默认态。
- [x] 证据目录新增三张 Figma PNG、三张浏览器 JPEG、三张 RGBA 归一化浏览器 PNG；同尺寸 `png-diff.mjs` 结果为 Loading `26.74% / RMSE 13.19`、Empty `16.98% / RMSE 16.88`、Error `17.81% / RMSE 16.50`，全部保持 `DIFF_REVIEW`。
- [x] 浏览器三态 DOM 检查确认视口宽度 `1440`、页面 `scrollWidth` 为 `1440`；状态测试继续覆盖三态恢复和既有规划流程。
- [ ] 本节不关闭真实计划读取/生成、冲突解决、购物清单持久化、后端任务、生产字体加载或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 34. 2026-08-12 生产字体、真实记忆接口与 Figma 全量验收

本节是当前交付状态的最新记录，优先级高于前文按阶段记录中的临时待办。Figma 文件 key 为 `MX18RZCfAmgprNzxItkHUH`，视觉实现仍以 Figma 为唯一来源；现有后端接口只按仓库已确认的契约接入，不扩写未提供的业务 endpoint。

- [x] 生产字体包已登记并由 `foodmate-ui/src/main.tsx` 加载：`@fontsource/noto-sans-sc@5.3.0` 的 `400/500/700/900`，`@fontsource/space-mono@5.3.0` 的 `400/400 Italic/700`，以及 Figma 登录/令牌画板仍实际使用的 `@fontsource/montserrat@5.3.0` `900`。构建产物已实际包含对应 `.woff2` 文件，未依赖系统字体才能通过构建。
- [x] Figma 当前全文件字体回读为 `Noto Sans SC` Black/Regular/Bold/Medium、`Space Mono` Regular/Italic/Bold 和 `Montserrat` Black；其中 `Montserrat Black` 仅剩 13 个可见品牌字标节点，位于登录、注册、找回/重置密码和令牌状态画板，不能从生产包中遗漏。
- [x] 真实记忆页面已接入已有 `/api/memories` 契约：`GET` 列表、`POST /{id}/confirm` 确认、`PATCH /{id}` 编辑、`DELETE /{id}` 逻辑删除；操作失败保留当前界面数据。前端同时兼容后端 record 默认 camelCase 与历史 snake_case 字段，更新请求使用后端 DTO 的 `memoryValue` 字段。
- [x] Figma `🎨 :: Design` 全量 Plugin API 回读：105 张顶层画板、19,985 个节点、7,102 个文本节点；最终可见文本 7,028 个，顶层画板无空内容，零尺寸文本 `0`，根画板边界外可见文本 `0`。
- [x] Figma Prototype 全量回读为 1,940 条 reaction；带目标的 reaction 无效目标 `0`。本轮修正了 8 个历史/搜索/会话状态中的长英文消息换行，并修正 1366x768 特殊视口底部侧栏可见性后重新验收通过。
- [x] Figma 截图验收请求 105/105 成功；已对首页、Agent 对话、饮食记录、摄入分析、餐食规划、个人中心、管理端和异常/异步状态进行高清抽查。Figma 文件自身的截图、结构和 Prototype 验收已完成。
- [x] Figma 设计系统当前真实结构已重新核对：`01 Foundations` 有 5 组变量集合对应的契约与 8 个本地文字/效果样式；`02 Components - User` 有 24 个 Component Set；`03 Components - Admin` 有 14 个 Component Set；`⚡ :: Assets` 保留 6 个基础 Component Set。不能把 `🎨 :: Design` 页面自身的 0 个 Component Set 误写成全文件 0 个。
- [x] 前端运行页面与 Figma 105 张 PNG 的自动化逐页像素差异报告已建立；当前 105 条结果均有同视口 PNG、diff JSON、几何/文字/DPR 运行时字段和人工视觉复核登记，汇总仍为 `105 DIFF_REVIEW / 0 UNMAPPED / 0 SIZE_MISMATCH / 0 PASS`。
- [ ] iconfont 实体资源登记仍为 `BLOCKED`。Figma `Iconfont Resource Registry` 节点 `1065:2` 明确要求来源/项目 URL、授权范围、字形类名、Unicode、字重、默认尺寸、语义名、对比度和无障碍标签，但仓库和当前 Figma 文件没有 `.woff/.woff2/.ttf/.otf`、CSS 映射、来源或许可证记录。因此不创建虚构 `@font-face`、`.icon-*` 或 codepoint，标准命令图标继续使用 Lucide。
- [ ] 生产级餐食生成、真实云 RAG/模型、异步导出/注销的发布级运维、管理端生产授权和浏览器兼容矩阵不因前端页面接入而宣称完成；当前 deterministic 业务路径已按对应方案和执行记录验收。

## 40. 2026-08-14 Agent 空态页面迁移

### 40.1 Figma 来源与页面边界

Agent 空态以 Figma `687:219` 为唯一视觉来源，目标画板为 `1440×1024`。前端入口为 `/chat?state=empty`，仅用于呈现新会话空态和进入既有 Agent 对话流程；`state` 与 `prompt` 查询参数不代表后端 AgentRun 已成功完成。

- 主区域隐藏 Trace 右栏，保留工作台壳层、居中引导、三张推荐问题卡和底部 Composer。
- Figma 关键值已落地：引导区约 `560px`、推荐卡区域 `720×123px`、卡片间距 `16px`、Composer `112px`；颜色使用 `#fcfcfc`、`#f4f6f5`、`#c79654`、`#fff6e2`、`#333`、`#6b7280` 和 Figma 绿色语义背景。
- 推荐卡交互将完整 prompt 写入 Composer；发送后进入 `/chat?prompt=...`，继续复用既有 mock/real Agent 页面边界。

### 40.2 响应式与验收

- 桌面 `1440×1024`：`document.body.scrollWidth === 1440`，标题、说明、推荐卡和 Composer 无页面级横向溢出或裁切。
- 移动 `390×844`：推荐卡切换单列；空态内容区使用 `calc(100dvh - 96px)`，Composer 完整位于视口底部，内容区内部允许纵向滚动。
- 浏览器证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/`：`chat-agent-empty-figma.png`、桌面/移动浏览器原始帧和 RGBA 归一化帧。
- `png-diff.mjs` 同尺寸结果为差异比例 `15.7739%`、RMSE `14.7325`，按项目验收规则登记为 `DIFF_REVIEW`，不写成自动化 PASS。
- `ChatPage` 定向测试 `2/2`、`npm run typecheck` 和本次触及文件 Prettier 检查通过；全量 `format:check` 仍受 8 个无关既有文件阻断。

### 40.3 后续顺序

下一步继续读取并迁移 Figma `agent-planning` 状态，之后按 `agent-tool-executing`、`agent-awaiting-clarification`、`agent-write-confirmation`、`agent-budget-limit`、`agent-tool-failed-retryable`、`agent-safety-degraded`、`agent-user-cancelled`、`agent-sse-reconnecting` 顺序执行。iconfont 实体资源缺少真实包、CSS、来源和许可证时继续保持 `BLOCKED`；shadcn/Radix 只作为组件基础设施，不替代 Figma 逐画板验收。

## 41. 2026-08-14 Agent Planning 状态实现约束

- [x] 入口固定为 `/chat?state=planning`，Figma 来源节点为 `687:342`；Planning 是前端状态 fixture，不替代真实 AgentRun/SSE 数据。
- [x] 页面必须隐藏 Trace rail，使用 Figma 的 `45px` 状态条、`1180px` 主区和 `112px` Composer；移动端使用 `390×844` 验证无横向溢出。
- [x] 规划步骤卡使用 Figma 已核实的紫色标题、绿色完成态、黑色当前态和低强调待处理态；输入禁用但停止动作保留可用。
- [x] 验收记录：定向测试 `3/3`、typecheck、触及文件格式化和 diff check 通过；自动 diff `14.9956% / RMSE 14.0682`，结果为 `DIFF_REVIEW`。
- [ ] 不得因为 Planning fixture 可见而宣称计划生成、Agent 编排或后端任务闭环完成；iconfont 实体资源继续 `BLOCKED`。

## 42. 2026-08-14 Agent Tool Executing 状态实现约束

- [x] 入口固定为 `/chat?state=tool-executing`，Figma 来源节点为 `687:475`；Figma 是颜色、字体、尺寸、间距和状态的唯一来源。
- [x] 共享布局必须保留 `260px` 侧栏、`860px` 对话区、`320px` Trace rail；状态条必须呈现 `Planning ✓ / Retrieving ✓ / Executing ● / Composing ○`。
- [x] 工具卡必须保留完成、运行中、待处理三种视觉状态；Trace 必须展示 `fst_trace_9821aa`、工具名称和状态；Composer 输入禁用、停止按钮可用。
- [x] 桌面几何已验收：Trace body `y=107`、首卡 `y=135`、工具卡 `181×250`、Composer `y=912/h=112`；移动 `390×844` 无横向溢出且隐藏 Trace。
- [x] 证据位于 `foodmate-ui/.qa/figma-pixel-acceptance/`；diff `50.5259% / RMSE 23.1703 / DIFF_REVIEW`，不得改写为 PASS。
- [x] 该状态复用 shadcn/Radix 基础设施和 Lucide 标准图标，不引入未经登记的 iconfont glyph；真实后端工具执行仍由既有 real mode 负责。
- [ ] 下一步实现 `agent-awaiting-clarification`，继续按 Figma 节点先读取、再编码、再做桌面/移动验收。

## 43. Agent Awaiting Clarification 状态实现约定（2026-08-15）

### 43.1 入口与职责

`ChatPage` 通过 `/chat?state=awaiting-clarification` 渲染 Figma 节点 `687:642` 对应的前端状态。该入口是视觉验收 fixture，职责是稳定复现消息、Planning 状态、澄清选项和 Composer；真实模式仍由既有 AgentRun/SSE 链路负责，不能把 fixture 当作后端实现。

### 43.2 组件与共享布局

- `ClarificationCard` 增加 `presentation="figma-compact"`，固定 Figma 澄清卡的 `222×193` 结构，并保留选项选中态与回调。
- `WorkspaceLayout` 的 `displayNameOverride`、`profileIdOverride`、头像资源和 `designChat` 仅用于设计 fixture；存在 override 时必须优先于认证用户值。
- Chat 入口在共享布局中将顶部“工作台”和侧栏“Agent 对话”标为当前项，以匹配 Figma 的导航选中态。
- 标准命令图标继续使用已登记的 Lucide；在 iconfont 实体包、CSS 映射、来源及许可证齐备前，不得添加伪造 `@font-face` 或 Unicode 映射。

### 43.3 验收约束

桌面验收固定使用 `1440×1024`，移动验收固定使用 `390×844`。本轮实测澄清卡桌面为 `x=340,y=237,w=222,h=193`，移动为 `x=64,y=251.8,w=222,h=193`；Composer 桌面为 `x=260,y=912,w=1180,h=112`，移动为 `x=0,y=732.8,w=375.2,h=112`。截图和 RGBA 归一化产物统一放在 `foodmate-ui/.qa/figma-pixel-acceptance/`。

自动 diff 必须使用同尺寸 Figma PNG 和浏览器 RGBA PNG。当前桌面结果为 `differentRatio=0.1600301 / RMSE=13.5626 / DIFF_REVIEW`；没有同尺寸移动 Figma 参考图时只能记录 `SIZE_MISMATCH`，不得跨视口推导像素结论。

## 44. 默认头像资源规则（2026-08-15）

- `foodmate-ui/public/assets/avatars/default-male.svg` 为男性默认头像，来源为用户提供的第一个 SVG 附件。
- `foodmate-ui/public/assets/avatars/default-female.svg` 为女性默认头像，来源为用户提供的第二个 SVG 附件。
- 头像解析优先级为：用户已上传头像、页面显式 fixture 头像、按 `gender` 选择的默认头像；未设置性别时不在通用用户详情中猜测，工作区当前用户使用男性默认兜底。
- Figma 专用验收页面的显式头像仍保留在 `public/assets/figma/`，不纳入业务默认头像替换，避免把 Figma 参考资源和业务默认资源混用。

## 45. Agent Write Confirmation 状态实现约束（2026-08-23）

- [x] 入口固定为 `/chat?state=write-confirmation`，视觉来源为 Figma 节点 `687:773`；该 query 只复现前端状态，不替代真实 AgentRun 或写入接口结果。
- [x] 页面展示写入目标、`2024年3月14日 午餐`、`三文鱼寿司 x6`、约 `620 千卡`、`38g` 蛋白质、`USDA FoodData Central`、标准份量估算和“确认写入/取消”操作。
- [x] 真实模式的确认动作继续调用既有写入确认接口，取消动作只结束当前确认流程，不在 fixture 中伪造后端成功响应。
- [x] 当前证据使用 Figma 与浏览器同尺寸 `1440×1024` PNG；运行时字体已加载、页面无横向溢出、窗口装饰点为 `0`；PNG diff 为 `25.7218% / RMSE 16.2155`，结论为 `DIFF_REVIEW`。
- [x] Agent Budget Limit 已按 Figma 节点 `687:918` 完成当前 fixture 的同视口截图、运行时和 PNG diff 证据登记；追加动作保持当前 Run 语义，未伪造新会话。
- [ ] 下一步按顺序处理 `agent-tool-failed-retryable`；iconfont 实体资源继续 `BLOCKED`，shadcn/Radix 只提供基础控件能力，不替代 Figma 的颜色、字体、尺寸、间距和像素验收。

## 46. Agent Budget Limit 状态实现约束（2026-08-23）

- [x] 入口固定为 `/chat?state=budget-limit`，视觉来源为 Figma 节点 `687:918`；该 query 只复现前端状态，不替代真实 AgentRun、预算接口或取消终态。
- [x] 页面展示 `50,000 tokens`、`100%`、预计费用和追加预算选项；“追加 20,000 tokens”在 fixture 中只记录当前 Run 的继续动作。
- [x] 真实模式追加预算继续调用既有 `extendAgentRunBudget`，结束会话继续调用既有 `cancelAgentRun`；前端不创建新的伪造会话，也不把 HTTP 接受响应当作运行终态。
- [x] 当前证据使用 Figma 与浏览器同尺寸 `1440×1024` PNG；运行时字体已加载、页面无横向溢出、窗口装饰点为 `0`；PNG diff 为 `25.3484% / RMSE 18.7204`，结论为 `DIFF_REVIEW`。
- [ ] 下一步处理 `agent-tool-failed-retryable`；iconfont 实体资源继续 `BLOCKED`。

## 47. Agent Tool Failed Retryable 状态实现约束（2026-08-23）

- [x] 入口固定为 `/chat?state=tool-failed-retryable`，视觉来源为 Figma 节点 `687:1439`；该 query 只复现前端状态，不替代真实工具失败事件。
- [x] 页面展示工具超时、外部知识库不可用、`TOOL_TIMEOUT_001` 和失败原因；“重试”只在明确 `retryable=true` 的错误语义下出现，同时保留“跳过此步骤”。
- [x] 真实模式重试继续调用既有 `recoverAgentRun`，等待后端新工具事件；前端不伪造重试成功。跳过动作保留数据范围受限语义。
- [x] 当前证据使用 Figma 与浏览器同尺寸 `1440×1024` PNG；运行时字体已加载、页面无横向溢出、窗口装饰点为 `0`；PNG diff 为 `20.1341% / MAE 2.241953 / RMSE 14.730809 / maxChannelDelta 245`，结论为 `DIFF_REVIEW`。
- [x] `agent-safety-degraded` 已完成 Figma 局部对齐和同尺寸 PNG diff 复核，结论保持 `DIFF_REVIEW`；iconfont 实体资源继续 `BLOCKED`。

## 48. Agent Safety Degraded 状态实现约束（2026-08-23）

- [x] 入口固定为 `/chat?state=safety-degraded`，视觉来源为 Figma 节点 `687:1563`；该 query 只复现前端状态，不替代真实 AgentRun 的安全降级事件。
- [x] 页面展示安全降级警告、有限数据范围和个人条件未完整应用提示；追问入口保持可用，结果不包装成完整分析或完整引用。
- [x] 当前证据使用 Figma 与浏览器同尺寸 `1440×1024` PNG；浏览器 RGBA 证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-safety-degraded-horizontal-boundary-browser-1440x1024-2026-08-30-rgba.png`，运行时字体已加载、页面无横向溢出、窗口装饰点为 `0`。已按 Figma 对齐警告文字层级、头像状态标签、回答卡和 Agent 时间戳，并将安全内容容器从 `608px` 对齐为 `612px`，警告与回答区保持 `560px`；最新 PNG diff 为 `25.5599% / MAE 3.223878 / RMSE 16.379168 / maxChannelDelta 255`，结论为 `DIFF_REVIEW`。
- [ ] 下一步处理 `agent-user-cancelled`；iconfont 实体资源继续 `BLOCKED`。

## 49. Agent User Cancelled 状态实现约束（2026-08-23）

- [x] 入口固定为 `/chat?state=user-cancelled`，视觉来源为 Figma 节点 `687:1684`；该 query 只复现前端状态，不替代真实取消事件。
- [x] 页面保留已接收部分文本，显示用户取消原因和重新开始入口；取消状态使用 `cancelled` 语义，不显示为系统失败。
- [x] 重新开始只提示真实运行需要后端创建新的 Run，前端不伪造新的运行结果。
- [x] 当前证据使用 Figma 与浏览器同尺寸 `1440×1024` PNG；运行时字体已加载、页面无横向溢出、窗口装饰点为 `0`；PNG diff 为 `17.9155% / RMSE 15.4846`，结论为 `DIFF_REVIEW`。
- [ ] 下一步处理 `agent-sse-reconnecting`；iconfont 实体资源继续 `BLOCKED`。

## 50. Agent SSE Reconnecting 状态实现约束（2026-08-23）

- [x] 入口固定为 `/chat?state=sse-reconnecting`，视觉来源为 Figma 节点 `687:1803`；该 query 只复现前端重连状态，不替代真实 SSE 连接。
- [x] 页面展示第 `2/5` 次重连、等待重连和持续失败时刷新提示；重连期间保留已显示文本并禁用 Composer。
- [x] 真实 `agentRunService` 继续使用 `Last-Event-ID`、`sse_event_id` 去重，终态 `completed`、`failed`、`cancelled`、`superseded` 关闭连接，重连达到上限进入 `exhausted`。
- [x] 当前证据使用 Figma 与浏览器同尺寸 `1440×1024` PNG；运行时字体已加载、页面无横向溢出、窗口装饰点为 `0`；按 Figma 收口 Agent 头像、`560px` 回答气泡、`1132×66px` 重连提示带和 loader SVG；PNG diff 为 `26.3488% / RMSE 15.2945`，结论为 `DIFF_REVIEW`。
- [ ] Agent 状态顺序已完成；下一步继续全量 105 张画板差异收口和剩余 shadcn 页面迁移，iconfont 实体资源继续 `BLOCKED`。

## 51. 摄入分析错误态实现约束（2026-08-23）

- [x] 入口为 `/analysis?state=error`，视觉来源为 Figma 节点 `692:2139`；错误态只显示 7/30/90 天范围和禁用导出，不显示不属于该画板的高级筛选控件。
- [x] 错误卡片采用当前 Figma 的 `440px` 视觉高度、`24px` 内部间距、`40px` 重载按钮和 `#ff665d` 警告色；标准命令图标仍使用 Lucide。
- [x] 当前证据视口为 `1440×1024`、字体已加载、无横向溢出、左上角窗口装饰点为 `0`；PNG diff 为 `12.0143% / RMSE 12.2186`，结论为 `DIFF_REVIEW`。
- [ ] 本项不代表摄入分析默认态、加载态、空态或真实营养数据闭环完成；iconfont 实体资源仍为 `BLOCKED`。

## 52. 2026-08-23 餐食规划列表默认卡片对齐

- [x] 重新读取 Figma 节点 `692:2662`，确认 `/planning?state=list` 的默认画板同时展示进行中、草稿和已归档三张计划卡。
- [x] 前端 fixture 默认态改为展示全部三张计划卡；点击草稿箱或已归档仍按状态筛选，真实模式仍按服务端计划状态筛选。
- [x] 浏览器实测视口为 `1440×1024`、字体状态为 `loaded`、`document`/`body` 无横向溢出；DOM 中三张计划卡、三种状态和对应操作按钮均存在。
- [x] 前端左上角红、黄、绿窗口装饰点仍为 `0`；仅保留 Figma 需要的顶部空白占位，未修改 Figma 设计稿。
- [x] 当前证据为 `foodmate-ui/.qa/figma-pixel-acceptance/meal-plan-list-browser-current-rgba.png`，独立结果为 `meal-plan-list-current-diff.json`；同尺寸 PNG diff 为 `28.3198% / MAE 3.9498 / RMSE 19.4020`，较旧记录改善，但仍保持 `DIFF_REVIEW`。
- [ ] 本项不代表餐食规划的卡片操作后端闭环、真实生成任务、购物清单持久化、全量 shadcn 页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 53. 2026-08-23 餐食规划列表菜单操作图标对齐

- [x] 重新读取实时 Figma `692:2662`，确认计划卡右侧“更多操作”使用三条横线菜单图标；前端已将 `MoreHorizontal` 替换为 Lucide `Menu`，未引入虚构 iconfont glyph。
- [x] 浏览器实测三个计划卡均有菜单按钮，菜单 SVG 数量为 `3`，页面 `scrollWidth=1440`、计划卡数量为 `3`，无横向溢出。
- [x] 当前同尺寸 PNG diff 为 `28.3198% / MAE 3.9498 / RMSE 19.4020`，继续登记为 `DIFF_REVIEW`；抗锯齿差异不作为 PASS 理由。
- [ ] 本项不代表餐食规划全量视觉收口、真实卡片操作闭环、shadcn 全量迁移或 iconfont 资源登记完成；iconfont 继续为 `BLOCKED`。

## 54. 2026-08-23 餐食规划列表顶部头像资源对齐

- [x] 重新读取 Figma `692:2662` 的原始图片资产，确认顶部用户头像不是旧渐变字标资源；已登记真实 Figma 男性头像到 `foodmate-ui/public/assets/figma/planning/meal-plan-list-topbar-avatar.png`。
- [x] `/planning?state=list` fixture 仅切换顶部头像资源，业务默认头像规则不变；浏览器实际加载该资源，计划卡数量为 `3`，视口宽度为 `1440`。
- [x] 当前同尺寸 PNG diff 为 `28.3485% / MAE 3.9456 / RMSE 19.3731`，继续登记为 `DIFF_REVIEW`，不因局部资源对齐标记整页 PASS。
- [ ] 本项不代表全量页面头像资源和 shadcn 迁移全部完成；iconfont 实体资源继续为 `BLOCKED`。

## 55. 2026-08-23 餐食规划列表排版尺寸对齐

- [x] 依据 Figma 节点 `692:2662` 调整 `/planning?state=list` 列表副标题、新建按钮、Tab 和计划日期的字号与行高；不修改 Figma 设计稿，也不改变业务状态圆点。
- [x] 浏览器截图已重新转换为 RGBA 并完成同尺寸比较，视口为 `1440×1024`，页面无横向溢出；最新证据为 `foodmate-ui/.qa/figma-pixel-acceptance/meal-plan-list-browser-current-rgba.png`。
- [x] 最新 PNG diff 为 `27.9107% / MAE 3.8105 / RMSE 18.9605`，完整结果登记在 `foodmate-ui/.qa/figma-pixel-acceptance/meal-plan-list-current-diff.json` 和 `figma-105-diff-results.json#meal-plan-list`。
- [ ] 该项只收口列表排版尺寸，整页仍为 `DIFF_REVIEW`；全量 shadcn 迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 56. 2026-08-23 餐食规划列表顶部头像圆形裁切

- [x] 依据 Figma 节点 `692:2662` 修正 `/planning?state=list` 顶部头像容器：保留 `32×32` 和圆角，并增加 `overflow: hidden` 以裁切 Figma 肖像资源边界。
- [x] 浏览器验收使用 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`；头像资源加载成功，前端左上角红、黄、绿窗口装饰点数量为 `0`，没有改动 Figma 或业务状态圆点。
- [x] 最新同尺寸 PNG diff 为 `27.8745% / MAE 3.8013 / RMSE 18.9252`，结果登记在 `foodmate-ui/.qa/figma-pixel-acceptance/meal-plan-list-current-diff.json` 和 `figma-105-diff-results.json#meal-plan-list`。
- [ ] 本项仍保持整页 `DIFF_REVIEW`；只完成头像裁切这一独立视觉修正，全量 shadcn 页面迁移和 iconfont 实体资源登记仍未完成。

## 57. 2026-08-23 餐食规划列表底部说明面板几何对齐

- [x] 依据实时 Figma 节点 `692:2662` 将列表底部说明面板设为 `222px` 高，并通过桌面端负边距使其几何位置对齐为 `x=260、y=802、width=1116、bottom=1024`；没有改变 Figma 设计稿。
- [x] 计划卡片仍从 `x=292` 开始，移动端在 `680px` 以下恢复自适应高度、零负边距，避免响应式内容溢出。
- [x] 浏览器证据使用 `1440×1024`、DPR `1.0000000149011612`、字体 `loaded`；PNG diff 更新为 `23.0783% / MAE 3.6995 / RMSE 18.7132`。
- [ ] 本项只完成底部说明面板几何对齐，整页仍为 `DIFF_REVIEW`；后续继续按 Figma 收口卡片内容、字体和图标差异，iconfont 仍为 `BLOCKED`。

## 58. 2026-08-23 餐食规划列表操作组间距对齐

- [x] 依据 Figma 节点 `692:2761`、`692:2778`、`692:2795`，将计划卡操作组的按钮间距调整为 `16px`；标准命令图标继续使用 Lucide `Menu`，未引入 iconfont。
- [x] 浏览器验收确认三个操作组均为 `gap=16px`，菜单按钮仍位于卡片右侧，视口 `1440×1024`、字体已加载、无横向溢出。
- [x] 最新 PNG diff 为 `23.1109% / MAE 3.6961 / RMSE 18.7010`；差异比例较前一版本略升但 MAE/RMSE 改善，记录以 Figma 明确几何值为准。
- [ ] 本项仍为局部视觉修正，整页继续 `DIFF_REVIEW`；全量页面迁移和 iconfont 实体资源登记尚未完成。

## 59. 2026-08-23 餐食规划列表信息标签样式对齐

- [x] 依据 Figma 节点 `692:2758`、`692:2775`、`692:2792`，将计划卡信息标签对齐为 `12px` 粗体、`#c79654` 和 `rgba(255,246,226,0.1)`；进行中/草稿/已归档状态徽章保持独立字号和颜色。
- [x] 说明面板增加顶部内容对齐，避免固定 `222px` 高度下 Grid 拉伸文本行；面板几何仍为 Figma 的 `x=260,y=802,width=1116,height=222`。
- [x] 最新 PNG diff 为 `26.1015% / MAE 3.8913 / RMSE 19.4011`，记录在 `meal-plan-list-current-diff.json` 和 `figma-105-diff-results.json#meal-plan-list`。
- [ ] 本项仍只完成局部 Figma 样式对齐，整页保持 `DIFF_REVIEW`；iconfont 继续 `BLOCKED`，全量 shadcn 页面迁移仍未完成。

## 60. 2026-08-23 餐食规划列表操作按钮高度对齐

- [x] 依据 Figma 节点 `692:2762`、`692:2779`、`692:2796`，将计划卡“进入计划”按钮从 shadcn 默认 `40px` 高度修正为 `36px`，与菜单按钮保持同高；按钮水平内边距仍为 `14px`。
- [x] 浏览器验收确认三个按钮均为 `36px` 高、操作组 `gap=16px`，视口 `1440×1024`、字体已加载、无横向溢出。
- [x] 最新 PNG diff 为 `26.1032% / MAE 3.8917 / RMSE 19.4005`，完整结果写入 `meal-plan-list-current-diff.json`。
- [ ] 本项只完成计划卡操作按钮几何，整页仍为 `DIFF_REVIEW`；全量 shadcn 迁移和 iconfont 实体登记继续未完成。

## 61. 2026-08-23 餐食规划列表更新时间文本行高对齐

- [x] 依据 Figma 节点 `692:2760`、`692:2777`、`692:2794`，将计划卡“最后修改”文本设为 `12px`、常规字重、`line-height: normal` 和 `#6b7280`。
- [x] 浏览器验收确认更新时间文本行盒为 `16.8px`，计划卡按钮仍为 `36px`，视口 `1440×1024`、字体已加载、无横向溢出。
- [x] 最新 PNG diff 为 `26.1088% / MAE 3.8937 / RMSE 19.4070`，结果写入 `meal-plan-list-current-diff.json`。
- [ ] 本项仍为局部 Figma 文本样式修正，整页继续 `DIFF_REVIEW`；iconfont 继续 `BLOCKED`。

## 62. 2026-08-23 餐食规划列表说明面板字体对齐

- [x] 依据 Figma 节点 `976:3` 至 `976:8`，将底部说明面板字体优先设为 `Noto Sans SC`，标题使用 `22px` 行盒，正文和辅助行使用 `18px` 行盒，保留 Figma 的颜色层级。
- [x] 浏览器验收确认面板几何 `x=260,y=802,width=1116,height=222`、内容 `gap=8px`，视口 `1440×1024`、字体已加载、无横向溢出。
- [x] 最新 PNG diff 为 `26.1068% / MAE 3.8953 / RMSE 19.4073`，结果写入 `meal-plan-list-current-diff.json`。
- [ ] 本项仍是局部 Figma 文本排版修正，整页继续 `DIFF_REVIEW`；iconfont 仍为 `BLOCKED`。

## 63. 2026-08-23 餐食规划列表状态徽章文字对齐

- [x] 依据 Figma 节点 `692:2753`、`692:2770`、`692:2787`，将进行中、草稿、已归档徽章文字设为 `11px` 粗体和 `line-height: normal`，保留各自背景与语义颜色。
- [x] 浏览器验收确认三个徽章高度均为 `24px`，视口 `1440×1024`、字体已加载、无横向溢出。
- [x] 最新 PNG diff 为 `27.9746% / MAE 4.0662 / RMSE 19.9112`，结果写入 `meal-plan-list-current-diff.json`。
- [ ] 本项只完成状态徽章文字样式，整页继续 `DIFF_REVIEW`；iconfont 仍为 `BLOCKED`。

## 64. 2026-08-23 餐食规划列表新建按钮文案结构对齐

- [x] 按 Figma 节点 `692:2739/2740` 将列表头部按钮从 Lucide `Plus` 加文本改为单一文案 `+ 新建膳食计划`；按钮仍使用现有 shadcn `Button` 封装和原有导航回调。
- [x] 真实模式 `PlanningPage.real.test.tsx` 已覆盖新的可访问按钮名称；浏览器实测新名称存在、旧名称不存在，页面无横向溢出。
- [x] 本次只核查前端左上角窗口装饰点，数量为 `0`，未误删业务状态圆点，也未修改 Figma。
- [x] 最新 diff 为 `27.9293% / MAE 4.0463 / RMSE 19.8687`，结果写入 `foodmate-ui/.qa/figma-pixel-acceptance/meal-plan-list-current-diff.json`。
- [ ] in-app 浏览器本次实际 DPR 为 `1.25`，因此 DPR 1 门禁仍未通过；本画板继续 `DIFF_REVIEW`，iconfont 继续 `BLOCKED`。

## 65. 2026-08-23 餐食规划列表计划卡水平布局对齐

- [x] Figma 节点 `692:2749` 的计划卡内容列为 `936px`，操作组位于内部 `x=960`，容器没有额外 flex gap。
- [x] 前端移除 `.planListCard` 的 `gap:24px`，内容列实测由 `908.8px` 增至 `932.8px`；操作按钮、菜单按钮和卡片右边界保持现有 Figma 对齐结果。
- [x] 本次截图像素结果未变化，仍为 `27.9293% / MAE 4.0463 / RMSE 19.8687`；结构几何证据已更新至 `figma-105-mapping.json`。
- [ ] 卡片垂直高度和行盒仍需下一小点继续处理；画板保持 `DIFF_REVIEW`，iconfont 继续 `BLOCKED`。

## 66. 2026-08-23 餐食规划列表计划卡文字行盒对齐

- [x] 按 Figma 节点数据将计划卡标题行、状态徽章、日期、详情、信息标签和更新时间分别对齐为 `22px / 21px / 16px / 17px / 22px / 14px`。
- [x] 浏览器实测卡片主列高度 `85px`，三张卡的文字内容无越界；页面无横向溢出。
- [x] 最新 diff 为 `23.5418% / MAE 3.5356 / RMSE 18.2830`，已登记到 `meal-plan-list-current-diff.json`。
- [ ] 卡片总高度仍为 `134.6px`，Figma 目标为 `133px`；下一小点继续处理边框参与布局的问题，当前仍为 `DIFF_REVIEW`。

## 67. 2026-08-23 餐食规划列表计划卡高度与边框对齐

- [x] 将计划卡外边框改为等效 `inset` 描边，保留 Figma 的 `#f4f6f5` 1px 视觉边界，同时不让边框参与内容盒高度计算。
- [x] 浏览器实测三张计划卡均为 `1116×133px`，主列高度 `85px`，卡片间距 `16px`；与 Figma 节点 `692:2749/2766/2783` 对齐。
- [x] 最新 diff 为 `22.9677% / MAE 3.2275 / RMSE 17.1978`，已登记到 `meal-plan-list-current-diff.json`。
- [ ] 顶部壳层、操作按钮宽度、字体和图标光栅化仍需继续验收；画板保持 `DIFF_REVIEW`，iconfont 继续 `BLOCKED`。

## 68. 2026-08-23 餐食规划列表操作按钮尺寸对齐

- [x] 重新读取 Figma 节点 `692:2762`，确认“进入计划”操作 frame 为 `80×32px`，操作组 `692:2761` 为 `132×36px`，菜单按钮保持 `36×36px`。
- [x] 前端仅对计划卡操作按钮设置 `width:80px`、`height:32px`、`min-height:32px`；浏览器实测三张卡按钮为 `80×32px`，操作组为 `132×36px`，卡片仍为 `1116×133px`。
- [x] 浏览器验收确认前端窗口装饰占位为 `0`、左上角未发现红黄绿装饰点、业务状态点未改动；视口 `1440×1024`、字体 `loaded`、页面无横向溢出。
- [x] 最新同尺寸 PNG diff 为 `37.0059% / MAE 4.4041 / RMSE 20.9698 / maxChannelDelta 245`，结果已更新至 `meal-plan-list-current-diff.json` 和 `figma-105-diff-results.json#meal-plan-list`。
- [ ] 本项只完成计划卡操作按钮几何修正，画板继续保持 `DIFF_REVIEW`；DPR 1 门禁、顶部壳层、字体/图标光栅化和其它页面差异仍未关闭。

## 69. 2026-08-23 餐食规划空态局部重构记录

- [x] 依据实时 Figma 节点 `692:2446` 将空态卡片按 `520×368px`、`24px` 分段间距、`100px` 图标、`424×82px` 文案组、`192×42px` 创建按钮实现；卡片边框使用等效内描边，避免 CSS border 挤压 Figma 内容盒。
- [x] `/planning?state=empty` 继续使用现有 shadcn `Button` 和 Lucide 图标，未引入未登记的 iconfont；真实模式与后端计划接口逻辑未改变。
- [x] 浏览器 `1440×1024`、DPR `1.0000000149011612` 实测卡片几何和提示行与 Figma 对齐，无页面横向溢出；前端左上角红黄绿装饰点为 `0`，Figma 设计稿未修改。
- [x] 实时 Figma / 浏览器 PNG diff 为 `13.2758% / MAE 1.2768 / RMSE 10.0411 / maxChannelDelta 244`，已同步 `figma-105-mapping.json` 与 `figma-105-diff-results.json#meal-planning-empty`，结论保留 `DIFF_REVIEW`。
- [x] `PlanningPage.test.tsx` `8/8`、`npm run typecheck`、目标文件格式化和运行时几何检查通过。
- [ ] 该记录只代表一个空态画板的局部前端收口，不代表 105 张画板像素级 `PASS`、shadcn 全量迁移或 iconfont 实体包已完成；iconfont 继续 `BLOCKED`。

## 70. 2026-08-26 餐食规划 Loading 继续迁移

- Figma 来源节点：`692:2256`，入口：`/planning?state=loading`。
- 本轮仅调整 loading fixture 的骨架结构和 Figma 几何：主 loading banner `776×102px`，日期骨架卡 `123.2×40px`，餐食骨架卡 `123.2×74px`，右侧约束行 `292×44px`。
- 验收证据：`foodmate-ui/.qa/figma-pixel-acceptance/meal-planning-loading-figma-live-2026-08-26.png`、`meal-planning-loading-browser-current-2026-08-26.jpg`、`meal-planning-loading-browser-current-2026-08-26-rgba.png`；PNG diff 为 `18.1852% / MAE 1.19237 / RMSE 9.65195 / maxChannelDelta 244`，继续标记 `DIFF_REVIEW`。
- 浏览器 `1440×1024`、DPR `1.0000000149011612`、字体加载完成、无横向溢出；前端左上角红黄绿窗口控制点为 `0`，Figma 设计稿保持只读，业务状态点保留。
- `PlanningPage.test.tsx` `8/8`、`npm run typecheck`、`git diff --check` 已通过。shadcn 全量迁移、105 张画板像素级 `PASS` 和 iconfont 实体资源登记仍未完成。

## 71. 2026-08-28 摄入分析错误态描述行盒收口

- [x] 重新读取 Figma 节点 `692:2139`，确认错误态描述文本目标行盒为 `17px`；前端仅在 `.stateCopy .errorDescription` 作用域覆盖通用 `20px` 行高，真实模式附加错误详情不受影响。
- [x] `/analysis?state=error` 浏览器实测错误描述 `238×17px`、错误卡片 `1116×440.6px`、图标容器 `64×64px`，字体已加载、页面无横向溢出、窗口装饰候选为 `0`，Figma 设计稿未修改，业务状态圆点保留。
- [x] 同尺寸 PNG diff 为 `12.9582% / MAE 2.06843 / RMSE 14.51005 / maxChannelDelta 255`；截图和映射见 `foodmate-ui/.qa/figma-pixel-acceptance/`，结论保持 `DIFF_REVIEW`。
- [ ] 本次 in-app 浏览器实际 DPR 为 `1.25`，DPR 1 门禁未关闭；错误态按钮高度、头像、图标光栅化和字体差异继续按 Figma 证据逐点处理，iconfont 仍为 `BLOCKED`。

## 72. 2026-08-28 摄入分析错误态重载按钮几何收口

- [x] 依据 Figma `692:2238/2239`，仅在 Figma fixture 作用域将“重新加载”按钮设为 `120×41px`、`17px` 行高；真实模式错误按钮不受影响。
- [x] 错误卡片边框改为等效内描边，浏览器实测卡片 `1116×440px`，图标容器 `64×64px`，描述 `238×17px`，按钮位置 `x=790,y=443`；页面无横向溢出，窗口控制点数量为 `0`。
- [x] 原始浏览器截图和 RGBA 归一化图均已登记；同尺寸 PNG diff 为 `12.8468% / MAE 2.00587 / RMSE 14.19979 / maxChannelDelta 255`，证据已同步 105 画板映射并保持 `DIFF_REVIEW`。
- [ ] in-app 浏览器实际 DPR 为 `1.25`，DPR 1 门禁和整页像素 `PASS` 仍未关闭；iconfont 继续 `BLOCKED`。

## 73. 2026-08-28 餐食规划列表通知按钮样式与窗口装饰复核

- [x] 依据 Figma 节点 `692:2662/692:2729`，仅在 `/planning?state=list` fixture 作用域将顶栏通知按钮设为 `36×36px`、白底、`1px #f4f6f5` 边框和 `18px` 圆角；空态和其它页面不共用该覆盖。
- [x] 目标视口浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向溢出；列表态与空态前端左上角红黄绿窗口控制点均为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 最新同尺寸 PNG diff 为 `36.9033% / MAE 4.23171 / RMSE 20.50580 / maxChannelDelta 255`，证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/meal-plan-list-notification-2026-08-28.png`，结果锚点为 `figma-105-diff-results.json#meal-plan-list`，继续保持 `DIFF_REVIEW`。
- [x] `PlanningPage` 与 `WorkspaceLayout` 定向测试 `16/16`、`npm run typecheck`、相关文件 Prettier 检查和 `git diff --check` 通过。
- [ ] 本小点不代表 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 74. 2026-08-28 餐食规划列表 Tab 垂直几何收口

- [x] 依据实时 Figma 节点 `692:2662`，将 `/planning?state=list` 列表标题行高收口为 `29px`、Tab 按钮高度收口为 `33px`，并将 Tab 容器底部间距设为 `11.2px`，修正浏览器 `1px` 描边参与布局时产生的累计偏移。
- [x] 浏览器实测视口 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向溢出；标题组为 `x=292,y=100,h=50`，Tab 容器为 `x=292,y=174,h=45`，三张卡片起点为 `y=243/392/541`，卡片均为 `1116×133px`。
- [x] 为列表 Tab 增加稳定的 `data-figma-role` 标记，并在 `PlanningPage.test.tsx` 覆盖三项 Tab 数量；定向测试 `11/11`、`npm run typecheck`、目标文件 Prettier 检查和 `git diff --check` 通过。
- [x] 最新同尺寸 PNG diff 为 `differentPixels=321004`、差异比例 `21.7695%`、`MAE=2.40082`、`RMSE=13.86524`、最大通道差异 `232`；证据已同步到 `figma-105-mapping.json`、`figma-105-diff-results.json` 和 `meal-plan-list-current-diff.json`。
- [x] 前端左上角红、黄、绿窗口装饰候选数量为 `0`，未删除业务状态圆点；Figma 设计稿未修改。
- [ ] 该画板继续保持 `DIFF_REVIEW`；105 张汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移和 iconfont 实体资源登记仍未完成，iconfont 继续为 `BLOCKED`。

## 75. 2026-08-29 餐食规划列表创建按钮几何收口

- [x] 依据实时 Figma 节点 `692:2662`，将 `/planning?state=list` 的“+ 新建膳食计划”前端按钮固定为 `128×37px`、内边距 `10px 16px`、文字 `14px / 700 / 17px`；Figma 设计稿保持只读。
- [x] 浏览器实测视口 `1440×1024`、按钮位置 `x=1280,y=106.5`、尺寸 `128×37px`，字体已加载且页面无横向溢出；前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留。
- [x] 最新同尺寸 PNG diff 为 `differentPixels=319327`、差异比例 `21.6557%`、`MAE=2.38728`、`RMSE=13.83342`、`maxChannelDelta=232`，结论继续为 `DIFF_REVIEW`；证据路径已同步到 `figma-105-mapping.json`、`figma-105-diff-results.json` 和 `meal-plan-list-current-diff.json`。
- [ ] 本小点只收口局部按钮几何，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 解阻塞；iconfont 继续为 `BLOCKED`。

## 76. 2026-08-29 餐食规划列表 Tab 水平间距收口

- [x] 依据实时 Figma 节点 `692:2662`，将 `/planning?state=list` 三个 Tab 的水平间距从 `4px` 调整为 `8px`；浏览器实测位置为 `x=292/374/456`、宽度 `74px`、高度 `33px`，与 Figma 目标一致。
- [x] 浏览器视口 `1440×1024`、DPR `1`、字体已加载、无横向溢出；前端左上角红黄绿窗口装饰候选数量为 `0`，业务状态圆点保留，Figma 设计稿保持只读。
- [x] 最新同尺寸 PNG diff 为 `differentPixels=319709`、差异比例 `21.6817%`、`MAE=2.37253`、`RMSE=13.77192`、`maxChannelDelta=232`，结论继续为 `DIFF_REVIEW`；证据路径已同步到 105 画板映射和汇总 JSON。
- [ ] 本小点只收口 Tab 局部几何，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 解阻塞；iconfont 继续为 `BLOCKED`。

## 119. 2026-08-29 Meal Planning 默认页右侧栏边界收口

- [x] 依据 Figma 节点 `640:901`、`640:1077`，将 `/planning?state=v2` fixture 的右侧栏 `border-left` 改为不参与布局的 `inset` 内描边；右侧栏保持 `x=1100,width=340px`，内部内容保持 `x=1124,width=292px`。
- [x] 约束行实测 `y=54/111/168/225,height=45px`，购物清单分组实测 `y=348/446,height=86px`；字体已加载、无横向溢出，左上角红黄绿窗口装饰候选为 `0`，业务状态圆点未删除，Figma 设计稿未修改。
- [x] 独立证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/meal-planning-v2-right-rail-inset-browser-2026-08-29-rgba.png` 和 `meal-planning-v2-right-rail-inset-2026-08-29-diff.json`；结果为 `23.1839% / MAE 2.24425 / RMSE 12.93822 / maxChannelDelta 234`，继续登记为 `DIFF_REVIEW`。
- [ ] 浏览器实际 DPR 为 `1.25`，DPR 1 门禁仍未通过；本条不关闭 105 张画板像素级 `PASS`、shadcn 全量迁移或 iconfont 实体资源登记，iconfont 继续为 `BLOCKED`。

## 122. 2026-08-29 Knowledge 来源不可用状态视觉收口

- [x] 依据 Figma 节点 `795:1151`，恢复 `/knowledge?state=source-unavailable` 的橙色左侧状态条、`PARTIAL ACCESS` 标签、橙色标题和 `600×260` 卡片视觉层；Figma 设计稿保持只读。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149` 下实测卡片 `x=550,y=300,width=600,height=260`，状态标签 `174×24`，左上角红黄绿窗口装饰候选数量为 `0`；业务状态圆点保留。
- [x] `KnowledgePage.test.tsx` 定向测试 `6/6` 通过；“稍后重试”恢复默认态，`search-failed` 和真实模式逻辑未被修改。
- [x] 最新 PNG diff 为 `35.1598% / MAE 3.94340 / RMSE 16.95234 / maxChannelDelta 255`；证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/user-knowledge-source-unavailable-browser-dpr1-2026-08-29-rgba.png` 和同目录独立 diff JSON，继续标记 `DIFF_REVIEW`。
- [ ] 该小点只收口 Knowledge 来源不可用状态，不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 121. 2026-08-29 Knowledge 检索失败状态视觉收口

- [x] 依据 Figma 节点 `795:968`，恢复 `/knowledge?state=search-failed` 的红色左侧状态条、`ERROR · RETRY AVAILABLE` 状态标签、红色标题和 `600×260` 卡片视觉层；Figma 设计稿保持只读。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149` 下实测卡片 `x=550,y=300,width=600,height=260`，状态标签 `174×24`，左上角红黄绿窗口装饰候选数量为 `0`；业务状态圆点保留。
- [x] `KnowledgePage.test.tsx` 定向测试 `6/6` 通过；重试后恢复默认态，来源不可用状态和真实模式检索错误逻辑未被修改。
- [x] 最新 PNG diff 为 `35.2425% / MAE 3.88158 / RMSE 16.57742 / maxChannelDelta 255`；证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/user-knowledge-search-failed-browser-dpr1-2026-08-29-rgba.png` 和同目录独立 diff JSON，继续标记 `DIFF_REVIEW`。
- [ ] 该小点只收口 Knowledge 检索失败状态，不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 120. 2026-08-29 Knowledge 空态前端窗口装饰复核

- [x] `/knowledge?state=empty` 按 Figma 节点 `795:786` 复核；设计稿中的红、黄、绿窗口装饰不进入前端，业务会话状态圆点保持不变。
- [x] `KnowledgePage.test.tsx` 增加 fixture 级断言，确保前端不渲染 `window-controls`、`traffic-light` 或三色窗口控制标记；Figma 文件保持只读。
- [x] 浏览器在 `1440×1024`、DPR `1.0000000149`、字体加载完成条件下实测空态卡片 `560×220px`、位置 `x=570,y=320`，页面无横向溢出。
- [x] 证据与 diff 已写入 `foodmate-ui/.qa/figma-pixel-acceptance/`：`22.0938% / MAE 2.30524 / RMSE 13.80022 / maxChannelDelta 244`，结果保持 `DIFF_REVIEW`。
- [ ] 该小点只固化前端窗口装饰边界和空态证据，不代表 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 解阻塞；iconfont 继续为 `BLOCKED`。

## 123. 2026-08-29 Admin Tool Calls 表格行高收口

- [x] 按实时 Figma 节点 `797:359`、`797:453-462`，将 `/admin?state=tool-calls` 工具调用表收口为 `1132×87px`，表头 `38px`、数据行 `49px`；边框采用不占布局的 inset 描边。
- [x] 浏览器 `1440×1024 / DPR 1` 实测字体已加载，表格 `1132×87px`、载荷卡片 `1132×321.4px`、说明面板 `1132×250px`，页面无横向或纵向溢出；左上角红黄绿窗口装饰候选为 `0`，权限状态圆点保留。
- [x] Admin 定向测试 `8/8` 通过；实时 Figma 参考图与 RGBA 浏览器证据 diff 为 `21.1480% / MAE 2.85604 / RMSE 17.03777 / maxChannelDelta 230`，映射和聚合 JSON 已更新。
- [ ] 该画板及 105 张画板仍为 `DIFF_REVIEW`；侧栏身份、图标和其他整页视觉差异仍需继续收口，iconfont 实体登记继续为 `BLOCKED`。

## 124. 2026-08-29 Admin Tool Calls 说明面板位置对齐

- [x] 依据实时 Figma 节点 `797:359`、`986:11`，将 `/admin?state=tool-calls` 的说明面板从浏览器 `y=656.4px` 对齐到 Figma 目标 `y=674px`，面板保持 `1132×250px`；仅修改前端，未修改 Figma。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149011612`、字体已加载、页面无横向或纵向溢出；工具调用表保持 `1132×87px`，payload 卡保持 `1132×321.4px`，左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 独立证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/admin-tool-calls-detail-spacing-browser-2026-08-29-rgba.png` 和 `admin-tool-calls-detail-spacing-2026-08-29-diff.json`；同尺寸 PNG diff 为 `19.4094% / MAE 2.451875 / RMSE 15.818763 / maxChannelDelta 230`，映射和聚合结果已更新。
- [x] Admin 定向测试 `8/8`、`npm run typecheck` 和 `git diff --check` 通过；目标文件 Prettier 检查仍受文件原有格式问题影响，未扩大格式化范围。
- [ ] 该画板及 105 张画板仍为 `DIFF_REVIEW`；侧栏身份、图标、字体和其他整页视觉差异仍需继续收口，iconfont 实体登记继续为 `BLOCKED`。

## 125. 2026-08-29 Agent 写入确认详情区背景对齐

- [x] 依据 Figma 节点 `687:887`，仅在 `/chat?state=write-confirmation` 的 `.fixtureWriteCard .fixtureDetails` 作用域增加 `#f4f6f5` 背景色；真实模式和其它 Agent 状态不受影响，Figma 设计稿保持只读。
- [x] `ChatPage.test.tsx` 保留写入确认详情区结构回归断言；定向 ChatPage 测试 `27/27`、`npm run typecheck` 通过。jsdom 不加载 CSS Module，颜色由浏览器运行时证据验证。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149`、字体 `loaded`、详情区 `265×168px`、背景 `rgb(244, 246, 245)`、确认卡 `305×319px`、页面无溢出，前端左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 最新同尺寸 PNG diff 为 `16.9899% / MAE 1.84570 / RMSE 13.04698 / maxChannelDelta 232`；独立证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-write-confirmation-details-background-browser-1440x1024-2026-08-29-rgba.png` 和 `write-confirmation-details-background-2026-08-29-diff.json`，画板继续标记 `DIFF_REVIEW`。
- [ ] 本小点只收口写入确认详情区背景，不代表该画板或 105 张画板达到像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 126. 2026-08-30 Agent 预算上限标题颜色对齐

- [x] 依据 Figma 节点 `687:918`，为预算上限标题增加独立 `fixtureBudgetTitleText` 样式目标，并提高预算卡专用选择器优先级，修正通用 `.fixtureCard h2` 将 Figma 橙色覆盖为深灰的问题；Figma 设计稿保持只读。
- [x] `ChatPage.test.tsx` 定向预算状态测试 `1/1`、`npm run typecheck` 通过；测试断言标题使用预算状态专用样式类。
- [x] 浏览器实测 `1440×1024`、DPR `1.0000000149`、字体 `loaded`、标题计算颜色 `rgb(199, 150, 84)`、预算卡 `286×289px`、选择区 `245.8×60px`、计量区 `245.8×27px`、按钮 `150×32px / 84×32px`、页面无溢出，前端左上角红黄绿窗口装饰候选数量为 `0`。
- [x] 最新同尺寸 PNG diff 为 `19.2302% / MAE 2.52541 / RMSE 15.46865 / maxChannelDelta 237`；独立证据为 `foodmate-ui/.qa/figma-pixel-acceptance/recaptured/agent-budget-limit-title-color-browser-1440x1024-2026-08-30-rgba.png` 和 `agent-budget-limit-title-color-2026-08-30-diff.json`，画板继续标记 `DIFF_REVIEW`。
- [ ] 本小点只收口预算标题颜色，不代表该画板或 105 张画板达到像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 127. 2026-08-30 Diet Records 默认态操作栏结构收口

- [x] 依据 Figma 节点 `640:588`，`/analysis?view=records&state=v2` 默认 fixture 现在展示餐次卡下方的“记录一餐”和“分析这一天”操作栏；Figma 不存在的额外“记录详情”大面板仅在非 Figma fixture 的默认态保留，避免改变真实模式和普通默认态行为。
- [x] 运行时几何为操作栏 `x=292,y=640,width=1116,height=48`，两个按钮分别为 `140×40px` 和 `164×40px`；详情面板未渲染、字体状态为 `loaded`、页面无横向溢出、前端左上角红黄绿窗口装饰候选为 `0`。
- [x] `DietRecordsPage` 定向测试 `10/10` 通过；浏览器 RGBA 证据和独立 diff 已登记，结果为 `35.7843% / MAE 2.681890 / RMSE 15.511048 / maxChannelDelta 234`。
- [ ] 浏览器采集实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过；整页仍为 `DIFF_REVIEW`，不能据此宣称像素级 PASS。105 张画板汇总仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`，shadcn 全页面迁移尚未完成，iconfont 继续为 `BLOCKED`。

## 128. 2026-08-30 找回密码页品牌强调色 Token 收口

- [x] 依据 Figma 节点 `680:275`，`/forgot-password` 通过 `AuthShell` 语义变量使用品牌强调色 `#48c78e`；仅修改前端，未修改 Figma 设计稿，Token 状态页保持独立样式。
- [x] `AuthPages.test.tsx` 新增回归断言，先验证 `1 failed / 21 passed`，实现后定向测试为 `22/22`；真实 `requestPasswordReset` 接口和交互未改变。
- [x] 运行时实测 `1440×900`、字体 `loaded`、无横向溢出、`--auth-accent: #48c78e`，前端左上角窗口装饰候选为 `0`；实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过。
- [x] 浏览器捕获、RGBA PNG 和独立 diff 已登记；按 Figma 参考图同尺寸结果为 `99.9337% / MAE 1.122291 / RMSE 6.930499 / maxChannelDelta 203`，映射和聚合 JSON 已同步，结论保持 `DIFF_REVIEW`。
- [ ] 本小点不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 129. 2026-08-30 重置密码页品牌强调色 Token 收口

- [x] 依据 Figma 节点 `680:307`，`/reset-password` 通过 `AuthShell` 语义变量使用品牌强调色 `#48c78e`；仅修改前端，未修改 Figma 设计稿，密码强度和真实重置接口保持不变。
- [x] `AuthPages.test.tsx` 新增回归断言，先验证 `1 failed / 22 passed`，实现后定向测试为 `23/23`；现有密码显示切换、缺 token 防护和返回登录交互未改变。
- [x] 运行时实测 `1440×900`、字体 `loaded`、无横向溢出、`--auth-accent: #48c78e`，前端左上角窗口装饰候选为 `0`；实际 DPR 为 `1.25`，严格 DPR 1 门禁未通过。
- [x] 浏览器捕获、RGBA PNG 和独立 diff 已登记；按 Figma 参考图同尺寸结果为 `99.1423% / MAE 1.643792 / RMSE 11.583254 / maxChannelDelta 212`，映射和聚合 JSON 已同步，结论保持 `DIFF_REVIEW`。
- [ ] 本小点不关闭 105 张画板像素级 `PASS`、shadcn 全页面迁移或 iconfont 实体资源登记；iconfont 继续为 `BLOCKED`。

## 130. 2026-08-30 Agent Chat 助手背景 Token 复采集

- [x] `/chat?state=figma-v2` 依据 Figma 节点 `640:428` 的原始 PNG 重新核对助手气泡背景，Figma 专用 Token 已为 `#f9fafb`，浏览器实际计算颜色为 `rgb(249,250,251)`；未修改 Figma，真实模式样式不受影响。
- [x] 复采集运行条件为 `1440×1024`、字体 `loaded`、无横向/纵向溢出、消息操作面板 `0`、前端窗口装饰点 `0`；业务状态圆点保留。Chat 定向测试 `30/30` 通过。
- [x] 最新 PNG diff 为 `29.1355% / MAE 3.766897 / RMSE 19.641207 / maxChannelDelta 255`，证据和结果已同步到 `foodmate-ui/.qa/figma-pixel-acceptance/` 以及 105 画板映射；结论继续为 `DIFF_REVIEW`。本次实际 DPR 为 `1.25`，运行时 `dprPass` 为 `95/105`。
- [ ] 本小点只完成助手背景 Token 的前端收口，不代表整页或 105 张画板达到像素级 `PASS`；剩余字体、图标、头像和浏览器光栅化差异、shadcn 全量页面迁移仍需继续，iconfont 实体资源继续为 `BLOCKED`。

## 131. 2026-08-30 Workspace Home 侧栏与状态面板证据收口

- [x] 依据 Figma 节点 `640:256`，`/?state=figma-v2` 已恢复会话搜索、9 条 Agent 会话历史、会话分页和“任务入口与状态”说明面板；真实模式不改变，Figma 设计稿保持只读。
- [x] `HomePage.test.tsx` 与 `WorkspaceLayout.test.tsx` 定向测试为 `14/14`；前端运行时和生产源码均未发现左上角红、黄、绿窗口装饰点，业务会话/状态圆点保持不变。
- [x] 当前 `1440×1024` 浏览器证据已加载字体且无页面溢出；同尺寸 PNG diff 为 `37.7674% / MAE 3.651656 / RMSE 17.505058 / maxChannelDelta 253`，结论保持 `DIFF_REVIEW`。
- [x] 浏览器实际 DPR 为 `1.25`，已在 `figma-105-mapping.json` 中如实登记 `dprPass=false`；视口、几何和文字检查仍通过。
- [ ] 本小点只完成 Workspace Home 前端结构与证据收口，不代表 105 张画板像素级 `PASS`、shadcn 全量页面迁移或 iconfont 实体资源登记完成；iconfont 继续为 `BLOCKED`。

## 185. 2026-08-30 非 Chat Figma Fixture 会话历史边界

- [x] `/analysis?view=records&state=v2`、`/analysis?state=v2`、`/planning?state=v2`、`/knowledge?state=empty` 和 `/profile?state=basic` 已按对应 Figma 画板隐藏会话搜索、历史列表和分页；真实模式不变。
- [x] 五个页面定向测试 `55/55`；`1440×1024` 运行时字体均为 `loaded`、无横向溢出，前端左上角红黄绿窗口控制点候选均为 `0`，业务状态圆点保留。
- [x] 最新 RGBA 证据已切换到 `*-sidebar-history-browser-2026-08-30-rgba.png`，PNG diff 差异比例为 `34.9078%`、`26.7036%`、`22.3250%`、`51.8127%`、`78.9528%`；映射、运行时检查和聚合结果已更新，五项均为 `DIFF_REVIEW`。
- [ ] 105 张画板仍为 `105 DIFF_REVIEW / 0 PASS / 0 UNMAPPED / 0 SIZE_MISMATCH`；本小点不关闭像素级验收、shadcn 全量迁移或 iconfont `BLOCKED`。

## 198. 2026-09-05 Workspace 业务页面批次收口

本批次按实时 Figma 节点 `640:588`（Diet Records）、`640:773`（Intake Analysis）和 `640:901`（Meal Planning）推进共享工作台 fixture。实现范围只包含前端壳层、页面 fixture、浏览器证据和映射登记，不包含后端真实闭环，也不修改 Figma 文件。

- `WorkspaceLayout` 将 Figma 窗口控制点规则从 Home 单页扩展为所有非 Knowledge 的 Figma 工作台 fixture，覆盖 `/analysis?view=records&state=v2`、`/analysis?state=v2` 和 `/planning?state=v2`；普通真实模式不渲染该装饰。
- 本批次重新采集 19 个入口：Diet Records 默认/加载/空态/错误态，Intake Analysis 默认/加载/空态/错误态，以及 Meal Planning 默认/加载/空态/错误态、列表、向导三步、冲突、购物清单和生成中。
- 所有证据均使用 `1440×1024`、DPR `1` 和字体加载完成条件；映射文件中的浏览器路径统一指向 `recaptured/dpr1-*-browser-2026-09-05.png`，并由 `scripts/png-diff.mjs` 生成聚合结果。
- 代表性 diff：Diet Records 默认态 `7.9746% / RMSE 17.0369`，Intake Analysis 默认态 `7.5442% / RMSE 14.4798`，Meal Planning 默认态 `23.1532% / RMSE 12.9382`；规划向导三步分别为 `35.9497%`、`35.3477%` 和 `38.1058%`。
- 本批次自动比较输入为 `105/105`，结构验证为 `structuralPass=true`、`strictDprPass=true`、`errors=[]`，但全部 105 项仍为 `DIFF_REVIEW`。非零 diff、字体光栅化、图标和局部组合差异必须继续逐页复核，不能用几何/DPR 通过替代像素级 PASS。
- 本批次统一质量门禁已验证 Vitest `39/39` 文件、`243/243` 用例、typecheck、build 和 `git diff --check`；`format:check` 与 `lint` 的换行问题已在本批次收口后修正。标准命令图标继续使用 Lucide，iconfont 仍保持 `BLOCKED`。
