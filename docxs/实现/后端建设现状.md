# FoodMate 后端建设说明文档

原始审计基线：2026-07-11

> 更新说明（2026-09-06）：本文早期 B3/B4-1 章节仍保留历史快照。当前 M1-2/M1-3 已完成真实认证、会话、消息、AgentRun、HTTP Runtime stub 与 SSE；根 POM 当前实际参与构建的是 5 个 Java 模块：`foodmate-shared`、`foodmate-application`、`foodmate-infra`、`foodmate-api`、`foodmate-bootstrap`；Python Runtime 已创建。M1-4 的 RocketMQ 异步主通道、M1-5 饮食业务、M2-1 公共知识库 RAG、M2-2 Tool/SQL 和 M2-3 管理核心切片均已有本地业务代码与验证证据；M2-1 已取得 Docker 真实 Embedding/Milvus/引用闭环，M2-2 已取得真实云 SQL Agent 只读闭环。营养目录当前为 1,000 条 approved/official USDA 食材和 1,518 条 approved USDA foodPortion 规则；V32/V33 validation 已通过本地 PostgreSQL。Docker Runtime 启动/readiness、Qwen3 Embedding 和 DeepSeek-V4-Flash Chat 的真实请求均已有本机证据；生产强化边界以[路线图](../项目/路线图.md)和[测试策略](../测试/测试策略.md)为准。

> M1-5 当前口径（2026-09-06）：核心业务已形成真实本地后端闭环，M1-5 核心范围已完成本地验收。饮食记录创建/查询/编辑/删除/恢复、today/7d/30d 营养分析、餐食计划创建/查询/修改/校验/保存/删除/恢复/购物清单，以及 `meal_plan.save_plan` 的 Proposal -> Confirm -> Execute 已实现并通过定向测试和本地 HTTP E2E。`food_log_writer` 的 create/update/delete/restore Proposal -> Confirm -> Execute 已实现，覆盖确认绑定、AgentRun/用户归属、参数摘要、幂等、revision、rejected/failed/superseded 和资源 ID 回填，并已通过真实 PostgreSQL HTTP/RocketMQ 各 11/11 跨进程回归。计划生命周期使用 V15 的 `revision`/幂等字段，修改后会让旧购物清单失效。营养目录当前为 1,000 条 approved/official USDA 食材和 1,518 条 approved USDA foodPortion 换算规则，V32/V33 seed/validation 已通过本地 PostgreSQL 验证，matched/pending 分支均已验证；M2-1 公共知识库和 M2-2 真实云 SQL Agent 只读链路也已有 Docker/跨运行时业务证据；性能和生产故障门禁仍后置，前端页面验收不改变这一事实。

> 当前业务闭环补充（2026-09-06）：M2-1 已包含批量上传、MinIO 私有对象、索引 Outbox、Python 四类文档解析、Redis stub/真实 Embedding + Milvus、索引结果回写、显式发布/下线/恢复、公共检索、AgentRun 引用和 `run.completed` SSE；M2-2 已包含真实云 Chat 驱动的只读 SQL Planner、Java SQL Guard、SQL 审计和终态 SSE；M2-3 已包含管理查询、RBAC、写操作、模型治理、工具注册表、脱敏查询和真实管理端状态收口。上述结论以业务正确性为门禁，不包含性能、长稳、依赖故障矩阵、生产部署、备份恢复或发布回滚。

## 1. 当前结论

本节以下内容记录 2026-07-11 的历史建设状态，不再作为 2026-07-26 当前完成度依据。

已完成的通用和 M1-5 第一切片能力：

- Java 21 + Spring Boot 3.5.16 多模块 Maven 后端工程骨架。
- Maven Wrapper，保证本机没有全局 Maven 也能构建。
- 5 个当前参与构建的 Java 模块，以及独立 Python Agent Runtime。
- 统一 API 响应、错误码、分页、Trace/Request 上下文、Snowflake ID。
- Spring MVC 基础 Web 层、统一异常处理、Trace 过滤器、SSE 事件 envelope。
- MyBatis-Plus 基础配置、PO 基类、软删除基础能力。
- 人工执行的 PostgreSQL 基线/增量脚本目录；应用启动不自动执行 SQL。
- 24 张核心业务表的建表 SQL、索引、约束、表注释、字段注释。
- 单元测试与架构测试，用来防止基础契约被破坏。

M1-5 核心范围已完成；以下是后续扩展或非 M1-5 生产门禁：

- 饮食记录编辑已提供 `PATCH /api/food-logs/{foodLogId}`；请求携带 `revision` 和 `Idempotency-Key`，服务端按整条内容替换执行并重新计算目录快照。真实 PostgreSQL HTTP 回归已覆盖幂等重放、revision 冲突和明细软删除/替换；当前 60 条 approved seed 的真实目录 matched 数据已由 HTTP/MQ writer 回归验证。
- `nutrition_foods` 当前有 60 条 USDA `SR Legacy` approved seed，`nutrition_unit_conversions` 当前有 60 条 USDA `foodPortions` approved 规则和 75 条精确质量换算；HTTP/MQ E2E 已验证真实食材的 matched 快照、换算 `conversion_id`/标准份量和无规则/未知食材的 `pending` 保护，不计入营养汇总。
- `meal_plan.save_plan` 和 `food_log_writer` 的 create/update/delete/restore 是当前已接入确认执行的写操作；writer 已通过 application/Tool Gateway 定向测试以及真实 HTTP/MQ E2E，覆盖拒绝、失败、superseded、revision 冲突和幂等重放。M2-2 的 Tool/SQL 本地业务闭环已完成，生产级模型、容量和故障门禁仍后置。计划资源自身的 create/read/update/validate/save/delete/restore 已由 Java API 提供，但不属于本轮 `food_log_writer` 回归范围。
- M2-1 公共知识库已接入 Java 批次上传、MinIO 私有对象、索引 Outbox、Python PDF/DOCX/Markdown/TXT 安全解析、Redis stub、确定性/真实 Embedding Milvus、结果回写、发布可见性和 `public_published` 引用；Docker 真实 Embedding、Milvus 和 AgentRun 引用闭环已有执行证据。M2-2 已完成共享真实 Chat Router 驱动的 SQL Planner、Java 只读 Guard、SQL 审计和 `run.completed` 闭环。M2-3 管理查询、写操作、模型治理和受控脱敏导出已完成当前业务测试；这些业务证据不等价于生产容量或完整依赖故障恢复。
- 本地基础 metrics 已通过 Actuator 手工验收并补充配置回归测试；`script/local/m1-6-dual-jvm.ps1` 已完成两个独立 Java JVM 共享 PostgreSQL 的有界会话读取基线，以及 Java 实例重启后的数据回读。历史基线为 160/160 成功、错误率 0%、吞吐 51.538 req/s、P50/P95/P99 为 17.107/57.937/94.523 ms；本轮依赖重启后的 20/20 复验为 0% 错误率、吞吐 14.765 req/s、P50/P95/P99 为 22.518/51.250/102.375 ms。两组结果只代表本机认证会话读取场景，不是生产容量结论。
- 本轮还使用 `agent-runtime/.venv` 启动 RocketMQ + Redis Runtime：readiness 返回 HTTP 200，checkpoint、Redis、RocketMQ event/proposal producer、command/result consumer 均为 `ready`；Python 全量 pytest 为 `189 passed, 2 skipped, 6 subtests passed`。Docker 入口、live/readiness、安全扫描和显式环境文件预检也已通过；D114 记录当前 Embedding 凭据的两个 401 认证失败。Redis AOF 探针在容器重启后保留，RocketMQ NameServer/Broker/Proxy 重启后均恢复 healthy 且 Topic/group 初始化成功。当前仍未完成 PostgreSQL 进程重启、ACK 丢失/租约接管/SSE 等完整故障注入矩阵、队列积压与重复业务执行统计，以及生产级吞吐和恢复指标。

B4-2 当前状态：`UserPo`、`UserProfilePo`、`AuthRefreshTokenPo`、`UserAvatarAssetPo`、`SessionPo`、`MessagePo` 及对应基础 Mapper 已存在；用户、资料、会话、消息和头像写路径已完成 application/infrastructure 业务验收。刷新令牌已接入 `/api/auth/refresh` 的 HttpOnly Cookie 轮换、旧 token 原子消费、注销/改密/账号注销/管理员撤销联动，并通过 Java/API 业务测试；头像资产只保存对象存储元数据，不保存图片二进制，私有对象 key 不进入业务响应。

以下历史描述保留用于追溯，不能作为当前状态：

- 本文较早章节中的 B3/B4-1 骨架、模块数量、旧业务表和“尚未真实持久化”结论均为历史快照。
- `foodmate-orchestrator`、`foodmate-rag`、`foodmate-model`、`foodmate-sql-agent` 的旧占位描述不代表当前独立 Python Runtime 的实现状态。

目标边界：

- Java：认证/RBAC、业务 API、事务、业务数据库、AgentRun、SSE、工具/SQL 执行和审计。
- Python：Agent 编排、Prompt、模型、RAG、记忆策略、SQL proposal、结构化输出和评测。
- Python 不直接写业务数据库；所有业务动作由 Java 授权并执行。

## 2. 后端技术选择与理由

| 选择 | 当前结果 | 理由 |
| --- | --- | --- |
| Java 21 | 根 `pom.xml` 固定 Java 21 | 长期支持版本，适合后端服务；也符合项目后续可维护性。 |
| Spring Boot 3.5.16 | 后端统一使用 Spring Boot 3 | 当前文档要求 Spring Boot 3，生态稳定；没有直接跳到 4.x，避免依赖兼容风险。 |
| Maven 多模块 | 当前保留 5 个 Maven 子模块 | 只保留根 POM 实际参与构建的 Java 职责；未来能力先按包演进，避免预建空模块。 |
| Maven Wrapper | 新增 `mvnw.cmd` / wrapper 配置 | 本机无全局 Maven 时也能执行 `clean verify`。 |
| MyBatis-Plus | infra 层配置基础能力 | 更贴近 Java 后端常用 Mapper/PO 写法，便于后续控制 SQL、软删除和复杂查询。 |
| Flyway | 固定 `db/migration` 目录 | 数据库变更必须可追踪、可回滚评估、可在不同环境重复执行。 |
| PostgreSQL | SQL 使用 PostgreSQL 类型和约束 | 支持 `JSONB`、部分索引、强约束，适合 Agent/RAG 这类半结构化数据。 |
| Spring MVC | Web 层基础 | 普通 HTTP API 简洁稳定；当前没有引入 WebFlux 的必要复杂度。 |
| SseEmitter | B5 事件流方案 | SSE 足够支撑 Agent 运行状态流式返回，比 WebSocket 更简单，和 HTTP 基础设施兼容更好。 |
| Python 3.11+ | 已创建的 Agent Runtime | 当前承载 V1 dispatch/cancel、Service JWT 与确定性 stub；真实模型和编排仍待 M1-4。 |
| FastAPI + Pydantic v2 | 目标内部服务与契约 | 负责 Java/Python HTTP、流式事件和 schema 校验。 |
| LangGraph | 优先评估项 | 用于状态化编排、暂停恢复和 human-in-the-loop；最终选型需在 Runtime 骨架中验证。 |

## 3. 模块建设情况

| 模块 | 当前作用 | 设计理由 |
| --- | --- | --- |
| `foodmate-bootstrap` | 应用启动装配，持有 `FoodMateApplication` 和 profile 配置 | 把启动入口集中在一个模块，避免业务模块相互污染。 |
| `foodmate-api` | Web 层、Controller、异常处理、SSE envelope、Trace filter | 对外 HTTP/SSE 契约统一放这里，后续 Controller 不直接碰 Mapper。 |
| `foodmate-application` | 应用服务编排骨架 | 后续承接用例级服务，例如注册、登录、创建会话。 |
| `foodmate-infra` | 基础设施层，MyBatis-Plus、PO 基类、Flyway 脚本、软删除 | 数据库相关能力必须集中在 infra，避免上层直接依赖持久化细节。 |
| `foodmate-shared` | 通用契约、错误、分页、ID、Trace | 只放无业务方向的公共能力，避免形成反向依赖。 |

当前根 POM 保留上述 5 个 Java Maven 模块。仓库中此前的 domain、tool、worker、model、rag、sql-agent、orchestrator 和 gateway-client 目录不属于当前 Reactor 构建模块；相应能力在有真实实现和稳定部署边界前，以 application、infra 或 Python Runtime 内部包演进。

## 4. 已实现的通用后端能力

### 4.1 统一 API 响应

已建立 `ApiResponse<T>`、`ErrorBody`、`ResponseMeta`。

理由：

- 所有接口成功/失败结构保持一致。
- 错误响应中统一带 `request_id` 和 `trace_id`，方便排查问题。
- 前端不用针对每个接口重新猜响应结构。

### 4.2 错误码与业务异常

已建立 `ErrorCode`、`BusinessException` 和 `GlobalExceptionHandler`。

理由：

- 业务错误和系统错误要分开表达。
- 参数校验、业务异常、未知异常都应该返回统一错误结构。
- 未知异常不能把堆栈暴露给前端。

### 4.3 分页契约

已建立 `PageRequest`、`PageResult`。

理由：

- 后续列表接口统一使用同一套分页字段。
- 避免每个业务模块自己定义分页参数。

### 4.4 Trace 与 Request 上下文

已建立 `TraceContext`、`RequestContext`、`TraceContextHolder`、`TraceContextFilter`。

理由：

- 每个请求都有可追踪 ID。
- 后续日志、审计、模型调用、工具调用都能串联同一个 trace。
- SSE 事件也可以携带同一上下文。

### 4.5 ID 生成与 Long ID 序列化

已建立 `IdGenerator`、`SnowflakeIdGenerator`、`LongIdJsonSerializer`。

理由：

- Java 内部使用 `Long` 存 Snowflake ID，数据库使用 `BIGINT`。
- 对外 JSON 把 Long ID 序列化为字符串，避免 JavaScript 数字精度丢失。

### 4.6 SSE 事件 envelope

已建立 `SseEventEnvelope`、`SseTraceContext`。

当前约定：

| 字段 | 用途 |
| --- | --- |
| `event_type` | 事件类型，例如后续 `run.started`、`run.delta`、`run.completed`。 |
| `run_id` | 本次 Agent 运行 ID，对外作为字符串。 |
| `timestamp` | 事件产生时间。 |
| `payload` | 事件业务负载。 |

理由：

- B5 以后 Agent 会流式返回状态，必须先固定事件外壳。
- 前端只需要识别 envelope，再根据 `event_type` 处理不同 payload。

### 4.7 持久化基础

已建立 `BasePo`、`SoftDeleteSupport`、`SoftDeleteRepositorySupport`、`RestoreCommand`、`MybatisPlusConfig`。

理由：

- 所有 PO 统一继承审计字段和软删除字段。
- 正常删除走逻辑删除，不直接物理删除数据。
- 恢复操作必须显式走 admin restore 语义。
- Mapper 只能放在 infra 层，避免 Controller/Application 层直接操作数据库实现细节。

## 5. 配置与启动

已建立配置文件：

- `application.yml`
- `application-local.yml`
- `application-dev.yml`
- `application-prod.yml`
- `application-local-stub.yml`

当前默认可用 `local-stub` profile 启动。

理由：

- B3 阶段需要在没有 PostgreSQL、Redis、Milvus、MinIO、RocketMQ 的情况下启动后端骨架。
- 真实数据库连接和 Flyway 执行放到后续 profile 中逐步启用。
- Python Runtime 落地后，Java profile 只保存 Runtime URL、服务身份、契约版本、超时和取消配置；模型、RAG、Milvus 和 Prompt 参数归 Python 环境配置。

常用命令：

```powershell
.\mvnw.cmd clean verify
.\mvnw.cmd -pl foodmate-bootstrap spring-boot:run
```

当前仓库没有 Python 项目环境，因此本文件不提供不可执行的 Python 命令。完成 B3-6 后必须补充项目虚拟环境、依赖同步、pytest、Runtime 启动和 `/health` 验证命令。

## 6. 已建立的测试

| 测试 | 验证内容 | 理由 |
| --- | --- | --- |
| `ApiResponseTest` | 成功/失败响应结构 | 保证接口响应契约稳定。 |
| `SnowflakeIdGeneratorTest` | ID 生成 | 保证 ID 可用且基本递增/唯一。 |
| `LongIdJsonSerializerTest` | Long ID 对外转字符串 | 防止前端数字精度问题。 |
| `PageResultTest` | 分页结构 | 保证分页契约稳定。 |
| `GlobalExceptionHandlerTest` | 统一异常结构 | 保证异常不会裸奔到前端。 |
| `SystemProbeControllerTest` | 最小 API 上下文 | 保证 Web 层可启动、可访问。 |
| `TraceContextFilterTest` | Trace 注入 | 保证请求上下文能建立。 |
| `SseEventEnvelopeTest` | SSE envelope 结构 | 保证事件流外壳稳定。 |
| `SseTraceContextTest` | SSE trace 上下文 | 保证流式事件可追踪。 |
| `FoodMateApplicationTest` | `local-stub` 启动 | 保证无外部依赖时后端能启动。 |
| `ModuleDependencyArchTest` | 模块依赖边界 | 防止 api 直接依赖 infra/model/rag 等错误方向。 |
| `BasePoTest` | PO 基类字段 | 保证审计/软删除基础字段存在。 |
| `SoftDeleteRepositorySupportTest` | 软删除恢复基础能力 | 保证逻辑删除约定可测试。 |
| `FlywayMigrationScriptTest` | 建表脚本、索引、表注释、字段注释 | 防止新增表字段时漏掉软删除和数据库注释。 |

迁移后架构测试应进一步禁止 Java Application 依赖旧 Agent 模块，并增加 Java/Python 契约、事件去重、取消、Runtime 不可用和 Python 数据库隔离测试。

最近验证命令：

```powershell
.\mvnw.cmd clean verify
.\mvnw.cmd -pl foodmate-infra -am test
```

结果：均通过。

## 7. 数据库迁移建设情况

迁移脚本位置：

```text
script/sql/FoodMate/baseline/V1__init_core_schema.sql
```

人工回滚脚本位置：

```text
script/sql/FoodMate/rollback/R1__drop_core_schema.sql
```

当前脚本做了这些事：

- 创建 24 张核心表。
- 使用 `BIGINT` 主键，配合 Java Snowflake ID。
- 使用 `JSONB` 存放可扩展结构。
- 使用 `VARCHAR + CHECK` 代替数据库 enum，便于后续迁移和兼容。
- 所有表都带审计字段和软删除字段。
- 建立关键唯一索引、查询索引和外键。
- 为每张表和每个字段添加 PostgreSQL `COMMENT ON TABLE` / `COMMENT ON COLUMN` 注释。
- 提供开发/测试环境可人工执行的反向删除脚本，按依赖关系反向删除核心表。

重要说明：

- 当前没有真实 PostgreSQL 数据库实例可用，所以脚本尚未 apply 到数据库。
- 等 Docker Desktop 或本地 PostgreSQL 可用后，启动真实 profile 时 Flyway 可以执行该迁移。
- 回滚脚本会删除表和表内数据，生产环境执行前必须先备份并人工审批。

## 8. 公共字段说明

以下字段在多数表中复用：

| 字段 | 用途 |
| --- | --- |
| `created_at` | 记录创建时间。 |
| `updated_at` | 记录最后更新时间。 |
| `created_by` | 创建人用户 ID。 |
| `updated_by` | 最后更新人用户 ID。 |
| `is_deleted` | 逻辑删除标记。正常查询默认过滤已删除数据。 |
| `deleted_at` | 逻辑删除时间。 |
| `deleted_by` | 执行逻辑删除的用户 ID。 |
| `tenant_id` | 租户 ID。当前预留多租户隔离能力，尚未建立 `tenants` 表。 |
| `trace_id` | 链路追踪 ID，用于关联请求、Agent 运行、工具调用、模型调用和审计。 |
| `status` | 业务生命周期状态，不同表含义不同。 |
| `*_json` | JSONB 扩展字段，用来存半结构化业务内容。 |

## 9. 数据库表与字段说明

### 9.1 `users` 用户账号表

用途：保存用户登录账号、安全状态和基础展示信息。

| 字段 | 用途 |
| --- | --- |
| `user_id` | 用户主键，Snowflake ID。 |
| `tenant_id` | 租户 ID，预留多租户。 |
| `user_no` | 对外展示或业务引用的稳定用户编号。 |
| `username` | 登录用户名。 |
| `email` | 登录邮箱。 |
| `password_hash` | 密码哈希，不存明文密码。 |
| `nickname` | 用户昵称。 |
| `role` | 用户角色，如普通用户、管理员、运营人员。 |
| `avatar_url` | 当前头像地址。 |
| `status` | 账号状态：正常、禁用、锁定。 |
| `last_login_at` | 最近一次登录时间。 |
| `password_updated_at` | 最近一次修改密码时间。 |
| `login_failed_count` | 连续登录失败次数。 |
| `locked_until` | 账号锁定截止时间。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- `user_no` 全局唯一。
- `username`、`email` 在未删除数据中唯一。
- `role`、`status` 使用 CHECK 约束限定合法值。

### 9.2 `user_profiles` 用户资料表

用途：保存用户营养目标、身体信息、饮食偏好。

| 字段 | 用途 |
| --- | --- |
| `profile_id` | 用户资料主键。 |
| `user_id` | 归属用户 ID。 |
| `display_name` | 资料展示名。 |
| `gender` | 性别。 |
| `birthday` | 生日。 |
| `height_cm` | 身高，单位厘米。 |
| `weight_kg` | 体重，单位千克。 |
| `activity_level` | 活动水平，用于营养估算。 |
| `diet_goal` | 饮食目标，例如减脂、增肌、控糖。 |
| `calorie_target` | 每日热量目标。 |
| `protein_target` | 每日蛋白质目标。 |
| `allergens` | 过敏原 JSON 数组。 |
| `dislikes` | 不喜欢的食物 JSON 数组。 |
| `preferred_units` | 偏好单位和展示设置。 |
| `profile_json` | 扩展资料。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 一个未删除用户只保留一份 active profile。

### 9.3 `auth_refresh_tokens` 刷新令牌表

用途：保存登录刷新令牌状态，支持 token 轮换、撤销和设备追踪。

| 字段 | 用途 |
| --- | --- |
| `refresh_token_id` | 刷新令牌主键。 |
| `user_id` | 归属用户 ID。 |
| `token_hash` | refresh token 的哈希值。 |
| `device_id` | 设备标识。 |
| `user_agent` | 客户端 User-Agent。 |
| `ip_address` | 登录 IP。 |
| `expires_at` | 令牌过期时间。 |
| `revoked_at` | 令牌撤销时间。 |
| `rotated_from_token_id` | 轮换前的旧 token ID。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- `token_hash` 唯一。
- 按 `user_id + expires_at` 建索引，便于清理和查询用户会话。

### 9.4 `user_avatar_assets` 用户头像资源表

用途：保存用户上传或生成头像的对象存储元数据。

| 字段 | 用途 |
| --- | --- |
| `avatar_asset_id` | 头像资源主键。 |
| `user_id` | 归属用户 ID。 |
| `storage_key` | 对象存储 key。 |
| `url` | 头像访问地址。 |
| `mime_type` | 文件 MIME 类型。 |
| `size_bytes` | 文件大小，单位字节。 |
| `width` | 图片宽度，单位像素。 |
| `height` | 图片高度，单位像素。 |
| `status` | 头像资源状态：active、replaced、deleted。 |
| `original_filename` | 经过路径安全校验后的原始文件名元数据。 |
| `content_sha256` | 图片字节 SHA-256 摘要，用于内容追踪，不保存图片二进制。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `user_id + status` 建索引，便于查当前头像资源。
- 上传前由 application 层校验文件签名、实际字节数、像素上限和 ImageIO 解码结果；数据库提交失败时补偿删除新对象。
- `users.avatar_url` 只保存稳定的 `/api/users/me/avatar` 应用路径；实际对象访问通过短时签名 URL，不把 `storage_key` 返回给用户。

### 9.5 `sessions` 会话表

用途：保存用户与助手的一次对话会话。

| 字段 | 用途 |
| --- | --- |
| `session_id` | 会话主键。 |
| `tenant_id` | 租户 ID，预留多租户。 |
| `user_id` | 归属用户 ID。 |
| `title` | 会话标题。 |
| `mode` | 会话模式，如 agent 或 chat。 |
| `status` | 会话状态：active、archived、closed。 |
| `last_message_at` | 最近一条消息时间。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `user_id + last_message_at` 建索引，用于会话列表倒序展示。

### 9.6 `messages` 消息表

用途：保存会话中的用户消息、助手消息、系统消息和工具消息。

| 字段 | 用途 |
| --- | --- |
| `message_id` | 消息主键。 |
| `session_id` | 所属会话 ID。 |
| `agent_run_id` | 关联的 Agent 运行 ID。 |
| `role` | 消息角色：user、assistant、system、tool。 |
| `content` | 文本消息内容。 |
| `structured_payload` | 结构化消息负载。 |
| `sequence_no` | 会话内消息序号。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 同一会话内未删除消息的 `sequence_no` 唯一。
- 按 `session_id + sequence_no` 建索引，便于按顺序加载会话消息。

### 9.7 `agent_runs` Agent 运行表

所有权：Java 是唯一业务状态源。Python 只回传版本化执行事件，不直接更新本表；技术 checkpoint 不能替代 `agent_runs.status`。

用途：保存一次用户请求触发的 Agent 执行过程和最终结果。

| 字段 | 用途 |
| --- | --- |
| `agent_run_id` | Agent 运行主键。 |
| `session_id` | 所属会话 ID。 |
| `user_message_id` | 触发本次运行的用户消息 ID。 |
| `intent` | 识别出的用户意图。 |
| `status` | Agent 状态，如 queued、planning、executing、completed、failed。 |
| `plan_json` | Agent 计划。 |
| `result_json` | Agent 最终结果。 |
| `error_code` | 失败错误码。 |
| `trace_id` | 链路追踪 ID。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 状态使用 CHECK 约束。
- 按 `session_id + created_at` 建索引，便于查询会话内运行历史。

### 9.8 `tool_calls` 工具调用表

所有权：Java 校验 Python 提交的 `proposed_tool_call` 后创建、执行并更新记录。Python 不直接写入本表。

用途：保存 Agent 运行过程中每一次工具调用的输入、输出、耗时和结果。

| 字段 | 用途 |
| --- | --- |
| `tool_call_id` | 工具调用主键。 |
| `agent_run_id` | 所属 Agent 运行 ID。 |
| `tool_name` | 工具名称。 |
| `tool_version` | 工具版本。 |
| `input_json` | 工具输入参数。 |
| `output_json` | 工具输出结果。 |
| `status` | 工具调用状态：pending、running、success、failed、timeout、cancelled。 |
| `latency_ms` | 调用耗时，单位毫秒。 |
| `error_code` | 失败错误码。 |
| `trace_id` | 链路追踪 ID。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `agent_run_id + created_at` 建索引，便于回放 Agent 执行过程。

### 9.9 `food_logs` 饮食记录表

用途：当前 `food_logs` 餐次主表说明。M1-5 已将食材和营养快照拆到 `food_log_items`；旧 `items_json`/`nutrition_json` 已从本地数据库移除，本文不再把旧 JSON 字段视为当前结构。

| 字段 | 用途 |
| --- | --- |
| `food_log_id` | 饮食记录主键。 |
| `user_id` | 归属用户 ID。 |
| `session_id` | 来源会话 ID，可为空。 |
| `meal_time` | 就餐时间。 |
| `meal_type` | 餐别，例如早餐、午餐、晚餐、加餐。 |
| `food_log_items` | 独立食材明细表，保存原始份量、营养目录匹配、单位换算和营养快照。 |
| `revision` | 编辑、删除、恢复使用的乐观并发版本。 |
| `idempotency_key` | 写入幂等键。 |
| `notes` | 备注。 |
| `source` | 记录来源：manual、agent、import。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `user_id + meal_time` 查询饮食时间线和统计；`user_id + idempotency_key` 唯一索引保障创建幂等，`revision` 用于删除/恢复并发校验。

### 9.10 `analysis_reports` 分析报告表

用途：保存营养分析、饮食复盘、阶段报告等生成结果。

| 字段 | 用途 |
| --- | --- |
| `report_id` | 报告主键。 |
| `user_id` | 归属用户 ID。 |
| `session_id` | 来源会话 ID。 |
| `agent_run_id` | 来源 Agent 运行 ID。 |
| `report_type` | 报告类型。 |
| `range_key` | 报告时间范围，例如 day、week、month。 |
| `report_json` | 报告内容。 |
| `status` | 报告状态。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `user_id + created_at` 建索引，用于报告列表。

### 9.11 `meal_plans` 膳食计划表

用途：保存 AI 或用户生成的备餐/膳食计划。

| 字段 | 用途 |
| --- | --- |
| `meal_plan_id` | 膳食计划主键。 |
| `user_id` | 归属用户 ID。 |
| `session_id` | 来源会话 ID。 |
| `plan_name` | 计划名称。 |
| `days` | 计划覆盖天数。 |
| `budget` | 预算金额。 |
| `constraints_json` | 用户约束，例如过敏、预算、口味、目标热量。 |
| `plan_json` | 生成的计划内容。 |
| `validation_json` | 校验结果，例如营养是否达标、预算是否超限。 |
| `status` | 计划状态：draft、validated、saved。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `user_id + status` 建索引，便于查草稿和已保存计划。

### 9.12 `shopping_lists` 购物清单表

用途：保存由膳食计划生成的采购清单。

第一版暂时保留 `items_json`，仅表示购物清单的当前 JSON 结构；它不代表饮食记录仍使用 JSON 明细。只有 `saved` 的餐食计划才能生成正式购物清单。

| 字段 | 用途 |
| --- | --- |
| `shopping_list_id` | 购物清单主键。 |
| `meal_plan_id` | 来源膳食计划 ID。 |
| `user_id` | 归属用户 ID。 |
| `items_json` | 采购条目 JSON 数组。 |
| `status` | 清单状态：draft、generated、confirmed。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `meal_plan_id` 建索引，便于从计划找到清单。

### 9.13 `user_memories` 用户长期记忆表

用途：保存助手从对话中沉淀出的用户长期偏好和约束。

| 字段 | 用途 |
| --- | --- |
| `memory_id` | 记忆主键。 |
| `user_id` | 归属用户 ID。 |
| `memory_type` | 记忆类型，例如 preference、allergy、goal。 |
| `memory_key` | 记忆键。 |
| `memory_value` | 记忆值 JSON。 |
| `confidence` | 置信度，范围 0 到 1。 |
| `source` | 记忆来源。 |
| `scope` | 记忆作用范围。 |
| `expires_at` | 记忆过期时间。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `user_id + memory_type + memory_key` 建索引，便于检索用户偏好。
- `confidence` 使用 CHECK 约束限制在 0 到 1。

### 9.14 `session_summaries` 会话摘要表

用途：保存长会话压缩摘要，降低上下文成本。

| 字段 | 用途 |
| --- | --- |
| `summary_id` | 摘要主键。 |
| `session_id` | 所属会话 ID。 |
| `summary_text` | 摘要文本。 |
| `key_constraints` | 会话中提取的关键约束。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 同一个未删除会话只保留一份摘要。

### 9.15 `knowledge_documents` 知识库文档表

用途：保存上传或接入的知识库原始文档元数据。

| 字段 | 用途 |
| --- | --- |
| `document_id` | 文档主键。 |
| `tenant_id` | 租户 ID。 |
| `source_type` | 文档来源类型。 |
| `title` | 文档标题。 |
| `status` | 文档状态：uploaded、parsed、indexed、disabled。 |
| `version` | 文档版本。 |
| `storage_key` | 原始文档对象存储 key。 |
| `metadata_json` | 文档元数据。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `tenant_id + status` 建索引，便于按租户管理知识库文档。

### 9.16 `knowledge_chunks` 知识库切片表

用途：保存文档切分后的文本片段，用于 RAG 检索。

| 字段 | 用途 |
| --- | --- |
| `chunk_id` | 切片主键。 |
| `document_id` | 所属文档 ID。 |
| `chunk_no` | 文档内切片序号。 |
| `chunk_text` | 切片文本。 |
| `section_path` | 文档章节路径。 |
| `tags` | 标签 JSON 数组。 |
| `version` | 切片版本。 |
| `embedding_id` | 向量库中的 embedding ID。 |
| `metadata_json` | 切片元数据。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 同一未删除文档内 `chunk_no` 唯一。
- 按 `document_id + chunk_no` 建索引，便于顺序加载切片。

### 9.17 `data_sources` 数据源表

用途：登记 SQL Agent 可以访问的外部只读数据源。

| 字段 | 用途 |
| --- | --- |
| `datasource_id` | 数据源主键。 |
| `name` | 数据源名称。 |
| `db_type` | 数据库类型。 |
| `purpose` | 数据源用途。 |
| `visibility` | 可见范围。 |
| `status` | 数据源状态。 |
| `readonly` | 是否只读，当前强制为 true。 |
| `connection_ref` | 连接配置或密钥引用。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 未删除数据源名称唯一。
- CHECK 约束强制 `readonly = TRUE`，避免 SQL Agent 写外部库。

### 9.18 `schema_catalogs` Schema 白名单表

用途：保存 SQL Agent 可见的表字段说明和安全边界。

| 字段 | 用途 |
| --- | --- |
| `schema_catalog_id` | Schema 元数据主键。 |
| `datasource_id` | 所属数据源 ID。 |
| `schema_name` | 数据库 schema 名。 |
| `table_name` | 表名。 |
| `field_name` | 字段名。 |
| `field_desc` | 字段业务说明。 |
| `data_type` | 字段数据类型。 |
| `is_sensitive` | 是否敏感字段。 |
| `sample_sql` | 经过审核的示例 SQL。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `datasource_id + schema_name + table_name` 建索引，便于 SQL Agent 装载白名单。

### 9.19 `sql_query_audits` SQL 查询审计表

所有权：Python 生成 SQL proposal；Java 完成授权 schema、SQL Guard、用户过滤、只读执行和本表审计。

用途：保存 SQL Agent 从问题到 SQL 到执行结果的审计链路。

| 字段 | 用途 |
| --- | --- |
| `sql_audit_id` | SQL 审计主键。 |
| `session_id` | 来源会话 ID。 |
| `agent_run_id` | 来源 Agent 运行 ID。 |
| `datasource_id` | 目标数据源 ID。 |
| `original_question` | 用户原始问题。 |
| `resolved_question` | 经过改写或补全后的问题。 |
| `sql_text` | 生成的 SQL。 |
| `status` | 状态：drafted、validated、rejected、executed。 |
| `reject_reason` | 被拒绝原因。 |
| `row_count` | 返回行数。 |
| `latency_ms` | 查询耗时，单位毫秒。 |
| `trace_id` | 链路追踪 ID。 |
| 公共字段 | 见“公共字段说明”。 |

设计理由：

- SQL Agent 属于高风险能力，必须保留问题、SQL、校验、执行结果和拒绝原因。

### 9.20 `tool_registries` 工具注册表

用途：保存可被 Agent 调用的工具定义。

| 字段 | 用途 |
| --- | --- |
| `tool_id` | 工具主键。 |
| `name` | 工具唯一名称。 |
| `display_name` | 工具展示名称。 |
| `description` | 工具说明。 |
| `category` | 工具分类。 |
| `risk_level` | 风险等级。 |
| `availability_scope` | 可用范围。 |
| `status` | 工具状态。 |
| `current_version` | 当前版本。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 未删除工具名称唯一。

### 9.21 `tool_schema_versions` 工具 Schema 版本表

用途：保存工具输入输出 schema 的版本化定义。

| 字段 | 用途 |
| --- | --- |
| `tool_schema_version_id` | 工具 schema 版本主键。 |
| `tool_id` | 所属工具 ID。 |
| `version` | 版本号。 |
| `input_schema` | 输入 JSON Schema。 |
| `output_schema` | 输出 JSON Schema。 |
| `permissions` | 调用所需权限。 |
| `timeout_ms` | 超时时间，单位毫秒。 |
| `retryable` | 失败后是否可重试。 |
| `idempotent` | 是否幂等。 |
| `published_at` | 发布时间。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 同一工具下未删除版本号唯一。

### 9.22 `model_usage_logs` 模型用量日志表

用途：保存模型网关调用记录、耗时、token 用量和成本估算。

| 字段 | 用途 |
| --- | --- |
| `model_usage_log_id` | 模型用量日志主键。 |
| `request_id` | 模型网关请求 ID。 |
| `trace_id` | 链路追踪 ID。 |
| `scene` | 调用场景。 |
| `provider_code` | 模型供应商编码。 |
| `model_name` | 模型名称。 |
| `usage_json` | token 用量和供应商指标。 |
| `latency_ms` | 调用耗时，单位毫秒。 |
| `cost_amount` | 估算成本。 |
| `status` | 调用状态。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- `request_id` 唯一，避免重复记录同一次模型请求。

### 9.23 `model_route_rules` 模型路由规则表

用途：保存不同场景下模型供应商、模型类型、成本和延迟约束。

| 字段 | 用途 |
| --- | --- |
| `model_route_rule_id` | 模型路由规则主键。 |
| `tenant_id` | 租户 ID。 |
| `scene` | 业务场景。 |
| `model_type` | 模型能力类型。 |
| `provider_code` | 主供应商编码。 |
| `fallback_provider_code` | 备用供应商编码。 |
| `max_cost` | 最大成本约束。 |
| `max_latency_ms` | 最大延迟约束，单位毫秒。 |
| `rule_json` | 其他路由规则。 |
| `status` | 规则状态。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `scene + model_type + status` 建索引，便于模型路由快速匹配。

### 9.24 `operation_audits` 操作审计表

用途：保存后台管理、敏感操作和关键变更的审计记录。

| 字段 | 用途 |
| --- | --- |
| `operation_audit_id` | 操作审计主键。 |
| `operator_id` | 操作人用户 ID。 |
| `request_id` | 请求 ID。 |
| `trace_id` | 链路追踪 ID。 |
| `target_type` | 操作对象类型。 |
| `target_id` | 操作对象 ID。 |
| `action` | 操作动作。 |
| `result` | 操作结果。 |
| `request_json` | 脱敏后的请求内容。 |
| `response_json` | 脱敏后的响应内容。 |
| `error_code` | 失败错误码。 |
| 公共字段 | 见“公共字段说明”。 |

关键约束与索引：

- 按 `operator_id + created_at` 建索引，便于追踪某个操作者的行为。

## 10. 为什么第一版这样建表

### 10.1 先覆盖核心链路

第一版表结构覆盖了：

- 用户与认证。
- 用户画像和头像。
- 对话会话与消息。
- Agent 运行与工具调用。
- 饮食记录、分析报告、膳食计划、购物清单。
- 用户记忆与会话摘要。
- 知识库文档和切片。
- SQL Agent 数据源、Schema 白名单、查询审计。
- 工具注册和工具 schema 版本。
- 模型用量和模型路由。
- 操作审计。

这样 B4 后续可以优先实现用户、认证、个人资料、会话，再逐步接 Agent 主链路，不需要频繁推翻底层表结构。

### 10.2 使用 JSONB 的理由

Agent、RAG、模型调用、营养分析结果都存在大量半结构化字段。如果一开始把所有 payload 都拆成极细的关系表，会导致早期 schema 变更非常频繁。

所以第一版采用：

- 关键查询字段结构化。
- 变化快的内容放入 `JSONB`。
- 等业务稳定后再把高频查询字段抽成独立列。

### 10.3 使用逻辑删除的理由

饮食记录、会话、审计、模型调用、工具调用都可能需要追溯。物理删除会让问题排查和用户恢复变难。

所以第一版所有核心表统一带：

- `is_deleted`
- `deleted_at`
- `deleted_by`

正常业务删除走逻辑删除，恢复必须显式处理。

### 10.4 不使用数据库 enum 的理由

状态字段当前使用 `VARCHAR + CHECK`。

理由：

- 比 PostgreSQL enum 更容易演进。
- 后续新增状态时迁移成本更低。
- 仍然能通过 CHECK 保持基本数据约束。

### 10.5 先不建 `tenants` 表的理由

当前部分表保留 `tenant_id`，但没有建立 `tenants` 表。

理由：

- 当前产品第一阶段主要是单用户/单应用闭环。
- 先预留多租户字段，避免未来大规模改表。
- 等真正进入组织、团队、商户或企业租户能力时，再补 `tenants`、成员、角色授权等表。

## 11. 当前风险与待补项

历史架构迁移风险记录：早期版本曾记录 Python Runtime 尚未建立、旧 Java Agent 模块仍在根 Maven 聚合中的状态；该状态已由 M1-3/M1-4 的独立 `agent-runtime` 和双运行时契约替代。历史迁移证据仍保留供追溯，但当前 M1-5 只能按本文顶部的第一切片状态判断，不能把部分实现推断为整体完成。

| 风险/待补项 | 说明 | 建议 |
| --- | --- | --- |
| 未真实执行 Flyway | 本机 Docker daemon 和 `psql` 不可用 | 启动 Docker Desktop 或安装 PostgreSQL 后执行真实迁移验证。 |
| 还没有 PO / Mapper | 当前只有建表脚本和基础 PO 能力 | 下一步做 B4-2，先覆盖用户、资料、token、头像、会话、消息。 |
| 还没有认证 API | 注册、登录、刷新 token 尚未实现 | B5-0 开始补认证应用服务和 Controller。 |
| JSONB 字段未细化 schema | 当前只固定存储位置，没有定义完整 JSON 内部结构 | 后续结合接口文档补充 payload schema。 |
| 审计字段自动填充未完全接入业务 | 基础字段已有，真实写入还没开始 | Mapper/Service 落地时补 MetaObjectHandler 或应用层填充。 |

## 12. 下一步建议

业务线按正常进度进入 B4-2，同时在 Phase 6 前完成 B3-6：

1. 建立 `users`、`user_profiles`、`auth_refresh_tokens`、`user_avatar_assets`、`sessions`、`messages` 的 PO。
2. 建立对应 MyBatis-Plus Mapper，且 Mapper 只放在 `foodmate-infra`。
3. 建立最小 Repository/Adapter，让 application 层不直接接触 Mapper。
4. 加入 H2 或 Testcontainers/PostgreSQL 集成测试方案。
5. PostgreSQL 可用后执行 Flyway 真库验证。
6. 再进入注册、登录、刷新 token、获取当前用户资料等 API。
7. 创建 Python `agent-runtime`、项目环境、健康检查和 pytest 基线。
8. 建立 Java Runtime Client 与 Run/Event/Tool/SQL/Cancel v1 契约。
9. 验证事件幂等、取消、超时和 Python 无业务库写权限后，再收缩旧 Java Agent 模块。

## 13. 文件清单

本轮后端相关重点文件：

| 文件 | 用途 |
| --- | --- |
| `pom.xml` | 根 Maven 聚合和版本管理。 |
| `foodmate-bootstrap/src/main/java/com/foodmate/bootstrap/FoodMateApplication.java` | 后端启动入口。 |
| `foodmate-bootstrap/src/main/resources/application*.yml` | 环境配置。 |
| `foodmate-shared/src/main/java/com/foodmate/shared/**` | 通用响应、错误、分页、ID、Trace。 |
| `foodmate-api/src/main/java/com/foodmate/api/**` | Web 基础、异常处理、Trace filter、SSE envelope。 |
| `foodmate-infra/src/main/java/com/foodmate/infrastructure/**` | MyBatis-Plus、PO 基类、软删除基础。 |
| `script/sql/FoodMate/baseline/V1__init_core_schema.sql` | 首版核心数据库人工建表脚本。 |
| `script/sql/FoodMate/rollback/R1__drop_core_schema.sql` | 首版核心数据库人工回滚脚本。 |
| `foodmate-bootstrap/src/test/java/com/foodmate/bootstrap/architecture/ModuleDependencyArchTest.java` | 模块依赖边界测试。 |
| `foodmate-infra/src/test/java/com/foodmate/infrastructure/persistence/FlywayMigrationScriptTest.java` | Flyway 脚本结构、注释、索引测试。 |
